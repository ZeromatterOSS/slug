# grpc:// (no TLS) → slug infers --tls=false from the URL scheme.
build --remote_executor=grpc://localhost:8980
build --remote_instance_name=fuse
build --extra_execution_platforms=root//platforms:platforms
