# Simple test for Phase 8c - Aspect execution

load(":test_aspect_8c_simple.bzl", "simple_rule")

# Create a dependency chain: c -> b -> a
simple_rule(name = "a")
simple_rule(name = "b", deps = [":a"])
simple_rule(name = "c", deps = [":b"])

# Create a wider dependency graph: d -> [b, c]
simple_rule(name = "d", deps = [":b", ":c"])
