module(
    name = "manual_test",
    version = "1.0.0",
)

# Working: bazel_skylib loads simple .bzl files correctly
# DISABLED Phase 8c-interim
# bazel_dep(name = "bazel_skylib", version = "1.5.0")

# bazel_features provides version comparison utilities
# DISABLED Phase 8c-interim
# bazel_dep(name = "bazel_features", version = "1.39.0")

# rules_cc dependency - cell registration works, but loading blocked on Phase 6 (native cc_common)
# The synthetic repos for bazel_features work, but rules_cc requires native C++ support
# DISABLED: Phase 8c-interim - rules_cc loads from @bazel_tools
# bazel_dep(name = "rules_cc", version = "0.2.16")
