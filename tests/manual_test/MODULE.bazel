module(
    name = "manual_test",
    version = "1.0.0",
)

# Working: bazel_skylib loads simple .bzl files correctly
bazel_dep(name = "bazel_skylib", version = "1.5.0")

# Blocked: rules_cc needs CcInfo provider (Phase 6) and full @bazel_tools (Phase 5c)
# Uncomment when Phase 6 is complete:
# bazel_dep(name = "rules_cc", version = "0.2.16")
