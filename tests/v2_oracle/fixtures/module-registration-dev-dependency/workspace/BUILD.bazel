load(":defs.bzl", "tc_impl", "uses_toolchain")

platform(name = "host_platform")

toolchain_type(name = "tc_type")

tc_impl(name = "tc_impl")

toolchain(
    name = "tc",
    toolchain = ":tc_impl",
    toolchain_type = ":tc_type",
)

uses_toolchain(name = "uses_toolchain")
