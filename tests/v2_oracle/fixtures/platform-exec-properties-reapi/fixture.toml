[fixture]
name = "platform-exec-properties-reapi"
description = "REAPI command Platform properties fixture."
comparison = "semantic"
manifest_roots = ["bazel-bin/pkg"]
oracle_notes = "Regenerate with upstream Bazel 9 and NativeLink once Stage 7 execution harness exists."

[[commands]]
name = "build_probe"
argv = ["build", "//pkg:probe", "--remote_executor=grpc://127.0.0.1:50051", "--remote_default_exec_properties=container-image=toolchain:v1"]
compare = "semantic"
expected_exit = 0