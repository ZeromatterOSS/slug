[fixture]
name = "toolchain-resolution-first-platform"
description = "Registered execution platform order selects the first compatible toolchain platform."
comparison = "semantic"
manifest_roots = ["bazel-bin"]
oracle_notes = "Regenerate with upstream Bazel 9 using --update-expected once Stage 6 toolchain resolution exists."

[[commands]]
name = "build_probe"
argv = ["build", "//:probe"]
compare = "semantic"
expected_exit = 0