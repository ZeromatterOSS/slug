[fixture]
name = "lockfile-error-mode-stale"
description = "Bazel 9 fixture proving --lockfile_mode=error rejects stale visible lockfile registry-file hashes."
comparison = "message_shape"
oracle_notes = "The workspace depends on rules_cc 0.2.17 and starts with a MODULE.bazel.lock containing an intentionally stale registryFileHashes entry for the BCR MODULE.bazel file. Bazel 9.1.1 rejects it in --lockfile_mode=error instead of refreshing the stale lockfile."

[[commands]]
name = "reject_stale_registry_hash_lockfile"
argv = ["mod", "graph", "--lockfile_mode=error"]
compare = "message_shape"
expected_exit = 37
stderr_contains = ["Error accessing registry https://bcr.bazel.build", "rules_cc/0.2.17/MODULE.bazel", "Checksum was", "but wanted"]