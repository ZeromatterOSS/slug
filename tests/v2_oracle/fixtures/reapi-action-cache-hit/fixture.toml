[fixture]
name = "reapi-action-cache-hit"
description = "Remote Action Cache hit: the second build replays the cached ActionResult without re-executing."
comparison = "semantic"
manifest_roots = ["bazel-bin/pkg"]
daemon = true
oracle_notes = "Same-daemon AC hit: the second build hits the NativeLink action cache and reports ac_hits=1."

[reapi]
remote_executor = true

[[commands]]
name = "prime_cache"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0

[[commands]]
name = "replay_cache"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0
