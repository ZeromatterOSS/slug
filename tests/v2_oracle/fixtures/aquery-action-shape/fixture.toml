[fixture]
name = "aquery-action-shape"
description = "Stage 8 aquery compares stable action text/JSON fields for a Starlark write action."
comparison = "semantic"
oracle_notes = "Regenerate with upstream Bazel 9 once Stage 6 action IR is connected to aquery output."

[[commands]]
name = "aquery_text"
argv = ["aquery", "//pkg:emit"]
compare = "semantic"
expected_exit = 0
stdout_contains = ["//pkg:emit", "FileWrite"]