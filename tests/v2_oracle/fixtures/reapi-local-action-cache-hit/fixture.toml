[fixture]
name = "reapi-local-action-cache-hit"
description = "Durable local AC hit should avoid remote lookup and direct-local execution."
comparison = "semantic"
manifest_roots = ["bazel-bin/pkg"]
oracle_notes = "Regenerate with upstream Bazel 9 and NativeLink once Stage 7 action-cache harness exists."

[[commands]]
name = "prime_cache"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0

[[commands]]
name = "replay_cache"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0