[fixture]
name = "module-resolution-basic"
description = "Root plus two transitive module dependencies for Stage 5 resolution fixtures."
comparison = "semantic"
oracle_notes = "Registry-backed expected output is deferred until a local Bazel 9 registry fixture is available."

[[commands]]
name = "build_all"
argv = ["build", "//:all"]
compare = "semantic"
expected_exit = 0