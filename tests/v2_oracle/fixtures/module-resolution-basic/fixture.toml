[fixture]
name = "module-resolution-basic"
description = "Root plus two transitive local module dependencies for Stage 5 resolution fixtures."
comparison = "semantic"
oracle_notes = "Generated with upstream Bazel 9.1.1 using local_path_override modules; registry policy remains covered by later dedicated fixtures."

[[commands]]
name = "build_transitive_dep"
argv = ["build", "@aaa//:from_bbb"]
compare = "semantic"
expected_exit = 0