filegroup(name = "early")

# Deliberately non-lexical input order for arbitrary some() selection.
filegroup(name = "zeta")
filegroup(name = "alpha")
filegroup(name = "middle")
filegroup(name = "single")

filegroup(name = "depth_root", srcs = [":depth_child"])
filegroup(name = "depth_child")
filegroup(name = "cycle_a", srcs = [":cycle_b"])
filegroup(name = "cycle_b", srcs = [":cycle_a"])
