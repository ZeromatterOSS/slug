[fixture]
name = "bare-remote-executor-reapi"
description = "Bare --remote_executor supplies CAS/AC endpoint unless --remote_cache overrides it."
comparison = "semantic"
manifest_roots = ["bazel-bin/pkg"]
oracle_notes = "Regenerate with upstream Bazel 9 and NativeLink once Stage 7 execution harness exists."

[reapi]
remote_executor = true

[[commands]]
name = "build_probe"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0
