[fixture]
name = "exec-groups-action-platform"
description = "Exec group action fixture for per-action platform/toolchain context."
comparison = "semantic"
manifest_roots = ["bazel-bin"]
oracle_notes = "Regenerate with upstream Bazel 9 using --update-expected once Stage 6 toolchain resolution exists."

[[commands]]
name = "build_probe"
argv = ["build", "//:probe"]
compare = "semantic"
expected_exit = 0