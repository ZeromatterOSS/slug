[fixture]
name = "reapi-paramfile-input-tree"
description = "Nested paramfiles must be part of the uploaded REAPI input tree."
comparison = "semantic"
manifest_roots = ["bazel-bin/pkg"]
oracle_notes = "Regenerate with upstream Bazel 9 and NativeLink once Stage 7 execution harness exists."

[[commands]]
name = "build_probe"
argv = ["build", "//pkg:probe"]
compare = "semantic"
expected_exit = 0