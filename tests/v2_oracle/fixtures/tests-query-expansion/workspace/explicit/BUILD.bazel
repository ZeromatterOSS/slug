load("//:defs.bzl", "oracle_test")

test_suite(
    name = "root_suite",
    tests = [":direct_test", ":nested_suite", "//cross:cross_suite"],
)

test_suite(name = "nested_suite", tests = [":nested_test"])
test_suite(name = "only_direct", tests = [":direct_test"])

oracle_test(name = "direct_test")
oracle_test(name = "nested_test")
oracle_test(name = "unlisted_test")
