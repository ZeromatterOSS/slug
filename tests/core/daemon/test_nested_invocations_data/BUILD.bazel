load(":defs.bzl", "normal_nested_invocation", "trace_nested_invocation")

genrule(
    name = "trivial",
    outs = ["trivial.txt"],
    cmd = "echo trivial > $@",
)

normal_nested_invocation(
    name = "nested_normal",
    slug_path = read_root_config("nested", "buck2_path"),
)

trace_nested_invocation(
    name = "nested_trace",
    slug_path = read_root_config("nested", "buck2_path"),
)
