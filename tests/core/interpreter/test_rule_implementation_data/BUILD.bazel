load(":defs.bzl", "bazel_style_rule", "kuro_style_rule")

# Target using Bazel-style rule definition
bazel_style_rule(
    name = "bazel_style",
)

# Target using Kuro-style rule definition
kuro_style_rule(
    name = "kuro_style",
)

# Error case targets are in separate packages to avoid contaminating root BUILD file:
# //error_both:both_params
# //error_no_impl:no_impl
