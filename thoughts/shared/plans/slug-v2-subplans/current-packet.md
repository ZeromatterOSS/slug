# Current Slug V2 Packet

Packet: `WP-6-7A-generated-package-load-bridge-proof-cap-correction-design`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owner: `06-analysis-toolchains-and-actions.md`
Authority: accepted bridge correction design, retained Rust candidate and
accepted Bazel 9.2 `module-extension-use-repo` evidence

Result: produce one bounded implementation-retry contract that preserves the
retained bridge semantics while lawfully completing its cap and missing proof
obligations.

## Design-only authority

Edit only the canonical plan, this manifest, the Stage 6 owner and routing log.
Keep all Rust, tests and fixtures non-writable. Read the retained candidate and
its exact baseline `4d83a829`, including:

- `app/slug_bzlmod_v2/src/host_module.rs`;
- `app/slug_core_v2/src/runtime/generated_repository_definition.rs`;
- `app/slug_core_v2/src/runtime/generated_package_route.rs`;
- `app/slug_core_v2/src/runtime/mod.rs`;
- `app/slug_core_v2/src/runtime/dice.rs`; and
- the existing lawful materialization/test surfaces in
  `root_apparent_repository_definition.rs`.

Also inspect the accepted fixture manifest, workspace and generated record, the
oracle runner's Slug comparison path, protected external-build tests, and the
existing command test helpers. Do not infer a helper or cap from the stopped
worker report alone.

## Required correction contract

The design must:

1. classify the retained diff into production and proof lines from
   `4d83a829`, name exact physical sizes and raise only the caps required by the
   accepted retained implementation plus bounded missing proofs;
2. preserve the five-file production design, public-route-first polarity,
   request-local fallback error, private bridge module, complete-only DICE
   equality, carrierless Need/outer, left-first mapping/definition epoch and
   event/lock/cache/task neutrality;
3. identify the minimum existing test-only materialization seam needed to
   produce a lawful successfully mapped non-Generated definition and prove the
   exact original Unsupported diagnostic is restored, without exposing private
   production kinds or fabricating semantic state;
4. consume the accepted `module-extension-use-repo` fixture unchanged through
   the rebuilt Slug V2 binary as the generated route -> package load -> exported
   source success permanence gate;
5. specify one local command-glue algebra proof for opaque observed lower outer
   translation: retain only the completed public-route prefix, emit typed
   GeneratedRoute::Compute and fabricate no bridge carrier or epoch; and
6. preserve direct-local, builtin, unknown and ordinary unsupported exact
   diagnostics plus cancellation/recovery, warm silence, upper-family
   nonactivation and protected suite baselines.

The implementation retry must have an exact file allowlist separating retained
production authority from test-only authority, exact cap/physical ceilings and
serial commands for focused/full Bzlmod, core/runtime, rebuilt CLI, Bazel and
Slug fixture replay, direct commands, formatting, allowlist/accounting,
no-skip, credential and diff gates.

## Compatibility and stops

Legacy route/build semantics and diagnostics remain exact. The bridge keys,
opaque classifiers, observed epoch association and opaque-outer translation
remain Slug-native. Public/query generated-repository publication, other
platforms and exact configuration/output identity remain unsupported/deferred.

STOP any Rust/fixture edit, production semantic redesign, public private-kind
exposure, diagnostic string classification, fabricated DICE state, general
test-hook API, unmeasured cap waiver, sixth production file, query/public
activation, milestone closure, M8/M7B or exact identity work. Obtain independent
architecture review of the completed retry contract. On ACCEPT, schedule only
that implementation retry; M7 remains partial and M7A -> M8 -> M7B remains.
