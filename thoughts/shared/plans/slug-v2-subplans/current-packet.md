# Current Slug V2 Packet

Packet: `WP-6-m2p-configured-query-forward-successor-audit`
Milestone: M4 configured query
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Result: read-only audit after all-depth unfactored configured graph admission.

## Observable slice

Select the next semantically closed configured-query behavior after accepted
single-root noimplicit `deps` label output and unfactored graph output at every
parser-valid nonnegative Java-`int` depth and unbounded. Prefer the smallest
forward consumer that can reuse the sole configured result graph without new
representation or oracle work.

## Ownership and stops

Reuse the sole configured analysis key, depth-aware closure, authoritative
ordered result edges, full configured/null keys, selected result handles/index,
and cursor-based unfactored renderer. Do not add a graph, copied adjacency,
key, cache, parser, evaluator, or retained representation. Keep factored/
default-implicit/external topology, reverse traversal, multi-root forms,
external labels, exact hashes, JVM/Java, and CI stopped unless one smaller
exact owner and accepted evidence close them.

## Validation

Cite accepted Bazel 9.2 evidence or pinned source and one live owner for every
observable. Preserve Need-before-error, null/full identity, selected-induced
edge filtering, arbitrary nonnegative depth, and one-shot/daemon symmetry.
Make no Rust, test, fixture, oracle, or standalone documentation commit in this
audit. End with one bounded implementation packet or `REPLAN`.
