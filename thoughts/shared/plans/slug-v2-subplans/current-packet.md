# Current Slug V2 Packet

Packet: `WP-4-7A-rules-cc-linking-helper-complete-loading-proof`

Milestone: M7A command/ruleset bootstrap closure.

Result: freeze the authenticated complete 675-line rules_cc
`cc/private/link/cc_linking_helper.bzl` producer over its eight accepted
children. Prove its complete imported/function surface without invocation.

## Learned facts and decision

Commit `99d9289da` accepts the complete 288-line `lto_indexing_action.bzl`
source over seven actual complete children. It proves all nine imports, both
lazy functions and exact nine-public/eleven-all inventories without invocation.
Focused, all 268 loading-library, 24/31 integration, locked analysis/core, CLI,
format/diff/source and archive-baseline gates pass within 0/420/420;
independent review returned `ACCEPT`.

The direct parent is rules_cc 0.2.17
`cc/private/link/cc_linking_helper.bzl`, 675 lines, SHA-256
`c45dd243835bd70803a7bb2e0a11167c9ea5ac912b3f02d415841879873b2a03`.
All eight source-order children are accepted: skylib paths, helper,
`cc_internal`, compilation outputs, C++ link action, library-to-link creator,
LTO indexing action and target types.

The module retains eleven public and three private imported identities, one
public lazy function and seven private lazy functions. It has no other
top-level binding shape and invokes nothing at top level.

Run only `WP-4-7A-rules-cc-linking-helper-complete-loading-proof`. Do not
invoke a function, inspect a callable default, register an action or claim a
configured consumer.

## Authorities, ownership and compatibility

Pinned Bazel 9.2 commit `8220c6198837d5c13d53fea211cf3282aa12408a`
and authenticated rules_cc bytes are sole exact authority. Source bytes plus
accepted complete-child and lazy-function regressions cover every top-level
binding shape; invocation-oriented upstream tests are skipped because their
configured toolchain/action behavior is an unsupported later phase. Clean
`../zabel` commit `0795445f3ab60f4e49070bdd0b94425c5610f73a` is concept-only
peer guidance: its generic-evaluator/Bazel-host split and producer-owned frozen
module lifetime support this architecture, but no Zig code, representation,
algorithm, diagnostic or behavior may be copied.

- **Exact:** complete source/hash/owner/mapping; all eight child labels and
  mappings; fourteen imported pointer identities/visibility; one public plus
  seven private function types/visibility; exact public and all-visibility name
  sets.
- **Slug-native:** realization through one starlark-rust frozen defining-module
  heap retaining all eight child heaps and owning its functions.
- **Unsupported/deferred:** every function invocation, signature/default-value
  inspection and result; linking variables, inputs, outputs, paths,
  toolchain/configuration behavior; action registration, ActionKey and
  execution; configured C++ consumers.

The frozen defining module is the natural producer and retained owner. No
evaluator borrow or invocation value escapes. Request/revision, DICE,
filesystem, cache, async and fallback concerns are inapplicable because this is
test-only source freezing with no production or retained-service change.

## Allowlist, caps and proof

Change only `app/slug_loading_v2/src/host_package_load_tests.rs`. The canonical,
current and Stage 4 documents may change only after terminal acceptance to roll
the result and next packet.

At base `99d9289da` the Rust test authority is 23,849 lines, SHA-256
`5df0ae8553ccfd927a86aa4cc9a60261e34c5c84dbe7de2334eb8f6455e0ea33`.
Its final ceiling is 24,799 lines. Each new proof/helper function must remain at
most 120 physical lines. The oversized module remains cohesive around its
private load harness and adjacent authenticated source constants; add no
production responsibility or generic source archive.

Caps are 0 production, 950 proof and 950 total additions; deletions do not buy
budget. Embed/hash all 675 authenticated lines. Evaluate at exact owner
`@@rules_cc+//cc/private/link:cc_linking_helper.bzl`, path
`/rules_cc/cc/private/link/cc_linking_helper.bzl`, with exact
`bazel_skylib -> bazel_skylib+` owner mapping and all accepted children at their
actual defining identities.

Prove exact child mappings: skylib `paths.bzl`, `cc_internal.bzl`,
`cc_compilation_outputs.bzl`, `lto_indexing_action.bzl` and `target_types.bzl`
have empty mappings; `cc_helper_internal.bzl`, `cpp_link_action.bzl` and
`create_library_to_link.bzl` carry `bazel_skylib -> bazel_skylib+`.

Prove these public imports pointer-identical to their actual child exports:
`paths`, `wrap_with_check_private_api`, `artifact_category` to
`artifact_category_names`, `EMPTY_COMPILATION_OUTPUTS`, `link_action`,
`make_library_to_link`, `create_lto_artifacts_and_lto_indexing_action`,
`LINKING_MODE`, `LINK_TARGET_TYPE`, `USE_ARCHIVER` and `is_dynamic_library`.
Prove private `_use_pic_for_binaries`, `_use_pic_for_dynamic_libs` and
`_cc_internal` pointer-identical to their child exports and absent publicly.

Prove public `create_cc_link_actions` plus private
`_create_dynamic_link_actions`, `_maybe_link_transitively`,
`_maybe_do_lto_indexing`, `_construct_dynamic_library_to_link`,
`_get_linked_artifact`, `_create_no_pic_and_pic_static_libs_actions` and
`_create_action_for_static_library` are type `function` with exact visibility.
Assert exact twelve-public/twenty-two-all name sets. Invoke nothing and add no
fixture/oracle.

Run focused proof, all `slug_loading_v2` library tests, `bzl_invalidation`,
`build_file_loading`, locked analysis/core checks, locked CLI build, formatting,
diff and archive hygiene. Measure caps/ceilings and obtain independent review of
bytes, complete child/import/function inventory, defining identities,
no-invocation/action/consumer boundary, compatibility split, recursive branch
selection and Zabel's peer-guidance role.

STOP and `REPLAN` for production change, source/hash mismatch, another missing
global/evaluator shape, copied/narrowed source or child, incomplete binding
coverage, invocation, callable-default inspection, lost imported identity,
evaluator-borrowed value, action/consumer claim, unpinned source, copied Zabel
content, dirty authority, allowlist escape or cap/function violation. Stop after
this producer and re-audit its first consumer in source order.

## Immediate predecessor

Commit `99d9289da` accepts only complete LTO-indexing-action defining-module
freezing. `8daf80a2c` accepts only the C++ link action and `cb71a302d` accepts
only the universal environment and bounded set subset. None accepts linking
helper function behavior, any configured C++ consumer or actions.
