# Current Slug V2 Packet

Packet: `WP-2A-m1-external-package-source-load-observation-design-resume`
Milestone: M1 one semantic spine
Owner: `slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`
Scheduling base: `33717f27`
Rust base: `33717f27`
Result: resume the docs-only external package source/load frontier design after
the accepted package-marker lookup prerequisite.

## Authority

Write only canonical, this manifest, Stage 2, and the orchestration routing
log. Do not write Rust, Cargo, BUILD files, fixtures, oracles, generated files,
exports, or callers. Docs caps are 40 net canonical, 220 Stage 2, 180 manifest,
30 routing, and 470 aggregate.

## Accepted predecessor

Commit `33717f27` accepts design `1f246c0d`. One mode-aware
`ExternalRepositoryPackageLookupKey` driver selects only matching legacy or
observed route-ignore/path children. The observed carrier retains one semantic
Result Arc plus the exact Arc-backed ignore/`BUILD.bazel`/`BUILD` epoch. Empty
canonical deletion and reached ignore deletion remain distinct; Need/typed
outer has no carrier; child events remain the only events.

Accounting against `2a8dd968` is +165 production/+7 test glue at 4,739
physical lines in `host_package.rs`, 358 tests in the new file, +530 aggregate
semantic and 5,097 physical combined. Focused 3/3 and full bzlmod pass; loading
and query pass; core retains the established 234/235 and 12/13 baselines. Fmt,
check, diff/accounting, compact-retention, cleanup and independent latest-diff
review pass.

## Resumed design audit

Audit the remaining upper frontier from live source:

- observed reuse of package lookup in both `RepositoryPackageSourceKey` and
  `DirectLocalIncludePackageHorizonKey`;
- direct-local module inspection/support and include-package ordering;
- selected BUILD `HostRepositorySourceFileObservationKey` bytes;
- recursive `ExternalBzlModuleEvalKey` source and child-load closure; and
- `RepositoryPackageLoadKey` BUILD parse/load/evaluation and local events.

Select exactly one first complete natural owner or uniquely smaller
prerequisite. It must retain one semantic Result Arc plus every exact reached
path Result Arc without a second family, preserve direct-local include and
recursive `.bzl` order, keep existing `.bzl`/package event owners, propagate
Need/typed outer/semantic prefixes, cancel without publication, and retain no
scratch beyond the compact epoch/carrier shape.

Freeze exact future files, per-file semantic/physical caps, large-file/test
split, proof, validation, compatibility classes, STOP/REPLAN and exactly one
implementation successor. Exact external bytes/values/errors/labels/events
remain exact; siblings/carriers/typed outer are Slug-native. Root query,
multi-build, one-shot publication and identity bytes remain deferred.

## STOP / REPLAN

STOP on Rust or public activation; partial/rebuilt epochs; duplicate families
or events; retained parser/load collections, store/cache/lock/task/direct Host
read; missing future caps; multiple successors; or M1 closure. `REPLAN` if
direct-local include re-entry or recursive `.bzl` loading requires a uniquely
smaller producer. After independent design review, schedule only that bounded
implementation/prerequisite, never query.
