# Current Slug V2 Packet

Packet: `WP-6-7A-bootstrap-critical-repository-ruleset-frontier-audit-r2`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owner: `06-analysis-toolchains-and-actions.md`
Base: accepted selected-registry extension definition source owner

Result: trace the exact bootstrap-critical developer graph beyond the newly
admitted selected source owner and select the uniquely smallest remaining M7A
repository/ruleset semantic owner, one bounded evidence prerequisite, or a
formal `REPLAN`.

## Accepted entry state

The private selected-registry source owner is accepted. A root-owned,
non-isolated request retains its exact Bzlmod-produced canonical selected
definition at both constructors. Loading projects a distinct selected route,
keeps same-repository loads in the producer view, switches mapped loads to each
child producer's view, and shares the owner across loaded-definition projection
and both pure reacquisitions.

Pinned Bazel 9.2 oracle evidence proves root association, producer-local
cross-package and mapped loads, and clean export. Direct proof plus all 543
Bzlmod unit tests and integrations, the full loading suite, dependent core
check and diff hygiene pass. Independent terminal review accepts 775
conservative additions and all seven physical ceilings.

Actual rules_rust remains the bootstrap discriminator. With selected source
loading admitted, its first unchanged terminal is the rejected top-level
`repository_rule(doc = ..., implementation = ...)` declaration in
`rust/extensions.bzl`; later collection schemas, repository effects,
toolchains/providers/actions/input trees and crate_universe remain downstream.
This entry fact is not permission to assume that `doc` is the next natural
owner without tracing all current declaration producers and consumers.

Pinned `../zabel` commit
`c7298478e2e56262a2f438e9c065325744c9f0fc` supplies architecture guidance
only. Use its producer-owned semantic descriptors, nonsemantic physical
realization and already-selected consumer views to test ownership choices.
Copy no Zig code, representation, scheduler, cache, digest, path or behavior.
Pinned Bazel 9.2 remains the sole behavioral authority.

## Audit questions

Starting from the exact 33-package M8 developer graph and the accepted
rules-rust/provider/toolchain needs:

1. Reproduce or reuse the exact current first terminal after selected source
   loading. Trace the Starlark `repository_rule` declaration producer,
   retained callable/schema representation and every already-admitted consumer
   far enough to distinguish a declaration-only owner from invocation,
   repository effects, tag classes and later analysis/action work.
2. Inspect pinned Bazel 9.2 `repository_rule` declaration behavior and the
   smallest discriminating upstream test or existing Slug oracle. Determine
   whether `doc`, collection-valued attributes or another earlier shape is the
   first semantic gap. Do not widen an exact claim from source inspection
   alone.
3. Name the natural Slug producer/key/value and retained lifetime for the first
   gap. Reject command repair, path inference, mutable/global registries,
   evaluator-heap retention and fallback scans. Keep declaration, schema,
   invocation, effects, generated repository publication, toolchains and
   actions as distinct owners unless evidence requires one bounded slice.
4. Select exactly one implementation/design successor, one smaller oracle or a
   formal `REPLAN`. Reuse existing evidence before adding a fixture. State
   request/revision, equality/invalidation, memory/cancellation, event/epoch,
   file/cap/proof and stop contracts applicable to that successor.

## Compatibility

- **Exact:** preserve the accepted Bazel 9.2 selected-source behavior and the
  exact current rules_rust declaration terminal; any newly selected semantic
  behavior requires pinned Bazel 9.2 source/test or discriminating oracle.
- **Slug-native:** Rust/DICE ownership, retained representation, typed errors,
  observation epochs, event batches and evaluator lifetime.
- **Unsupported/deferred:** implementation during this audit; nonroot/isolated
  selected extensions; selected nonregistry/generated definitions; unselected
  repository-rule declaration/schema/invocation/effects; broader
  `repository_ctx`; toolchains/providers/actions/input trees; crate_universe;
  M8/M7B; exact configuration/output identity bytes.

## Authority and validation

This packet is docs-only. Rust, tests, fixtures, oracles, Cargo/BUILD,
generated/vendor content and `@bazel_tools` are read-only. Read authority is
the exact live developer graph, accepted Slug owners/evidence, pinned Bazel
9.2 sources/tests and pinned Zabel guidance.

Write authority for the terminal scheduling record is exactly:

- `thoughts/shared/plans/2026-06-26-slug-v2-clean-restart.md`
- this manifest
- `04-starlark-loading-and-build-packages.md`
- `05-bzlmod-and-repository-graph.md`
- `06-analysis-toolchains-and-actions.md`
- `.codex/skills/slug-agent-orchestration/references/routing-log.md` only for a
  `REPLAN`, unusual route or reusable ownership lesson

Use net documentation caps <=50 canonical, <=260 current, <=180 Stage 04,
<=180 Stage 05, <=240 Stage 06 and one routing row, <=910 aggregate. Keep the
current manifest compact on rollover.

Validation is targeted source/consumer searches, exact live-terminal reuse or
read-only reproduction, pinned-source/test anchors, scheduling consistency,
documentation structure and `git diff --check`. Obtain independent review for
any reserved ownership/DICE/identity decision and before selecting a writable
successor.

STOP Rust/test/fixture/oracle edits, repository-rule implementation, public
command activation, `@bazel_tools` invention, Java/JVM, milestone closure,
M8/M7B, exact identity bytes, a second successor or any compatibility/cap/file
widening. `REPLAN` rather than infer behavior from Zabel or admit adjacent
repository/toolchain/action breadth.
