# Current Slug V2 Work Packet

Packet: WP-4-7A-registration-effect-diagnostic-r1
Status: diagnostic design review

## Observable result

Replace the opaque `[diagnostic incomplete: Effect]` reached by the first
unchanged configured CLI selector with a bounded structural rendering of the
already-retained `HostSelectedRepositoryFileEffectError`. Run that one selector
once to identify the next authentic semantic owner. This packet changes no
repository effect, source, fixture, registration, configured graph, action or
execution behavior. The combined R2/group/computed-default/alias/initializer
stack remains unaccepted and may not merge or push.

The immediate predecessor is `c817e5b12`, whose initializer checkpoint passed
independent rereview and all loading owner gates. The first CLI selector then
stopped during fixture setup after 10.59 seconds at toolchain registration row
8 for `rules_java++toolchains+local_jdk`, where the canonical load route owns a
typed effect error that the current presentation deliberately hides.

## Compatibility and owner boundary

`ModuleRegistrationExpansionError` and its complete typed descendants remain
the equality domain. `registration_diagnostic.rs` remains the sole borrowed,
bounded presentation owner: add `Node::Effect` and traverse the existing error
without cloning, formatting it generically or changing any retained type. Keep
the 3,072-byte ASCII/escaping limit and 32-node depth limit.

Render every `HostSelectedRepositoryFileEffectError` variant finitely:

- certificate errors continue through the existing certificate node;
- compute, projection, host-input and invocation messages retain escaped text;
- missing ordinal, unsupported defining label and invalid result type retain
  only their decisive ordinal/label/type fields;
- invalid/repeated plan paths retain the path and variant;
- label-path-route errors prefix the requested address and continue through the
  existing canonical-load node;
- label-path errors prefix the requested address and render every finite
  `HostRepositoryLabelPathError` variant and its decisive repository/message;
- host-Bzl errors expose their already-owned message through one read-only
  crate-private accessor. A `#[cfg(test)]` crate-private `diagnostic_test`
  factory may construct that otherwise-private leaf for the exhaustive sibling
  proof; the runtime constructor remains private.

No certificate debug rendering, workspace/materialized physical path, source
bytes, environment value, plan content, evaluator error object or unrelated
predecessor enters the diagnostic. No key, cache, DICE dependency, observation,
publication, retry or cancellation boundary changes.

## Scope, caps and stops

Production may edit only
`app/slug_loading_v2/src/registration_diagnostic.rs` and the read-only accessor
plus test-only factory in
`app/slug_loading_v2/src/module_extension_repository_file_effect.rs`.
Proof may edit only
`app/slug_loading_v2/src/registration_diagnostic_tests.rs`. Scheduling/status
sections in the canonical plan, this manifest, Stage 4, bootstrap readiness and
the configured CLI ledger may change.

From `c817e5b12`, allow 90 gross production and 120 gross proof Rust lines, 210
total, without deletion credit. Physical caps are 380 lines for
`registration_diagnostic.rs`, 4,220 for the effect owner and 780 for its test
file. Replan for another production owner, retained-state change, generic Debug
or Display rendering, source/effect behavior, fixture change, cap breach or a
diagnostic that still cannot identify the selected typed leaf.

## Discriminating evidence

Add and preflight exact selector
`registration_diagnostic_repository_effect_variants_are_bounded_and_structural`.
It must cover every effect and label-path variant, existing certificate and
canonical-route recursion, canonical address rendering, ASCII escaping, output
limit, depth limit, and exclusion of certificate/workspace/source/content and
unselected poison fields. Update the former incomplete-boundary test so Effect
is no longer expected to stop opaque. Existing non-Effect incomplete boundaries
remain exact.

Compile within 60 seconds. Run the exact diagnostic selector and the full
loading unit harness under the 12-second/15-second limits, then query compile
coverage and format/diff/cap checks. Independent review verifies exhaustiveness,
bounded traversal, information minimization, unchanged identity/lifecycle and
test coverage.

After acceptance, commit the diagnostic checkpoint and rebuild/preflight the
unchanged CLI harness. Run only
`configured_action_conflicts::one_shot_build_conflict_is_atomic_and_recovers`.
Record its newly exposed typed terminal and stop. Do not run the remaining nine
CLI selectors or F3 until the selected semantic owner has a reviewed successor
and all joint gates pass.
