# Current Slug V2 Packet

Packet: `WP-2A-m1-post-neutral-publication-owner-audit`
Milestone: M1 one semantic spine
Owner: `slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`
Scheduling and Rust base: `941db0d0`
Result: identify the uniquely smallest complete public command owner after the
accepted neutral singleton-root-`Single` publication, without presuming that
build aggregation, query, cquery or an adapter is the correct next cut.

## Authority and caps

Write only:

- `thoughts/shared/plans/2026-06-26-slug-v2-clean-restart.md`;
- this manifest;
- `thoughts/shared/plans/slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`; and
- `.codex/skills/slug-agent-orchestration/references/routing-log.md`.

Documentation caps against `941db0d0` are 40 canonical, 180 Stage 2, 160
manifest, 30 routing-log and 410 aggregate net lines.

## Required audit

Trace the live public build, query and cquery entry points from constructor
admission through native-demand preparation, DICE root/key families, selected
snapshot validation, request-revision finalization and consuming publication.
Inventory at least:

- multi-target and recursive root build aggregation after the accepted
  PackageAll and singleton root-Single owners;
- root and external identities still using `BuildCommandRootKey`;
- query/cquery roots and any one-shot workspace adapter that reconstructs or
  bypasses the accepted native-demand publication path;
- analysis/action-closure recursion, source certificates and exact selected
  observation Arcs reachable from each candidate; and
- event ownership, cancellation, warm reuse, mutation/retry and retained-state
  consequences of moving the boundary.

Rank candidates by the first natural owner that can supply a complete carrier
and exact public publication without computing both legacy and observed
families. Select exactly one bounded design packet, one uniquely smaller
prerequisite, or record `REPLAN`. Do not combine independent command families
merely to make an M1-close claim.

## Compatibility and evidence

Preserve all accepted exact public build/query/cquery bytes, errors, outputs
and events. Observation-family association, exact shared-Arc validation,
request-revision retry and any internal owner cutover remain Slug-native.
External/repository/materializer breadth, native-Windows raw bytes and exact
Bazel configuration/output/ActionKey bytes remain unsupported/deferred unless
the audit proves they are structurally unavoidable for the selected owner.

Reuse accepted source/oracle evidence. The audit must name the future Rust
allowlist, semantic and physical caps, proof matrix, retention/event owners,
STOP/REPLAN conditions and the single successor. It may not add a fixture or
oracle.

## STOP / REPLAN

STOP on Rust, Cargo/BUILD, fixture, oracle or generated writes; public API or
behavior drift; a new Host read, retained store/collection/cache/interner/lock/
task; duplicate family/event/publication owners; premature M1 closure; or cap
excess. `REPLAN` when no one candidate can own a complete selected carrier and
publication boundary without crossing another semantic owner. Acceptance
schedules only one docs-only design or one uniquely smaller prerequisite.

## Immediate predecessor

`941db0d0` accepts the frozen two-file neutral root-Single retry. It selects one
observed anchor/package/classification path, observed configured analysis for
rules and loaded-only filegroups, preserves exact exported-source certificate
and FileBytes Arcs, and publishes through the existing native-demand validator.
It activates no legacy/second package or configured-analysis family and stores
no parent event batch or partial rule carrier.
