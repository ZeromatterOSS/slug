# Current Slug V2 Packet

Packet: `WP-6-m4-configured-query-successor-audit-8`
Milestone: M4 configured query
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Cross-owner: `slug-v2-subplans/08-ruleset-and-command-conformance.md`
Result: read-only selection audit after accepted configured `visible`.

## Observable slice

Select one exact next M4 behavior supported by retained configured analysis
state and the shared Buck2-derived evaluator.

## Ownership and stops

Do not infer traversal, attrs, labels, providers, tests, or configuration from
missing state. Add no package read/reload, retained metadata, graph/key/cache,
parser/evaluator/compiler, protocol/formatter change, patterns/externals,
exact hashes, JVM/Java, CI, or shims.

## Validation

Return one bounded implementation packet with exact semantics, ownership,
evidence, allowlist, lifecycle validation, and hard stops. Do not commit the
audit alone.
