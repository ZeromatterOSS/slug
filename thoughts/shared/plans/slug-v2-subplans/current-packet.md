# Current Slug V2 Packet

Packet: `WP-6-7A-effective-module-override-observation-implementation-retry-2`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owner: `06-analysis-toolchains-and-actions.md`
Rust base: `a3efa1b7`
Accepted semantic design: `c2d1f893`
Accepted proof-cap corrections: `5ebc274a`, `b832736d`

## Exact Rust authority and corrected caps

Write only `app/slug_bzlmod_v2/src/module_eval.rs`, from the 6,052-line
`a3efa1b7` baseline: <=200 production, <=420 tests, <=620 aggregate semantic
and <=6,750 physical lines. The file is a cohesive large-owner exception and
every touched helper remains <200 lines. Every other file is read-only.

The retained candidate is +175 production/+390 tests/+565 aggregate at 6,617
physical lines and passes the focused 2/2 proof. Use the remaining 25
production, 30 test, 55 aggregate and 133 physical lines only for final proof
correction or formatting.

## Frozen owner, proof and compatibility

Do not change the accepted key, carrier, driver, order, terminal algebra,
values, errors, event authority or compact retention. The extra room may fund
only the already frozen production-used pure reducer, legacy projection seam,
real dependency-row/event/cancellation fixtures and discriminating root/command
lifecycle assertions. Add no key, hook, semantic branch, caller, event owner,
retained value or state.

Preserve the crate-private structural observed key and carrier constructor and
borrowed accessors. One Legacy/Observed driver selects only matching
`RootModuleFilesKey` or `RootModuleFilesObservationKey`, then the common
`RootModuleCommandPolicyKey` and pure root-name/command/root/None projection.
Legacy moves the exact Result Arc. Observed forwards the exact root epoch before
semantic inspection. Need/typed outer is carrierless; root compute failure is
empty-prefix; root semantic and all later terminals retain the root prefix.
Both siblings remain eventless and retain only one local Result Arc plus compact
epoch.

Prove crate-private construction/access and distinct identity; matching family
selection; exact Result and epoch Arc projection; root Need/outer/semantic and
command-policy prefixes; real parent dependency rows and later suppression;
root-name, None, root and command parity; root plus command A-B-A; child-owned
cold events, eventless parent, warm silence, real poll-drop/same-DICE recovery;
both family directions and zero selected-graph/discovery/preparation/
repository-definition activation. Complete validity/equality remains semantic
Result+epoch or typed outer by value; Need remains invalid/self-unequal.

Exact: existing values/errors/order/path normalization and legacy behavior.
Slug-native: the sibling/carrier/typed outer/epoch association.
Unsupported/deferred: selected graph, discovery, preparation, registry and
nonregistry closure, extensions/generated repositories, external rules_rust
actions, M8/M7B and exact identity bytes.

Run focused owner tests, full bzlmod, affected accepted loading/query/core
baselines, fmt, diff-check, exact cap accounting and AI-cleanup/Buck2 retention
review.

## STOP and successor

STOP on every other file/key/caller/export, semantic or event change, retained
child carrier/collection, direct Host read, cache/store/interner/lock/task,
later-owner activation, cap excess or milestone closure. REPLAN rather than
weaken or fabricate proof.

After independent implementation ACCEPT, schedule only
`WP-6-7A-selected-module-graph-observation-frontier-design`.
