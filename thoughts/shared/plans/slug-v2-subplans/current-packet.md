# Current Slug V2 Work Packet

Packet: WP-7-10-m7a-local-generated-input-aquery-r1
Status: design ACCEPT; one bounded analysis-only generated-input query pending

## Outcome and boundary

Observe whether the configured CLI root reaches the two first-party generated
Rust source build scripts and whether their declared output-directory tree
artifacts are inputs to the corresponding `starlark_syntax` and
`slug_reapi_v2` Rustc actions. This is an exact pinned Bazel 9.2 configured
action-graph observation, not generated byte content or Slug behavior
admission. The preceding workspace-feature packet at `dc881f9b5` reconciled
saved feature-list scope only; build-script generated flags and compilation
remain open.

Freeze clean main `dc881f9b5`, `Cargo.lock` SHA-256
`a19882e78b50a82ed900fce570f4a6aee778553448790eaf08e2e08a591f76d8`,
`Cargo.Bazel.lock` SHA-256
`6335564ccbb3c915d0a2fa23b8aec0d69986911e062b586e13c5ec9812c2f35e`,
`MODULE.bazel` SHA-256
`109585b7b40d274ea912d32c73f54c25cc4bef1bc5e60526e18ac9bfe48d210c`,
and `MODULE.bazel.lock` SHA-256
`bcdda78dd82aab5fd7ab1796985a78322b50178dceed7669c1b20173c2634302`.
The accepted offline CLI-root configured query receipt SHA-256 is
`459c41aad42c9259adc239b74271c0e7b380bb6970135e4e266ee91e13b322bc`.
First-party `starlark_syntax/BUILD.bazel` declares a `cargo_build_script`
with `grammar.lalrpop` as data/compile_data and a Rust library depending on
that script. `slug_reapi_v2/BUILD.bazel` declares a build script with five
checked-in protos, vendored protoc selection plus raw executable inputs, and
a Rust library depending on that script. The source build scripts invoke
LALRPOP and tonic-build respectively. The exact generated bytes remain
unobserved.

## One bounded action query

Use pinned Bazel 9.2.0 SHA-256
`7668a95db1250f12c40407251e4e203b4ec8bf39bc495d2f485b2d8c99048694`
in batch mode with the reused offline output base, verified nightly six-file
distdir, nightly toolchain channel, download disabled, lockfile error mode
and ignored rc files. Run one CLI-root `aquery` with `--output=jsonproto`,
`--include_artifacts=true`, and the exact expression
`outputs(".*(starlark-rust/starlark_syntax|app/slug_reapi_v2).*", deps(//app/slug_cli_v2:slug))`.
The regex matches full output paths and is only a candidate selector;
exact canonical owner/configuration and output paths decide accepted rows.

A supervisor caps this one analysis-only Bazel spawn at 12 seconds and
stdout/stderr at 16 MiB each, uses a new process group with TERM then
two-second KILL cleanup, and saves raw output and a receipt with command,
exit/stop, hashes/bytes, frozen file hashes and tracked status before/after.
The 12-second cap is this packet's bound, not a general test deadline. No
build or test action may execute. Failure, cap or ambiguous graph mapping
stops without an identical retry.

On clean exit, select exact non-tool target-configuration actions for
`//starlark-rust/starlark_syntax:build_script`,
`//starlark-rust/starlark_syntax:starlark_syntax`,
`//app/slug_reapi_v2:build_script` and
`//app/slug_reapi_v2:slug_reapi_v2`. Require one `CargoBuildScriptRun`
producer and one `Rustc` library consumer for each package. Traverse the
saved ActionGraphDump artifact/depset IDs rather than matching path strings
alone to establish that each producer's `build_script.out_dir` tree output is an
input to its library Rustc action. Record the full configuration checksum,
owner, mnemonic, producer output path/artifact ID, consumer input membership,
and whether grammar, proto and vendored protoc paths appear in the producer
input tree. Pinned rules_rust stages these files through a runfiles tree; the
run action may consume only that tree artifact. Claim individual file
membership only if the saved graph connects an included staging action to
the exact file by artifact IDs. Otherwise mark the individual grammar,
proto or protoc membership unknown, not absent. A missing/ambiguous
producer-to-Rustc edge leaves generated-input reachability open.

The tracked allowlist is this manifest, canonical plan status, Stage 10 and
bootstrap readiness. Independent design review precedes the query; final
review checks raw action graph and receipt. Generated bytes, compiler success,
other action families and M7A remain open.
Independent design review `ACCEPT` confirmed the exact full-path selector,
artifact/depset traversal, and runfiles-tree caveat. Individual staged
source/tool membership needs a visible artifact-ID path; missing output
bytes cannot be inferred from an analysis-only graph.
