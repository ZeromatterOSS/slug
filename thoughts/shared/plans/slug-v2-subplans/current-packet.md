# Current Slug V2 Packet

Packet: `WP-6-7A-bootstrap-critical-repository-ruleset-frontier-audit`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owner: `06-analysis-toolchains-and-actions.md`, with read-only Stage 4/5 inputs
Base: accepted generated repository file-effect vertical and harness
implementation `3ac0a85b`

Result: select exactly one uniquely smallest remaining M7A repository/ruleset
semantic owner or one bounded prerequisite before any new implementation.

## Accepted starting boundary

The generated repository-rule string-path `repository_ctx.file` subset is
accepted end to end: selected owner/ordinal producer, structural generated
route/request, private candidate application, atomic immutable-root publication
and exported-source package/build consumption. The Bazel 9.2 and Slug replays
of `module-extension-use-repo` both return `status: ok`.

Harness commit `3ac0a85b` is +59 production/+98 proof/+157 aggregate at these
final values:

| Path | Lines | SHA-256 |
|---|---:|---|
| `tools/v2_oracle_lib/fixture.py` | 558 | `0519fd68c03dc29238a01c3f4a02ae000c269dbe1df89be99a5aabf653fe06fe` |
| `tools/v2_oracle_lib/compare.py` | 184 | `b858232c7831e945fadb99582d12d348215c4a1ab0576b093ee9a09c2fdb85db` |
| `tests/v2_oracle/test_v2_oracle.py` | 2,748 | `60d6dac1772d4891f9a03dcc3c2e187d430292efb8398525746a7986d983c226` |
| `tests/v2_oracle/fixtures/module-extension-use-repo/fixture.toml` | 30 | `9a9270b30e1cc2cf236bd8d66d77b48438653015c8e57b2e25e8f25b3ab11fa8` |

Common assertions remain universal; tool-aware contracts require both Bazel
and Slug shapes and select only the actual tool. Exact Bazel canonical-target,
source-file and completion claims are unchanged. Slug's JSON presentation is
Slug-native. All retained Rust, fixture workspace and oracle bytes stay frozen.

## Audit scope

Start from the accepted exact 33-package BuildBuddy developer graph and its
named remaining repository sources, rules_rust/provider/toolchain semantics,
action/input-tree and bootstrap consumers. Trace current admitted repository
rules, Bzlmod selection, package loading, configured analysis, action ownership
and bootstrap entry points only far enough to isolate the first missing M7A
repository/ruleset owner.

Check strictly smaller prerequisites first:

- missing pinned Bazel 9.2 oracle/source evidence for a discriminating public
  behavior;
- verbatim upstream `@bazel_tools` content needed before semantic design;
- a producer/request ownership or demand-selection boundary that must precede
  a larger repository-rule or ruleset owner; or
- an already-admitted semantic owner that only lacks observed/public
  association.

Do not presume another `repository_ctx` API, rules_rust, toolchain, action or
bootstrap owner before the trace. Reject an umbrella packet that mixes
independent repository, loading, analysis/action or execution boundaries.

## Required audit output

Record exact natural owner and consumers, child order, structural identity and
invalidation inputs, event/observation ownership, retained versus compute-local
state, cancellation/warm behavior and compatibility class. Separate semantic
configuration identity, display/path, Bazel checksum/ActionKey and REAPI/CAS
domains.

Reach exactly one terminal:

1. one independently reviewed bounded owner design authorizing at most one
   implementation successor;
2. one uniquely smaller bounded evidence/content/association prerequisite; or
3. formal `REPLAN` when no bounded Rust-native slice exists.

## Authority and compatibility

This packet is docs-only. Write authority is exactly the canonical plan,
current packet, Stages 4/5/6 and routing log, at net caps <=40/<=220/<=180/
<=180/<=260/<=30 and <=910 aggregate lines. Rust, tests, fixtures, oracles,
Cargo/BUILD, public APIs, `@bazel_tools` content and callers are read-only.

Pinned Bazel 9.2 remains exact behavioral authority. Pinned `../zabel` commit
`c7298478e2e56262a2f438e9c065325744c9f0fc` must be consulted only for
architectural guidance on natural producer, selected demand and immutable
publication ownership. Copy no Zig code, representation, scheduler, digest,
root layout or cache policy.

STOP implementation, fixture/oracle edits, invented `@bazel_tools` content,
Java/JVM, public breadth, unrelated repository APIs, M7A/M7 closure, M8/M7B and
exact configuration/output identity bytes. `REPLAN` before widening authority
or selecting more than one successor.
