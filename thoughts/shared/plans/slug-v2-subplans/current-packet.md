# Current Slug V2 Packet

Packet: `WP-2A-m1-repository-package-source-observation-proof-cap-correction-design`
Milestone: M1 one semantic spine
Owner: `slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`
Scheduling base: `e4fdfd9e`
Rust base: `1815c019`
Result: correct only the measured proof cap for the retained, unaccepted
repository-package source observation candidate.

## Authority and caps

Write only canonical, this manifest, Stage 2, and
`.codex/skills/slug-agent-orchestration/references/routing-log.md`. Against
`e4fdfd9e`: at most 40/220/160/30 net lines respectively and 450 aggregate.
The dirty three-file Rust candidate is retained but non-writable during design.

Future retry authority remains exactly `host_package.rs`,
`host_package_observation_tests.rs`, and `lib.rs`. Keep host production at
260 net/5,050 physical and lib at 6/405. Correct only proof from 360/750 to
480 net/880 physical; aggregate becomes 746 semantic and 6,335 physical.

## Required correction

Preserve the accepted owner, hidden exports, matching-family support -> lookup
-> selected-source driver, left-first union-before-semantic, exact first Arc,
prefix/error/Need/outer/equality algebra, eventless parent, compact Result-Arc
plus epoch retention, compatibility classes, upper nonactivation and all
production semantics.

The retry proof must discriminate support/lookup/source Need and typed outer
with no carrier and later-child nonactivation; exact error variant and exact
epoch prefix for support Unsupported/evaluation, LookupCompute, invalid,
canonical Deleted, ignore-retained Deleted, NoBuild, lookup semantic,
SourceCompute, source semantic, SelectedSourceAbsent and success; parent-created
conflict and operation mismatch through the production union/reducer seam;
exact ROOT -> route-REPO -> evaluation child batches/text/order including empty
batches, parent none and warm suppression; complete reverse family exclusion
and zero external-Bzl/load/query/build activation. Retain cancellation recovery,
BUILD.bazel -> BUILD -> restore, exact bytes/epoch Arcs and legacy projection.

Allow only a small pure reducer used by the live driver to make support outer
and prefix cases discriminating. Add no key/hook/state/event/semantic change,
new file/caller, upper activation or retained collection. If the retry cannot
fit corrected caps, REPLAN again.

## Compatibility and terminal

Exact source/support/lookup values/errors, BUILD precedence/bytes, legacy Arc
behavior and child events remain exact. Sibling/carrier/typed outer/epoch are
Slug-native. Recursive external `.bzl`, load/query/build publication and
identity bytes remain deferred.

STOP on Rust now, Cargo/BUILD/fixture/oracle writes, any cap or semantic change
beyond the proof correction, caller/public behavior, upper activation, multiple
successors or M1 closure. After independent design ACCEPT, schedule exactly
`WP-2A-m1-repository-package-source-observation-implementation-retry` with
the retained candidate and corrected caps; after implementation ACCEPT return
only to the docs-only upper-source audit.
