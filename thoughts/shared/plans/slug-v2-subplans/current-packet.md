# Current Slug V2 Packet

Packet: `WP-4-5-selected-repository-file-effect-producer-proof-contract-correction-implementation`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owners: `04-starlark-loading-and-build-packages.md` and
`05-bzlmod-and-repository-graph.md`
Base: cap correction `874bf442` plus retained passing seven-file producer
candidate

Result: complete only the independently accepted lawful/composed proof contract.

## Concrete STOP and frozen candidate

The implementation worker stopped at the packet's cap gate after formatting,
compile and four focused tests. No scope or semantic defect was found. Exact
formatted additions against the frozen working baselines are:

| Path | Production | Proof | Total | Physical/hash |
|---|---:|---:|---:|---|
| new Bzlmod effect child | 119 | 75 | 194 | 194; `9d4ef0bc…` |
| Bzlmod `lib.rs` | 9 | 0 | 9 | 469; `0d33a0ba…` |
| new loading producer child | 730 | 487 | 1,217 | 1,217; `783070e3…` |
| frozen-rule seam | 16 | 0 | 16 | 649; `4ac465f1…` |
| instantiated-call seam | 4 | 0 | 4 | 2,094; `f85cde59…` |
| certificate seam | 8 | 0 | 8 | 1,951; `fed3d8cb…` |
| loading `lib.rs` | 15 | 0 | 15 | 119; `af17ce93…` |
| **Total** | **901** | **562** | **1,463** | |

The retry corrected the named `legacy_utf8` ABI in one production line and
added lawful direct ABI, owner/ordinal, parity, epoch, A/B/A, event,
cancellation and semantic-terminal coverage. Seven loading and two Bzlmod
focused tests plus the dependent loading check pass. Independent review finds
the retained implementation correct and within scope, but the old proof matrix
still requires states rejected below this key.

The correction design froze every Rust and fixture byte and was independently
accepted after exact arithmetic review.

## Accepted cap-only correction

Independent review accepts exactly these corrected ceilings:

- <=930 production, <=850 proof and <=1,780 aggregate added Rust lines;
- new Bzlmod child <=300 physical lines and Bzlmod `lib.rs` <=475;
- new loading child <=1,450, rule <=680, instantiation <=2,125, validation
  <=1,990 and loading `lib.rs` <=120 physical lines.

Against the retained retry these leave 29 production, 288 proof and 317
aggregate lines of headroom; the loading child has 233 physical lines left.
They do not authorize a new semantic type, key, event family, retained value,
file, caller or behavior. The public doc-hidden plan builder remains invocation
scratch required by the accepted loading -> Bzlmod dependency direction; it is
not a user API.

## Proof-contract correction

Keep direct upper-key proof for exactly the remaining lawful behavior:

- a real Legacy/Observed `Need` with no parent invocation batch;
- two retained repository calls where the selected ordinal succeeds and a
  sibling implementation would fail, proving no sibling execution;
- a semantic invocation terminal retaining its one local print batch;
- structural key identity across owner and ordinal; and
- retained source/shape absence of module, callable, heap, evaluator, context
  and I/O state.

Retain the existing direct proof for ABI/defaults/binding, success and semantic
Legacy/Observed parity, exact certificate-left/HostBzl-right epoch composition,
implementation-output A/B/A, cold/warm success events, cancellation/recovery,
MissingOrdinal, equality and validity.

Reframe only impossible injected states as composed proof:

- Label/StarlarkPath arguments cannot be constructed from the admitted loading
  globals; direct bool/list cases plus the production `unpack_str` arm cover
  their shared rejection behavior;
- defining-label/export/kind/schema mismatch cannot arise because certificate
  capture and Host-Bzl reload observe one immutable transaction snapshot. Add
  exact source-shape assertions for export lookup, rule downcast, projection
  equality and implementation selection; reuse
  `observed_bzl_module_retains_recursive_arcs_events_and_a_b_a_identity`;
- observed child outer and epoch-merge conflict cannot be delivered because
  each global epoch is validated at construction. Reuse
  `observed_bzl_module_preserves_decisive_terminals_need_and_outer_polarity`
  and `observed_pure_identity_finisher_and_prefix_algebra`, plus exact source-
  shape assertions for carrierless forwarding and certificate-left merge.

Do not fabricate carriers/epochs, add an injection harness or match diagnostic
strings. Correct no caps: <=930 production, <=850 proof, <=1,780 aggregate and
all seven physical ceilings remain. Preserve all exact/Slug-native/unsupported
classes, Bazel 9.2 exact authority and pinned `../zabel` `c7298478…` as concept-
only natural-owner/effect guidance.

Write authority is the same seven Rust paths, but production may change only
for a demonstrated same-contract proof defect within the 29-line headroom.
Prefer proof-only edits in the new loading child. Run formatting, focused
Bzlmod/loading tests, full Bzlmod then loading serially, dependent core check,
archive status, diff hygiene, exact accounting and independent review. Do not
run the terminal fixture because this callerless prerequisite does not change
materialization.

STOP fixtures, callers, injection, new types/keys/files/events, route/
materialization/public activation, Java/JVM, M7 closure, M8/M7B and identity-
byte work. `REPLAN` before any other widening or cap failure.
