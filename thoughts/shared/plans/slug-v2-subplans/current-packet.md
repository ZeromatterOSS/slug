# Current Slug V2 Packet

Packet: `WP-2A-m1-package-load-observed-publication-owner-audit`
Milestone: M1 one semantic spine
Owner: `slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`
Accepted implementation: `93f43264`
Result: audit the package-load and immediate upper publication boundary and
select exactly one bounded next design, prerequisite, or `REPLAN`.

## Authority and completion evidence

Write only the canonical plan, this manifest, Stage 2, and
`.codex/skills/slug-agent-orchestration/references/routing-log.md`, within
40/180/220/30 net lines respectively and 470 aggregate. Rust, Cargo/BUILD,
fixtures, oracles, generated artifacts, caller/public activation, implementation
and M1 closure are stopped.

Accepted `93f43264` adds the private external-Bzl observation sibling and the
matching request-scoped cycle family. Against Rust base `ac7b8bdf`, accounting
is `bzl_module.rs` +400 at 6,595 physical, `cycle_detector.rs` +42 at 640,
and proof +547 at 2,878; aggregate +989 at 10,113. Focused observed tests are
5/5; loading is 133 unit plus 66 integration, bzlmod 426 plus 193, and query
120. Fmt/diff and exact caps pass. Archive status has only the inherited
missing-ref/record and retained authoring-doc baseline. Retention/AI cleanup
and independent review ACCEPT the one-Result-Arc/epoch carrier, compact cycle
identity, compute-local scratch, bounded helpers, family/event isolation and
complete lifecycle proof.

## Audit

Trace `RepositoryPackageLoadKey` from the accepted
`RepositoryPackageSourceObservationKey` through BUILD bytes/parse/load-label
resolution, AST-order recursive `ExternalBzlModuleObservationKey` children,
package evaluation and its local Complete BUILD event batch. Inspect only
enough loading-query/core consumers to locate the first complete publication
owner. Determine whether package load is the uniquely smallest natural owner,
whether a smaller reusable package-attempt/evaluation producer is required, or
whether the boundary must `REPLAN`; do not preselect an answer.

For every candidate, inventory exact legacy and observed structural identity,
Result-Arc ownership, complete epoch order and first-Arc behavior, DICE failure,
Need/typed-outer/semantic precedence, later-child suppression, equality/
validity, child-before-parent event text/order, warm replay, cancellation and
edit/delete/recreate/A-B-A. Account the selected BUILD bytes, recursive frozen
module lifetime, loaded package graph and every retained or compute-local
collection. Prove matching package-source/external-Bzl families and zero second
family, query/build/public activation or duplicate event owner.

Exact compatibility candidates are BUILD parsing/evaluation, package values/
errors, load order and local event text/order. Observation sibling/carrier,
typed outer and epoch association are Slug-native. Loading-query/build
publication and exact identity bytes remain deferred.

## Terminal

End with exactly one docs-only package-load observation design, one uniquely
smaller prerequisite design/audit, or formal `REPLAN`. Before scheduling any
implementation, freeze the exact future Rust allowlist, per-file semantic and
physical caps, owner/order/terminal/event/memory contract, compatibility
classes, discriminating proof, validation and STOP/REPLAN conditions. Schedule
at most one immediate successor and keep M1 partial.
