# Current Slug V2 Packet

Packet: `WP-6-m4-configured-query-successor-audit-3`
Milestone: M4 configured query
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Cross-owner: `slug-v2-subplans/08-ruleset-and-command-conformance.md`
Result: read-only selection audit after accepted configured label filtering.

## Observable slice

Select one exact next M4 behavior supported by retained configured analysis
state and the shared Buck2-derived evaluator. Prefer direct reuse over new
command-specific machinery.

## Ownership and stops

Do not select `deps` without observable platform/constraint nodes, configured
`kind`/`attr` without retained metadata, or providers without a complete
qualified dictionary and builtin-value runtime. Add no second graph/parser/
evaluator, patterns, externals, exact hashes, JVM/Java, CI, or compatibility
surface.

## Validation

Return one bounded implementation packet with exact observable, ownership,
file/test allowlist, reusable or required Bazel 9.2 evidence, validation, and
stops. Do not commit the audit alone.
