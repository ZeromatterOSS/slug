# Current Slug V2 Work Packet

Packet: WP-5-7A-sentinel-build-cost-audit-r1

Status: selected after the probe's focused compilation reached timeout60, exit124.
Read-only prerequisite investigation; no automatic compile retry or cap increase.

## Observable result and immediate predecessor

Determine a bounded compilation route for the unchanged authentic native sentinel
diagnostic, or record the first unresolved build prerequisite. Do not substitute
a narrower command, fake source input or different semantic API to make it faster.

User-installed /usr/bin/strace6.8 passes harmless namespace/prlimit/trace preflight
outside the tool sandbox (sandbox ptrace returns EPERM). The focused pinned-toolchain
cargo test -q -p slug_cli_v2 --test payload_demand_probe --no-run --message-format=json
timed out at60seconds: no reported executable/build-finished,0 compiler errors,
300 warnings,682 artifact records,57 build-script records;20 artifact records
fresh=false across10 Slug crate names. No fixture staging or evaluation occurred.
An escalated process-name check found no cargo/rustc/rust-lld/cc/ld/slugd survivors.
The log does not identify the precise phase at timeout or prove a root cause.

Uncompiled, unaccepted draft: /tmp/slug-sentinel-draft.Lln8y0/candidate.patch,
SHA256 8eef40138afa23caa2601b89e6006de40f7e6d139f40124f4c2c430ae5fdf0a2.
Its adjacent validation.txt owns caveats/review. Static review requires a bounded
finally-style kill/reap/close phase on supervisor exceptions before any execution;
the saved draft is REVISE, not accepted. Do not restore or execute it here.
Compiler evidence: /tmp/slug-sentinel-compile.pv7ol5/compiler.jsonl,
SHA256 bd23fde38480b2a64d30b24571a4814128efe8bb30a2ce0a8fc1c047707dc544.
Previous diagnostic contract remains in Git at e720da26d:this manifest path;
its native API, authentic inputs, trace discriminator and all runtime limits stand.

## Read-only scope and evidence

Root owns the audit. Inspect existing compiler JSON, Cargo.toml, the CLI manifest,
.cargo/config.toml and rust-toolchain first. At most12 additional files/2MiB total
text may be read: exact relevant Cargo fingerprint metadata or installed official
Cargo documentation selected from a concrete unresolved build question. No broad
target/cache scan, artifact deletion, environment dump or credential access.
Compare observed artifact variants only when evidence identifies the relevant
profile/target/configuration inputs; duplicate names alone do not prove a cause.
Prefer a compiler-reported/local documented boundary over guessed executable
paths, warmed-cache assumptions or a silently changed test profile.

Result must separate observed facts, inference and unresolved timing. Name one
next bounded action with an explicit validation/stop contract, or request a user
decision if it needs a longer build or materially different test workflow.
A read-only audit cannot authorize its own retry. Any changed diagnostic owner,
Cargo profile, dependency/interface or execution policy requires a reviewed packet.
No performance improvement or Bazel parity claim follows from this investigation.

## Ownership, exclusions and close

This is Slug-native developer-tooling evidence,0 production/0 proof changes.
No semantic producer/key/request/revision/retained memory changes: those remain
owned by the existing Slug runtime. Only bounded phase-scratch audit text is new.
No Bazel/Buck2 oracle is needed for a tooling-only observation; existing diagnostic
source/provenance remains authoritative for a future native run. No new fixture,
fallback, donor reuse, source acquisition, source execution or cache mutation.

Writable: this manifest, canonical Live Status, relevant Stage5 status and routing
REPLAN row; <=100 added doc lines outside this manifest; PROGRESS.md<=500.
Preserved R2 and sentinel draft remain read-only. All edits use apply_patch.
Validate diff, both preserved patch hashes/applicability and archive checker
(known3 thoughts-path failures only). Independent terminal/next-contract review;
commit/push accepted evidence. New source owner, read cap or implementation need
is a stop/REPLAN, not permission to widen the audit.

Never run Cargo, a test, CLI, daemon, Bazel, checkout-wide/bounded replay or network
acquisition in this packet. Never inspect/print/copy ~/.bazelrc or derived secrets.
R2 /tmp/slug-conflict-r2.XZJWwv/candidate.patch SHA256
90c725e40a7aa46f5f0e81112bfe5f91a429679d9bd3824ae7dda9417725d94e
remains complete, preserved and unaccepted; actual CLI conflicts, positive sharing
and full relevant gates remain open. Never partially restore or ship R2.
