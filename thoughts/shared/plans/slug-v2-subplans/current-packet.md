# Current Slug V2 Work Packet

Packet: WP-7-15-m7a-cache-leaf-build-script-materialization-r1
Status: accepted; cache-leaf build-script generated bytes materialized

## Outcome and compatibility

Build only `//app/slug_reapi_cache_v2:build_script` with the pinned Bazel 9.2
nightly Rust toolchain, then inspect whether its declared `build_script.out_dir`
contains the protocol Rust files. This is evidence for the live first-party
generated-source producer. It changes no Slug behavior or compatibility class
and cannot by itself prove that the leaf Rustc consumer, CLI root, or M7A builds.

WP-7-14 at `757a96799` proved the new producer-to-consumer *declared* artifact
edge in the CLI-root action graph, with zero actions run. Its no-fetch query
warned that the external crate repository was stale. The actual generated bytes
and compilation remain the next distinct gate.

## Owner and bounded observation

The checked-in `Cargo.toml`, Cargo/Bazel locks, `MODULE.bazel`,
`app/slug_reapi_cache_v2/{BUILD.bazel,build.rs,proto/**}`, and the pinned
rules_rust toolchain own this build. Bazel's output tree is command scratch,
not a Slug DICE fact or a published runtime artifact. Do not infer Slug
execution, REAPI bytes, or CLI buildability from this command.

From a clean source/lock baseline on main, run one
`bazel --ignore_all_rc_files --batch build` of the
single target with `--@rules_rust//rust/toolchain/channel=nightly` and
`--lockfile_mode=error`. Allow Bazel to materialize its pinned external inputs;
do not read the user's `.bazelrc` or enable BuildBuddy. Bound the build at 60
seconds with TERM and short KILL cleanup, cap output, and record the exact
command, elapsed time, exit status, stdout/stderr and tracked-input status.
This is a compilation prerequisite, not a test; no test is selected. Do not
repeat the same stopped build without a new diagnosis.

On success, inspect the exact declared `build_script.out_dir` tree. Require
nonempty generated Rust files for the selected REAPI/Google packages and no
tracked source/lock changes before claiming generated-byte materialization. A
failure or timeout proves only the reported build limit, not missing generated
code. A lockfile or source change requires separate review before continuing.
Do not broaden this packet to the leaf library or CLI build. Validation is the
build receipt, output-tree inspection if available, plan status checker, and
`git diff --check`.

Allowlist: this manifest, canonical Live Status, Stage 10's live inventory
delta, and bootstrap-readiness's first row. No production or test edit is
selected. Commit and push a complete, reviewable result; a source/lock/tool
failure selects a separate corrective successor.

## Build receipt (2026-09-16)

From source/lock baseline main `757a96799`, with only packet documentation
modified, the sole build command was:

```text
ulimit -f 131072
/usr/bin/time -f 'elapsed_seconds=%e\nexit_status=%x' -o target/wp715/time.txt timeout -k 3s 60s bazel --ignore_all_rc_files --batch build --lockfile_mode=error --@rules_rust//rust/toolchain/channel=nightly //app/slug_reapi_cache_v2:build_script > target/wp715/stdout.txt 2> target/wp715/stderr.txt
```

It exited 0 in 31.36 seconds. Bazel reported 396 actions, including the
build-script compile and run, and the target up to date at
`bazel-bin/app/slug_reapi_cache_v2/build_script.out_dir`. Stdout is empty
(SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`);
the 3,956-byte stderr SHA-256 is
`a29f3cd684ee42cd1b3a0d7cfe291d6e85d86310f98fe2ffcb024dcd1a676474`.
The ignored `target/wp715/` scratch retains the exact streams and timing.
Its sorted six-file SHA-256 inventory is `tree.sha256` (inventory SHA-256
`1db38406783c8e8833c1c2e9bcccb1d74415663ebbc3807e91cc1e0ff0d30424`).

The output tree has exactly six nonempty generated Rust modules, matching all
six `tonic::include_proto!` names in the leaf source: `build.bazel.remote.execution.v2`
(168,691 bytes), `build.bazel.semver` (738), `google.api` (40,629),
`google.bytestream` (14,196), `google.longrunning` (18,040), and `google.rpc`
(1,288). The generated files contain the expected `ActionResult`, `SemVer`,
`HttpRule`, and `ReadRequest` definitions. `Cargo.lock`, `Cargo.Bazel.lock`,
`MODULE.bazel.lock`, and the build-script source remained tracked-clean; no
test ran. This closes only the cache-leaf build-script byte-materialization
slice. The library Rustc action, full CLI build, and M7A remain open.
