# Current Slug V2 Packet

Packet: `WP-2A-m1-post-package-source-upper-owner-audit`
Milestone: M1 one semantic spine
Owner: `slug-v2-subplans/02-rust-skeleton-and-runtime-substrate.md`
Accepted base: `ac7b8bdf`
Result: select the first complete natural owner above observed repository
package source without activating an upper caller.

## Authority

Write only canonical, this manifest, Stage 2, and
`.codex/skills/slug-agent-orchestration/references/routing-log.md`. Against
`ac7b8bdf`, caps are 40/200/180/30 net lines respectively and 430 aggregate.
Rust, Cargo/BUILD, fixtures, oracles, generated artifacts and public/caller
activation are forbidden.

## Audit

Trace the accepted `RepositoryPackageSourceObservationKey` into
`ExternalBzlModuleEvalKey`, its recursive child evaluations and local Complete
`.bzl` batch, then through `RepositoryPackageLoadKey`, BUILD evaluation and
its local Complete batch. Continue only far enough into loading-query/build
consumers to prove the first complete ownership boundary.

For each edge, record structural family identity, exact semantic Result Arc,
complete path epoch availability/order, Need/typed-outer/semantic precedence,
recursive batch ordering, cancellation publication, warm reuse, lifecycle and
retained-state ownership. Determine whether a distinct observed recursive
external-Bzl evaluation sibling is the uniquely smallest complete owner,
whether it needs one smaller observed source/preparation prerequisite, whether
package load can own the whole closure without mixed families or duplicate
events, or whether the constraints require formal REPLAN. Do not presume that
dependency-owned epochs can be discarded or that package load can reconstruct
them.

Exact compatibility remains external `.bzl`/BUILD values, errors, load order
and child event text/order. Structural siblings, complete carriers and typed
outers are Slug-native. Query/build publication and exact identity bytes remain
deferred.

End in exactly one bounded docs-only design, one uniquely smaller prerequisite
design that returns directly to this audit, or formal REPLAN. Freeze exact
future files, measured semantic/physical caps, terminal algebra, event and
memory ownership, discriminating proof and STOP conditions before any
implementation. Require independent design review and schedule at most one
successor. Do not close M1.
