# Current Slug V2 Packet

Packet: `WP-6-m2-pure-native-family-source-anchor-and-enum-route-evidence`
Milestone: M2 authoritative target configuration
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Role: close exact pinned source anchors and concrete finite-enum routes before
the second corrected family-contract retry.
Predecessor: the corrected family table retained accepted renderer/identity
evidence but reached terminal `REPLAN` on broad or inaccurate source ranges,
grouped enum families, missing scalar-String special-null, and incomplete
default-only runs grammar. Its unaccepted table was discarded.

Produce source evidence only at Bazel commit
`8220c6198837d5c13d53fea211cf3282aa12408a`:

- one exact anchor row for every future family, naming the full source path,
  class/method, inclusive verified line range, returned Java type/value owner,
  and any distinct parameterized subclass boundary;
- explicit scalar `F-Text` `N=None` for annotation default `"null"`;
- one stable `F-Enum-*` row per concrete finite enum family, with converter
  owner, returned enum owner, complete member set, alias/case rule, ordinary or
  overridden renderer, and standard `D(s)`/`E(s)`/`X(s)` route template; and
- one default-only runs record: `D("1")` creates catch-all `.*` with original
  option text `"1"`; deferred `U("+2")` proves positive `Integer.parseInt`
  accepts the plus sign and retains original `"+2"` text.

No enum grouping is allowed. In particular separate every Android converter,
Apple configuration distinguisher, Java classpath mode, and Java one-version
level. `F-Cancel` must list `NEVER`, `ON_FAILED`, and `ON_PASSED`. Include
CompilationMode's enum/toString owner separately from its converter, and use
exact pinned ranges rather than shorthand spans.

This packet must not recreate the discarded family byte table or re-prove the
accepted renderer, reverse UTF-16, list/Entry, timeout, or forced-sharding
identity evidence. It adds no descriptor rows and authorizes no general
runs-per-test occurrence decoder: nondefault numeric and regex occurrences
remain within the eight Java-regex descriptors.

Allowlist:

- `thoughts/shared/plans/slug-v2-subplans/06-analysis-toolchains-and-actions.md`
- `thoughts/shared/plans/slug-v2-subplans/current-packet.md`
- `thoughts/shared/plans/2026-06-26-slug-v2-clean-restart.md`
- `.codex/skills/slug-agent-orchestration/references/routing-log.md`
- `.codex/skills/slug-agent-orchestration/references/routing-history-2026-08.md`

Cap: 260 formatted net documentation lines total, at most 190 in Stage 6. No
family-contract or descriptor table, Java/JVM probe, Rust, fixture, generated
artifact, dependency, registry, command, DICE, or downstream change.

Stop and `REPLAN` if any range/owner/member/alias is unverifiable; a finite enum
still needs grouping or descriptor-specific semantic judgment; `+2` original
text cannot remain a deferred default-source record; accepted evidence must be
reopened; or scope/cap is exceeded.

On acceptance run only
`WP-6-m2-pure-native-family-byte-contract-ledger-retry-2`, built from accepted
renderer/forced evidence plus this source closure. Preserve 287/8/5/41 and
defer contextual/regex/Host/repository conversion, normalization,
checksum/wire implementation, DICE changes, and configured-target cycles.
