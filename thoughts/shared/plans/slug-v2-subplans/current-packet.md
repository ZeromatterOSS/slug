# Current Slug V2 Packet

Packet: `WP-6-m2f-root-toolchain-platform-topology-activation-audit`
Milestone: M4 configured query
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Result: read-only successor audit after analysis-error publication.

## Observable slice

Reconcile the accepted Bazel 9.2 toolchain/delegation graph evidence with the
live configured/null node owner and identify the smallest root-repository
platform, constraint, and selected-toolchain topology that can be activated
before cquery traversal. Separate already retained structural configuration and
analysis toolchain facts from missing graph nodes and edges.

## Ownership and stops

Read the accepted toolchain-topology oracle sections and the live analysis,
loading, identity, and core owners. Produce an exact node/edge/owner table,
external-repository stops, discriminating existing evidence, and bounded packet
caps. Do not edit Rust, tests, fixtures, or oracle payloads in this audit.

Literal `@bazel_tools` remains stopped where real activation requires its
apparent repository route, verbatim upstream content, external glob, or
`@platforms`. Do not synthesize builtin nodes/content. Exclude cquery
traversal/output, new parser work, exact Bazel hash bytes, JVM/Java, CI, and
compatibility behavior. Vendored Buck2 `starlark-rust` remains the sole
Starlark parser/evaluator substrate.

## Validation

Every proposed node and edge must cite accepted Bazel 9.2 evidence and a single
live owner. Distinguish root-only implementable topology from external or
unproven boundaries, preserve exact implicit/tool bits and ordered identity,
and stop with `REPLAN` if no bounded activation slice exists without synthetic
content or a second graph/key/result/cache.
