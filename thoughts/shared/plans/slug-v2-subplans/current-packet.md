# Current Slug V2 Packet

Packet: `WP-4-5-6-generated-repository-file-effect-handoff-application-design`
Milestone: M7A bootstrap-critical command/ruleset breadth
Owners: `04-starlark-loading-and-build-packages.md`,
`05-bzlmod-and-repository-graph.md` and
`06-analysis-toolchains-and-actions.md`
Base: accepted selected repository file-effect producer over `3fe1ab29`

Result: design one bounded demand-only handoff from the accepted selected-call
effect producer through generated repository routing into core's existing
immutable-root publication boundary.

## Accepted predecessor

The producer is independently accepted at +901 production/+747 proof/+1,648
aggregate Rust lines. Physical lines are 194/469/1,402/649/2,094/1,951/119 in
its seven-file order; the loading child hash is `826148e3…` and the other six
match the frozen packet hashes. Full Bzlmod and loading suites and dependent
core check pass. The archive checker has only its existing committed three-
thought-path baseline failure.

The accepted producer owns exactly one selected extension owner plus flattened
repository-call ordinal, reloads and authenticates the root Host rule, invokes
only the admitted Bazel 9.2 string-path `repository_ctx.file` ABI, and returns
an ordered heap-free Bzlmod effect plan. It is callerless. It does not change a
route, request, root, source, package or command.

## Design questions

Trace and freeze the smallest dependency-safe ownership chain for:

1. the existing generated-definition/route owner to retain or recover the exact
   selected owner plus ordinal without canonical-name rescanning;
2. the first demand-side caller of the loading observed producer, preserving
   definition/certificate observations before effect observations;
3. a structural request or route value that carries the complete ordered plan
   without making Bzlmod depend on loading or changing global aggregates;
4. core's immutable materialization boundary to validate every normalized path,
   reject collisions and publish all files atomically with executable polarity;
5. source/package observation after publication, including Need/error/event,
   cancellation, warm reuse and A/B/A restoration; and
6. exact integrity association among semantic route identity, effect bytes,
   immutable root lease and existing source observation.

Choose one smallest implementation successor or a prerequisite `REPLAN`.
Separate route/request identity from filesystem I/O and retain no callable,
module, FrozenValue, Starlark heap/evaluator/context, root lease or open handle
in the effect plan. Do not execute repository rules globally or from lockfile
generation; execution begins only from the requested generated package/source
demand.

The fixture's ordered ASCII bytes and executable polarity remain **exact Bazel
9.2**. Rust valid-Unicode normalized paths, selected owner/ordinal identity,
effect-plan structural identity and immutable-root association are
**Slug-native**. Other repository-context members, nonroot rule definitions,
Label/StarlarkPath arguments, repeated output paths, public query breadth and
exact Bazel configuration/output bytes remain **unsupported/deferred**.

Pinned Bazel 9.2 is behavioral authority. Inspect pinned `../zabel` commit
`c7298478e2e56262a2f438e9c065325744c9f0fc` only for architectural guidance on
natural generated-repository producer/request ownership and immutable effect
handoff. Copy no Zig code, representation, scheduler, digest, manifest/root
layout or output vector.

Docs write authority is exactly canonical/current/Stages 4, 5 and 6/routing at
<=40/<=260/<=160/<=180/<=220/<=30 and <=890 aggregate net lines. Rust, tests,
fixtures, Cargo/BUILD, oracles, APIs and materialization are read-only. STOP a
caller, new key/store/cache/lock/task, eager/global execution, Bzlmod -> loading
dependency, retained Starlark state, direct filesystem write, public behavior,
Java/JVM, M7 closure, M8/M7B and identity-byte work. `REPLAN` before widening.
