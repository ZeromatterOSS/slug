# Current Slug V2 Packet

Packet: `WP-5-m1-external-restricted-visibility-caller-reuse-redesign`
Milestone: M1, one semantic loading spine
Owner: `slug-v2-subplans/05-bzlmod-checkpoint-evidence-3.md`
Role: design/evidence writer after implementation REPLAN
Evidence: accepted oracle/consumer/typed-projection designs and the terminal
reviewed five-file implementation REPLAN in the owner plan.

Change only the canonical plan, owner plan, current-packet manifest, and
orchestration routing records. Inspect the live loading-environment unit-test
harness, query activation trackers, and rejected implementation record.

Resolve the contradictory requirements for a different-external caller and
the prohibition on a second repository route. Freeze a direct request-local
`visible()` discriminator using a synthetic canonical different-external
caller identity against the one existing target route, without declaring,
materializing, loading, or querying a second route. Freeze activation-based
cold/warm evidence that distinguishes DICE reuse rather than only result
equality. Preserve the accepted typed rebind, five production/test files,
220/600/820 caps, remaining evidence matrix, six-row comparator, and every
other stop gate unless live code proves a bounded correction is necessary.

Cap this design at 160 net planning lines. Run no Bazel or Cargo and edit no
Rust, fixture, tool, schema, Cargo metadata, or generated evidence. Obtain
independent latest-text review. Stop with **REPLAN** if either discriminator
requires a second route, new key/owner/tracker, extra Rust file, or cap change.
