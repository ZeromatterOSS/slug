# Current Slug V2 Work Packet

Packet: WP-5-7A-native-probe-observer-authentic-attempt-r1

Status: independently ACCEPTED. The one command below is authorized only after
this exact docs checkpoint is committed and pushed.

## Accepted baseline and question

Commit `64c7d2475` independently accepts the default-off native observer and its
harmless proofs. The exact ignored CLI library test retains the prior authentic
BuildRequest, native API call, terminal projection/publication and output. It now
installs one inherited512byte memfd observer and records bounded phase/activity
scalars. Core production callbacks allocate no memory and perform no I/O, wait,
lock, thread creation or graph formatting. Parent decoding occurs only after the
whole supervised process tree is killed/reaped/drained.

The earlier uninstrumented run at `/tmp/slug-sentinel-demand.8dkd3P/logs`
compiled in48.043s and hit its native wall deadline at15.0093s. It retained
stdout16/stderr0/trace319 bytes, reaped three PIDs, proved ECHILD/no open pipes/no
live group and found no exact abseil open. That result remains INCONCLUSIVE: it
did not reveal a phase, classify unsampled work or explain the historical14GB
allocation.

This packet asks one narrow question: what bounded phase, event-counter and last
sample state does the accepted observer retain during the same authentic attempt?
It does not ask for a successful build, performance comparison, full source
closure, output-conflict proof, deadlock diagnosis or historical memory cause.

## Immutable command and admission checks

After this packet is independently ACCEPTED, committed and pushed, root may run
exactly once from a clean `main` descendant of `64c7d2475`:

`bash tools/v2_oracle/run_payload_demand_probe.sh`

That single shell invocation is consumed even if preflight, self-check,
compilation, staging or native execution fails. Do not retry a failed phase or
invoke `--self-check`, `--compile-only`, Cargo, the ignored test or `strace`
separately. Before the command, require:

- no active Cargo/rustc/linker/Slug/probe process;
- clean worktree and `HEAD == @{upstream}` at the accepted design checkpoint;
- driver SHA-256
  `dec3e6aa523cb601adba01520af8a224463bc90983583f9d09dcbca4c4056a69`;
- ignored probe SHA-256
  `03802a57262ce81599bcf16ba62de69d040a29fcf7b4ea5bd14ce112bd4543de`;
- complete output-conflict R2 and old draft hashes remain exact.

A mismatch is a no-run REPLAN. Do not edit code, Cargo, driver, fixture, source
policy or limits to make admission pass.

## Driver-owned execution envelope

The accepted driver alone owns preflight, harmless finalizer self-checks, one
serialized `slug_cli_v2 --features native-probe-observer --lib --no-run`
compilation and at most one exact ignored-test launch. Compiler timeout is60s;
the native supervisor wall and CPU limits are15s each. The native process tree
has AS2GiB per process and FSIZE16MiB. The existing run-phase wrapper remains58s.

The native child receives an empty/minimal environment with no HOME or
credentials, an isolated user/network namespace, stdout/stderr caps8192 each,
exact-path `openat/openat2` trace cap57344 and raw512 plus decoded telemetry cap
8192. No daemon, live shared-memory read, polling transport, new thread, fallback
fixture, network acquisition or credential-bearing replay is allowed.

Fresh `/tmp/slug-sentinel-demand.XXXXXX` owns the same exact staged recipe:
183 hash-verified registry metadata objects, platforms1.0.0 and rules_shell0.6.1
archives, the rules_shell version patch, preserved-R2 root BUILD/defs, root MODULE
without only the fake-platform override, and explicit file-mirror configuration.
Prior inventory was183 metadata/310070 total bytes. The new inventory must satisfy
the same <=1MiB bound and exact source/hash checks; its digest may change only for
fresh path-dependent framing. The exact abseil mirror path must begin absent.
No cache scan, dependency enumeration, alternate payload or source acquisition.

The subreaper finalizer unconditionally kills the group, reaps all children and
drains/closes pipes. Valid evidence requires `cleanup_complete=true`, ECHILD,
`open_pipes=0`, `group_alive=0`, retained logs and an authoritative post-command
no-survivor check. Incomplete cleanup is INCONCLUSIVE and forbids live telemetry
decoding. EXIT may remove only this invocation's workspace/registry/mirror;
retained logs are evidence.

## Evidence validity and outcome matrix

The driver result is provisional until human review checks supervision, trace,
raw telemetry and decoded JSON together. Any output-cap stop, read/seek/close
error, raw size other than512, unavailable/bad header or frame, invalid selector,
incomplete cleanup or missing log makes all observer conclusions INCONCLUSIVE.
Do not reinterpret unavailable as zero. A valid decoded record must have
`available=true`; report installed PID, disabled/activity-claim flags, overflow,
dropped samples, all six counters, latest committed phase and activity verbatim.

Apply the first matching outcome:

1. `positive-demand`: no supervisor stop, complete cleanup and uncapped trace,
   valid telemetry, plus the exact traced open of the one absent abseil mirror
   path. This proves only that this native attempt demanded that exact path.
2. `sentinel-only-non-demand`: no supervisor stop, raw status0, complete cleanup
   and trace, valid telemetry, no exact open, and the exact
   `SLUG_SENTINEL_NATIVE_SUCCESS_PUBLISHED_0` marker. This proves only successful
   native publication without that exact open in this staged attempt.
3. `bounded-observer-inconclusive`: complete cleanup, valid telemetry, complete
   uncapped trace and no stream-cap or transport-validity failure, but a wall
   deadline, signal, semantic failure, missing success marker or other semantic/
   process stop. Record the last committed phase/activity/counters as bounded
   location evidence.
4. `observer-unavailable-inconclusive`: cleanup is incomplete, any stdout/stderr/
   trace/telemetry output is capped, or telemetry/trace is missing, malformed or
   invalid. Record only the failure receipt that is independently valid; do not
   use observer zeros or partial frames.
5. `pre-native-inconclusive`: preflight, self-check, compiler selection or staging
   fails before launch. Record that no authentic native evidence was produced.

Driver exit0 is necessary but not sufficient for outcomes1-2 because this review
also requires valid telemetry. Driver exit2 is expected for outcome3 and is not a
test failure to repair. Any other exit is classified from the earliest valid
receipt above. Never weaken a driver `inconclusive` result.

## Interpretation limits

Counters are six independent atomic observations, not one coherent snapshot;
never subtract Started/Finished to claim in-flight work. Overflow invalidates the
affected total, and a nonzero dropped count proves sampling contention only.
An activity tag is at most64 raw prefix bytes; truncation is not complete identity.
Selector0 means no committed sample. `activity_claim=1` after forced termination
means a sample may have been interrupted; only the previously committed frame is
authoritative.

Phase Entry/Exit/Aborted and wall/process-CPU timestamps prove only the last
committed boundary in this process. They do not provide inclusive duration,
retired instructions, RSS, a causal leaf, a deadlock classification or the nature
of time after the last sample. CPU loop, scheduler delay, kernel wait, external
wait, teardown and unsampled DICE work remain indistinguishable unless separately
proved. Resource/RSS absence is UNKNOWN, not zero. One warm run supports no
speedup/regression claim.

An exact missing-path open establishes only positive demand for that path, not
global source unavailability or complete CLI closure. No open plus successful
sentinel establishes only non-demand in this staged native attempt. Neither is an
actual CLI/daemon/Bazel run or output-conflict acceptance.

## Close and preserved boundaries

After the command, update this manifest, canonical Live Status, Stage5 summary and
`~/PROGRESS.md` with exact evidence paths/bytes/times/classification; outside this
manifest add at most100 lines. Recheck hashes, forward applicability, archive exact
known-three baseline, clean process state and scoped scratch cleanup. Obtain an
independent terminal evidence review before commit/push. No automatic follow-up
run, code fix, cap change, acquisition or output-conflict restoration is selected.

Preserve `/tmp/slug-conflict-r2.XZJWwv/candidate.patch` at SHA-256
`90c725e40a7aa46f5f0e81112bfe5f91a429679d9bd3824ae7dda9417725d94e`
and `/tmp/slug-sentinel-draft.Lln8y0/candidate.patch` at SHA-256
`8eef40138afa23caa2601b89e6006de40f7e6d139f40124f4c2c430ae5fdf0a2`.
Never inspect/print/copy/commit `~/.bazelrc` or derived credentials. Complete
output-conflict R2 remains preserved/unaccepted and may not be partially restored.
