# Current Slug V2 Packet

Packet: `WP-6-m2n-configured-query-forward-successor-audit`
Milestone: M4 configured query
Owner: `slug-v2-subplans/06-analysis-toolchains-and-actions.md`
Result: read-only audit after configured graph depths 0 through 2.

## Observable slice

Select the next semantically closed configured-query behavior after accepted
single-root noimplicit deps label output and unfactored graph output at depths
0, 1, 2, and unbounded. First determine whether pinned Bazel source plus live
ownership can close a larger-depth boundary without new representation or
oracle work; otherwise prefer a strictly smaller forward consumer.

## Ownership and stops

Reuse the sole configured analysis key, depth-aware closure, authoritative
ordered result edges, full configured/null keys, selected result handles/index,
and cursor-based unfactored renderer. Do not add a graph, copied adjacency,
key, cache, parser, or evaluator representation. Keep factored/default-
implicit external topology, reverse traversal, external labels, exact hashes,
JVM/Java, CI, and multi-root order claims stopped unless a smaller exact owner
and accepted evidence close them.

## Validation

Cite accepted Bazel 9.2 evidence or pinned source and one live owner for every
observable. Preserve Need-before-error, null/full identity, selected-induced
edge filtering, and one-shot/daemon symmetry. Make no Rust, test, fixture,
oracle, or standalone documentation commit in this audit. End with one bounded
implementation packet or `REPLAN`.
