# Current Slug V2 Work Packet

Packet: WP-7-14-m7a-cache-leaf-generated-input-edge-r1
Status: accepted; new cache-leaf generated-input edge declared

## Outcome and compatibility

Verify that the accepted `slug_reapi_cache_v2` protocol build script supplies
its generated Rust output tree to the leaf library's Rustc action in the live
Bazel 9.2 configured action graph rooted at `//app/slug_cli_v2:slug`. Verify
that the retired `slug_reapi_v2` build script is absent and the accepted
`starlark_syntax` LALRPOP producer/consumer edge remains. This is exact
configured build-graph declaration evidence only. It changes no Slug semantic
or compatibility class, runs no build action, and does not prove generated
bytes, compilation, FileWrite runtime behavior, or M7A bootstrap readiness.

The accepted predecessor WP-7-13 landed at `5437a1474`: it pinned authentic
REAPI/Google sources, moved protocol/CAS/AC ownership into the graph-independent
leaf, routed admitted FileWrite through it, and passed focused Cargo and direct
NativeLink cold/AC-hit/ByteStream checks. Stage 10's older 33-package and
five-proto aquery receipts remain historical. The new crate and build-script
owner require their own current configured-input observation.

## Source and ownership

The current root `Cargo.toml`, `Cargo.lock`, `Cargo.Bazel.lock`, `MODULE.bazel`,
`app/slug_reapi_cache_v2/{BUILD.bazel,build.rs,proto/**}` and
`app/slug_reapi_v2/BUILD.bazel` define the live producer and consumer edges.
Bazel 9.2 and rules_rust 0.73 own the generated build-script and Rustc action
shapes. The Bazel action graph is only evidence of declared inputs; it is not
a Slug DICE or runtime fact. Query JSON and analysis stay bounded command
scratch under ignored `target/`; no new retained semantic value or fallback is
created.

## Single observation and acceptance

From clean main `5437a1474`, run one read-only, no-fetch, batch Bazel `aquery`
with `--ignore_all_rc_files`, the explicit
`--@rules_rust//rust/toolchain/channel=nightly` flag, root
`deps(//app/slug_cli_v2:slug)`, and `--output=jsonproto`. Cap it at 30 seconds
with TERM/short KILL cleanup. Record command, elapsed time, exit status,
raw-output SHA-256, stderr and output size. Parse the emitted graph by artifact
ID/depset closure, not by a path substring alone. Record the cache leaf
build-script output-tree artifact ID, its producer action owner, the leaf Rustc
consumer owner and the exact membership path through its input depsets.
Record the protocol source and vendored-protoc inputs of the producer; confirm
that `slug_reapi_v2` has no build-script action. Recheck the unchanged
`starlark_syntax` grammar producer/consumer edge in the same graph. A complete
positive result advances only the new generated-input declaration gate.

If the query emits no complete graph, fails on resolution, or reaches its cap,
record the exact failure and stop this packet without treating absent output as
an absent edge. Do not rerun the same command without a new diagnosis. Do not
compile, invoke an oracle fixture, or execute tests for this analysis-only
packet. No upstream test is needed because this packet observes Bazel's own
configured action graph; generated bytes and compiler behavior remain later
gates. Validation is the source/structure analysis, packet status checker and
`git diff --check`.

Allowlist: this manifest, canonical Live Status, Stage 10's live inventory
delta, and bootstrap-readiness's generated-input row. Preserve all historical
accepted receipts and counts. Commit and push only a complete, reviewable
observation checkpoint; if the observation fails, record the limit and select
a separate successor rather than silently widening this packet.

## Observation receipt (2026-09-16)

The sole `bazel --ignore_all_rc_files --batch aquery
'deps(//app/slug_cli_v2:slug)' --output=jsonproto --nofetch
--@rules_rust//rust/toolchain/channel=nightly` exited 0 in 5.12 seconds,
emitting 8,025,136 JSON bytes and 2,143 actions; Bazel ran zero actions.
Raw JSON SHA-256 is
`ffc50a44648f85b464988eb5dd6c6dd152269f342121a006839d3e8c7b4c0be8`;
the artifact/depset analysis SHA-256 is
`d2c65332bc88f6d97d80e2bc60af7af8ec7bd16723479ecfd6d78ada64c97418`.
The ignored `target/wp714/` scratch retains the command output and parser.

The `//app/slug_reapi_cache_v2:build_script` CargoBuildScriptRun produces tree
artifact 3518 at `app/slug_reapi_cache_v2/build_script.out_dir`; the
`//app/slug_reapi_cache_v2:slug_reapi_cache_v2` Rustc input depset chain
615 → 616 → 617 contains that exact artifact ID. Its producer input closure
has all 15 pinned proto files and eight vendored protoc binaries, with
`SLUG_BAZEL_PROTOC` selecting Linux x86_64. The retired
`//app/slug_reapi_v2:build_script` target/action is absent. The unchanged
`starlark_syntax` build script produces tree artifact 3502, present in its
library Rustc input chain 884 → 885 → 886 with `grammar.lalrpop` as a
producer input.

Bazel warned that the cached external `slug_crates` repository was not up to
date while fetching was disabled. The result establishes the current
first-party generated-input declaration and named protoc selection, not fresh
external-crate resolution, generated bytes, compiler success, or M7A
readiness. No test or build action ran.
