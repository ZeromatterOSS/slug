# Current Slug V2 Work Packet

Packet: WP-7-10-m7a-num-traits-build-script-output-r1
Status: result REPLAN; sole build timed out without generated flags

## Outcome and boundary

Observe the actual generated `_bs.flags` bytes for the CLI-root-reachable
`num-traits 0.2.19` non-tool target by building only its generated
`build_script_build` alias and required dependencies. This is an exact pinned
Bazel 9.2 observation, not Slug behavior admission. It cannot establish full
CLI compiler arguments, all external feature parity, a CLI build, or M7A
readiness. The immediately prior environment query at checkpoint `21cce4fc0`
found the exact non-tool `_bs` action and no aquery-visible wrapper key;
inherited wrapper values and the generated bytes remained unknown.

Freeze clean main `21cce4fc0`, `Cargo.lock` SHA-256
`a19882e78b50a82ed900fce570f4a6aee778553448790eaf08e2e08a591f76d8`,
`Cargo.Bazel.lock` SHA-256
`6335564ccbb3c915d0a2fa23b8aec0d69986911e062b586e13c5ec9812c2f35e`,
`MODULE.bazel` SHA-256
`109585b7b40d274ea912d32c73f54c25cc4bef1bc5e60526e18ac9bfe48d210c`,
and `MODULE.bazel.lock` SHA-256
`bcdda78dd82aab5fd7ab1796985a78322b50178dceed7669c1b20173c2634302`.
The accepted source audit SHA-256 is
`0befd1d6b4187984859ee3a07769a50ff314f092217d8b1d303f5bc14251bb17`;
the accepted action-environment analysis SHA-256 is
`b3e727f268ad66c7ae9c6ba7cd90c067d5ee041a5d69054bc29f2f3375240ae4`.
The generated BUILD makes `build_script_build` an alias of `_bs`; `_bs`
depends on `autocfg 1.5.1`, has `build.rs` as its root, and generates the
action output path recorded by the prior aquery. The pinned build script
requests bare `has_total_cmp`; its probe child inherits stdout, so the
generated file must be observed rather than inferred.

## One bounded build invocation

Use pinned Bazel 9.2.0 SHA-256
`7668a95db1250f12c40407251e4e203b4ec8bf39bc495d2f485b2d8c99048694`
in batch mode with reused
`/tmp/slug-m7a-configured-offline-cquery-output-base`, verified nightly
six-file distdir, nightly toolchain channel, download disabled, lockfile
error mode and ignored rc files. Build only
`@@rules_rust++crate+slug_crates__num-traits-0.2.19//:build_script_build`
with one job and local strategy. Remove `RUSTC_WRAPPER` and
`RUSTC_WORKSPACE_WRAPPER` from the client environment before spawning Bazel
and record their absence. The result describes only this wrapper-scrubbed
invocation; it does not retroactively prove the prior aquery's inherited
environment. Do not run tests or a CLI build.

A supervisor caps this single Bazel spawn at 20 seconds and stdout/stderr at
16 MiB each, uses a new process group with TERM then two-second KILL cleanup,
and records command, exit/stop, elapsed time, stream hashes/bytes, frozen
file hashes, tracked before/after status and process-group cleanup. The
20-second bound is a packet resource limit below the user's roughly
30-second test-scrutiny threshold, not a general test deadline. If it times
out, fails, produces no exact output, or changes frozen/tracked inputs, stop
without retry and preserve the receipt for replan.

On clean success, require a regular, nonsymlink exact non-tool action output
at
`bazel-out/k8-fastbuild/bin/external/rules_rust++crate+slug_crates__num-traits-0.2.19/_bs.flags`
under the reused execroot, with resolved path still under that execroot and
size at most 1 MiB before reading it. Record its raw byte count and SHA-256
and preserve raw bytes in `/tmp` with the receipt. The pinned rules_rust runner
serializes `cargo::rustc-cfg` as `--cfg=<payload>` and copies
`cargo::rustc-flags` payloads verbatim, joining entries with newlines. Classify
the file as having no named feature cfg only when every line is the expected
bare `--cfg=has_total_cmp` (or the file is empty). Report a named feature cfg
only for an unambiguous complete `--cfg=feature=...` line; other forms or
indirect arguments are unknown, not absent. Generated bare cfgs do not alter
the previously observed direct feature list. Do not infer that the five
target crates or other host units now match Cargo, or that `num-traits`
itself compiles.

The tracked allowlist is this manifest, canonical plan status, Stage 10 and
bootstrap readiness. Independent design review precedes execution;
independent final review checks the receipt, exact output provenance and
claim limit. Compilation beyond this build-script target and M7A remain open.
Independent design review `ACCEPT` confirmed the alias/output wiring,
single-invocation bound, exact output-file cap and fail-closed flag
classification. The result must remain scoped to the wrapper-scrubbed run.

## Stopped result

The sole offline build of the exact alias stopped at the 20-second limit
(20.095 seconds elapsed, exit 143 after TERM). Bazel analyzed the target and
reported progress through 211 of 246 actions, mostly while bootstrapping
rules_rust tools; that counter neither proves the `num-traits` `_bs` run
action executed nor predicts completion time. No `_bs.flags` output was
observed. The supervisor confirmed clean process-group cleanup, stream sizes
below caps, scrubbed wrapper variables, and unchanged frozen/tracked inputs.
Receipt SHA-256 is
`5b998a1de1942c4d54d4d1131be659b07467371729df6e042b7e466ccc3f0d74`.
Independent result review `REPLAN` confirmed a resource stop, not a semantic
failure or a named-feature result. The packet forbids a retry. A warmer cache
alone does not justify another build under the user's preference for short,
infrequent checks. Generated flags, compiler arguments, external feature
parity and M7A remain open; continue with read-only/source evidence.
