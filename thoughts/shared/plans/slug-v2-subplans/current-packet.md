# Current Slug V2 Work Packet

Packet: WP-5-7A-source-observation-registration-diagnostic-proof-r2

Status: SELECTED RECOVERY, INDEPENDENTLY ACCEPTED; authorize its exact corrections
and gates only after this docs checkpoint is pushed. The Rust remains unaccepted.

## Accepted predecessors

The boundary audit is accepted in `2a86d108f`; the exact diagnostic design is
accepted in `e8e2c92c2` after correcting built-in source kind versus path node
kind. The observer attempt remains consumed and `bounded-observer-inconclusive`.
No replay, acquisition, output-conflict R2 restoration or historical14GB claim is
available.

## Implementation frontier

The required natural Loading red expectation was added before production. A
Loading `--lib --no-run` preparation then compiled the red source in16.18s; its
focused test executed in0.01s and failed exactly at the old incomplete marker.
Two preliminary launcher attempts ended in0.7s/0.3s before compilation because
Snap's cargo/rustc process tracker is unavailable. The exact installed
nightly-2025-09-14 Cargo and rustc binaries resolved that environment issue.

The unaccepted candidate now contains only the designed five owners. It adds the
borrowed Bzlmod writer, exhaustive scalar helpers/proofs, the private module
declaration and the Loading leaf delegation/expectation. Current pre-format net
scope is303 production/479 proof/782 gross; helper/test files are295/468 lines,
and Loading production/test net growth is6/11, all below the accepted caps.

Bzlmod `--lib --no-run` compiled the candidate in16.27s. Its six focused tests
executed in0.01s: five passed and the source-class proof exposed a synthetic
`local_repository` missing its required `path` attribute. That exact test fixture
was corrected; its named rerun compiled incrementally and passed in4.84s with
0.00s test time.

The first post-production Loading test command was shell-capped15s but spent the
entire allowance recompiling Bzlmod/Loading; no test-start, test result or compiler
error was emitted. Shell elapsed was15.2s including termination overhead. No
Cargo/rustc/linker/test process survived. It must not be retried as a combined
compile/test command or granted a longer test limit.

The first recovery review returned REVISE: boolean disposition projection was not
future-exhaustive; `raw_os_error=None` lacked proof; and the Loading surface lacked
a natural legacy handoff plus direct same-display/lifetime evidence. The five
pre-correction file SHA-256 anchors are:

- `repository_source_observation.rs`: `0da3ea6bd4db6b9d9f9bc1a02d6dea256c422415eeeccc0f8cd5965cfe04888a`
- helper: `ddcdf55113ca8aa397621f1b3ea93176d3603ce5398cc47df57ec9b66b5f6f8e`
- helper tests: `6177dbf40ca20224f8d8b11edfcc4edfee7cd0439d67e733e69a4440601099d8`
- Loading renderer: `e0e9125367eab2f2111fe3f217b81e0dac1c056e0e09061199ec7d095c3bc197`
- Loading tests: `eee82b4e5870ddc06e42103292dd7616a79daca0ebd1b067e7102fbec3d1e8de`

## Goal

Apply only the three review corrections, then validate the corrected six-file
candidate without letting compilation consume a test budget. Separate one final
current-source compile-only preparation from all executions; every subsequent
named test gets a12s shell limit, leaving margin below the absolute15s maximum.

## Authorized work after review and push

1. Recheck the five pre-correction hashes, source scope, no-survivor state and
   clean docs/index baseline.
2. Apply only these reviewed corrections: replace boolean source-class projection
   with nested exhaustive input/disposition matches; add the `raw_os_error=None`
   grammar case and a full-error A/B/A same-display proof differing only in
   omitted lstat scalars; extend `registration_expansion_tests.rs` to return both
   natural legacy and observed canonical SourceObservation errors, then make the
   diagnostic test assert their identical exact leaf output and unchanged Child
   Arc count/final weak release. No production behavior beyond the exhaustive
   source-class rewrite may change.
3. Run rustfmt only on those six Rust owners, timeout12. Record corrected hashes
   and recount400 production/700 proof/1100 gross plus320/600/40/120/80 caps for
   helper/helper-test/Loading-production/diagnostic-test/expansion-test growth.
4. Run exactly one current-source combined compile-only preparation, no tests:
   `cargo test -p slug_bzlmod_v2 -p slug_loading_v2 --lib --no-run`, using the
   exact installed nightly Cargo and `RUSTC`, timeout55. Any compiler error or
   timeout is REPLAN; do not patch/retry in this packet.
5. Only after compile success, run the Bzlmod diagnostic group once with
   timeout12, then the natural Loading SourceObservation diagnostic once with
   timeout12, then the existing Loading `registration_diagnostic_` focused group
   once with timeout12. No command may compile changed source between these tests.
6. Run one combined default `cargo check -p slug_bzlmod_v2 -p slug_loading_v2`
   with the exact toolchain and timeout30. Run diff/cap/credential/archive/hash/
   process hygiene without executing a runtime scenario.
7. Obtain independent implementation/proof review. Only terminal ACCEPT may
   update docs, commit the six Rust owners plus docs and push `main`.

## Proof requirements

- The Bzlmod group must cover four source classes, five top-level kinds, four
  built-in variants and both built-in source kinds, eleven request variants, ten
  materialization kinds, six operations, four node kinds, four observation-error
  forms, all39 I/O kinds, byte-exact non-UTF8 path output and immediate/capped
  writer stops.
- The natural Loading proof must emit the exact canonical leaf and
  `CanonicalRequest: Request.WrongKind ... actual=Directory`, retain recursive
  raw-load prefixes for both legacy and observed handoffs and exclude hidden
  poison. The owner proof must show typed A/B/A inequality/restoration despite
  identical diagnostics when omitted lstat scalars differ. The Loading proof must
  show rendering does not increase the Child Arc count and final drop releases its
  weak reference. Existing Need/outer precedence and generic output/depth tests
  must remain unchanged and the focused Loading group must pass.
- Source inspection must confirm no diagnostic allocation, fallback Debug,
  request/route/graph formatting, unsafe code, I/O, lock, wait, environment read,
  retained field, key, cache, dependency or DICE/semantic change.

## Stops and prohibitions

No test command may exceed12s and no compile/check command may exceed its stated
cap. A timeout, compiler error, test failure, hash/scope drift or survivor is
REPLAN with no automatic retry or limit extension. Do not edit any Rust after the
compile preparation. Do not run the probe, `strace`, CLI scenario, daemon, Bazel,
network, cache scan, source acquisition, replay, broad/whole suite or credentials.
Do not restore any output-conflict R2 section.

Preserve `/tmp/slug-conflict-r2.XZJWwv/candidate.patch` at SHA-256
`90c725e40a7aa46f5f0e81112bfe5f91a429679d9bd3824ae7dda9417725d94e`
and `/tmp/slug-sentinel-draft.Lln8y0/candidate.patch` at SHA-256
`8eef40138afa23caa2601b89e6006de40f7e6d139f40124f4c2c430ae5fdf0a2`.
Never inspect/print/copy/commit `~/.bazelrc` or derived credentials.

Independent rereview ACCEPTS the three exact corrections, six-file cap, prehash
characterization and compile-first/no-edit-after-compile proof sequence.
