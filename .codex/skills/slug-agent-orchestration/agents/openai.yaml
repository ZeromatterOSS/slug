interface:
  display_name: "Slug Agent Orchestration"
  short_description: "Run the Slug V2 plan through reviewed work packets"
  default_prompt: "Use $slug-agent-orchestration: read Live Status, select one packet, route bounded work, and retain root integration; request Sol review when the packet crosses a reserved boundary."
