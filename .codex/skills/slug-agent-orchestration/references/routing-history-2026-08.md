# Slug Agent Routing History: 2026-08

Archived terminal packet records displaced from the bounded live routing log.

| 2026-08-27 | M7A canonical load-route acceptance and package-adapter split | One bounded writer, root full serial validation, independent terminal/correction review, then read-only caller-chain audit; Bazel 9.2 authority and Zabel peer guidance only | accepted apparent-free source/load route; next public DICE boundary spans Bzlmod policy and loading package chains | `85593f300` passes root-unmapped transitive source/listing, independent spec/mapping A/B/A and lifecycle/ownership proof; direct boundary retarget is insufficient because REPO/ignore/source and subtree/`.bzl`/package load form two ownership chains | Implementation `ACCEPT`; successor `REPLAN` to docs-only `WP-4-5-7A-canonical-external-package-loading-adapter-design`, freezing Bzlmod convergence before loading adaptation |

| 2026-08-27 | M7A canonical source/policy fixed-result preflight stop | Root and one read-only architecture worker traced exact source key values, builtin lifetime and exhaustive loading/core consumers before edits | shared zero-copy repository source-result carrier and consumer migration; accepted wrappers retained | Root keys return materialized `HostRepositorySourceFileValue`; canonical keys return builtin-capable `HostRepositorySourceObservation`; DICE values cannot vary by route enum and lawful convergence exceeds the ten-file allowlist | `REPLAN`; no Rust retained; design only `WP-4-5-7A-repository-source-result-convergence-design` before retrying source/policy convergence |

| 2026-08-27 | M7A canonical repository load-route R2 alias-free proof stop | One bounded writer, root lifecycle/hash correction, full serial validation and independent terminal review; Bazel 9.2 authority and Zabel peer guidance only | unchanged six-file canonical source/load route; transitive selected proof only | The 1,174/1,097 candidate passed 13 focused, 576 bzlmod, 352 loading, dependent, CLI and structural gates, but selected successes retained root aliases and selected spec/mapping were not independently discriminated | `REPLAN`; retain no Rust and retry only `WP-4-5-7A-canonical-repository-load-route-implementation-r3` under 1,200/1,450 caps with a root-unmapped transitive registry source/listing proof and separate selected spec/mapping A/B/A rows |

| 2026-08-26 | M7A complete compile-variable acceptance and action-template frontier audit | Root exact native/producer proof, full serial validation, alias tightening and independent review; recursive compile/proxy/toolchain source trace; clean Zabel peer guidance only | proof-only complete compile variables; next proof-only complete action templates | `97faa6e71` accepts native alias, all producer eager identities and ordered set; all six children of `compile_action_templates.bzl` are accepted and its eager surface is imports plus lazy functions | `ACCEPT`; implement only `WP-4-7A-rules-cc-compile-action-templates-complete-loading-proof` under 0/600/600 and invoke nothing |

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-08-28 | M7A target-platform and builtin-mapping ownership stops | Root retained the platform candidate through three correction reviews, terminal implementation review and mapping-design pre-review; Bazel 9.2 authority and Zabel peer guidance only | platform/alias work remains coherent; exact builtin source lacks mapping; first mapping design selected the overbroad route owner | R2-R4 corrected cycle/proofs, terminal review found the missing mapping, then design review showed `HostSelectedModuleRoutesKey` also awaits unrelated registry RepoSpecs/source metadata unlike Bazel's graph-only mapping owner | `REPLAN`; commit no candidate Rust; design only `WP-4-5-7A-builtin-bazel-tools-selected-mapping-design-r2`, extracting one graph-only full-mapping owner before corrected R5 |
| 2026-08-26 | M0/M7A post-universe archive-gate stop | Root ran the required clean-root archive checker after integrating `cb71a302d`; no implementation candidate | checker-only admission of the accepted `app/slug_starlark_v2/**` path | the exact 16-file universe packet could not update the checker's explicit app allowlist, so all three new crate files are falsely classified non-V2; longstanding thoughts rows are unchanged | `REPLAN`; run only `WP-0-4-5-slug-starlark-archive-whitelist-correction` under 0/3/3, then resume the unchanged helper retry |
| 2026-08-09 | M3 `attr` typed attribute-string design | Parallel Terra pinned-Bazel semantics and live V2 representation audits with root source synthesis | not exposed | Source closes whole-value leaf formatting and selector combination semantics, but V2 detaches default position, normalizes ordered values, drops non-label shape, and lacks native/universal attributes | Exact candidate order, correlation, multiplicity, canonical external labels, and total native inventory cannot be reconstructed from current query state; no Rust/fixture/oracle changed | `REPLAN`; design only a focused candidate-order/native-inventory oracle before retrying the cross-crate representation design |
| 2026-08-09 | M3 Rust-native filter/kind implementation | Terra implementation worker, root serial validation, focused Sol lock-edge review, and independent Sol latest-diff review | not exposed | Locked `regex` 1.13.1 implementation passed 30 library, 56 loading, six parser, and two focused CLI/daemon tests plus CLI build/fmt/diff; Cargo changed only the required local direct-dependency edge | The accepted one-edge contract correction was followed by a terminal evidence miss: rendered outputs and the generic helper did not directly exercise regex filtering across multiple callback batches with distinct full provenance | `REPLAN`; retry only with one direct loading-environment multi-batch ID/order/owner/boundary discriminator before unchanged validation and rereview |
| 2026-08-08 | M2 Slug-native configuration identity boundary design | Terra live consumer/constructor audit, root five-domain synthesis, independent Terra identity/cache correction review | not exposed | Structural configuration already owns the root transition while display omits it; four production `first-build` routes, all production Host sources unsupported, configured layout test-only, workspace `bazel-bin` live, aquery absent, and REAPI AC/CAS exact/content-derived | Review required accepted `str(target.label)` cquery bytes to remain exact; corrected projection evidence to internal or separately admitted output and returned `ACCEPT` | `ACCEPT`; implement the complete reviewed `WP-6-m2-slug-native-default-configuration-vertical` after its concrete serialization/cache pre-edit review |
| 2026-08-08 | M2 Rust-only semantic-compatibility direction reset | Explicit user architecture decision, root live-owner audit, Terra scheduling/identity audit, reserved Sol review | not exposed | JVM/Java integration is permanently excluded; Rust-native Host and valid-Unicode regex behavior are admitted; Bazel configuration/output-directory/ActionKey bytes move to later Rust-only conformance while semantic identity and content/CAS integrity remain strict | Reviews required exact/Slug-native/deferred compatibility labels, a five-domain semantic-identity firewall, fail-closed unmodeled inputs, no new `first-build` callers, and opaque-token normalization limited to identity bytes | `ACCEPT`; design only `WP-6-m2-slug-native-configuration-identity-boundary-design`, then review a complete default-configuration vertical before Rust |
| 2026-08-07 | M8 transported BuildBuddy prime output/semantics evidence | Root retained-session validator, two independent Terra lifecycle/design audits, root bounded cleanup | not exposed | Outer/child zero, `DELIVERED`, empty child stderr, fixed `PRIME_SEMANTICS_REJECTED`; one matching temporary root remained beyond 30 seconds despite no matching process or `slugd`, then was safely removed and later audits were clean | No root contents, raw output, home RC, artifact, invocation, or service data was read; reviews quarantined the semantic stage because the surviving root was not identity-bound | `REPLAN`; implement an offline empty-namespace lifecycle guard before any further live probe, then route semantics only from `LIFECYCLE_CLEAN` |
| 2026-08-07 | M8 transported BuildBuddy BEP-stage evidence | Root retained-session validator, Terra pinned-source audit/design, root lifecycle audit | not exposed | Outer/child zero, `DELIVERED`, empty child stderr, fixed `BEP_COUNTER_REJECTED`; Bazel 9.2 source proves successful int32 exit code zero is omitted by ProtoJSON | No live artifact/home/service data read; source review bounded the repair to absent `code` defaulting before unchanged strict validation | `REPLAN` from live counter stage; repair only the Bazel 9 omitted-zero boundary, then schedule new transported BEP evidence |
| 2026-08-07 | M8 transported BuildBuddy prime-stage evidence | Root retained-session anonymous validator, Terra BEP follow-up design, root lifecycle audit | not exposed | Outer and child zero, `DELIVERED`, empty child stderr, fixed `BEP_PHASE_REJECTED`; private root absent afterward, Git clean, no `slugd` | No raw/home/artifact/service data inspected and no code/config change; result localizes only the BEP parser boundary | `ACCEPT` transport and route; implement only the offline BEP fixed-stage discriminator before another live packet |
| 2026-08-07 | M8 BuildBuddy prime-stage live evidence | Root one-shot live invocation, Terra transport replan, root lifecycle audit | not exposed | Frozen CLI issued once from clean `ba003ec3`; terminal yield lost its session ID and therefore exit/stderr/JSON, while the private root later disappeared with clean Git and no `slugd` | No retry, artifact read, home-RC access, raw-output inference, or code/config change; Terra accepted a distinct retained-session envelope packet | `REPLAN`; run only transported live evidence with one child, anonymous bounded validation, and retained session polling |
| 2026-08-05 | M8 BuildBuddy developer-gate design | Parallel Terra live-config and pinned Bazel/BEP audits, root synthesis, and reserved Sol decision review | not exposed | The 43-green/one-expected-red target boundary and Bazel 9.2 `SpawnExec`/BEP evidence vocabulary are closed, but the root has no `.bazelrc`, named remote profiles, endpoints, instance, or executor platform | Review rejected inventing hosted/on-prem connection and platform values; no remote command, config, evidence, code, lock, or credential was accessed or changed | `REPLAN`; obtain an explicit user-reviewed repository-safe connection/platform decision before separately designing cache-only and RBE implementation |
| 2026-08-05 | M2 pure native value/default/rendering kernel retry 4 | Two serial Terra test writers, root literal-partition completion/cap compaction, and independent Terra pre-production review | not exposed | Test-only Phase 1 reached an active literal 287/8/5/41 partition and exact 45/13/6/2 counts within cap, but its behavior matrix contradicted accepted Runs `+2`, used tautological strings, and hid missing discriminators behind undefined disabled helpers | The test-first gate worked: production was never authorized, and root discarded the entire unaccepted tests diff back to clean `28e9ddc9` | `REPLAN`; retry 5 keeps the gate but routes mechanical test transcription to root and Terra to review-only until matrix acceptance; configured cycles remain deferred |
| 2026-08-05 | M2 pure native value/default/rendering kernel retry 3 | One Terra implementation writer, root exact-ledger audit, and independent Terra stop review | not exposed | The seven-file draft passed crate test/check, format, and GNU-Windows check, but its shallow tests omitted frozen privacy, empty comma/set, Env, shard, Runs, and Fission routes and production contradicted all six | Independent review confirmed the zero-correction stop; root discarded all unaccepted Rust with `apply_patch` and restored clean `fdb5fdb5` | `REPLAN`; retry 4 uses an unusual serial test-matrix-first gate with independent review before any production edit; configured cycles and all contextual work stay deferred |
| 2026-08-04 | M2 pure native family source-anchor and enum-route evidence retry | Terra prevalidated exact-table writer, root source/cap audit, and independent Terra terminal pinned-range review | not exposed | The 60-line source-only retry binds 34 exact family/value/inherited-converter anchors and 16 separate finite-enum member/route rows, including String special-null and runs `D("1")`/deferred `U("+2")`; no unadmitted family remains | Root stopped and cleaned one hung partial-clone scope grep before any edit, then the writer rebuilt from frozen facts using local pinned objects; review accepted every full path/class/method range, member set, scope, count, and deferral | `ACCEPT`; synthesize only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-2` from accepted evidence, without source discovery or descriptor rows |
| 2026-08-04 | M2 pure native family source-anchor and enum-route evidence | Terra exact-source table writer, root pinned-range audit, and independent Terra correction plus terminal rereview | not exposed | The 63-line source-only table split every enum, recorded String special-null and runs `+2`, removed an unadmitted family, and corrected the first exact-range review | Terminal review still found class/method span mismatches for Shard, Fission, EmptyList, TestTimeout, and PerLabelOptions after the correction pass; all unaccepted Stage 6 text was discarded | `REPLAN`; retry `WP-6-m2-pure-native-family-source-anchor-and-enum-route-evidence-retry` with the five ranges frozen before family-contract retry 2 |
| 2026-08-04 | M2 corrected pure native family byte-contract ledger retry | Terra corrected family writer, root source/inventory audit, and parallel independent Terra terminal source/mechanical reviews | not exposed | The 66-line draft retained accepted renderer/forced evidence and correctly split lists, assignment defaults, timeout limit/cases, UTF-16 set order, and structural forced equality, but broad or inaccurate source spans and grouped Android/Java enums still prevented mechanical attachment | Terminal review also found missing scalar String `N=None` and runs `+2` original-text grammar; the corrected packet's source-closure stop fired and all unaccepted Stage 6 text was discarded | `REPLAN`; run only `WP-6-m2-pure-native-family-source-anchor-and-enum-route-evidence`, then a second family-contract retry; no descriptor rows or Rust |
| 2026-08-04 | M2 forced-sharding identity evidence | Parallel Terra pinned-Bazel and live-Slug/Buck2 audits, root source-chain synthesis, one Terra Stage 6 writer, and independent Terra terminal review | not exposed | Fresh forced objects are raw-identity unequal and shallow clones preserve their reference, but identical cache text yields identical BuildOptions checksum/equality/hash, checksum-cache representative, interned BuildConfigurationKey, ConfiguredTargetKey, and structural Slug/DICE warm key | Review accepted source-only Outcome 1 without a JVM probe: structural `Forced(i32)` is exact for semantic configuration identity; direct OptionsBase/BuildOptions.matches identity remains deferred parser/command behavior, and no counter/interner/pointer identity is authorized | `ACCEPT`; resume corrected `WP-6-m2-pure-native-family-byte-contract-ledger-retry` with structural forced-count equality |
| 2026-08-04 | M2 pure native family byte-contract ledger retry | Terra compact family-ledger writer/source audit, root exact-source review, and parallel independent Terra representation/family audits | not exposed | The 61-line draft covered requested families and exposed that fresh `TestShardingStrategyForced` values have Java object identity equality despite identical count/cache text because the class has no equals/hash override and `OptionsBase.equals` uses `Objects.equals` | Reviews also found combined allow-empty/reject-empty list grammars, missing repeatable assignment `N=[]`, incomplete timeout split/empty-token discriminators, and non-mechanical finite enum owners. The new forced identity boundary contradicts the assumed structural native variant, so all unaccepted Stage 6 text was discarded | `REPLAN`; run only `WP-6-m2-forced-sharding-identity-evidence` before any family/descriptor ledger or Rust |
| 2026-08-04 | M2 Java/Guava renderer authority evidence retry | Terra one-file evidence writer/probe, root exact-byte and transcript audit, and independent Terra terminal review | not exposed | A 95-line Stage 6 ledger binds Bazel's exact Zulu 25.0.2 runtime, Java SE 21 contracts, pinned Guava 33.5.0 JAR SHA/source tag, records, collections/maps, escaping, durations, lowercase timeout keys, and Bazel overrides; reverse input units `E000,D800 DC00` pass through `distinct().sorted().toList()` to `D800 DC00,E000` and bracketed cache bytes | Root corrected Java escape notation, the single-backslash retained String versus doubled field bytes, and added the explicit UTF-16 matrix row; review accepted all owners, bytes, runtime/compiler separation, cleanup, scope, and no-JVM boundary | `ACCEPT`; run only `WP-6-m2-pure-native-family-byte-contract-ledger-retry`, with no descriptor rows or Rust |
| 2026-08-04 | M2 Java/Guava renderer authority evidence | Terra runtime/source/probe writer, root provenance and byte audit, one Terra correction probe, and terminal independent Terra rereview | not exposed | Bazel's Zulu 25.0.2 runtime, Java SE 21 contracts, pinned Guava 33.5.0 JAR/source, records, maps, escaping, lowercase timeouts, durations, and Bazel enum/string overrides all matched, but the UTF-16 row inserted U+10000 before U+E000 and therefore did not discriminate sorting | The bounded correction fixed list brackets and missing CompilationMode/StripMode/PlatformType/TestTimeout owners; terminal review then proved its purported natural-sort input was already ordered. All unaccepted Stage 6 text and temporary Java/JAR/source artifacts were discarded | `REPLAN`; retry `WP-6-m2-java-guava-renderer-authority-evidence-retry` with reverse-order actual scalars through the distinct-then-sort path; no Rust or JVM architecture authority |
| 2026-08-05 | M8 atomic fixture payload migration preflight | One Terra Python writer, root Rust/Bazel integration draft and serial Cargo preflight, then Terra clean-baseline stop audit | not exposed | Draft graph-output passed 5/5 and loading-query 55/55, but CLI retained its pinned broken-Bzl diagnostic failure and server retained two scratch-only missing-package failures; clean focused reruns reproduced all three, and Stage 5 already records them | Root fixed three draft guard lifetimes, preserved the Bazel-authoritative diagnostic assertion, interrupted the writer before deletion, and removed the complete unaccepted payload/helper/Python/Rust/BUILD/TOML diff back to clean `9344ea0d` | `REPLAN`; correct only four missing server BUILD markers, then design Host-Bzl diagnostic parity before retrying the unchanged atomic migration |
| 2026-08-05 | M8 fixture payload compile-input evidence | Root isolated Cargo/Bazel probe, Terra rules_rust audit, and independent Sol successor review | not exposed | The shared helper literal `include_bytes!("../../v2_fixture_payload/fixtures.payload")` passed the same Cargo and Bazel test; Bazel `srcs`/`compile_data` supplied consumer/helper/payload to Rustc and runfiles omitted helper/payload | The prior execroot-relative env proposal was discarded; the accepted literal resolves from the helper source identically, has no cfg/platform branch, and every temporary probe/Cargo/lock/target file was removed before docs | `ACCEPT`; implement only the one-commit `WP-10-m8-bazel-canonical-fixture-payload-migration-implementation` with the exact relative literal and frozen atomic caps |
| 2026-08-05 | M8 sole-canonical fixture payload migration design | Parallel Terra Python/oracle and Cargo/Bazel/platform audits with root exact-format and atomicity synthesis plus independent Sol correction reviews | not exposed | The fixed 14-workspace corpus is 112 directories/163 files/24,939 bytes with 403 mutation-free commands; one 50,103-byte length-framed raw payload can preserve every path/mode/byte and potentially serve all Python/Cargo/Bazel consumers | First review rejected serial commits, ambiguous path/projection bytes, implicit compile ownership, an unproved native race claim, and unmeasured caps; correction froze those, but rereview proved `$(execpath)` remains relative to the execroot while `include_bytes!` resolves from the helper source | `REPLAN`; prove only `WP-10-m8-bazel-fixture-payload-compile-input-evidence` in a removed temporary probe before rescheduling any payload, consumer switch, target activation, or deletion |
| 2026-08-05 | M8 nested-fixture immutable snapshot design | Parallel Terra Bazel ownership/drift audits with root canonical-source alternative synthesis | not exposed | A snapshot is a declared compile input, but Bazel cannot mandatorily compare it to 105 nested source packages without the same no-follow watch hole, graph-changing package exports, or skippable ambient/Cargo/CI enforcement | Making the payload sole authority avoids drift but changes Python `copytree` oracle ownership, direct Cargo paths, and 14 fixture trees; review classified that as a separate broad provenance migration, not a snapshot correction | `REPLAN`; design only `WP-10-m8-bazel-canonical-fixture-payload-migration-design`, with no deletion/payload/harness/application edit until the full consumer and replay boundary is accepted |
| 2026-08-05 | M8 nested-fixture ownership redesign | Parallel Terra consumer, pinned-Bazel repository/invalidation, and adversarial remote/platform audits with root synthesis plus correction rereviews | not exposed | The exact union is 14 workspaces/112 directories/163 regular 0644 files with no symlinks/case collisions, but `watch_tree` follows directory links while watched `readdir` hashes names only and watched `File` collapses directory and directory-symlink identity to `DIR` | First review rejected recursive watching; the corrected per-directory design still permits a same-shaped directory-symlink replacement to evade reevaluation, exhausting the second material ownership correction | `REPLAN`; design only `WP-10-m8-bazel-nested-fixture-snapshot-design`, preserving the valid length-framed compile-embedding idea but accepting no duplicate until deterministic no-follow drift enforcement is frozen |
| 2026-08-05 | M8 query unit PreparationRestart design | Parallel Terra live DICE/Need and pinned-Bazel caller audits with root synthesis and independent Terra design review | not exposed | The one-route canonical fake-caller expectation remains oracle-authoritative; external lookup correctly Needs routed `/workspace/dep/.bazelignore`, while the direct test epoch alone omits its Missing Lstat | The sentinel is valid transient Need propagation, not a production error; review rejected a retry loop, second route, bypass, DICE change, or expectation change and accepted one test-input line | `ACCEPT`; implement only `WP-10-m8-bazel-query-unit-ignore-observation-correction`, then map the green 28-case crate-mode target separately |
| 2026-08-05 | M8 Bzlmod proxy-span repair design | Parallel Terra pinned-Bazel, starlark-rust, and consumer audits with root synthesis and independent Terra design review | not exposed | Bazel 9.2 `Eval`/`ModuleFileGlobals` store the call `(` as a point Location; starlark-rust and `nonroot_span` already produce exact `2:22`, `3:10`, and innate `5:5` zero-width spans with logical include identity | The clean 277/278 failure is four stale full-call test expectations, not a production span defect; review rejected AST/source/representation/DICE changes and accepted a four-literal correction | `ACCEPT`; implement only `WP-10-m8-bazel-bzlmod-caller-location-expectation-correction`, then map the now-green 278-case crate-mode target separately |
| 2026-08-05 | M8 core host-tool owner design | Parallel Terra source/test and rules_rust/runfiles audits with root boundary synthesis | not exposed | Core owns 141 unit plus 13 runtime-integration cases; exactly four unit cases invoke bare `git`/`tar`, while all 13 runtime cases need only unique temporary workspaces and source cfgs | No pinned cross-platform Git/tar executable labels exist; rust_test data/env cannot derive a PATH directory from rootpath, and Windows may be manifest-only, so ambient PATH, wrappers, host discovery, writable runfiles, and platform exclusion are rejected | `REPLAN` only the core unit target pending a declared-path Linux/macOS/Windows supplier; implement the independent `WP-10-m8-bazel-core-runtime-test-implementation` with no host-tool adapter |
| 2026-08-05 | M8 Bzlmod lockfile scratch semantic adapter | One Terra two-file worker, root isolated serial validation, and independent Terra lifecycle/platform review | not exposed | Runtime `TEST_TMPDIR` makes the one writable lockfile case hermetic under Bazel while the exact manifest-relative Cargo fallback remains; Bazel and Cargo pass 11/11, GNU-Windows compiles, and all three lock hashes stay stable | Root stopped duplicated non-isolated Bazel clients before acceptance, reran the required `--ignore_all_rc_files` command once, and review accepted the unchanged suffix/pre-clean/create/write/no-cleanup lifecycle at 14 net lines | `ACCEPT`; reuse the runtime-test-scratch/Cargo-fallback pattern only for source-owned test helpers with the same lifecycle, and design `WP-10-m8-bazel-core-host-tools-design` before mapping core tests |
| 2026-08-05 | M8 Bzlmod lockfile scratch design | Terra rules_rust/source audit with root pinned-source synthesis | not exposed | Exactly one of 11 cases writes under a compile-time manifest-relative helper; rules_rust overwrites rustc CARGO_MANIFEST_DIR with compile-action `${pwd}`, and native rust_test has no chdir owner | Runtime env cannot rewrite `env!`; rustc-env-file, relative CWD, runfile writes, and platform wrappers are stale, non-hermetic, or nonportable | `REPLAN` metadata-only mapping; implement `WP-10-m8-bazel-bzlmod-lockfile-test-semantic-adapter-implementation` with TEST_TMPDIR preference plus byte-preserved Cargo fallback in one test source/one BUILD at 45 implementation/100 total lines |
| 2026-08-05 | M8 Bzlmod fixture-free tests | One Terra one-BUILD worker, root exact-count/baseline reproduction, and reserved Sol REPLAN review | not exposed | Ten integration targets pass Bazel and Cargo 175/175; the unit target reproduces its known clean-baseline span failure 277/278 while its current-exe child test passes | Review rejected a red or filtered unit target and corrected a duplicate-glob count: Bzlmod remains 278+186=464, split into 278 unit, 175 green integrations, and 11 lockfile cases | `ACCEPT` the ten integrations and `REPLAN` the unit target; design only `WP-10-m8-bazel-bzlmod-lockfile-scratch-design` before mapping the final 11 cases |
| 2026-08-05 | M8 loading tests | One Terra one-BUILD worker, root exact-count/lock validation, and independent Terra platform/scratch review | not exposed | Six private targets pass credential-free nightly Bazel and serial Cargo with 117 Linux-active cases from 118 source-declared cases; all three lock hashes remain stable | Exact dependency labels suffice, synthetic scratch workspaces pass in the sandbox, and the one Windows-only unit case remains cfg-owned with no platform exclusion or path adapter | `ACCEPT`; attempt `WP-10-m8-bazel-bzlmod-fixture-free-tests-implementation`, correctly 453 cases in eleven targets while the 11-case lockfile scratch boundary remains deferred |
| 2026-08-05 | M8 REAPI integration test | One Terra one-BUILD worker, root exact-dependency correction and serial validation, then focused Terra correction review | not exposed | One private target passes credential-free nightly Bazel; Cargo reports 13 passed/one ignored with the endpoint unset, generated REAPI ownership unchanged, and all three lock hashes stable | The first compile proved ignored `#[tokio::test]` code still needs Tokio metadata; one exact label fixed it without a broad helper or service/env/data/tool activation | `ACCEPT`; map only `WP-10-m8-bazel-loading-tests-implementation`, preserving platform cfgs and synthetic scratch workspaces in one BUILD file at 190 metadata/docs lines |
| 2026-08-05 | M8 analysis tests | One Terra one-BUILD implementation, root serial validation, and independent Terra latest-diff review | not exposed | Five private targets pass credential-free nightly Bazel and serial Cargo 24/24; all three lock hashes remain stable | Unit reuse plus four source-owned integrations have exact direct/dev dependencies and no fixture/env/data/tool/process adapter; 81-net/190-line and structural gates pass | `ACCEPT`; implement only `WP-10-m8-bazel-reapi-integration-test-implementation`, preserving the ignored NativeLink case and accepted generated library output |
| 2026-08-05 | M8 query parser test | One Terra one-BUILD implementation, root serial validation, and independent Terra latest-diff review | not exposed | One private source-owned parser target passes credential-free nightly Bazel and serial Cargo 6/6; all three lock hashes remain stable | The unit target is absent at its semantic REPLAN, loading-query is absent at its fixture REPLAN, and no dev/fixture/env/data/tool/filter adapter enters the 30-net/80-line packet | `ACCEPT`; implement only `WP-10-m8-bazel-analysis-tests-implementation`, one unit plus four integration targets/24 cases in one BUILD file at 190 metadata/docs lines |
| 2026-08-05 | M8 fixture-free query tests first attempt | One Terra one-BUILD implementation, root Bazel/Cargo reproduction, and independent Terra boundary review | not exposed | The six-case parser target passes, but the crate-mode target fails the same `PreparationRestart` case 27/28 under both Bazel and Cargo `--lib`; prior clean-HEAD evidence pins it at `f9f3c3d8` | A red target, expected-failure/filter adapter, or Rust semantic repair is outside Gate C; loading-query's 53 cases remain independently fixture-blocked | `REPLAN` the 34-case packet; implement only `WP-10-m8-bazel-query-parser-test-implementation` as one source-owned target at 80 metadata/docs lines, deferring the entire unit target |
| 2026-08-05 | M8 workspace unit test | One Terra one-BUILD implementation, root serial validation, and independent Terra latest-diff review | not exposed | One private crate-mode target passes credential-free nightly Bazel; Cargo passes 39 Linux-active cases from 41 source-owned cases, and all three lock hashes remain stable | Generated `normal_dev` aliases/deps add only Tokio; two Windows cases remain cfg-owned, and no source/env/data/tool/platform adapter or normal-dependency restatement enters the +9-net/80-line packet | `ACCEPT`; implement only `WP-10-m8-bazel-query-fixture-free-tests-implementation`, 28 unit plus six parser cases while loading-query remains whole-target `REPLAN` |
| 2026-08-05 | M8 events and identity tests | One Terra two-BUILD implementation, root serial validation, and independent Terra latest-diff review | not exposed | Five private targets pass credential-free nightly Bazel; serial Cargo passes the same 9+1+15+2+2 cases, and all three lock hashes remain stable | Crate-mode reuse and three source-owned integrations need no dev edge, fixture, env, data, tool, platform exclusion, or process adapter; 43-net/140-line and structural gates pass | `ACCEPT`; implement only `WP-10-m8-bazel-workspace-unit-test-implementation`, one 41-case crate target with its exact dev-only Tokio edge at 80 metadata/docs lines |
| 2026-08-05 | M8 simple V2 integration tests | One Terra three-BUILD implementation, root serial validation, and independent Terra latest-diff review | not exposed | Six private standalone targets pass under credential-free nightly Bazel; serial Cargo passes the same 2+22+16 cases, and all three lock hashes remain stable | Direct dependency sets exactly match the six source imports; archive, scope, 72-net/180-line cap, and diff gates pass with no unit/suite/adapter/input expansion | `ACCEPT`; implement only `WP-10-m8-bazel-events-identity-tests-implementation`, five fixture-free targets/29 cases in two BUILD files at 140 metadata/docs lines |
| 2026-08-05 | M8 transitive V2 test boundary | Three parallel Terra live package/test audits with root mechanical reconciliation and packet synthesis | not exposed | The 13 non-CLI packages own exactly 592 unit plus 413 integration cases in 33 standalone crates; fixture-free, scratch-path, host-tool, generated-output, platform, subprocess, and ignored-service boundaries are partitioned | Gate C0 recurs for query loading and inseparable server units; Bzlmod lockfile needs writable manifest-relative scratch, core unit tests need hermetic `git`/`tar`, and the ignored REAPI service case must stay unactivated | `ACCEPT`; implement only `WP-10-m8-bazel-simple-v2-integration-tests-implementation`: six pure integration targets/40 cases in three BUILD files at 180 metadata/docs lines |
| 2026-08-05 | M8 CLI library unit test | Root one-target implementation and serial validation with reserved Sol latest-diff review | not exposed | One private small `rust_test(crate = ":slug_cli_v2")` passes under credential-free nightly Bazel and serial Cargo; all three lock hashes remain stable | The target inherits the accepted production source/dependency graph and adds no integration source, fixture, env, data, platform, binary, process, or daemon adapter | `ACCEPT`; design only `WP-10-m8-bazel-transitive-v2-test-boundary-design` while both CLI integration crates remain at the accepted C0 `REPLAN` boundary |
| 2026-08-05 | M8 CLI test runfiles design | Terra live test/fixture inventory, root rules_rust/env audit and isolated Bazel 9.2 source-directory probe, then Terra boundary rereview | not exposed | CLI owns one library unit case plus 39/3 integration cases; the integrations reference 14 fixture workspaces, 163 files, and 105 nested Bazel packages; rules_rust can provide the binary env exactly but has no complete nested-package fixture owner | Root glob ownership stops at subpackages; explicit source-directory export fails action traversal, while its possible local runfile symlink is untracked for incremental invalidation and unsupported remotely; package-local exports alter oracle inputs/targets and the copy/ambient/Windows workarounds are rejected | `REPLAN` both CLI integration binaries; implement only `WP-10-m8-bazel-cli-library-unit-test-implementation`, then inventory independent transitive-package tests pending a deliberate fixture/runfiles redesign |
| 2026-08-05 | M8 Bazel CLI production graph and protoc bridge | Terra BUILD/bridge workers, root rules_rust/Bzlmod correction and serial validation, and reserved Sol latest-diff review | not exposed | `48cf4bb7` gives exactly 14 V2 packages to `//app/slug_cli_v2:slug`; Bazel sandboxed REAPI generation and the nightly production build pass, serial Cargo REAPI/CLI checks pass, all three lock hashes are stable, and the handwritten net is 445 lines | Review corrected the missing action input, replaced impossible transitive hub aliases with stable Bzlmod apparent repository names, added direct `platforms` visibility, fixed Rust path typing, and made `src/main.rs` binary-only | `ACCEPT`; design only `WP-10-m8-bazel-cli-test-runfiles-design` before mapping the library unit target or either CLI integration binary |
| 2026-08-05 | M8 Bazel CLI production graph first attempt | One Terra 14-package BUILD writer, root sandbox/source audit, Terra bridge design, and reserved Sol review | not exposed | All 14 V2 packages analyzed and compiled through the REAPI build-script launch, which then exposed a stale absolute protoc path embedded by the vendored platform crate | Review accepted stable apparent names, exec-platform selection, and a private Bazel override with Cargo fallback, but found that rules_rust 0.73 does not carry `cargo_build_script.tools` into the runner action inputs; root audit also proved transitive platform crates cannot publish hub aliases | `REPLAN`; run only `WP-10-m8-bazel-cli-production-protoc-bridge`, importing the eight generated repositories under stable apparent names and carrying their raw files through data while retaining the selected exec tool edge |
| 2026-08-04 | M2 pure native family byte contract | One Terra 75-line family-contract writer, root source/byte audit, and two independent Terra JDK/enum renderer reviews | not exposed | The family-only draft eliminated descriptor breadth and named Bazel/Java renderer chains, but exact Guava list/entry, record, and map-renderer authority remained incomplete | Pinned source also corrected TestTimeout map keys from presumed uppercase to its explicit lowercase `toString`; exact enum ranges, executable escaping/UTF-16 bytes, and full JDK renderer chains still failed the packet's hard stop; all unaccepted Stage 6 text was discarded | `REPLAN`; run `WP-6-m2-java-guava-renderer-authority-evidence` before retrying any family or descriptor ledger; no Rust authority |
| 2026-08-04 | M2 pure native converter source-closure ledger | One Terra 287-row pinned-source ledger writer, root source/structure audit, and two independent Terra family/provenance correction reviews | not exposed | The draft mapped all 287 pure descriptors to compact value families with verified declaration lines and preserved 287/8/5/41, but its corrected 388-line family/row ledger remained byte-incomplete | Review fixed outer owner, aliases, casing, timeout splitting, and initial enum provenance, then found missing exact enum converter/value/renderer chains, EnvVar/Duration bytes, executable UTF-16 cases, and default discriminator definitions; the entire unaccepted Stage 6 diff was discarded cleanly | `REPLAN`; run `WP-6-m2-pure-native-family-byte-contract-ledger`, then mechanically map descriptors in a later packet; no Rust authority |
| 2026-08-04 | M2 pure native value/default/rendering kernel retry | One Terra clean retry writer/source auditor, root stop enforcement, and two independent Terra successor-boundary audits | not exposed | The rebuilt seven-file draft incorporated the frozen timeout/duration/strip/fission/runs/Void/DottedVersion corrections but stopped before validation when pinned source disproved generic lowercase enum retention/rendering | Ordinary Java enums retain typed identity and render uppercase names while only selected enums override lowercase; BoolOrEnum, forced sharding, and PlatformType also reject one generic enum/string model; the whole unaccepted retry diff was discarded cleanly at `368f8fa1` | `REPLAN`; produce a documentation-only 287-row pure converter/value/equality/rendering source-closure ledger before any third Rust attempt |
| 2026-08-04 | M2 pure native value/default/rendering kernel | One Terra seven-file implementation/correction worker, root serial validation and pinned-source review, plus two independent Terra source/representation reviews | not exposed | The corrected draft reached 13 passing crate tests and passed check, formatting, archive, scope, cap, and diff gates while retaining the 287/8/5/41 route and closed CompactString/Arc/Allocative representation | The permitted correction fixed timeout parsing/rendering, StripMode, empty fission, runs-seed, and Void behavior; a terminal audit then found DottedVersion integer-overflow and descriptive-underscore misses, triggering the second-correction stop; all unaccepted Rust was discarded cleanly | `REPLAN`; retry the same seven-file kernel only after freezing the complete source discriminator set, with no new oracle, representation, context, normalization, or cycle semantics |
| 2026-08-04 | M2 native value cohort and rendering design | Terra pinned-source writer/audit, parallel Terra live representation audit, root source corrections, and reserved Terra latest-text review | not exposed | All 341 descriptors route disjointly to 287 pure, eight Java-regex, five Host, and 41 repository/loading paths; 45 repeat/13 old-name/six expansion/two implicit rows remain orthogonal command metadata; source pins null/empty/default/rendering and convert-before-normalize boundaries | Review moved regex-capable `runs_per_test` out of the pure cohort while preserving its numeric default seed, deferred label-bearing P/C normalization, and selected CompactString/Arc/Allocative structural values with Java UTF-16 ordering and no runtime registry map | `ACCEPT`; implement only `WP-6-m2-pure-native-value-default-and-rendering-kernel` in seven crate files at 1,550 production/1,250 test/2,800 total lines |
| 2026-08-04 | M2 native configuration metadata/cache grammar | One Terra eight-file implementation/cap-correction worker, root exact-ledger and serial Rust validation, and independent Terra retained-representation review | not exposed | `b043d54d` adds a zero-dependency static 17-class/341-option registry, independent 341-row expected table, and exact outer cache-field grammar at 446 production/600 test/1,047 total lines; four tests, doctests, check, format, archive, source comparison, scope, and diff gates pass | Review compressed the first 8,529-line draft, corrected two focused assertions, preserved the ignored lockfile and archive allowlist, and rejected context-free whole P/C/T because label-bearing normalization can hide invalid inputs | `ACCEPT`; design only `WP-6-m2-native-value-cohort-and-rendering-design` at zero Rust/720 docs before any dynamic typed values |
| 2026-08-04 | M2 Bazel 9 target-configuration input ledger | One Terra 341-row native-ledger writer, parallel Terra non-native graph and live-owner audits, root synthesis, and reserved Terra architecture review | not exposed | Pinned Bazel 9.2 yields 17 FQCN-ordered classes/341 unique options with complete descriptor metadata and cache grammar; non-native RC/Host/repository/platform/Starlark/scope/project/transition inputs now each have one serial owner | Review accepted a new lowest-level configuration crate but rejected executing all converters context-free: AutoCPU needs Host facts, label converters need repository context, and command/Starlark semantics belong later; cycle semantics remain user-deferred | `REPLAN`; implement only `WP-6-m2-native-configuration-metadata-and-cache-grammar` in eight root/new/archive-guard files at 2,400 production/1,400 test/3,800 total lines; keep the ignored workspace lockfile untracked |
| 2026-08-04 | M2 general target-configuration input-chain design | Parallel Terra pinned-source, live command/DICE/analysis, and reserved architecture audits with root synthesis | not exposed | Bazel 9.2 registers 17 native `FragmentOptions` classes containing 341 cache-key options, then incorporates canonical Starlark values/scopes and graph-derived host/platform/mapping inputs; live Slug has raw ignored/rejected flags, no shared wire identity or producer, and constructs opaque `first-build` roots before the command transaction | Review corrected the predecessor's fourteen-class premise, rejected one broad Rust packet, and split typed normalization, wire identity, graph inputs, Starlark inputs, and transactional key integration; configured-target cycles remain explicitly deferred by user approval | `REPLAN`; run only `WP-6-m2-bazel-9-target-configuration-input-ledger` at zero Rust/680 documentation lines before selecting the first implementation packet |
| 2026-08-04 | M2 action-query identity evidence | One Terra isolated fixture writer, root raw/structured identity audit, parallel Terra pinned-source and feasibility reviews, and independent latest-fixture review | not exposed | `f00e99db` pins 18 retained Bazel 9.2 text/jsonproto commands at 227 authored lines: configuration changes checksum/output root but not FileWrite key; platform and content change the key; output name changes only the artifact path; every C0/C1, P0/P1, content, and path restoration is exact | Review replaced registration-order platform selection with distinct constraints and one required-toolchain compatibility mutation, corrected the full default-exec-group FileWrite source chain, bounded the generated-record cap, and rejected the false assumption that output path enters FileWrite ActionKey | `ACCEPT`; design only `WP-6-m2-general-target-configuration-input-chain-design`, with configured artifacts/platform/ActionKey/aquery still serially gated |
| 2026-08-04 | M2 implicit DefaultInfo decoder implementation | One Terra two-file worker, root serial full-analysis/cross-target validation, and independent Terra latest-diff review | not exposed | `7c6eeae5` conditionally appends the existing empty default after successful Starlark provider decoding at 8 production/37 test/45 total lines; custom retrieval, empty default/outputs/actions, explicit write-action behavior, and strict generic rejection all pass | Full analysis, focused build-API/analysis, formatting, GNU-Windows no-run, diff/scope/cap gates pass; review accepted unchanged strict collection and no representation/utility change | `ACCEPT`; resume `WP-6-m2-positive-string-build-setting-transition-implementation` from the clean normalized decoder baseline |
| 2026-08-04 | M2 implicit DefaultInfo decoder design | Parallel Terra pinned-source/live-owner audits, root synthesis, and reserved review | not exposed | Accepted oracle proves custom-only and explicit-empty rule returns both expose present zero-file DefaultInfo and dependency constructor indexing; live code already has an empty default and strict always-default collection | Review rejected permissive `from_values(false)` and any build-API edit, selecting a one-function conditional empty-default append before unchanged strict construction | `ACCEPT`; implement only `WP-6-m2-implicit-default-info-decoder-implementation` in one production/one test file at 20/120/140 |
| 2026-08-04 | M2 implicit DefaultInfo provider oracle | One Terra six-file fixture writer, root exact-output/cap audit, and independent Terra latest-diff review | not exposed | `d4e7e47e` pins four Bazel 9.2 successful rows: custom-only and explicit-empty targets both expose present zero-file DefaultInfo, consumer indexing succeeds for both, and warm replay is byte-identical | Generation/replay, six-file/zero-link inventory, 123 authored/255 total lines, provenance, credentials, archive, and diff gates pass; pytest unavailable | `ACCEPT`; design exact decoder synthesis ownership before resuming string transitions |
| 2026-08-04 | M2 positive string transition implementation | One Terra bounded implementation worker, root stop audit, parallel Terra live/pinned provider audits, and reserved successor review | not exposed | The fixture-shaped focused test reached strict Starlark provider decoding and failed `collection did not receive a DefaultInfo provider`; all partial Rust was discarded cleanly. Live code has a permissive constructor, but pinned Bazel 9.2 instead synthesizes an observable implicit empty DefaultInfo | The stopped packet could neither widen its two-file boundary nor choose permissive absence. Review accepted one positive-only six-file/four-row oracle comparing custom-only, explicit-empty, dependency indexing, and warm replay | `REPLAN`; run only `WP-6-m2-implicit-default-info-provider-oracle`, then design decoder normalization before retrying transitions |
| 2026-08-04 | M2 positive string build-setting transition design | Parallel Terra pinned-source/utility and live-owner audits, root synthesis, adversarial cap check, and reserved correction review | not exposed | The effective compact string can extend the existing target configuration as semantic Eq/Hash/Allocative data; request/resolved modes of `RootConfiguredTargetAnalysisKey` preserve root loading Needs/events and recursively distinguish two transitioned edges to the same label | Review rejected delegation to the legacy package-load analysis family, required zero legacy-key activations, accepted the packet-fixed label without duplicated storage, and raised realistic caps to 850 production/450 test/1,300 total | `ACCEPT`; implement only `WP-6-m2-positive-string-build-setting-transition-implementation` in seven production/three test owners with no public command change |
| 2026-08-04 | M2 string build-setting transition oracle | One Terra six-file fixture writer, root live record audit, and reserved Sol replan review | not exposed | Eight Bazel 9.2 rows generated and replayed at 168 authored lines; successful rows exposed only provider values, but the invalid-transition diagnostic named `//:parent (a7a71fd) -|left|-> //:consumer` in raw and normalized stderr | The explicit no-checksum stop rejected normalization or an inexact failure envelope; all six untracked fixture files were removed and the worktree was restored clean. Review accepted successful semantic evidence as a separate positive-only packet while deferring invalid programs | `REPLAN`; run only `WP-6-m2-positive-string-build-setting-transition-oracle`, with no failure row, Rust, or normalization |
| 2026-08-04 | M2 aquery formatter/configuration boundary | Parallel Terra live action-owner and pinned Bazel formatter audits, root synthesis, and reserved Sol next-packet review | not exposed | The accepted Bazel 9.2 recursive fixture and live `AnalysisResult` prove reusable target-owned actions, but text exposes configuration, execution platform, action key, and configured paths while summary still exposes configuration/platform; older shape fixtures are Bazel 9.1.1 | Review rejected a Slug-specific reduced renderer, hard-coded `first-build`/checksum/path/platform, and a speculative general option representation; it selected provider-visible string build-setting and transition evidence as the first semantic configuration discriminator | `REPLAN`; run only `WP-6-m2-string-build-setting-transition-oracle`, with no aquery or Rust change |
| 2026-08-04 | M2 root cquery Starlark-label boundary design | Parallel Terra live-owner and pinned Bazel formatter audits, root typed-error/wire synthesis, and reserved Sol latest-text review | not exposed | Exact canonical-label projection can directly drive the existing root analysis key; a minimal typed target-missing variant provides raw exit-1 Bazel diagnostics without a second load or display parsing | Review required exact root-key activation identities/counts, explicit zero action/REAPI counters, dependency-miss separation, additive narrow cquery wire, and no new retained utility/DICE owner | `ACCEPT`; implement only `WP-6-m2-root-cquery-starlark-label-implementation` in eight production/five test files at 650/600/1,250 |
| 2026-08-04 | M2 root cquery Starlark-label evidence | One isolated retained Bazel 9.2 three-command probe and root evidence synthesis | not exposed | Exact success/recovery stdout is 18 bytes `@@//parent:parent\n` with no configuration token; missing exits 1 with empty stdout and the stable skipping/no-such-target diagnostic; retained server recovery is byte-identical | The probe uses the accepted recursive workspace, leaves its frozen oracle untouched, and supersedes the old Bazel 9.1.1 expression record as acceptance authority | `ACCEPT`; design only `WP-6-m2-root-cquery-starlark-label-boundary-design` through the existing analysis key |
| 2026-08-04 | M2 root configuration identity design | Parallel Terra pinned-source and live DICE/command audits, root synthesis, and reserved Sol review | not exposed | The original audit counted fourteen sorted native option fragments with host/platform defaults plus Starlark options/scopes; a later complete registry audit corrected this to seventeen classes and 341 cache-key options. Slug only propagates opaque `first-build` and owns no matching command inputs or serialization | All reviews rejected a Slug-local digest or fixture constant; reserved review preserved the existing analysis key and selected a configuration-opaque Starlark-label cquery slice | `REPLAN`; run only `WP-6-m2-root-cquery-starlark-label-evidence`, leaving default/label unsupported until the general configuration substrate |
| 2026-08-04 | M2 root cquery label-output evidence | One isolated retained Bazel 9.2 four-command probe, parallel Terra pinned-source/live-owner audits, and root synthesis | not exposed | Default and explicit `label` stdout are byte-identical `//parent:parent (a7a71fd)\n`; missing target exits 1 with empty stdout; same-server recovery is exact. Pinned source proves the suffix is the `BuildOptions` SHA-256 short ID, while Slug only stores opaque `first-build` | Audits rejected a formatter-only slice, hard-coded digest, or new cquery graph; the existing analysis key remains reusable once configuration identity is authoritative | `REPLAN` before Rust; design only `WP-6-m2-root-configuration-identity-design`, with cquery wire/formatting deferred |
| 2026-08-04 | M2 root configured-target/cquery boundary design | Parallel Terra live ownership and accepted-oracle audits, root synthesis, and reserved Sol review | not exposed | Existing `RootConfiguredTargetAnalysisKey` can be driven directly without a new DICE key/graph/evaluator, but accepted Bazel 9.2 rows cover only Starlark output and cannot specify default/label configuration text or missing-target recovery | Terra isolated the exact evidence gap; Sol accepted a four-command retained-server default/label/missing/recovery probe and rejected broader analysis-error evidence unless implementation later proves it necessary | `REPLAN`; run only `WP-6-m2-root-cquery-label-output-evidence`, with no Rust/fixture/oracle edit |
| 2026-08-04 | M1 trusted direct-nonregistry evaluator adapter | One-file implementation, root serial validation, and independent latest-diff review | not exposed | `c683c239` lands the private `module_eval.rs` adapter at 190 production/208 test/398 total net lines; focused 17/18 and full library 270/271 each failed only the known clean-HEAD `records_exact_proxy_tag_and_innate_call_spans` baseline, while GNU-Windows no-run, formatting, archive, and diff gates passed | Review accepted empty declarations, execute/finalize then name-first conditional-version validation, declared-version retention, every-occurrence prepare with last raw-label wins, repeated execution, strict-seam isolation, and captured/direct print behavior | `ACCEPT`; implement only `WP-5-m1-direct-local-nonregistry-evaluation-owner` at 230/720/950, with public unsupported publication approval-gated |
| 2026-08-04 | M1 direct-local package-preflight helper refactor | One Terra one-file writer with nested independent review and root focused validation | not exposed | `34a2340e` extracts the sole private route+requests preflight at 9 production/4 test/13 total net lines; all five focused tests, formatting, archive, scope, cap, and diff gates passed | Review accepted exact behavior preservation: the initial key still solely owns inspection, while the helper owns unchanged parse/rebind/dedupe/grouped lookup/finisher logic and no cycle or fragment behavior | `ACCEPT`; design only `WP-5-m1-direct-local-support-gated-acyclic-closure-design`, with public capability publication approval-gated |
| 2026-08-04 | M1 direct-local include-cycle support boundary | Parallel Terra unsupported-architecture and full activation-path audits, root synthesis, and bounded refactor sizing | not exposed | A private support-gated preparation owner can truthfully distinguish supported acyclic closure from a Slug capability boundary using active ancestry, while all current direct-local closure keys remain callerless | The design forbids a cycle variant in semantic closure values and freezes public unsupported-feature publication pending explicit user approval; it first extracts the sole shared route+requests package preflight at 20 production/12 test/32 total lines | `ACCEPT` for private preparation only; implement `WP-5-m1-direct-local-package-preflight-helper-refactor` with no cycle or fragment behavior |
| 2026-08-04 | M1 direct-local occurrence-closure design | Parallel Terra pinned Bazel traversal and live DICE ownership audits with root synthesis | not exposed | Acyclic behavior is fully pinned, but Bazel's unvisited self/multi-file cycles produce endless breadth-first horizons and no finite diagnostic; the accepted root-bound package horizon also needs a shared route+requests helper before nested use | Audits rejected visited truncation, invented Need/error/limit, recursive DICE, and intentional hang as inexact or unsafe; they also rejected speculative Rust caps before a support boundary is approved | `REPLAN`; design only `WP-5-m1-direct-local-nonregistry-include-cycle-boundary-design`, authorizing no Rust or public activation |
| 2026-08-04 | M1 direct-local route package-horizon implementation | One Terra one-file writer, parallel Terra test audit, root serial validation, and independent Terra latest-diff correction reviews | not exposed | `1d5edc7c` lands the private inspection-to-external-lookup horizon at 298 production/647 test/945 total lines; 5 focused, 32 source-preparation, and 30 host-package tests, GNU-Windows check, formatting, archive, scope, and diff gates passed | Reviews replaced non-discriminating synthetic/value-only checks with real mixed precedence and multi-kind union seams, zero-lookup parse failure, typed sources, downstream pruning, reorder/delete/recreate, genuine horizon warm reuse, and exact child-versus-horizon event ownership | `ACCEPT`; design only `WP-5-m1-direct-local-nonregistry-occurrence-closure-design`, with evaluator and public activation deferred |
| 2026-08-04 | M1 direct-local route package-horizon design | Parallel Terra live-owner and pinned-source audits with root synthesis and correction | not exposed | Existing private inspection and external package lookup bound the implementation to one `source_preparation.rs` key at 300 production/650 test/950 total lines; no new oracle is needed | Pinned `advanceHorizon` review rejected the draft global-Need precedence and restored source-order mixed terminal/Need selection after whole-group request and Need union; the one-file design avoids a root-horizon change or module ownership cycle | `ACCEPT`; implement only `WP-5-m1-direct-local-route-package-horizon-implementation`, with fragment reads, closure, evaluator, and public activation deferred |
| 2026-08-04 | M1 public selected-BUILD source/loading migration | Four-file implementation, root serial validation, and independent latest-diff review with focused source/event corrections | not exposed | `9b5246af` lands the opaque public selected-source owner and atomic loading migration at 211 production/368 test/579 total lines; formatting, 30 bzlmod host-package tests, all 57 loading tests, GNU-Windows loading check, and archive/diff/scope gates passed with existing warnings only | Review proved atomic FileBytes disappearance is typed Source::InconsistentState while SelectedSourceAbsent is structural-only, required repo-relative UTF-8 display, and separated captured/uncaptured routed REPO events from loader-local evaluation | `ACCEPT`; design only `WP-5-m1-direct-local-nonregistry-route-package-horizon-design`, with no fragment read, closure, evaluator, or public activation |
| 2026-08-04 | M1 routed external package-policy/lookup implementation | Four-file implementation, root serial validation, and independent latest-diff review with focused event corrections | not exposed | `42ef64cd` lands the private atomic policy/lookup graph at 449 production/739 test/1188 total lines; formatting, 21 host-package, 9 REPO, 17 ignore, 8 retained path/source tests, GNU-Windows check, and archive/diff/scope gates passed with existing warnings only | Review corrected the exact routed REPO event lifecycle and added the present uncaptured-print direct branch; corrected evidence retained deletion-first order, typed Needs/errors, marker priority, lifecycle, and every no-public/no-loading/no-horizon stop | `ACCEPT`; implement only `WP-5-m1-public-selected-build-source-loading-migration` in four files at 260/650/910 |
| 2026-08-04 | M1 Host repository path-key implementation | One-file implementation, root serial validation, and independent latest-diff review with one focused correction | not exposed | `00e85153` lands the sole routed path-state seam at 168 production/350 test/518 total lines; formatting, 27 source-preparation units, 28 source-preparation DICE tests, and 56 loading library tests passed with existing warnings only | Review replaced a changed-key immutable lifecycle with a same-key generation 41→42 transition; corrected evidence proves operational invalidation while retaining exact Needs/errors, no path-only byte request, source compatibility, route/lifecycle coverage, and all structural stops | `ACCEPT`; implement only the atomic four-file `WP-5-m1-route-policy-and-package-lookup-implementation` at 650/1350/2000 |
| 2026-08-04 | M1 direct-local external package-policy/selected-BUILD implementation design | Parallel Terra pinned-source and live-owner audits, root synthesis, one Terra plan writer, and independent Terra latest-text correction review | not exposed | Existing sparse path/retry is complete; direct local route canonicalizes to `name+`; Bazel checks global deleted packages before external policy, then REPO/ignore and `BUILD.bazel` before `BUILD`; existing external loading reads markers too early | Review required the one-file bytes-free path owner to become the sole route resolution seam, preserve non-file kinds as complete path states, expose only crate-private sibling access, and correct both successor allowlists; final rereview accepted | `ACCEPT`; implement only `WP-5-m1-host-repository-path-key-implementation` (170/360/530), then the 650/1350/2000 and 260/650/910 serial packets |
| 2026-08-04 | M1 direct-local nonregistry closure/evaluation design | Parallel Terra pinned-source/include and live DICE/evaluator audits, one nested live-runtime reconciliation, root synthesis, and independent correction review | not exposed | Bazel requires route-aware external package preflight before fragment reads, occurrence-preserving BFS horizons, compile-all-before-execute, empty expected version with separately empty declarations, and fresh-only nonregistry print; live sparse path/retry is already complete | A first audit relied on superseded path-state residuals; live evidence corrected the blocker to absent route-aware external package policy/lookup. Review then required canonical deletion to short-circuit before materialization or route-local Needs/events; corrected rereview accepted | `REPLAN`; design only `WP-5-m1-direct-local-nonregistry-external-package-policy-design` |
| 2026-08-04 | M1 direct-local inspection and corrected evaluation audit | Terra one-file inspection writer, root validation, independent corrected-diff review, then parallel pinned Bazel identity and live evaluation/DICE audits | not exposed | `8aae11d6` accepted the private parser projection at 98 production/342 test lines; Bazel 9.2 rewrites nonregistry overrides to `ModuleKey(name, EMPTY)`, validates declared name but not version for that key, and assigns contextual mappings only after selection | The initial live-only audit incorrectly required an unavailable MVS-selected version; pinned source corrected the exact empty sentinel while confirming that complete include closure and evaluation/event ownership remain undesigned | `REPLAN`; design only the direct-local nonregistry empty-key include-closure/evaluation boundary |
| 2026-08-04 | M1 external Restricted-visibility query oracle | One Terra oracle writer with a nested cap audit, root inventory/archive checks, pinned Bazel generation and distinct-root replay, and independent Terra latest-diff review | not exposed | New isolated exact fixture has seven regular files, five assets, seven rows, zero links, and 278 lines; rows prove raw top-only visibility, effective NODEP plus include topology, reverse dependency, exact DOT, and root-versus-same-external caller identity | Both fresh Bazel 9.2 runs, 107 harness tests, archive/scope/cap/JSON checks, cleanup, and two independent reviews passed; row seven is explicitly Bazel-only and existing fixtures remain unchanged | `ACCEPT`; implement only `WP-5-m1-external-restricted-visibility-query-implementation` in four Rust files without flag support |
| 2026-08-04 | M1 external Restricted-visibility query consumer design | Three parallel Terra complete-consumer/flag, implementation/DICE, and oracle/hygiene audits; root scope synthesis; independent Terra reserved-boundary latest-text review | not exposed | One explicit native filegroup plus already-loaded same-package groups is bounded through existing owners when raw attributes, effective NODEP edges, include edges, repository-context contents, and the full enabled consumer surface remain distinct | Root rejected widening command/server/root-query policy for NODEP flags, kept both dependency-filter families deferred, froze the 20-row mixed fixture, and selected one isolated seven-row evidence-first fixture with one final Bazel-only NODEP discriminator | `ACCEPT`; generate only `WP-5-m1-external-restricted-visibility-query-oracle` before Rust |
| 2026-08-04 | M1 external package-group visibility query design | Three parallel Terra source/identity, implementation/DICE, and generic-consumer audits; root synthesis; independent Terra reserved-boundary conflict review | not exposed | Repository-relative package matching and include traversal fit existing owners, but Restricted visibility in the shared graph also exposes a raw `visibility` attribute and effective `VisibilityNodep` edge to labels, dependency/reverse/path, and graph consumers | Two audits initially proposed hiding the non-`visible()` surfaces; the consumer audit proved that partial, and independent conflict review accepted REPLAN while requiring separate raw/effective semantics and complete consumer enumeration | `REPLAN`; design only `WP-5-m1-external-restricted-visibility-query-consumer-design`, with no Rust/evidence change |
| 2026-08-04 | M1 Bazel-tools query repository-closure owner | Three parallel Terra installed-source, route/DICE, and generic-consumer audits; root synthesis; independent Terra reserved-boundary review | not exposed | Installed tools bytes are source-pinnable, but selected registry modules, contextual mappings, generated RepoSpecs, exact Host registry bytes, and whole-package query semantics have no bounded owner; a 44-target table would be observably partial | Review rejected more speculative tools inventory/substrate breadth and accepted terminal unsupported status plus a pivot to already-loaded same-package visibility | `REPLAN`; design only `WP-5-m1-external-package-group-visibility-query-design` and keep external test-base/tools unsupported |
| 2026-08-04 | M1 external Starlark test-base query closure | Three parallel Terra source, route, and consumer audits; root synthesis; independent Terra reserved-boundary review and correction rereview | not exposed | Pinned Bazel 9.2 proves a finite direct unconfigured test-base edge set, while its transitive 44-target probe crosses installed `@bazel_tools`, rules_shell/rules_java/platforms, and extension-generated remote coverage content absent from current direct-local owners | Correction retained the source-proven unconfigured direct edges and removed an overbroad configuration claim; no Bazel, Cargo, Rust, fixture, tool, or generated evidence changed | `REPLAN`; design only `WP-5-m1-bazel-tools-query-repository-closure-owner-design`, keeping external test rules and suites frozen |
| 2026-08-04 | M1 external Bzl activation and bare query `--noshow_progress` compatibility | Retained six-path implementation worker state, exact two-file prerequisite correction, root validation, and independent Terra DICE, consumer, and parser latest-diff reviews | not exposed | The eight-path `+829/-21` result activates same-package external macros through sole package ownership, retained lifetimes/events and route-aware query provenance; bare query progress compatibility lets the frozen three-row oracle reach Slug and all three rows pass exactly | Reviews accepted the core without correction, isolated the pre-query flag blocker, and accepted the `+36/-0` bare-only parser fix; focused/cross-target/lifecycle/format/archive/diff gates pass and the fixture remains byte-frozen | `ACCEPT`; design only `WP-5-m1-external-dependency-free-starlark-rule-projection-design`, leaving external test-rule/tool closure deferred |
| 2026-08-04 | M1 external Bzl macro-query oracle | One Terra implementation worker, root structural and harness validation, and one independent Terra latest-diff fixture reviewer | not exposed | Exact four-path `+112` fixture delta adds one macro-created native filegroup and three anchored Bazel 9.2 query rows; fresh generation and distinct-root replay passed all 20 while the first 17 expected objects remained JSON-deep-equal | The full 107-test harness, exact caps, archive, and diff gates passed; review accepted the assets, records, provenance, protected bytes, outputs, and deliberate exclusion of lifecycle/activation claims | `ACCEPT`; implement only `WP-5-m1-external-bzl-package-query-activation-implementation` against the frozen 20-row oracle |
| 2026-08-04 | M1 external Bzl package/query activation design | Parallel Terra loading/DICE, generic-consumer, and oracle/harness audits with root live-owner synthesis | not exposed | The accepted private module owner and request-local query identity make same-package activation bounded, but the protected 17-row fixture has no Bzl load and the accepted ad-hoc probes create a custom rule, not a native target from a macro | Audits froze sequential child ownership, manifest/lifetime transfer, evaluation-only events, Filegroup/ExportedFile target gating, external fake-candidate consumers, and no-root-companion behavior; fixture hygiene selected an isolated `dep/macro` subpackage rather than changing the mixed-target dependency BUILD | `REPLAN`; design then generate a three-row Bazel 9.2 macro-filegroup provenance oracle before any Rust activation |
| 2026-08-04 | M1 dependency-free external Starlark-rule query redesign | One Terra-medium design/probe writer, parallel Terra-high loading/DICE and generic-consumer audits, root synthesis, and one independent Terra-high reserved-boundary reviewer with correction rereview | not exposed | Bazel 9.2 probes plus live code prove literal/kind/self traversal and the future external Bzl owner are bounded, but real/fake query candidates erase repository route in siblings, same-package reverse deps, loading-file provenance, and companions; Private visibility demonstrably compares same package fragments across repositories while Restricted remains unresolved | Review corrected an overbroad visibility interpretation and added enabled default Text to the complete output inventory. The correction preserves demonstrated Private semantics, isolates Restricted source/evidence work, freezes the 17-row fixture, and rereview accepted | `REPLAN`; design only `WP-5-m1-external-query-package-identity-design`, keeping the bounded two-file external Bzl owner deferred |
| 2026-08-04 | M1 external repository source identity | One Terra-high three-file implementation worker, root retained-value audit, and one independent Terra-high implementation reviewer with correction rereview | not exposed | Public Host source results now retain shared bytes plus the exact requested normalized path across a real requested-symlink/physical-FileBytes DICE chain, while the legacy immutable key remains bytes-only; owner 10, loader 4, retained-engine lifecycle 1, downstream checks, both GNU-Windows no-run targets, format/archive/diff all pass | Review accepted production immediately but rejected a self-comparison and missing Absent assertion, then initially overlooked the existing generation/root/instance retained-engine regression. The focused assertions were corrected, the exact existing lifecycle passed, and evidence-backed rereview accepted without duplicating its large harness | `ACCEPT` in `980373f9`; redesign only the dependency-free external Starlark-rule loading/query slice, keeping the 17-row fixture frozen |
| 2026-08-04 | M1 external Starlark test-rule query design | One Terra-medium design/evidence writer, root owner audit, and one Sol-low reserved-boundary reviewer | not exposed | Fresh Bazel 9.2 probes prove an attribute-free external `test = True` rule has a successful ordinary dependency closure spanning `@bazel_tools`, platforms, rules_java, rules_shell, and remote coverage; live DICE inspection also proves the root-only Bzl key/cycle family cannot represent external source identity | The packet stopped before Rust or fixture edits. Review accepted the REPLAN, corrected the pinned source range through test coverage/run-under attributes, confirmed direct suite membership is not independently atomic, and bounded a future private route-keyed external Bzl key without authorizing test-rule projection | `REPLAN`; design only one dependency-free non-test external Starlark rule/loading slice, then treat test-base/tool-repository graph breadth separately |
| 2026-08-04 | M1 dependency-free external Starlark-rule query design | One Terra-medium design/evidence writer, root consumer/identity audit, and one Sol-low reserved-boundary reviewer with correction rereview | not exposed | Bazel 9.2 proves the non-test rule itself has exact literal/kind/self-only deps and external load/build provenance, but live code proves the bytes-only repository source value cannot populate the public module identity's normalized evaluated path; route-erasing generic consumers also need explicit treatment | Review rejected a synthetic `<output_base>` identity, query-owned frozen pointer, raw key components, vague test allowlist, and incomplete consumer audit. The correction removed all implementation/fixture authority, retained the evidence, froze the five blockers, and rereview accepted | `REPLAN`; design only the external source-identity prerequisite before retrying external Starlark loading/query |
| 2026-08-04 | M1 external repository source-identity design | One Terra-medium design writer, root retained-value/consumer audit, and one independent Terra-high DICE/representation reviewer | not exposed | Live source proves the requested normalized path is created before `ResolvedPathKey`, while the current Host route is direct-local-only and the shared legacy immutable value intentionally prunes generation-root/instance changes when bytes are equal | Root rejected changing the shared value and required a Host-specific public result plus the direct loading pattern migration. Independent review accepted the sole-key, complete-only, transient-mapper, `Arc`/`Dupe`/`Allocative`, exact three-production-file boundary with no second observation or identity activation | `ACCEPT`; implement only `WP-5-m1-external-repository-source-identity`, preserving legacy bytes-only equality and freezing external Bzl/query activation |
| 2026-08-04 | M1 external Restricted-visibility query implementation | One Terra implementation worker, root bounded diff/test audit, a nested Terra reconstruction-safety audit, and independent Terra latest-diff review | not exposed | The four-file prototype fit at 189 production/300 test/489 total net lines and passed focused/full Rust checks, but its visibility-label projection reparsed typed parts because `CanonicalLabel` exposes no typed rebind API; the generic Slug oracle comparison also treated Bazel progress stderr as a mismatch | Review enforced the accepted literal no-parse boundary and found missing dedicated warm, visibility/include-edit, route-remap, different-external caller, and pre-synthesis sentinel evidence at a saturated test cap; root removed exactly the rejected prototype | `REPLAN`; redesign only the typed projection API, complete evidence matrix, revised allowlist/caps, and exact existing-tool Slug comparison policy |
| 2026-08-04 | M1 typed external Restricted-visibility implementation | Terra five-file implementation worker, root cap/source audit, identity pre-review, and independent Terra latest-diff review | not exposed | Typed reconstruction and all semantic/lifecycle checks fit 207 production/576 test/783 total; 211 core tests, dependents, and Windows checks passed, but the different-external test added a forbidden second route and warm evidence asserted only value equality | Review enforced the route stop and identified the manifest's actual-caller/one-route contradiction; root removed exactly the rejected Rust before CLI replay | `REPLAN`; redesign only a single-route different-canonical caller discriminator and activation-based warm evidence |
| 2026-08-04 | M1 external Restricted-visibility typed projection redesign | Terra identity/API audit with nested reconstruction review, root live runner/plan synthesis, and independent Terra latest-text review | not exposed | The smallest successor adds one typed provisional-root-to-nonroot `CanonicalLabel` rebind that clears stale mapping provenance; five Rust files fit 220 production/600 test/820 total and the complete missing lifecycle/caller/sentinel matrix is frozen | Review accepted the API direction checks, unchanged global identity semantics, exact first-six exit/stdout/manifest plus empty-Slug-stderr comparator, byte-frozen fixture/tool, expanded identity stops, plan consistency, and routing rotation | `ACCEPT`; implement only `WP-5-m1-external-restricted-visibility-query-typed-implementation` after identity-boundary pre-review |
| 2026-08-04 | M1 external Restricted-visibility caller/reuse redesign | Parallel Terra request-local caller and rich-activation audits, root live-seam/cap synthesis, and independent Terra latest-text correction review | not exposed | A genuine different-external check uses one cached dep route with same-/other-canonical fake caller owners in the crate-private environment; warm reuse uses rich public root-key activations. Exact 542+140+30 arithmetic requires 720 test/940 total caps while production remains 220 | Review rejected overlapping estimates and an ambiguous same-package claim; correction hard-bounded the cap, fixed the target in dep root/callers in viewer, preserved Private/Java fragment behavior, and rereview accepted | `ACCEPT`; implement only `WP-5-m1-external-restricted-visibility-single-route-typed-implementation` |
| 2026-08-04 | M1 corrected direct-local handoff implementation | Fresh Terra one-file writer, root scope/cap audit, and independent Terra final-cap audit | not exposed | Independently rebuilt a 100-production/472-test/572-total draft with exact bootstrap/materialization/path Need comparisons and eight passing focused tests | Later latest-diff review found the presumed lifecycle/reuse/no-data families used weaker substitutes, invalidating the claim that 472 lines were evidence-complete; the 440-test cap stop and full Rust discard remain valid | `REPLAN`; retry was attempted under 100/480/580, whose evidence basis is now revoked |
| 2026-08-04 | M1 direct-local module-file handoff implementation | Terra one-file writer with root serial validation and independent Terra latest-diff review | not exposed | Corrected draft fit 97 production/349 test lines; focused 12 passed, 184 integrations/downstream/doctest/Windows gates passed, and the sole 224-lib failure reproduced at clean HEAD. Non-vacuous cold/warm activation correction passed | Review accepted the owner boundary but required exact bootstrap/path Need forwarding and full materialization request/RepoSpec equality; remaining cap could not hold them without dropping mandatory lifecycle/error evidence, so root discarded all Rust | `REPLAN`; redesign only the evidence test/total cap with production contract and 100-line cap frozen |
| 2026-08-04 | M1 native-Windows Host glob ordering design | Parallel Terra live-carrier/evidence audit and independent latest-text review, followed by two Terra discovery-prerequisite audits | not exposed | Linux host has no native Windows/Wine runtime; typed directory entries reach `OsString`, but actual candidate/traversal computations are Unix-only and their `Arc<[u8]>`/Latin-1 boundary cannot prove non-BMP or lone-surrogate UTF-16 identity/order | Review corrected the manifest's initial implication that native evidence and Windows glob carriers already existed; rereview accepted the explicit `UnsupportedHost`, shape-only GNU-Windows, and representation stops | `REPLAN`; design only a direct-local nonroot source-preparation handoff before the absent full discovery owner graph |
| 2026-08-04 | M1 final direct-local handoff implementation | Fresh Terra one-file reconstruction, root diff audit, and independent Terra latest-diff review | not exposed | The one-file draft again measured 100 production/472 test lines and its focused tests passed, but review proved its capture-disabled error path, value-only version comparison, and request-only source lifecycle did not discriminate three mandatory claims | Root revoked the unsupported 480-test cap and discarded all Rust before the serial acceptance suite; a compact read-only repair estimates 521 formatted test lines but receives no cap authority yet | `REPLAN`; redesign only successful completion/lifecycle/activation evidence and freeze its measured cap before another Rust retry |
| 2026-08-04 | M1 direct-local completion-evidence redesign | Parallel Terra fixture/cap and DICE-sequence audits, root scope adjudication, and independent Terra review | not exposed | Existing epochs can complete the same direct key through A-to-B-to-A and Present/edit/Absent/recreate; capture-enabled successful cold evaluation and a version-only warm reuse require only replacement tests and shared fixture plumbing | Review rejected an expanded bootstrap/captured-child/materialization-error proposal, retained the mandatory nodep route error, and froze 525 mandatory test lines plus 20 formatting-only slack | `ACCEPT`; implement only the one-file 100-production/545-test/645-total completion retry |
| 2026-08-04 | M1 direct-local support-gated acyclic closure design | Parallel Terra live-owner/root-closure and cycle-precedence audits with root synthesis and independent latest-text correction review | not exposed | One private `source_preparation.rs` owner can validate the present root before downstream activation, enumerate every cycle-free simple occurrence path, retain exact breadth-first acyclic closure semantics, and classify only an otherwise-successful exhausted cycle-pruned worklist within 600 production/1650 test/2250 total net lines | Audit rejected current-horizon-only capability timing because a sibling can fail later; review also required root prepare/identifier validation before the first package horizon. The corrections preserve every later Need or real terminal and prove root compile failure has zero include-package lookup or fragment-source activations beyond root inspection | `ACCEPT`; implement only `WP-5-m1-direct-local-support-gated-acyclic-closure-implementation`, with public capability publication approval-gated |
| 2026-08-04 | M1 direct-local support-gated acyclic closure implementation | One Terra one-file implementation worker, root serial validation, parallel evidence/cap audits, and independent latest-diff review | not exposed | `f2b626f2` lands the private preparation owner at 434 production/1320 test/1754 total net lines; 13 focused, 46 source-preparation, and 30 host-package tests plus GNU-Windows, archive, formatting, and diff gates passed; the sole full-lib `records_exact_proxy_tag_and_innate_call_spans` failure reproduced clean | Review accepted exact BFS occurrence retention, whole-group/source-order precedence, later side-branch Need/error dominance, private capability equality, and no owner-local or copied child event data | `ACCEPT`; design then implement the trusted nonregistry evaluator/default/event successor, with public capability publication approval-gated |
| 2026-08-04 | M1 direct-local nonregistry evaluator/event-owner design | Parallel Terra pinned-source and live evaluator/DICE audits, root synthesis, and exact occurrence-map reconciliation | not exposed | Bazel starts declarations empty beside the requested key, validates name before conditional version, preserves declared version for `ModuleKey(name, EMPTY)`, compiles every include occurrence into a last-raw-label-wins map, and permits fresh nonregistry print | Audit split the bounded correction into a 190 production/430 test/620 total `module_eval.rs` trusted adapter and a later 230/720/950 `source_preparation.rs` DICE/event owner; both remain private and public unsupported publication stays frozen | `ACCEPT`; implement only `WP-5-m1-direct-local-trusted-nonregistry-evaluator-adapter`, then the private evaluation owner |
| 2026-08-04 | M2 positive string build-setting transition implementation | One Terra implementation/retry worker, root serial broad validation, parallel Terra architecture review, and reserved correction review | not exposed | `dfc1705e` lands the root request/resolved semantic configuration vertical at 514 production/450 test/964 total lines; 111 loading and 16 analysis tests pass, core is 125/126 at its known visibility baseline, and three GNU-Windows no-run checks pass | Reviews restored legacy extraction for non-overlay roots, required exact setting validation before explicit values, added true warm reuse, and replaced nondeterministic sibling activation order with an exact sorted 33-event multiset; lifecycle passed ten times | `ACCEPT`; run only positive first-compatible toolchain Bazel 9.2 evidence before integrating dormant toolchain substrate |
| 2026-08-04 | M2 first-compatible toolchain evidence and root-registration retention design | One Terra oracle writer, root serial evidence validation, parallel Terra pinned-source/live-owner/utility audits, and reserved correction review | not exposed | `ed4baf08` pins six successful Bazel 9.2 provider-only rows at 161 authored/381 total lines: first, warm first, reordered second, restored first, edited-first, restored first; zero actions and no configuration/platform/toolchain/path identity | Review split the vertical serially, retained ordered direct `ApparentLabel` slices through the existing Need-aware anchor, required existing root dev-policy semantics, and corrected the label parser with a fail-closed local wildcard/recursive-pattern guard; no new key/digest/cache | `ACCEPT`; implement only `WP-6-m2-root-toolchain-registration-retention-implementation` in three production/two test owners at 220/300/520 |
| 2026-08-04 | M2 root toolchain-registration retention implementation | One Terra four-file worker, root serial focused/cross-target validation, and independent Terra correction rereview | not exposed | `4a3af8df` retains ordered direct platform/toolchain labels through semantic root MODULE values and the existing Need-aware anchor at 83 production/166 test/249 total net lines; focused registration/anchor, GNU-Windows, downstream core, archive/diff/scope/cap gates pass; full bzlmod is 463/464 at its known source-span baseline | Review corrected the initial overbroad literal-ellipsis rejection to exact no-target recursive and package-wildcard guards; tests now retain a direct literal-ellipsis label and reject root/apparent recursive patterns | `ACCEPT`; design only `WP-6-m2-native-toolchain-declaration-loading-design` before resolution or `ctx.toolchains` |
| 2026-08-04 | M2 native toolchain declaration-loading design | Parallel Terra pinned-source, live-owner, and adversarial boundary audits with root synthesis and reserved Terra conflict review | not exposed | Accepted Bazel 9.2 evidence and live RootPackageLoad ownership support five typed native target declarations followed serially by frozen rule requirements/load-only platform_common, then one integrated resolution/context vertical | Review rejected a side list, silent query omission, user-provider ToolchainInfo, and dormant resolver key; it required real PackageTargetKind identity, fail-closed root-query projection, existing external-kind rejection, NODEP implementation labels, and no ToolchainInfo value before real analysis consumption | `ACCEPT`; implement only `WP-6-m2-native-toolchain-target-loading-implementation` in three production/four test owners at 360/520/880 |
| 2026-08-04 | M2 native toolchain target-loading implementation | One Terra six-file worker, root serial focused/full/cross-target validation, clean-HEAD baseline reproduction, and independent Terra latest-diff review | not exposed | `6a457406` retains five native declaration classes as real package targets at 193 production/404 test/597 total net lines; all 114 loading tests, 53 loading-query integrations, six parser tests, checks, GNU-Windows, archive/diff/scope/cap gates pass | Root proved the sole query-library `PreparationRestart` failure reproduces unchanged at clean `f9f3c3d8`; review accepted ordered canonical slices, NODEP implementation retention, pre-projection root deferral, external unsupported-kind gating, and lifecycle/event/anchor evidence | `ACCEPT`; implement only `WP-6-m2-toolchain-rule-provider-loading-implementation` in one production/two test files at 340/310/650 |
| 2026-08-04 | M2 toolchain rule-requirement/provider-symbol loading implementation | One Terra three-file worker, root serial focused/full/downstream/cross-target validation, and independent Terra correction rereviews | not exposed | `1d6106bd` retains zero-or-one definition-relative required toolchain labels and a freeze-only ToolchainInfo symbol at 67 production/269 test/336 total lines; 117 loading, 53 query-loading, and 16 analysis tests plus checks and three GNU-Windows builds pass | Full loading found and corrected external empty-requirement parsing; review required verbatim accepted fixture shape, explicit empty/cross-package coverage, and zero ordinary dependency assertions before final ACCEPT | `ACCEPT`; design only `WP-6-m2-integrated-toolchain-resolution-context-design`, with no dormant resolver key or implementation authority |
| 2026-08-04 | M2 integrated toolchain resolution/context design | Parallel Terra live-owner, pinned Bazel, and adversarial audits with root synthesis and reserved Terra conflict/cap review | not exposed | Existing six-row Bazel 9.2 evidence plus pinned source supports root-only first-compatible platform/toolchain selection, builtin marker decoding, and exact ctx.toolchains access without new evidence | Reserved review rejected a separate context key and private marker truth, then used the first sizing prototype to require Need-first errors, exact builtin keys, duplicate-setting and exact leaf/capability guards while retaining the atomic seven-file boundary | `ACCEPT`; correct the in-place `WP-6-m2-integrated-toolchain-resolution-context-implementation` at 740 production/760 test/1,500 total lines |
| 2026-08-04 | M2 integrated toolchain resolution/context implementation | One Terra in-place implementation/correction worker with nested test audit, root serial validation, and independent Terra cap plus latest-diff correction reviews | not exposed | `1533569f` lands the root-only first-compatible selection/context vertical at 724 production/630 test/1,354 total lines; 24 analysis, 22 build-API, and 117 loading tests, core check, three GNU-Windows, formatting, archive, diff, scope, and cap gates pass | Reviews raised the unrealistic initial cap, then corrected Need precedence, external-reference projection, duplicate settings, exact builtin identity, defaulted tags, leaf/capability/callable guards, event ownership, lifecycle, and omitted marker evidence before final ACCEPT | `ACCEPT`; design only `WP-6-m2-root-action-closure-boundary-design`, while public configuration identity and aquery remain REPLAN |
| 2026-08-04 | M2 root command action-closure boundary design | Parallel Terra live-owner, accepted-evidence, and Buck2 utility/DICE audits with root synthesis and reserved Terra conflict review | not exposed | Existing recursive Bazel 9.2 evidence proves parent/second/first target-owned actions; live BuildCommandEvaluation retains only requested roots and therefore drops recursive actions from counts and its existing REAPI iterator | Reserved review chose BFS frontier joins and shared Arc analysis handles over DFS/deep cloning, preserving full configured-key identity, Need precedence, child invalidation edges, and target-local event ownership without a new graph/key/cache; pre-edit review added one mechanical Arc-return test owner | `ACCEPT`; implement only `WP-6-m2-root-action-closure-implementation` in two production/four test owners at 360/650/180-doc/1,190 total lines |
| 2026-08-04 | M2 root command action-closure implementation | One Terra implementation/correction worker, root serial validation, parallel Terra successor audits, and independent Terra latest-diff correction review | not exposed | `afd2a606` lands shared root-analysis values and a deterministic full-configured-key BFS action closure at 162 production/458 test/620 total lines; 24 analysis, four focused closure, recursive CLI, formatting, diff, scope, cap, and archive gates pass; full core is 129/130 plus 13/13 integrations at the established visibility-wording baseline | Review required direct same-frontier Need-before-error evidence and a non-executing borrow of the existing REAPI-style iterator; both were corrected while retaining public count-three coverage, direct child invalidation, event ownership, and no new key/cache/lock | `ACCEPT`; design only `WP-6-m2-action-query-identity-boundary-design` over the four remaining configuration/path/platform/ActionKey owners, with configured-target cycles deferred by approval |
| 2026-08-04 | M2 action-query identity boundary design | Parallel Terra live-owner and accepted-evidence audits with root synthesis and reserved Terra atomicity review | not exposed | Raw Bazel 9.2 action output shows configuration, configured bazel-out paths, execution platform, and distinct ActionKeys, but the protected fixture normalizes those identities and proves ownership only; live Slug has placeholder configuration, package-relative strings, discarded selected platform, no Bazel ActionKey, and only an aquery placeholder | Review rejected one atomic implementation and any partial checksum/path/platform/key shortcut; it fixed the serial order as complete configuration, configured artifacts, per-action platform, Bazel ActionKey, then aquery, and required isolated discriminating evidence first | `REPLAN`; run only `WP-6-m2-action-query-identity-evidence` at zero Rust/340 authored fixture-doc lines, without modifying protected evidence |
| 2026-08-04 | M2 pure native family byte-contract ledger retry 2 | Terra sole Stage 6 writer, root terminal ledger review, and retry-3 scheduling transition | not exposed | The 64-line synthesis used accepted renderer, forced-identity, and source-route evidence and retained the 16 separate enum routes, but did not freeze all required default/null, whole-field repeat cache, timeout, forced-radix/rendering, fission, and enum-substitution facts | Terminal review stopped before acceptance; the entire unaccepted Stage 6 ledger was discarded, with no source lookup, probe, descriptor, Rust, or runtime change | `REPLAN`; run only docs-only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-3` with the terminal facts frozen before mechanical descriptor attachment |
| 2026-08-04 | M2 pure native family byte-contract ledger retry 3 | Terra sole Stage 6 writer, root terminal review, and retry-4 scheduling transition | not exposed | The 59-line synthesis retained retry-3 defaults, repeats, map bytes, forced structural identity, and 16 separate enums, but lacked terminal nonrepeat/attachment, per-entry fallback, Dotted, runs-cache, and platform-default discriminators | Terminal review discarded the entire unaccepted Stage 6 ledger before acceptance; no source lookup, probe, descriptor, Rust, or runtime change occurred | `REPLAN`; run only docs-only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-4` with all retry-3 facts plus the terminal discriminators frozen |
| 2026-08-04 | M2 pure native family byte-contract ledger retry 4 | Terra sole Stage 6 writer, root terminal review, and retry-5 scheduling transition | not exposed | The 59-line synthesis froze retry-4 nonrepeat/attachment, timeout, shard, Dotted, runs, platform, and enum-route facts, but did not freeze actual enum default inventories or the final AllowComma/Dotted/runs default distinctions | Terminal review discarded the unaccepted ledger before acceptance; no source lookup, probe, descriptor, Rust, or runtime change occurred | `REPLAN`; run only docs-only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-5` with actual enum defaults and terminal default-route facts frozen |
| 2026-08-04 | M2 pure native family byte-contract ledger retry 5 | Terra sole Stage 6 writer, root terminal review, and retry-6 scheduling transition | not exposed | The 59-line synthesis correctly froze actual enum defaults and the remaining scalar/default bytes, but left the no-attachment list boundary, runs default-materializer ownership, and Dotted full-input identity implicit or contradictory | Terminal review discarded the entire unaccepted Stage 6 ledger; no source lookup, probe, descriptor, Rust, or runtime change occurred | `REPLAN`; run only docs-only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-6` with those three terminal ownership distinctions frozen |
| 2026-08-04 | M2 pure native family byte-contract ledger retry 6 | Terra sole Stage 6 writer, reserved acceptance, independent terminal review, and retry-7 scheduling transition | not exposed | The 57-line synthesis preserved retry-5 bytes and clarified the unadmitted-list, Runs, and Dotted boundaries, but omitted a global retained-value structural identity/order rule and Fission's exact `yes` bytes | Independent review discarded the entire unaccepted Stage 6 ledger despite reserved acceptance; no source lookup, probe, descriptor, Rust, or runtime change occurred | `REPLAN`; run only docs-only `WP-6-m2-pure-native-family-byte-contract-ledger-retry-7` with the global rule and exact Fission `yes` case frozen |
| 2026-08-05 | M2 pure native value/default/rendering kernel retry 2 | Two serial Terra implementation workers, root bounded diff audit, and independent Terra correction review | not exposed | The private seven-file draft compiled and passed seven focused tests while preserving the 287 count, but its occurrence, timeout, Dotted, and ordered-map implementations did not satisfy accepted contracts | Review enforced the packet's second-material-correction stop; root discarded the complete unaccepted Rust diff with `apply_patch` and restored clean `d6570a74` | `REPLAN`; run only retry 3 with the four failed typed seams plus Bool/Tri null and typed Runs frozen before breadth; all contextual and configured-cycle work stays deferred |
| 2026-08-05 | M2 Host input observation contract retry | Root pinned-source/lifetime audit with independent latest-text review | not exposed | A single immutable snapshot is inexact: `user.home` is read per eligible `~/` conversion, while OS/architecture/capacity retain independent lazy timing and failure; the Windows long-name result remains request-scoped | Root stopped before selecting a schema; no Host read, DICE, configuration, request/daemon, Rust, probe, or dependency work was retained | `REPLAN`; design only `WP-6-m2-host-input-lifetime-partition-design` with explicit lazy process-source cells and request/eligible-conversion facts; core -> configuration only and configured cycles remain deferred |
| 2026-08-05 | M2 Host input observation contract design | Parallel Terra pinned-Bazel, live-owner, and retained-representation audits with root pinned-tag correction | not exposed | OS/CPU tokens, process-lifetime Host resource capture, and home expansion closed, but the presumed finite lexical path policy was disproved: Bazel 9.2 Windows `PathFragment.create` calls `GetLongPath` for recognized 8.3 segments during both shell and platform-mapping option conversion | Root rechecked the pinned tag after the first source report incorrectly claimed no filesystem access; no docs design, Rust, probe, dependency, Host read, DICE, or runtime edit was retained | `REPLAN`; design only `WP-6-m2-windows-option-path-short-name-resolution-design` around the existing Windows-long-path observation seam before selecting any Host snapshot schema; configured cycles remain deferred |
| 2026-08-05 | M2 option-label context identity | Root test-matrix preparation, Terra pinned-source audit, and parallel Terra live-API/utility design reviews | not exposed | The planned provenance-free option label and three parser modes matched mapped source paths, but pinned review proved an absent apparent mapping converts to Bazel's non-visible repository identity rather than rejecting | Root enforced the unresolved-identity stop and discarded the test-only draft before production; existing Slug mapping fallback cannot represent the apparent-plus-owner non-visible state, so no Rust or API change remains | `REPLAN`; design only `WP-6-m2-option-label-nonvisible-repository-identity-design` before retrying identity implementation; loading/materialization and configured cycles remain deferred |
| 2026-08-05 | M2 pure native value/default/rendering kernel retry 6 | Root 287-binding test matrix, serial independent Terra acceptance, one Terra production writer, root validation, and parallel terminal Terra audits | not exposed | The accepted +1,155 test matrix and 677-line private kernel passed 13 tests, check, formatting, GNU-Windows, archive, scope, cap, and diff gates, but terminal review found nonrepeat list occurrence shape, uppercase Dotted, general timeout/malformed handling, Java duration range/overflow, and Fission special-case defects | Root enforced the no-new-correction stop and discarded the complete unaccepted seven-file diff back to clean `875b4006`; no dependency, lockfile, production, or test change remains | `REPLAN`; retry 7 restores the binding gate and adds direct tests for all five failures before production; configured cycles remain deferred |
| 2026-08-05 | M2 pure native value/default/rendering kernel retry 5 | Root direct test-matrix transcription, independent Terra pre-production review, and one Terra Phase-2 writer/stop audit | not exposed | Test-only Phase 1 reached an active direct 287/8/5/41 matrix at 1,021 lines and passed focused crate tests, but Phase 2 found that its nonempty ObjC fastbuild default expectation was attached to empty-default `PlatformOptions#extra_execution_platforms` | The serial gate again prevented retained production; the Phase-2 writer removed its draft and root discarded the complete unaccepted matrix back to clean `a64f0661` | `REPLAN`; retry 6 binds all 287 attachment keys and exact registry tuples to their expected default outcomes before materialization and review; configured cycles remain deferred |
| 2026-08-05 | M2 Host request observation projection design | Root reserved converter-call/source audit and live core/configuration representation audit | not exposed | Defaults validate at multiple parse checkpoints and `FieldOptionDefinition` memoizes them; only priority-accepted single-value occurrences convert; equal Windows raw UTF-16 values may resolve differently per occurrence; capacity needs exact `ResourceConverter` eligibility | Root stopped before Rust: the raw-deduplicated schema loses occurrence/multiplicity, while native capture and the production native-demand driver remain absent | `REPLAN`; design only `WP-6-m2-native-conversion-schedule-and-host-fact-redesign` with actual converter-call schedule and per-occurrence Host facts; native capture and configured-target cycles remain deferred |
| 2026-08-05 | M2 production native conversion schedule-driver design | Root docs-only live call-graph and lifecycle audit | not exposed | One-shot/daemon build/query/cquery converge on `drive_command`; parser schedule must survive daemon transport; `PathObservationDemand` identity deduplicates equal raw Windows demands without `ConverterCallId` | Root stopped before Rust: capture, typed schedule transport, and occurrence-keyed Windows observation identity remain absent; no generic-observation change was retained | `REPLAN`; design only per-converter-call Windows observation identity/selection/rollback, preserving core -> configuration and deferred configured-target cycles |
| 2026-08-05 | M2 Process Host native-capture source boundary evidence | Root docs-only Bazel/actual-runtime/nearest-upstream source audit | not exposed | Bazel 9.2 pins property and resource order; Zulu 25.0.2+10 lacks proven Azul source/patch/flag provenance; nearest `jdk25u` proves mutable `-D`-precedence properties, Java string conversion, and VM/container processor boundaries | JDK 25 removes the Java 21 SecurityManager property callback; JLS 25 preserves recursive-init normal completion and erroneous reuse | `REPLAN/Unsupported`; keep the non-reading owner placeholder and next split 41 supplied label routes from five terminal Host routes without Host/capture or configured cycles |
| 2026-08-05 | M2 nine-route label source closure evidence | Root docs-only pinned-source audit | not exposed | Six default literals and LabelToStringEntry closed at Bazel 9.2 `8220c619`; LabelMap trim depends on Guava and FlagAlias `\\w` domain on JDK Pattern | Root retained no Rust and separated converter facts from downstream command/C normalization | `REPLAN`; implement only bounded seven-route label converter, leaving Map/Alias for Guava/JDK evidence, Host/mixed/regex/cycles deferred |
| 2026-08-05 | M8 Host-Bzl parse diagnostic prerequisite | Root two-file exact formatter repair, parallel Terra pinned-source/consumer audits, and Sol design/final review | not exposed | Root/non-root Host Parse summaries now use validated slash paths; focused 2, loading 58, query 53, server 34, direct broken-Bzl CLI, and GNU-Windows loading/query checks pass | Design review added the missing root-package discriminator; final review found the packet's promised full CLI green state was false because the unchanged test now reaches the user-deferred cyclic-Bzl DICE baseline | `REPLAN` for the second material contract correction while retaining the exact accepted prerequisite; design only an honest unchanged-cycle negative gate before retrying the atomic fixture payload migration |
| 2026-08-05 | M8 atomic canonical fixture payload migration | Terra implementation/audits and absolute-root Bazel sweeps, root integration, and independent Sol latest-diff review | not exposed | One 50,103-byte payload replaces 14 workspaces/163 files exactly; Python 110, Cargo 5/55/36, three Bazel targets, Windows compile, and two 14-fixture/403-command Bazel 9.2 sweeps pass; CLI is the approved 40/41 sole cycle failure | The bounded correction added complete shared conformance, no-follow packing, preflight/type/mode checks, compile-input/runfiles proof, cap compaction, and archive ownership; terminal review returned `ACCEPT` | `ACCEPT`; design only `WP-10-m8-bazel-buildbuddy-developer-gate-design`, keeping core Git/tar, deferred cycles, CI, and self-hosting out of scope |
| 2026-08-09 | M4 configured-node result substrate | Terra implementation and next-packet audit, root serial integration/validation, and independent Sol identity/DICE correction review | not exposed | One structural/null node identity, immutable retained result, and ordered classified edge substrate lands at 152 net production/216 net test lines; analysis 32 and server 48 pass, while core and CLI retain their single known unrelated failures | Review corrected complete-error DICE validity/equality and accepted safe null rejection in core/server/CLI; the successor audit excluded synthetic external `@bazel_tools` until apparent-route, glob, and `@platforms` ownership exist | `ACCEPT`; implement only `WP-6-m2d-root-delegating-native-node-activation` without cquery traversal/output or parser changes |
| 2026-08-09 | M3 `attr` candidate-order oracle design | Parallel Terra fixture/source audits, root pinned-source synthesis, and independent Terra terminal review | not exposed | `TargetUtils` supplies ordered candidates, but `RegexFilterExpression` performs a pure existential search and emits only the target; candidate position and equal-candidate multiplicity cannot affect ordinary-query output | Review accepted the finite nine-class native inventory, non-rule package-group boundary, and native-toolchain graph prerequisite; no fixture, oracle, Rust, Cargo, Bazel invocation, or JVM artifact was added | `REPLAN`; design only `WP-4-8-m3-attr-observable-candidate-oracle-design`, retaining exact whole-value combination/format/default/canonical/native semantics while excluding unobservable candidate order/multiplicity |
| 2026-08-09 | M3 `attr` observable-candidate oracle design | Parallel Terra fixture/schema audits, root pinned-source synthesis, and independent Sol terminal review | not exposed | `AttrFunction` queries the full inherited `RuleClass` schema; current native/Starlark targets expose hidden, computed, automatic, overridden, BOOLEAN, macro, and test-base built-in defaults beyond V2-retained fields | Review confirmed retained-field evidence would falsely claim exact 16/16 coverage and confined residual risk to late-bound/null and canonical built-in-label source closure; no fixture, oracle, Rust, Cargo, Bazel invocation, or JVM artifact was added | `REPLAN`; design only `WP-4-8-m3-attr-total-ruleclass-schema-source-ledger-design` before returning to observable-candidate oracle design |
| 2026-08-09 | M3 `attr` observable-candidate oracle design retry | Parallel Terra fixture-growth, discriminator-matrix, consumer-scope, and hygiene audits; root synthesis; independent Sol terminal review | not exposed | The smallest honest design extends the 39-row canonical attribute-metadata workspace with 18 uniquely labeled union lanes, three virtual files, and complete Starlark/native/default/selector/container/renderer coverage; hygiene reset at `51540963` found no removable asset | Review returned `ACCEPT` and confined residual risk to dense fixture transcription; exact external-token freezing, all-39 protected replay, derived Python/Rust payload integrity, and +3-file/+18-row/+1,000-line caps own that risk | `ACCEPT`; generate only `WP-4-8-m3-attr-observable-candidate-oracle-generation`; generic-external consumption and native-toolchain graph projection remain separate prerequisites |
| 2026-08-09 | M3 `attr` total RuleClass schema source ledger | Parallel Terra pinned Starlark/native and live-V2 loss audits, root pinned-source synthesis, independent Sol terminal review | not exposed | Immutable Bazel 9.2 source closes 22/25/39/24 Starlark built-ins, nine native final sets, loading computed/late-bound/default sources, whole-value renderers, and finite discriminator families; the neighboring checkout HEAD differed and was excluded | Review returned `ACCEPT` for exact counts, deltas, configurability, removals, loading-only boundary, V2 loss map, successor scope, and permanent Rust-native/no-JVM boundary; no fixture, oracle, Rust, Cargo, Bazel invocation, or JVM artifact was added | `ACCEPT`; design only `WP-4-8-m3-attr-observable-candidate-oracle-design-retry`, always reading the pinned Bazel tree through `git show <commit>:<path>` rather than checkout HEAD |
| 2026-08-18 | M1 post-query owner audit and source-certificate prerequisite | Root live-owner/certificate trace plus independent natural-owner review | not exposed | Existing observed build root can isolate external singleton syntax and all route/package/source carriers exist, but the accepted source carrier's complete resolution epoch cannot be represented or reobserved by the current one-FileBytes certificate, especially for absent/wrong-kind, symlink and Materialization namespaces | Expanding the build root now would either certify only one incomplete edge or alter a lower carrier; the native acceptance boundary can instead reuse the compact complete epoch and active materializer roots without new DICE state | `REPLAN` to the uniquely smaller docs-only `WP-2A-m1-source-certificate-epoch-acceptance-design`, then return directly to external singleton build design |
| 2026-08-18 | M1 loading-query observed publication | Root bounded implementation across three formal correction designs, serial validation, Buck2/AI cleanup and independent final review | not exposed | Accepted `2e1c1334` publishes the structurally observed RootQuery terminal with exact selected path/repository Result Arcs and compact atomic accepted-event reconciliation; query 121, loading 204 and complete bzlmod validation pass | Stable-parent and loading-proof authority, query-only repository association, then fresh-tracker Some/None/no-transition event state each corrected a real acceptance-boundary miss without weakening child observations or event ownership | `ACCEPT`; audit only external exported-source build, multi-build and one-shot owners before another cutover or M1 closure |
| 2026-08-18 | M1 root exported-source neutral owner and observed-analysis prerequisite | Root live-owner/DICE audit, reserved design review, stopped neutral attempt, bounded prerequisite plus neutral retry implementation, serial validation and independent review | not exposed | `69d37ddb` supplies the observed configured-analysis family; `941db0d0` publishes singleton root Single through one neutral observed anchor/package/classification path with exact source carrier/revision Arcs and no partial rule carrier | The first neutral Rust was discarded after legacy analysis re-entry; the accepted retry passes 48 analysis, 194 loading, focused public lifecycle and documented core baselines within 359/430/789 semantic and 12,271/2,666/14,937 physical caps | `ACCEPT`; audit the remaining build aggregation and query/cquery publication owners before selecting one bounded successor or claiming M1 closure |
| 2026-08-18 | M1 post-neutral publication-owner audit and observed cquery publication | Root live-owner audit, bounded design, formal parallel-harness REPLAN, focused correction, implementation, serial validation and independent review | not exposed | `03f2db3e` keeps the sole `CqueryCommandRoot`, cuts direct/deps/rdeps edges to observed families, fully inspects batches with outer > Need-union failure > Need > semantic precedence, and retains exact selected Result Arcs with child-only events | The stable-parent correction and reducer/cancellation/Arc proofs pass 19/19 cquery, 48 analysis and 194 loading tests; broader core results retain only the two documented baselines within 58/282/340 semantic and 11,548/1,073/12,621 physical accounting | `ACCEPT`; audit multi-build aggregation, loading-query and one-shot publication owners before selecting one bounded successor or claiming M1 closure |
| 2026-08-18 | M1 post-cquery route and external loading prerequisites | Root live-owner audits, bounded route/source/policy/lookup designs and implementations, serial validation, focused corrections and independent latest-diff reviews | not exposed | `e4555dca`, `e4ee0a8e`, `2a8dd968`, and `33717f27` accept observed routing, routed Host source, REPO/ignore policy, and the shared external package-marker lookup with exact prefix Arcs and child-only events | Lookup proof passes 3/3 plus full bzlmod/loading/query within +530 semantic and 5,097 physical; core retains only documented baselines | `ACCEPT`; resume the upper external package source/load design before `RootQueryCommandKey` publication |
| 2026-08-18 | M1 external source/load direct-local file, inspection and horizon frontier | Root live-owner audits, bounded file/inspection designs and implementations, serial validation, cleanup and independent reviews | not exposed | `99d78875` and `a61de5d4` add private observed file and inspection siblings with matching families, exact Arcs and child-only events | Inspection passes focused 2/2 plus full bzlmod/loading/query and documented core baselines within +109 production/+273 tests/+382 semantic and 13,991 physical; the shared include-package horizon is the next legacy lookup owner | `ACCEPT`; design only the observed shared horizon before preparation/fragment/evaluation or query publication |
| 2026-08-11 | M7 FileWrite Test handoff design | Root pinned-source/live-model audit after accepted Run activation | not exposed | Bazel 9.2 creates mandatory TestProvider/TestRunner action/result artifacts while Slug retains only the declared FileWrite producer and test loading metadata | Reusing the client-owned Run launcher would bypass runfiles, test action identity, environment/timeout, logs/cache/XML, and result analysis | `REPLAN`; design the retained TestRunner semantic/action model and identity boundary before any Test command activation |
| 2026-08-11 | M7 embedded bazel_tools test closure design | Root pinned-source/archive and live-owner audit | not exposed | Bazel 9.2 `tools/test/BUILD` loads rules_shell, platforms/config settings, toolchains, filegroups, and coverage surfaces while Slug has only a reserved mapping token and local-override `RepoSpec` route | A pruned/synthetic package or host Bazel scan would violate verbatim content and structural DICE ownership | `REPLAN`; own one immutable canonical built-in bazel_tools route/source catalog before package or Test closure |
| 2026-08-12 | M7 selected Host module graph design r3 | Root pinned Bazel 9.2/live-owner audit after accepted effective override routing | not exposed | Root version-bearing values retain discarded build metadata, nonroot values normalize it, the exact comparator is lockfile-private, and the legacy resolver comparator is ineligible | Implementing selection now would duplicate version semantics or give DICE keys false identity | `REPLAN`; design one shared crate-private Bazel module-version grammar/normalization/order owner and checked Host-key adapter before discovery/MVS |
| 2026-08-11 | M7 FileWrite run implementation endpoint-injection stop | Root implementation and focused validation, then source-boundary audit | not exposed | Pinned Bazel 9.2 refresh passed; Slug replay proved the harness starts NativeLink but its build-only argv injector leaves Run without `--remote_executor` | The implementation packet had used its sole correction for executable-write Starlark forwarding and explicitly excluded harness edits, so the second independent scope miss stopped before a silent widening | `REPLAN`; design only the Build/Run endpoint-injection boundary and focused negative regression, retaining the unaccepted production diff in place |
| 2026-08-11 | M7 FileWrite run fixture-admission stop | Root implementation retry, focused harness validation, and complete-fixture source audit | not exposed | Build/Run-only endpoint injection passed seven cases; the marker-leaf correction advanced Slug into executable rule analysis, which then rejected stale `ctx.configuration.host_path_separator` use | The independent fixture-source miss exhausted the retry correction budget; production and reviewed harness diffs remain unaccepted in place | `REPLAN`; design only a POSIX `run-basic` source/host guard after auditing all five workspace files, then resume |
| 2026-08-10 | M4 close and M5 configured FileWrite/platform identity entry | Terra milestone/source audits, Terra implementations, root integration, independent design/final reviews | not exposed | M4 consumes the sole provider/action/edge-bearing configured result graph and closes accepted; M5 isolates the toolchain-backed default-group FileWrite, resolves complete selected-platform semantics, and encodes exact Slug-owned framed identity bytes with unchanged Buck2 parser stacks | Reviews rejected premature opaque formatting, separated source Starlark from query parsing, corrected default-group/platform-chain boundaries, made the resolved view invariant-preserving, and required direct constraint and selected-platform lifecycle regressions | `ACCEPT`; design the Bazel-shaped FileWrite text formatter next, with any short display token explicitly non-identity and ordinary no-toolchain/non-Write shapes fail-closed |
| 2026-08-10 | M4 configured reverse traversal and fixed filter wrapper | Terra source/successor audits, Terra implementation, root integration, independent design/final reviews | not exposed | Direct and `deps` universes normalize to Bazel's unbounded re-closure; bounded reverse BFS preserves full configured keys/aliases; fixed filter preflights regex before all activation and retains selected-induced output | Reviews corrected the false bounded-inner-universe premise and the initial filter error-order premise before acceptance; the public preflight compiles deterministically and retains no regex or DICE state | `ACCEPT`; prefer failure-free `executables(rdeps direct)` next, and audit every future wrapper for pre-operand work before reusing eager activation |
| 2026-08-09 | M4 configured-query root traversal, output, and forward-filter closure | Parallel Terra evidence/live-owner audits, bounded Terra implementations, root serial integration/validation, independent design/final reviews | not exposed | Single-root `deps --noimplicit_deps` consumes the sole configured/null graph at every Java-int depth; exact executable, label, and target-kind filters plus named-executable and named-kind chains preserve structural/null strings, duplicate configured keys, and selected-induced edges; query 115, commands 19, server 48, focused core/CLI pass | Reviews closed the useful fixed-filter lane, preserved full identity/Need/order/daemon boundaries, rejected `some` cancellation, and proved naïve reverse alias-edge scanning disagrees with Bazel delegation unwinding | `ACCEPT`; design only `WP-6-m31-cquery-reverse-delegation-normalization-design` before any reverse traversal implementation |
| 2026-08-09 | M4 root toolchain/platform topology audit and activation | Parallel Terra evidence/live-owner audits, root bounded implementation and serial validation, independent correction review | not exposed | Accepted Bazel 9.2 root type/selected-implementation/candidate-platform and platform/value/setting topology is retained in one configured result with structural Target/Exec roles and exact implicit/tool bits; configuration 37, analysis 36, server 48 pass, with unchanged core and CLI baselines | Review narrowed a general role cast to Target-to-Exec, consolidated selection facts, then removed a marker-shaped cross-package inference; final scope admits intrinsic same-package registered implementations, preserves cross-package execution without claiming reverse topology, and proves unrelated rules do not observe the MODULE anchor | `ACCEPT`; run read-only `WP-6-m2h-configured-query-root-traversal-activation-audit` before traversal/output breadth |
| 2026-08-09 | M4 analysis-error activation sidecar prerequisite | Terra live DICE/sidecar audit, root two-file correction and serial validation, independent Sol review | not exposed | Build/cquery analysis errors publish while configured-analysis errors stay invalid/non-equal; build recovery and mixed cquery successful-sibling events are retained; core is 157/158, server 48/48, CLI 47/48 on the two unchanged baselines | Audit added the initially omitted build path and identified the unclaimed transient-error-root event batch; correction narrowly classifies semantic analysis terminals and prunes only exact unavailable roots, leaving every other closure failure strict | `ACCEPT`; run read-only `WP-6-m2f-root-toolchain-platform-topology-activation-audit` before traversal breadth |
| 2026-08-09 | M4 root delegating native-node activation | Terra implementation/evidence audit, root serial integration and validation, independent Sol correction review | not exposed | The Bazel 9.2 fixture-proven root alias/source/generated/package-group slice uses one configured/null key/result; resolved-path source invalidation and the downstream action closure are discriminatingly covered; vendored `starlark-rust` is unchanged | Audit removed unproven filegroup/`exports_files` scope; review required physical source observation, fail-closed external visibility/includes, custom-provider alias edit/restoration, and configured-only build-closure publication | `ACCEPT`; run only `WP-6-m2e-analysis-error-activation-sidecar-prerequisite` to restore semantic error terminals without making analysis errors DICE-valid/equal |
| 2026-08-09 | M4 configured retained-state/default-function and topology closure | Reserved architecture reviews, Terra-high implementations/oracles, root integration, independent Terra reviews | not exposed | Direct functions accepted; toolchain oracle pins exact platform/constraint graph; delegation oracle pins transitioned/alias/source/output/package-group/implicit/rdeps topology across fresh roots | Graph ownership REPLANed to evidence. Toolchain retry recovered after a discarded pattern attempt; delegation split on package-group noise, then replaced unstable label order with deterministic anchored DOT and accepted one provenance correction | `ACCEPT`; run reserved target/exec/host platform and configured-node identity ownership design before Rust graph changes |
| 2026-08-09 | M3 `attr` five-source executable reconstruction retry 2 | Fresh root-only emitter and first Bazel 9.2 package-load gate | not exposed | Static 165-record/body/producer assertions passed, but Bazel rejected `attr.label(..., allow_none=True)` during package load as an unexpected keyword before any primary query or candidate render | Retry-2 has no correction budget; emitter, scratch root/output base, and process were removed at clean `1b1f5936`, retaining no fixture/code/JVM/configured/toolchain material | `REPLAN`; run only `WP-4-8-m3-attr-five-source-executable-reconstruction-retry-3` with `allow_none` absent, a valid nullable label declaration, and a passing package-load preflight before replay |
| 2026-08-09 | M3 `attr` five-source executable reconstruction retry | Root-only executable correction, two-root Bazel 9.2 replay, and correction rereview | not exposed | The corrected source passed all 18 primary lanes, nine literal-empty controls, and focused probes in two independent roots, but the generated-file producer was `l01_generated_nonrule_owner` rather than the accepted `l01_generated_owner`; emptiness did not reveal producer identity | The first correction closed the prior five construction blockers; this second material source-contract miss exhausted the packet, so the complete candidate and every temporary artifact were removed | `REPLAN`; run only `WP-4-8-m3-attr-five-source-executable-reconstruction-retry-2` with the exact `output_rule(name = "l01_generated_owner", nullable_output = "l01_generated_nonrule")` declaration and a source/producer assertion before replay |
| 2026-08-09 | M3 `attr` six-ownership-mismatch evidence | Root-only four-construction Bazel 9.2 evidence and independent terminal review | not exposed | Literal focused queries selected only `l05_a003_yes`, `l13_a011_yes`, `l13_a017_yes`, `l16_a007_yes`, `l16_a013_yes`, and `l17_a012_yes`; package-license, explicit metadata, alias fallback, and label-list rendering are all source constructions | Review accepted one no-manifest-change decision; temporary harness/output were removed, with no fixture/code/configured/toolchain/JVM work | `ACCEPT`; retry only `WP-4-8-m3-attr-five-source-executable-reconstruction-retry` with the four frozen construction corrections, one executable source, two-root 18-lane replay, controls/probes, and mechanical pending render |
| 2026-08-09 | M3 `attr` five-source executable reconstruction | Root-only disposable source synthesis and two-root Bazel 9.2 replay | not exposed | One executable source verified the 165-row stream and completed 18 primary lanes, nine controls, and focused probes in two roots, but strict ownership missed `l05_a003_yes`, `l16_a007_yes`, `l16_a013_yes`, `l17_a012_yes` and selected `l13_a011_no`, `l13_a017_no` | No candidate was rendered; the sole script/result, roots/output bases, and processes were removed at clean `2f83f90b` | `REPLAN`; run only `WP-4-8-m3-attr-six-ownership-mismatch-evidence` with at most five focused constructions/pinned-source checks to distinguish the six source omissions or semantic boundaries before a full replay |
| 2026-08-09 | M3 `attr` five-source template retry 3 | Root source-recovery audit and bounded scheduling review | not exposed | Retry-3 made no Stage4/8 edit, temporary root, or Bazel invocation: the discarded exact five bodies and unaccepted hashes are absent from `HEAD`, reachable log history, and unreachable Git objects | Reconstructing the deleted body by hand would violate the packet's single executable-source/no-second-representation contract, so root stopped before any source or command transcription | `REPLAN`; design only `WP-4-8-m3-attr-five-source-executable-reconstruction-design` to construct fresh five LF bodies from the accepted 165-row manifest in one executable source that owns argv, probes, two-root execution, and mechanical docs rendering; old hashes are not requirements |
| 2026-08-09 | M3 `attr` five-source template retry 2 | Terra-high candidate writer, independent review, one focused correction, correction rereview, and root cleanup | not exposed | Five bodies and the 165-row semantic construction remained viable, but correction rereview found nondiscriminating double-backslash OI control argv and a generator probe spelling different from the primary argv | Retry-2 had consumed its allowed correction; root discarded the entire unaccepted Stage4/8 candidate and all temporary material, retaining no fixture/code/JVM/configured/toolchain work | `REPLAN`; design only `WP-4-8-m3-attr-five-source-template-oracle-design-retry-3` with one executable argv representation run before docs, two independent sibling workspace/out roots, 18 lanes twice, nine controls, focused probes, and terminal review |
| 2026-08-09 | M3 `attr` five-source template retry | Terra-high clean retry with minimal disposable Bazel 9.2 license probe and root stop audit | not exposed | Required `licenses([notice])` syntax loads beside package metadata, but anchored ordinary `attr(licenses)` on the filegroup returns empty; explicit rule licenses render notice but contradict the frozen package-derived construction | A new material license-default boundary stopped the retry before source/plan edits; the probe root/output was removed and Git remained clean with no fixture/code/JVM material | `REPLAN`; run only `WP-4-8-m3-attr-license-default-source-evidence` to pin source plus native/Starlark/config-setting package/default/explicit behavior before correcting the manifest or resuming templates |
| 2026-08-09 | M3 corrected `attr` five-source template design | Terra-high two-root source writer, independent Sol full-diff review, Terra focused correction, and root stop/cleanup audit | not exposed | Five bodies and 18 primary lanes passed twice, but review exposed hidden construction drift; correction then proved the immutable `l11_a003_no size=short` is invalid in Bazel 9.2 and yields illegal computed timeout, while valid `small` is required for timeout `short` | The semantic-row contradiction exhausted the packet correction; root removed the entire unaccepted 1,901-line Stage4/8 template diff and worker removed all temporary roots/outputs/helpers, leaving clean Git and no fixture/code/JVM material | `REPLAN`; correct only `l11_a003` to `size=small,timeout=short` and recompute the digest in `WP-4-8-m3-attr-test-timeout-manifest-correction`, preserving the other four source-template review blockers for retry |
| 2026-08-09 | M3 `attr` five-source template design | Fresh Terra-high source synthesizer with disposable Bazel 9.2 root and root stop audit | not exposed | All five proposed source bodies reached successful loading, but an anchored ordinary query proved `l12_a003` renders `@@bazel_tools//tools/allowlists/function_transition_allowlist:function_transition_allowlist`; the accepted shorter regex selects nothing | The mismatch requires a semantic row/checksum change, so the packet stopped before plan/source-template edits or a second replay; every temp root/output/helper was removed and Git stayed clean | `REPLAN`; correct only `l12_a003` and the 165-record digest in `WP-4-8-m3-attr-transition-allowlist-manifest-correction`, then resume exact source-template design |
| 2026-08-09 | M3 manifest-bound `attr` oracle generation preflight | Reused Terra manifest writer and root no-inference audit | not exposed | The accepted 165-record vector and `3352106d…faf2` digest pass, but the seven fields retain semantic shorthand rather than exact callable definitions, selector dictionaries, supports, macros/locations, native declarations, and five source bodies | Worker stopped before files or Bazel; worktree remained clean and no fixture, payload, expected record, Rust, Cargo, JVM artifact, or generated source was created | `REPLAN`; design only `WP-4-8-m3-attr-five-source-template-oracle-design`, freezing LF-exact source bodies and literal argv/stdout with temporary two-root Bazel validation before generation |
| 2026-08-09 | M3 two-package `attr` oracle generation preflight | Fresh Terra generation worker and root manifest-completeness audit | not exposed | The reviewed 165-pair families and external baseline are viable, but only `l02_a007` had a stable ID-to-attr/regex/schema/value binding; no fixture writer could audit the remaining 164 inferred choices against authoritative text | Worker stopped before payload or Bazel; root removed its empty directories and retained no fixture/content change | `REPLAN`; design only `WP-4-8-m3-attr-atomic-discriminator-manifest-design`, freezing all 165 atom bindings before generation |
| 2026-08-09 | M3 isolated `attr` oracle generation retry | Terra semantic-binding worker, root pinned-source stop audit, Terra complete pair-count audit, and Sol correction review | not exposed | Preflight corrected the no-reuse ledger from 170 to 165 pairs, then pinned `deprecationDefault` proved one package cannot expose both positive package deprecation and a same-schema null control because explicit `None` leaves the computed default active | The accepted sole correction fixed all lane counts; the second material contradiction fired `REPLAN` before Bazel, and the entire incomplete fixture/payload draft was removed with no retained evidence or Rust change | `REPLAN`; design only `WP-4-8-m3-attr-two-package-observable-candidate-oracle-design`, preserving 165 atoms while adding an honest baseline package and recalculating caps |
| 2026-08-09 | M3 `attr` observable-candidate oracle generation | Terra generation worker, root integration review, independent Terra completeness audit | not exposed | A five-file draft froze `@@ext+//leaf:label` and passed update/clean replay for 57 Bazel rows, but its first transcription omitted ledger atoms and reused positive union labels; the focused correction proved required `attr.string_list()` in shared `pkg/defs.bzl` breaks the protected 29-row Slug CLI consumer before row one | Root rejected the green-but-incomplete record; production Starlark expansion is forbidden in an oracle packet and weakening the matrix is forbidden, so the complete unaccepted draft was restored to `6c9a529e` with no retained payload/oracle/Rust change | `REPLAN`; design only `WP-4-8-m3-attr-isolated-observable-candidate-oracle-design`, using a Bazel-only workspace absent from protected Slug consumers and unique target pairs for every atom |
| 2026-08-11 | M7 built-in bazel_tools owner implementation | Root bounded implementation, serial validation, cleanup, and independent Sol final review | not exposed | Seven archive files were byte/mode exact and owner/dependent suites passed except the documented core baseline | Final review found a snapshot/path-only file key could not prove the frozen distinct directory/wrong-kind algebra after the Host-guard correction had consumed the retry budget | `REPLAN`; discard all Rust/assets and design only the typed source-kind/error identity before retrying |
| 2026-08-12 | M7 nonregistry package-preflight implementation cap | Root implementation, serial focused/full validation, and independent review | not exposed | The accepted route-independent REPO, ignore, and package-preflight keys compile and the full owner suite passes; review found no semantic ownership/order defect but measured about 440 production lines against a frozen 360 cap | Bounded boilerplate reduction could not recover the required margin without collapsing the three DICE owners; the uncommitted Rust diff remains unaccepted | `REPLAN`; correct only the measured production/total caps, retain the same five Rust files and semantics, then add missing immutable/event/source-kind proofs before implementation acceptance |
| 2026-08-12 | M7 nonregistry MODULE closure implementation cap | Root implementation, exact section accounting, serial validation, cleanup audit, and independent review | not exposed | Route-free Host closure and unchanged direct adapters pass focused 2/2, full slug_bzlmod_v2, loading/core checks, formatting, and diff checks; no semantic or identity defect was found | The shared BFS removed the unsafe duplicate, but retained value/error identity and distinct Host/direct source, preflight, and adapter algebras measured 587 production plus 468 test lines against 420/480 | REPLAN; preserve the exact two-file diff and all semantics/stops, correct only caps to 620/500/1,120, then resume the same implementation as revision two |
| 2026-08-18 | M1 repository-package source through package-load owner audit | Root bounded implementations, source proof-cap REPLAN/retry, serial validation, cleanup/review and natural-owner audits | not exposed | Accepted source `ac7b8bdf` and external-Bzl `93f43264`; package load alone owns observed-ready source/children, BUILD evaluation semantic Result and local batch, while its attempt helper is compute-local | Source proof needed one cap correction; external cycle completion needed one stale-Cycle source-terminal fix and lifecycle compaction; no smaller package-load prerequisite remains | `ACCEPT`; design only `WP-2A-m1-repository-package-load-observation-design` before implementation or upper activation |
| 2026-08-18 | M1 repository-package-load observation | Root bounded implementation, serial validation, proof compaction, cleanup and independent review | not exposed | Accepted `a9270586` adds the hidden matching-family package-load sibling, exact source/child epoch association and sole local BUILD-batch ownership; 204 loading and 120 query tests pass with complete bzlmod validation | Review required a cohesive helper split, every-position outer/prefix proof, BUILD plus `.bzl` lifecycle and discriminating concurrent family dependency rows; all closed at the exact test cap | `ACCEPT`; audit only the first complete upper query/core owner before any caller cutover or M1 closure |
| 2026-08-18 | M1 post-package-load upper-owner audit | Root live-owner trace plus independent natural-owner review | not exposed | `RootQueryCommandKey` is the sole native query terminal and its environment reaches every graph/provenance/subtree/companion edge; accepted package-load `a9270586` closes the external carrier gap that forced the earlier query REPLAN | Private observed graph/subtree siblings are cohesive query-root mechanisms, while core exported-source build adds a distinct selected-source/certificate boundary | `ACCEPT`; resume only `WP-2A-m1-loading-query-observed-publication-design-resume` before implementation or any core cutover |
| 2026-08-18 | M1 loading-query publication proof/parallel stop | Root implementation, focused/full validation and independent pre-review | not exposed | The production candidate passes focused/full query and bzlmod, but prior loading proof still forbids the required observed package-load query edge; default-parallel core also mutates shared `/tmp` ancestors used by two byte-frozen query bodies | Both failures require test-only authority outside the seven-file packet; hiding the key or treating the failures as inherited would make the accepted proof nondiscriminating | `REPLAN`; design only one line-neutral loading assertion correction plus three test-exclusive crate-target stable parents, then retry the same implementation |
| 2026-08-18 | M1 loading-query external repository-selection stop | Root correction retry and isolated external-query validation | not exposed | Stable workspace isolation closes the parallel failures, but external query legitimately selects repository requests/validations and the inherited path-only observed-terminal validator rejects all nonempty repository sidecars | Query cannot duplicate post-compute activation-closure selection in its compact carrier; strict build roots must continue rejecting extras | `REPLAN`; design one private typed root policy permitting exact closure-selected repository sidecars only for observed query while preserving full path-epoch validation |
| 2026-08-18 | M1 loading-query accepted-event replay stop | Root correction retry, isolated external-query validation and independent owner review | not exposed | Repository selection is now valid, but unrelated sibling-file churn recomputes exact observed children with equal local batches; terminal selection drops node identity and native acceptance retains no prior event epoch, so unchanged events replay | Weakening path epochs or child equality would retain stale carriers; the acceptance boundary already owns exact closure selection and atomic accepted snapshot replacement | `REPLAN`; design one compact Arc-backed node/batch epoch at native acceptance, preserving child ownership and exact path validation, then resume the same query candidate |
| 2026-08-18 | M1 external singleton observed-build loading-proof authority stop | Root implementation, focused validation, independent review and full loading validation | not exposed | The observed external branch correctly consumes `RepositoryPackageLoadObservationKey` and passes its public lifecycle proof within +611 semantic lines, but the accepted loading proof still statically forbids any core reference to that key | The proof file is outside the exact two-file authority; hiding the key or reverting to legacy package loading would invalidate matching-family ownership | `REPLAN`; freeze only the line-neutral core-positive loading assertion, retain the candidate, then resume the same implementation |
| 2026-08-18 | M1 external singleton source-retry event carry stop | Root correction retry, focused/full validation, accepted-base archive comparison and native event-owner audit | not exposed | The candidate fixes the base's root-switch replay but newly replays an equal child-owned package BUILD batch after source deletion; base and current server runs distinguish this from four unchanged query failures | The build root cannot filter its child; discarded provisional event closure state across certificate revision retry lets an equality-reused final root hide and drop the prior child | `REPLAN`; design one command-local provisional event-epoch carry at native acceptance, retain the candidate, then resume the same implementation |
| 2026-08-18 | M1 external singleton revision-carry root association stop | Root retry-2 implementation, exact accepted-epoch instrumentation and independent acceptance-owner review | not exposed | The command-local tombstone carry compiles, but the pre-revision equality-reused root already omits its accepted package event descendant, so the edit acceptance drops that node before the later delete replay | Unconditional prior seeding confuses a hidden reused descendant with a genuinely removed owner under a different or reevaluated root; compact accepted ordered root IDs plus reuse-only seeding are required | `REPLAN`; design only `WP-2A-m1-external-singleton-event-root-association-correction-design`, retain the four-file candidate, then resume the same implementation |
| 2026-08-18 | M1 external singleton retry-carry transition correction | Root retry-3 implementation, exact root/event-node transition trace and independent event-owner review | not exposed | The fixed root is always Known(None), while the equal package event node remains present as Known(None); the accepted root-NoTransition predicate can never seed and the edit acceptance still drops the package batch | For source-certified roots, semantic Complete owners store Some including empty; first-retry present-prior KnownNone is transient, while removal is closure absence | `REPLAN`; design only per-node current-closure reconciliation, retain the candidate, then resume retry 4 |
| 2026-08-18 | M1 external singleton direct-finalization event stop | Root retry-4 implementation, accepted/provisional entry trace and independent native-policy review | not exposed | Source preflight consumes the edit, so cold/warm/edit all accept without RevisionRetry; the correct provisional exists but is unused while ordinary Strict reconciliation drops the equal package batch | Global KnownNone reinterpretation breaks other roots; only the source-certified external terminal can safely select current-closure carry for normal and retry acceptance | `REPLAN`; design a private terminal-scoped event reconciliation policy, retain the candidate, then resume retry 5 |
| 2026-08-18 | M1 observed terminal epoch-association stop | Root retry-5 implementation, focused event/external validation, unchanged server lifecycle and independent native-owner review | not exposed | Source-certified event reconciliation fixes equal package replay, but external -> root PackageAll -> external wrong-kind reuses a terminal carrier whose exact Host route Lstat Arc differs from the equal fresh command-epoch Arc | Whole-terminal pointer validation is correct; the acceptance owner has both epochs and can reconcile terminal-first before selected-demand filtering without another producer/state | `REPLAN`; design only `WP-2A-m1-observed-terminal-epoch-association-correction-design`, retain the candidate, then resume retry 6 |
| 2026-08-18 | M1 external singleton observed-build publication | Root bounded implementation, six reviewed correction designs/retries, serial validation, cleanup/retention and two independent final reviews | not exposed | Accepted `3f1d4dd4` adds nonroot-Single observed admission, exact full source certificates, terminal-first Arc authority and certificate-gated current-closure event reconciliation within +1,021 semantic lines | Corrections preserved strict roots and child event ownership while closing loading-proof authority, revision carry, direct-finalization replay and terminal Arc association; multi-build and one-shot remain distinct | `ACCEPT`; audit only `WP-2A-m1-post-external-singleton-remaining-owner-audit` before another design or M1 closure |
| 2026-08-18 | M1 post-external-singleton remaining-owner audit | Root read-only live owner trace and independent audit review | not exposed | Existing root-only multi-target `BuildCommandRootKey` owns ordered branch/action aggregation and native acceptance; accepted lower carriers and epoch-shaped certificates close every prerequisite | One-shot workspace evaluators are fresh-runtime snapshot adapters outside `AcceptedCommand`, not a smaller native publication owner | `ACCEPT`; design only `WP-2A-m1-multi-build-observed-publication-design`, keep external/recursive/one-shot cutovers deferred |
| 2026-08-18 | M1 multi-build observed publication analysis-error stop | Root bounded implementation, focused mixed success proof and independent pre-review | not exposed | Ordered observed multi success, aggregate certificates and selected dependency remainder work, but a semantic analysis error makes the root transient; strict selection sees an unavailable root while legacy pruning leaves no closure demands for its nonempty local epoch | Native acceptance must explicitly select the trusted terminal-local lower prefix only for this transient multi Analysis terminal; branch certificates must also become aggregate-only | `REPLAN`; design only `WP-2A-m1-multi-build-analysis-error-acceptance-correction-design`, retain the Rust candidate, then resume one bounded retry |
| 2026-08-18 | M1 multi-build observed publication | Root bounded retry, focused/broad validation, compact-retention cleanup and independent final review | not exposed | Accepted `51127df8` adds root-only observed multi aggregation, request-ordered aggregate certificates, strict selected dependency supersets and the Analysis-only terminal-local acceptance correction within +898 semantic lines | Aggregate branch certificates are moved out of the legacy-shaped Result; repositories remain strict, child events remain sole-owned, and no retained selected state or lower producer was added | `ACCEPT`; audit only `WP-2A-m1-post-multi-build-remaining-owner-audit` before any one-shot migration or M1 closure |
| 2026-08-18 | M1 post-multi-build remaining-owner audit | Root live caller/owner trace and independent read-only review | not exposed | Typed one-shot and daemon callers already use the accepted native command roots; residual workspace-target/query snapshot APIs are test/compatibility projections returning non-published values | No alternate revision, certificate, repository or event acceptance owner remains; migrating snapshot adapters for uniformity would duplicate the accepted spine | `ACCEPT`; run only `WP-2A-m1-completion-next-milestone-transition-audit` before M1 acceptance or M7A scheduling |
| 2026-08-18 | M1 completion and next-milestone transition | Root eight-clause live evidence audit plus independent exit-gate review | not exposed | Shared typed roots, overlap/cancellation, mutation retry, exact final certificate reobservation, atomic materializer/path/repository/event publication, lifecycle restoration, warm nonreplay and compact retention all have discriminating accepted proof | Snapshot workspace-target/query APIs are exact non-published compatibility adapters, not missing semantic owners; no M1 exit dependency remains | `ACCEPT`; close M1 and run only `WP-6-7A-bootstrap-action-owner-context-evidence-audit` before M7A design, evidence generation or implementation |
| 2026-08-18 | M7A immutable action-owner-context evidence audit | Root live ActionSpec/configured-result/aquery/REAPI trace plus independent first-gate review | not exposed | Slug retains intrinsic ActionSpec separately from topology and reconstructs only a default-group platform; the dedicated exec-group fixture is an ungenerated one-action scaffold with no expected commands | A focused Bazel 9.2 two-action default/named-group owner discriminator is uniquely smaller than guessing the immutable Rust row; no lower DICE or M1 prerequisite remains | `ACCEPT`; run only `WP-1-6-7A-exec-group-action-owner-context-evidence`, then design the immutable owner row from accepted evidence |
| 2026-08-19 | M7A immutable action-owner implementation absence stop | Root full analysis validation plus independent representation/ownership review | not exposed | Existing zero-toolchain and selected-toolchain-implementation actions have no truthful prepared `ToolchainSelection`; the mandatory concrete context rejects six exact action tests | Fabricating a selection corrupts identity; one explicit selected-toolchain/selected-platform-only/unresolved state correction preserves intrinsic actions and the existing projection boundary without a new DICE owner | `REPLAN`; design only `WP-6-7A-action-owner-context-absence-correction-design`, retain the eleven-file candidate, then resume one bounded retry |
| 2026-08-19 | M7A immutable configured-action owner context | Root corrected bounded implementation, full affected validation, cap/retention cleanup and independent final review | not exposed | Accepted `cb5073e0` retains one configured-action slice with selected-toolchain, selected-platform-only and unresolved-default contexts; FileWrite identity/aquery/REAPI borrow the row and selected Platform terminals suppress later analysis | The owner is complete without fabricated identity, a new DICE key or retained topology/result state; M7A still needs the exact bootstrap repository/rules/action/input/aquery/REAPI closure | `ACCEPT`; audit only `WP-6-7A-post-owner-context-bootstrap-closure-owner-audit` before another design, evidence packet or M7A closure |
| 2026-08-19 | M7A post-owner-context bootstrap closure audit | Root live registration/rules_rust/action/aquery/REAPI owner trace plus independent audit review | not exposed | Direct Bazel 9.2 registration, root selection and action-owner evidence is accepted, but rules_rust 0.73 extension-generated `@rust_toolchains//:all` mapping and selected Rust provider/action context are not discriminated; the older rules_rust record is Bazel 9.1.1/message-shape only | External toolchain design would guess pattern expansion, contextual mapping and provider/action ownership; an isolated analysis-only 9.2/0.73 fixture is uniquely smaller than code or broad ruleset work | `ACCEPT`; run only `WP-1-6-7A-rules-rust-0.73-toolchain-action-owner-evidence`, then design the external rules_rust toolchain owner |
| 2026-08-19 | M7A rules_rust 0.73 generated-owner evidence and lower-frontier stop | Root exact Bazel 9.2/rules_rust 0.73 fixture, live bzlmod/loading owner trace and independent review | not exposed | Evidence `b7390392` pins generated registration, canonical selection, provider edges and action ownership, but root apparent mapping still crosses carrierless selected-graph, extension input/evaluation/instantiation, route/source and package-load owners | A mapping-only or analysis-local carrier would duplicate extension/repository ownership and miss the complete epoch; the natural lower candidate spans the existing bzlmod/loading owners and must be frozen before analysis | `REPLAN`; design only `WP-6-7A-root-generated-repository-observation-frontier-design`, audit the full extension-evaluation chain, then resume the external rules_rust owner after implementation acceptance |
| 2026-08-19 | M7A generated-repository frontier root-prefix stop | Root full candidate-chain audit plus independent scope review | not exposed | The accepted root-module observed value drops extension usages, while completed visible-lockfile reads retain no epoch; selected graph therefore lacks its first exact reusable root prefix before any registry/extension work | `RootModuleFilesKey` is the uniquely smaller existing owner and can combine the accepted root-module carrier with a mode-exact observed lockfile projection without another DICE owner | `REPLAN`; design only `WP-6-7A-root-module-files-observation-completion-design`, then return to selected-module-graph frontier design after implementation acceptance |
| 2026-08-19 | M7A root MODULE-files implementation cap/proof stop | Root bounded implementation, full bzlmod compile/test and independent cap/ownership pre-review | not exposed | The cohesive candidate is +285 production in `module_eval.rs` against +180; its observed root/lockfile semantic errors also use Debug-derived text rather than discriminating the accepted legacy projections | A 105-line forced reduction would compress the shared owner; retain the two-file candidate, raise only measured caps and require explicit legacy-equivalent error projection proof | `REPLAN`; design only `WP-6-7A-root-module-files-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A selected-module-graph frontier first-child audit | Root completion, live selected-graph/discovery/preparation dependency trace and bounded owner analysis | not exposed | Accepted `a3efa1b7` supplies the exact root MODULE+lockfile epoch, but selected graph, discovered modules, preparation, nonregistry preflight and repository selection all still begin through carrierless `HostEffectiveModuleOverrideKey` | That key is the smallest shared consumer of root files plus command policy; selecting graph or discovery now would duplicate override precedence and lose the reusable prefix | `REPLAN`; design only `WP-6-7A-effective-module-override-observation-design`, then return to selected-module-graph frontier work after implementation acceptance |
| 2026-08-19 | M7A effective-module-override proof/cap stop | Root bounded implementation, focused/full bzlmod validation and independent proof review | not exposed | The sound one-file carrier/driver candidate is +170 production/+236 tests against +160/+240, while the remaining four test lines cannot discriminate the live root reducer, parent dependency row, exact legacy Arc, cold events, poll-drop recovery and command lifecycle | Forcing the old cap would use synthetic proof or opaque compression; retain the candidate and raise only measured one-file proof/seam limits without changing semantics | `REPLAN`; design only `WP-6-7A-effective-module-override-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A effective-module-override second proof-cap stop | Root implementation retry, focused proof validation and independent compaction review | not exposed | The complete proof is +390 tests/+565 aggregate against +320/+520; removing 70 test lines would weaken real dependency-row, cancellation, event, Arc and lifecycle discrimination | Preserve the accepted one-file production owner and raise only measured test/aggregate/physical ceilings; no semantic or owner change | `REPLAN`; design only `WP-6-7A-effective-module-override-observation-proof-cap-correction-2-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A selected-module-graph frontier materialization-request stop | Root live selected-graph/discovery/materialization trace plus independent owner review | not exposed | Accepted `3d174006` supplies the effective-override epoch; nonregistry discovery crosses the carrierless request, while a complete observed registry-preparation owner must preserve its existing nonregistry branch | The request key is the smallest reusable owner of workspace, override, canonical repository and request-kind projection; observing preparation/closure/discovery first would duplicate or bypass it | `REPLAN`; design only `WP-6-7A-repository-materialization-request-observation-design`, then return to selected-graph frontier work after implementation acceptance |
| 2026-08-19 | M7A materialization-request observation proof-cap stop | Root bounded implementation, focused/full validation and independent proof review | not exposed | The sound one-file candidate is +161 production/+319 tests/+480 aggregate at 14,227 physical, but its one remaining test line cannot discriminate the frozen observed terminal and command/request-kind lifecycle matrix | Compacting enough proof would discard accepted dependency-row, family, event, cancellation or Arc discrimination; retain the candidate and raise only measured test/aggregate/physical proof caps | `REPLAN`; design only `WP-6-7A-repository-materialization-request-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A selected-module-graph frontier materialization stop | Root accepted request completion, live selected-graph/discovery/materialization trace and independent owner review | not exposed | Accepted `cc847c98` closes request projection, but nonregistry closure and repository source still share carrierless `RepositoryMaterializationKey`; registry preparation remains a separate later frontier | Observing source/closure/discovery/selected graph first would duplicate materialization semantics; the neutral result child adds no path epoch or event | `REPLAN`; design only `WP-6-7A-repository-materialization-observation-design`, then return to selected-graph frontier work after implementation acceptance |
| 2026-08-19 | M7A selected-module-graph frontier repository-source stop | Root accepted materialization completion, live source/preparation/closure/registry trace and independent owner review | not exposed | Accepted `ae8aa35e` supplies the request/materialization epoch, but shared `RepositorySourceFileKey` still loses resolution and FileBytes observations; registry `RegistryFileKey` remains a distinct later frontier | The repository-source key is the smallest reusable owner of materialization -> resolution -> FileBytes; the route sibling cannot substitute and a detached path key has no independent consumer | `REPLAN`; design only `WP-6-7A-repository-source-file-observation-design`, then return to selected-graph frontier work after implementation acceptance |
| 2026-08-19 | M7A repository-source observation proof-cap stop | Root bounded implementation, focused/full validation and two independent proof/retention reviews | not exposed | The sound two-file candidate is +298 production/+500 proof/+798 aggregate at 15,238/2,970/18,208 physical, but the exact proof cap leaves parent outer/conflict rows, cold child events, same-request recovery and immutable lifecycle undiscriminated | Preserve the accepted owner/semantics and raise only the proof/aggregate/physical ceilings; do not compress away exact ordering, Arc, family, event or lifecycle assertions | `REPLAN`; design only `WP-6-7A-repository-source-file-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A selected-module-graph frontier nonregistry REPO-file stop | Root accepted repository-source implementation, live selected-graph/preflight/ignore trace and two independent owner audits | not exposed | Accepted `12f68983` closes materialization -> resolution -> FileBytes, but the Host include horizon still crosses package preflight -> repository ignore -> the carrierless, event-owning nonregistry REPO-file key | The REPO-file key is the smallest source-first owner and its accepted source child is sufficient; observing ignore/closure/discovery/graph would duplicate its batch, while registry preparation is separate | `REPLAN`; design only `WP-6-7A-host-nonregistry-repo-file-observation-design`, then observe ignore before returning to the selected-graph frontier |
| 2026-08-19 | M7A nonregistry REPO-file proof-cap stop | Root bounded implementation, focused compile/proof and measured cap review | not exposed | The sound one-file candidate is +163 production/+421 proof/+584 aggregate at 3,263 physical, while +320/3,200 cannot retain exact family/Arc/lifecycle proof and add the required Need/outer/error/event/cancel/upper discriminators | Preserve the accepted source-first owner and raise only proof/aggregate/physical ceilings; no production semantic, event, memory or family change | `REPLAN`; design only `WP-6-7A-host-nonregistry-repo-file-observation-proof-cap-correction-design`, then retry the same implementation before ignore design |
| 2026-08-19 | M7A nonregistry REPO-file second proof-cap stop | Root implementation retry, focused proof, exact accounting and independent compaction/retention review | not exposed | The production-sound one-file candidate is +170 production/+628 proof/+798 aggregate at 3,477 physical, already over the first corrected proof/aggregate/physical caps while exact key, terminal, batch, row, lifetime and legacy-parity proof remains | Safe factoring recovers only 25--40 lines; forcing <=550 would delete discriminating Need/outer, cancellation, family, epoch or lifecycle evidence | `REPLAN`; design only `WP-6-7A-host-nonregistry-repo-file-observation-proof-cap-correction-2-design`, then retry the same one-file implementation before ignore design |
| 2026-08-19 | M7A nonregistry REPO-file observation | Root retry-2, focused/full validation, cleanup/retention and independent final review | not exposed | Accepted `b08b7f2e` adds the private matching-family source-first REPO-file carrier within +170 production/+718 proof at 3,567 physical lines | Exact local REPO batch ownership and one Result Arc+compact epoch close the reusable child; ignore remains the uniquely smaller next aggregate owner | `ACCEPT`; design only `WP-6-7A-host-nonregistry-repository-ignore-observation-design`, then package preflight after implementation acceptance |
| 2026-08-19 | M7A nonregistry repository-ignore proof-cap stop | Root bounded implementation, focused parent proof, exact accounting and independent proof review | not exposed | The production-sound one-file candidate is +180 production/+484 proof/+664 aggregate at 3,961 physical, already beyond the original proof/aggregate/physical caps while real source outer, parent union/order, event silence and independent REPO lifecycle proof remains | Preserve the accepted repo -> source -> parser owner and raise only proof/aggregate/physical limits; no semantic, event, family, memory or caller change | `REPLAN`; design only `WP-6-7A-host-nonregistry-repository-ignore-observation-proof-cap-correction-design`, then retry the same implementation before package preflight |
| 2026-08-19 | M7A nonregistry repository-ignore observation | Root corrected retry, focused/broad validation, cleanup/retention and independent final review | not exposed | Accepted `754e7619` adds the private effective REPO/source/parser carrier within +179 production/+715 proof/+894 aggregate at 4,191 physical; sole core miss is the inherited stale visibility wording baseline | The package-preflight key is now the smallest complete aggregate owner because effective override, ignore and marker sources all have observed carriers and deleted policy is neutral | `ACCEPT`; design only `WP-6-7A-host-nonregistry-package-preflight-observation-design`, then module closure only after implementation acceptance |
| 2026-08-19 | M7A nonregistry package-preflight proof-cap stop | Root bounded implementation, focused observed-preflight proof and exact cap accounting | not exposed | The production-sound two-file candidate is +319 production/+15 colocated proof and +832 external proof (+1,166 aggregate) at 15,601/4,002/19,603 physical, already beyond the 720/1,040 proof/aggregate limits while exact compute, semantic-prefix, policy-error and child-batch proof remains | Forcing a 126-line deletion would discard discriminating family, event, Arc, lifecycle or cancellation evidence; retain the candidate and raise only proof/aggregate/physical ceilings | `REPLAN`; design only `WP-6-7A-host-nonregistry-package-preflight-observation-proof-cap-correction-design`, then retry the same implementation before module-closure design |
| 2026-08-19 | M7A nonregistry package-preflight second proof-cap stop | Root implementation retry, focused proof, exact accounting and independent proof/cleanup review | not exposed | The production-sound candidate is +319 production/+24 colocated proof/+949 external proof (+1,292 aggregate) at 15,610/4,119/19,729 physical, leaving only 11 external/eight aggregate lines while exact later-terminal epochs, complete events, real upper prefixes and cancellation child silence remain | Preserve the owner and raise only external proof/aggregate/physical caps; no production semantic/event/family/memory change | `REPLAN`; design only `WP-6-7A-host-nonregistry-package-preflight-observation-proof-cap-correction-2-design`, then retry-2 before module-closure design |
| 2026-08-19 | M7A nonregistry package-preflight observation | Root retry-2, focused/full validation, exact accounting, cleanup/retention and independent owner audit | not exposed | Accepted `18166691` adds the matching-family eventless package-preflight carrier within +320 production/+24 colocated proof/+1,060 external proof, while closure is now the smallest complete aggregate and its horizon/fragment reducers have no separate consumer | Freeze one private closure Result-Arc+epoch sibling with matching-family BFS/full-batch algebra; keep discovery, selected graph and registry inactive | `ACCEPT`; design only `WP-6-7A-host-nonregistry-module-closure-observation-design`, then one implementation before returning to the selected-graph frontier |
| 2026-08-19 | M7A nonregistry module-closure observation | Root bounded implementation, focused/full validation, exact accounting, cleanup/retention and independent final review | not exposed | Accepted `28fab9b0` adds the private matching-family eventless closure carrier within +519 production/+5 colocated proof/+1,032 external proof; registry preparation remains a separate carrierless frontier before selected graph | The complete nonregistry branch is reusable without moving child ROOT/REPO batches; the remaining registry/discovery chain must be audited neutrally before another owner is frozen | `ACCEPT`; audit only `WP-6-7A-selected-module-graph-observation-frontier-audit`, then schedule at most one independently reviewed design |
| 2026-08-19 | M7A selected-module-graph frontier registry-policy stop | Root accepted closure, live selected-graph/discovery/registry trace and independent owner audit | not exposed | Accepted `28fab9b0` closes nonregistry closure, but both registry source preparation and registry files still share carrierless `RegistryPolicyKey`; registry I/O, patches, discovery and selected graph remain later | Policy is the smallest owner of injected URLs -> mode -> accepted root files, and its only path child already has a reusable carrier; larger owners would duplicate the prefix | `REPLAN`; design only `WP-6-7A-registry-policy-observation-design`, then return to the selected-graph frontier after one implementation |
| 2026-08-19 | M7A registry-policy observation proof-cap/invariant stop | Root bounded implementation, focused/broad validation, exact accounting and independent proof review | not exposed | The production-sound candidate is +166 production/+609 proof/+775 aggregate at 2,188 physical; exact child batches, family exclusion and independent four-edge lifecycle exceed +520, while failed InjectedKeys expose no activation row and valid epochs cannot inject a root outer | Preserve the one-file owner; raise only proof/aggregate/physical ceilings and use the production root projector plus accepted lower outer proof without test hooks or invalid epochs | `REPLAN`; design only `WP-6-7A-registry-policy-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-19 | M7A registry-policy observation | Root corrected retry, focused/broad validation, exact accounting, cleanup/retention and independent final review | not exposed | Accepted `d0ebd79d` adds the matching-family policy Result-Arc+epoch carrier within +166 production/+634 proof/+800 aggregate at 2,213 physical; registry preparation/discovery remain carrierless before selected graph | Resume the neutral selected-graph frontier to locate exactly one smallest remaining producer without presuming registry file, preparation, discovery or graph ownership | `ACCEPT`; audit only `WP-6-7A-selected-module-graph-observation-frontier-audit-resume`, then schedule at most one independently reviewed design |
| 2026-08-19 | M7A selected-module-graph frontier registry-file stop | Root accepted policy, live registry/preparation/discovery/graph trace and independent owner audit | not exposed | `RegistryFileKey` is the complete scheme/semantic IO owner shared below module preparation and selected repo specs; preparation still owns patch observations and discovery owns evaluation batches | A private one-file matching-family Result-Arc+epoch sibling is smaller than preparation/discovery/graph; dormant HostRegistry is post-selected-graph policy | `REPLAN`; design only `WP-6-7A-registry-file-observation-design`, then return to the frontier after one implementation |
| 2026-08-19 | M7A registry-file observation | Root bounded implementation, focused/full validation, exact accounting, cleanup/retention and independent final review | not exposed | Accepted `0f9a0559` adds the private matching-family registry-file Result-Arc+epoch carrier within +246 production/+760 proof/+1,006 aggregate at 3,219 physical | Registry patches/source preparation, discovery and selected graph remain a neutral frontier; no upper owner is yet selected | `ACCEPT`; audit only `WP-6-7A-selected-module-graph-observation-frontier-audit-resume-2`, then schedule at most one independently reviewed design |
| 2026-08-19 | M7A selected-module-graph frontier module-preparation stop | Root accepted registry file, live preparation/patch/discovery/graph trace and independent owner audit | not exposed | ModuleSourcePreparationKey owns effective selection, registry attempts and the owner-local two-phase patch resolution/FileBytes pipeline; discovery separately owns evaluation/events | Accepted lower carriers close every preparation edge and no patch-only key has another consumer; discovery/graph would duplicate or reconstruct the prefix | `REPLAN`; design only `WP-6-7A-module-source-preparation-observation-design`, then return to discovery/frontier after one implementation |
| 2026-08-20 | M7A module-source preparation proof-cap stop | Root bounded implementation, focused/full bzlmod validation, exact accounting and independent proof/retention review | not exposed | The production-sound two-file candidate is +344 source/+1,332 external proof (+1,676 aggregate) at 16,479/6,594/23,073 physical, while only 108 external and 60 colocated proof lines remain for complete compute/semantic-prefix, mismatch, terminal-position and lifecycle proof | Preserve the accepted one-driver owner and raise only external proof/aggregate/physical ceilings; deleting exact family/event/cancel/Arc/lifecycle discriminators would weaken the proof | `REPLAN`; design only `WP-6-7A-module-source-preparation-observation-proof-cap-correction-design`, then retry the same implementation before discovery/frontier work |
| 2026-08-20 | M7A module-source preparation second proof-cap stop | Root corrected retry, full bzlmod validation, exact accounting and two independent terminal reviews | not exposed | Production-called projectors and real patch matrices close the prior gaps, but +1,780/+7,042 external proof leaves only 20 lines for distinct-key hash, exact cumulative-epoch reconstruction, lockfile mode/content restoration and symlink-retarget restoration | Preserve the accepted two-file owner and every existing discriminator; raise only external proof/aggregate/physical ceilings rather than compress lifecycle association into inference | `REPLAN`; design only `WP-6-7A-module-source-preparation-observation-proof-cap-correction-2-design`, then retry-2 before discovery/frontier work |
| 2026-08-20 | M7A module-source preparation observation | Root retry-2, focused/full bzlmod validation, exact accounting, cleanup/retention and two independent terminal reviews | not exposed | Accepted `223c8112` adds the private shared-driver preparation Result-Arc+epoch carrier within +367 source/+1,969 external proof (+2,336 aggregate) at 16,502/7,231/23,733 physical | Discovery still separately owns evaluation/events and selected graph joins its horizons; resume the neutral frontier without presuming either owner | `ACCEPT`; audit only `WP-6-7A-selected-module-graph-observation-frontier-audit-resume-3`, then schedule at most one independently reviewed design |
| 2026-08-20 | M7A selected-module-graph frontier discovered-module stop | Root live discovery/graph/consumer trace and two independent owner audits | not exposed | `HostDiscoveredModuleKey` is the first common semantic parent above accepted closure/preparation and alone evaluates MODULE bytes and owns the matching event batch; selected graph only joins its leaves | All mutable discovery children have observed siblings and builtin is immutable, so no smaller prerequisite remains; observing selected graph first would reconstruct epochs and move event authority | `REPLAN`; design only `WP-6-7A-host-discovered-module-observation-design`, then return to the frontier after one implementation |
| 2026-08-20 | M7A discovered-module observation proof-cap stop | Root bounded implementation, focused/full bzlmod validation, exact accounting and independent terminal review | not exposed | Production is sound at +309 source/+809 external proof (+1,118 aggregate), but only 11 external lines remain for real builtin/nonregistry/registry terminals, complete ordered family/event parity and separated held-handle lifecycles | Preserve the accepted private owner and production contract; raise only external proof/aggregate/physical ceilings rather than replace branch, event or lifecycle discrimination with inference | `REPLAN`; design only `WP-6-7A-host-discovered-module-observation-proof-cap-correction-design`, then retry the same implementation before selected-graph work |
| 2026-08-20 | M7A discovered-module observation | Root corrected retry, focused/full bzlmod validation, exact accounting, cleanup/retention and independent final review | not exposed | Accepted `c6b1e108` adds the private shared-driver discovered-module Result-Arc+epoch carrier within +309 source/+1,175 external proof (+1,484 aggregate) at 16,811/8,406/25,217 physical, with exact builtin/nonregistry/registry family and event parity | Every mutable discovery child and discovery itself now has an accepted carrier; resume the neutral selected-graph frontier to verify its BFS/fixed-point aggregate or find one strictly smaller remaining edge | `ACCEPT`; audit only `WP-6-7A-selected-module-graph-observation-frontier-audit-resume-4`, then schedule at most one independently reviewed design |
| 2026-08-20 | M7A selected-module-graph frontier aggregate stop | Root accepted discovery, live selected-graph/consumer trace and independent owner audit | not exposed | `HostSelectedModuleGraphKey` is the first common semantic aggregate above accepted root/effective/discovery carriers and alone owns request transformation, BFS/fixed-point and graph selection; repo-spec/routes are later consumers | No smaller carrierless edge remains; preserve exact legacy horizon error-over-Need precedence and child event ownership in one private Result-Arc+epoch sibling | `REPLAN`; design only `WP-6-7A-host-selected-module-graph-observation-design`, then return to the frontier after one implementation |
| 2026-08-20 | M7A selected-module-graph observation proof-cap stop | Root bounded implementation, focused/full bzlmod validation, exact accounting and independent terminal review | not exposed | Production is sound at +370 production/+295 colocated/+807 external proof (+1,472 aggregate), but the old cap cannot hold live-stage terminals, observed topology/event parity and independent held-carrier lifecycles | Preserve the accepted two-file owner and full-horizon correction; raise only proof/aggregate/physical ceilings rather than replace behavioral discrimination with inference | `REPLAN`; design only `WP-6-7A-host-selected-module-graph-observation-proof-cap-correction-design`, then retry the same implementation before returning to the frontier |
| 2026-08-20 | M7A selected-graph frontier visible-lockfile stop | Root accepted selected graph, live repo-spec/Host-registry/lockfile trace and independent owner audit | not exposed | `HostVisibleLockfileKey` preserves file -> mode -> parse and still parses present bytes under Off, while the accepted root-files carrier is mode-first, returns Ignored/empty and includes unrelated root observations | A private one-file Host-visible Result-Arc+exact-epoch sibling is the smaller prerequisite; HostRegistryFunction and repo specs remain deferred | `REPLAN`; design only `WP-6-7A-host-visible-lockfile-observation-design`, then resume Host-registry/repo-spec frontier after one implementation |
| 2026-08-20 | M7A Host-registry-function frontier stop | Root accepted Host-visible lockfile, live Host-registry/repo-spec trace and independent owner audit | not exposed | `HostRegistryFunctionKey` is the smallest complete boundary and its sole path child now has an accepted exact epoch; selected repo specs separately own graph/registry-file/effective/spec aggregation | Add one private matching-family Host-registry Result-Arc+exact-visible-epoch sibling; keep repo specs/routes/extensions/callers inactive | `REPLAN`; design only `WP-6-7A-host-registry-function-observation-design`, then return to repo-spec frontier after one implementation |
| 2026-08-20 | M7A selected repo-spec aggregate stop | Root accepted Host registry, live repo-spec/routes/extension trace and independent owner audit | not exposed | `HostSelectedRegistryRepoSpecsKey` is the first complete aggregate above the accepted graph/Host-registry/registry-file/effective carriers; owner-local entry semantics have no independent consumer | Add one private matching-family Result-Arc+cumulative-epoch sibling with stage-aware outer and full cross-entry fold; keep routes/extensions/public inactive | `REPLAN`; design only `WP-6-7A-selected-registry-repo-specs-observation-design`, then return to the route/extension frontier after one implementation |
| 2026-08-20 | M7A route/extension frontier route-owner stop | Root accepted repo specs, live route/extension/consumer trace and independent owner audit | not exposed | `HostSelectedModuleRoutesKey` is the first reusable semantic owner above accepted graph/repo-spec carriers and alone owns canonical identity, repository mappings and selected routes | Repo specs do not retain graph semantics; canonical definitions and extension mappings independently reuse routes, while extension mappings add root usages/override semantics | `REPLAN`; design only `WP-6-7A-host-selected-module-routes-observation-design`, then return to the extension frontier after one implementation |
| 2026-08-20 | M7A extension frontier mappings-owner stop | Root accepted routes, live mappings/load/root-mapping trace and independent owner audit | not exposed | `HostSelectedExtensionMappingsKey` is the first reusable owner above accepted routes/root files and alone owns extension usage, isolation, override and repository-mapping projection | Definition-load requests and public root mapping independently consume it; both mutable children have accepted siblings and root usages need no separate carrier | `REPLAN`; design only `WP-6-7A-host-selected-extension-mappings-observation-design`, then return to the extension frontier after one implementation |
| 2026-08-20 | M7A extension definition/evaluation request-association stop | Root accepted extension mappings, live request/loading/evaluation/root-mapping trace and independent owner audit | not exposed | `HostSelectedExtensionDefinitionLoadRequestsKey` is the first reusable association above accepted mappings and feeds both evaluation-input construction and actual definition loading | Keep the sibling private/callerless now; later loading design decides the single cross-crate export plus accepted `HostBzlModuleObservationKey`, while public root mapping stays deferred | `REPLAN`; design only `WP-6-7A-host-selected-extension-definition-load-requests-observation-design`, then resume the frontier after one implementation |
| 2026-08-20 | M7A extension definition-request proof cap | Root live candidate, full crate validation and independent proof review | not exposed | Production and compact retention fit, but exact upper-family exclusion, same-semantic/different-epoch equality, separated pure-request lifecycle and reachable pure-terminal shared-Arc proof cannot fit the remaining 22 proof lines | The missing cases require bounded proof headroom without a semantic, owner or authority change | `REPLAN`; retain the Rust candidate non-writable, raise only proof/aggregate/physical ceilings, then schedule one same-file retry |
| 2026-08-20 | M7A extension definition/evaluation frontier input-owner stop | Root accepted request carrier, live evaluation-input/loading/prepared trace and independent owner audit | not exposed | `HostSelectedExtensionEvaluationInputRequestsKey` is the smallest immediate owner: it joins accepted request and root-files carriers entirely within bzlmod and is consumed first by prepared inputs | Loaded definitions is a larger parallel sibling requiring cross-crate carrier promotion plus per-request Bzl observation; all prepared/evaluated/generated/public owners remain later | `REPLAN`; design only `WP-6-7A-host-selected-extension-evaluation-input-requests-observation-design`, then return to loaded-definition ownership after one implementation |
| 2026-08-20 | M7A loaded-definition observation proof correction | One Terra-high implementation/correction worker, root cleanup/cap audit, Terra proof redesign and independent Sol correction rereview | not exposed | The one-file candidate passes focused/full loading and core checks; review accepts its matching-family driver, exact request/Host-Bzl error asymmetry, left-first epoch merge, compact retention and child-only events | The correction still lacks parent-specific stage/prefix, terminal-order/event, held-handle, cancellation, parity and upper-nonactivation proof; the second-correction rule freezes production and replaces the omnibus proof | `REPLAN`; implement only `WP-6-7A-loaded-module-extension-definitions-observation-proof-correction-implementation` with one production-used pure finisher seam and three bounded parent tests |
| 2026-08-20 | M7A loaded-definition proof operation-stage contract stop | One Terra-high proof worker, root exact-type audit and reserved Sol stop review | not exposed | The retained production finisher correctly maps real child conflict/operation mismatch to carrierless `Bzl`; valid-epoch parent merge can retain equal left Arcs or return value conflict, never operation mismatch | The first correction contract contradicted epoch construction by requiring Merge-stage operation mismatch; the worker stopped without correction edits rather than relabel accepted semantics or add malformed hooks | `REPLAN`; retry only `WP-6-7A-loaded-module-extension-definitions-observation-proof-correction-implementation-retry-2` with production frozen and the corrected Bzl-vs-Merge proof classification |
| 2026-08-20 | M7A loaded-definition opaque request-outer proof stop | One Terra-high retry worker with tooling resume, root cap/freshness audit and independent Sol terminal review | not exposed | Production stayed frozen while key/finisher proof improved at +864/-109 and 7,637 physical; the opaque Bzlmod request outer cannot be constructed locally by a consistent loading snapshot | The retry retained coarse order/event/lifecycle tests and duplicate tracker rows; local request-outer equality is infeasible without forbidden API/hook authority, and the full suite predates final compaction | `REPLAN`; retry only `WP-6-7A-loaded-module-extension-definitions-observation-proof-correction-implementation-retry-3`, reusing accepted lower outer proof and raising only test headroom |
| 2026-08-20 | M7A loaded-definition combined-proof split | One Terra-high retry/correction worker, root cap and stop audit, independent Sol terminal review | not exposed | Production and lawful finisher/tracker foundations remain frozen at +910/-110 and 7,682 physical; focused identity passes after direct typed operation-mismatch correction | The correction still left two-request order/events and aggregate-only lifecycle; another combined retry would repeat an oversized proof packet | `REPLAN`; repair only `WP-6-7A-loaded-module-extension-definitions-real-order-event-proof-repair`, then one lifecycle/cancellation slice before final acceptance |
| 2026-08-20 | M7A loaded-definition lower command-policy proof stop | One Terra-medium focused writer with tooling resume, root focused reproduction and reserved Sol family review | not exposed | The three-root draft reached fresh observed evaluation but its broad command substring ban rejected required `BzlmodCommandPolicyKey`/`RootModuleCommandPolicyKey` lower inputs | Production and independent proof slices stayed frozen; hiding the rows would make upper nonactivation nondiscriminating | `REPLAN`; retry the same real-order/event slice with exact lower policy allowlist and exact legacy/upper/public prefix denylist |
| 2026-08-20 | M7A loaded-definition policy-value DynKey stop | One Terra-medium retry worker, root type-definition audit and independent Sol stop review | not exposed | The accepted retry required `DynKey` downcasts to `BzlmodCommandPolicyKey`/`BzlmodEnvironmentPolicyKey`, but both are policy values without DICE key implementations; the actual injected graph keys are the root-module policy keys | The passing three-root draft is retained, production and other proof slices remain frozen, and exact parity/event obligations are still outstanding | `REPLAN`; run only retry 2 with actual `RootModuleCommandPolicyKey`/`RootModuleEnvironmentPolicyKey` classification, exact lower peers and unchanged exact upper/legacy denylist |
| 2026-08-20 | M7A loaded-definition exact-event helper stop | One Terra-medium retry/correction worker, root line/event audit and independent Sol terminal review | not exposed | Retry 2 passes full validation and completes parity/terminal/family proof, but its 213-line test violates the <200 cap and `Some` checks do not discriminate exact print batches; the sole correction stopped without edits under the two-helper authority | The retained +1,056/-114, 7,824-line candidate is feasible with one extracted event helper and no production/other-test change | `REPLAN`; retry only the exact event/size correction with one additional test helper, then schedule the serial lifecycle slice |
| 2026-08-20 | M7A loaded-definition retained-test rustfmt stop | One Terra-medium retry worker, root shared-filesystem/formatter preflight and independent Sol stop review | not exposed | Retry 3 did not persist its reported event helper, and mandatory rustfmt also changes the explicitly frozen identity/finisher test; temporary-copy formatting is production-neutral at +1,222/-111 and 7,993 physical | Exact event extraction remains feasible only with narrow retained-test formatting authority and rebased proof/physical caps | `REPLAN`; retry only exact events plus semantic-neutral test-module rustfmt under +1,450/-250 and 8,150, then schedule lifecycle |
| 2026-08-20 | M7A loaded-definition real-order/event proof acceptance | One fresh Terra-medium retry/correction writer, root persisted-source/cap audit and independent Sol terminal review | not exposed | Retry 4 preserves production and accepts exact legacy parity, nine terminal rows/prefix/suppression, lawful lower families, exact upper denylist and exact fresh/warm/reused/failure child batches at +1,235/-111 and 8,006 | Only aggregate lifecycle, cancellation recovery and full held-layer/nonactivation proof remain before final production acceptance | ACCEPT real-order/events; schedule only `WP-6-7A-loaded-module-extension-definitions-lifecycle-cancellation-proof-repair` |
| 2026-08-20 | M7A loaded-definition lifecycle Arc-identity stop | Two Terra-high lifecycle writers, root focused diagnostics and independent Sol DICE-law review | not exposed | The four-axis +1,410/-111 candidate proves snapshot replacement and separate epoch computes may return semantically equal carriers/results with fresh inner Arcs; blanket unaffected/global pointer identity is not a DICE guarantee | Production and finisher-local left-first retention remain sound; lifecycle must compare separate computes semantically and reserve identity for exact cached/Reused values | `REPLAN`; retry only lifecycle assertions under corrected semantic-equality/conditional-Reused identity law |
| 2026-08-20 | M7A loaded-definition lifecycle canonical-frontier stop | One Terra-high retry worker, root stable-sequencing diagnostics and independent Sol frontier-law review | not exposed | Request-order replacement keeps ext Bzl Result equal but lawfully adds a nondispositive workspace BUILD Lstat demand; observation epochs are valid transaction-local frontiers, not canonical cross-transaction maps | Parent semantic A-B-A passes; each carrier epoch must be validated as a semantic subset of its own transaction global epoch while held historical handles stay immutable | `REPLAN`; retry only lifecycle epoch/restoration assertions with four-helper extraction under existing aggregate caps |
| 2026-08-20 | M7A loaded-definition observation acceptance | One Terra-high retry/correction writer, root persisted lifecycle audit and independent Sol terminal review | not exposed | `3a68afa5` accepts the matching-family loaded-definition Result-Arc+transaction-local epoch owner at +1,518/-112 and 8,288 with exact order/terminal/event/lifecycle/cancellation/nonactivation proof | Accepted evaluation-input requests and loaded definitions are parallel immediate carriers; their first common prepared/pure consumer is not yet owned | ACCEPT production; resume one read-only prepared/evaluation frontier audit before any upper design |
| 2026-08-20 | M7A prepared/evaluation frontier visibility stop | Root live key/consumer/ownership trace over prepared, pure, instantiated, validated and publication branches | not exposed | `HostPreparedModuleExtensionInputsKey` is the sole first semantic consumer of both accepted carriers, but loading cannot name Bzlmod's private evaluation-input observation key/carrier/outer; loaded observation is already same-module | A minimal doc-hidden one-way carrier promotion is strictly smaller than prepared ownership; pure execution, generated publication, root mapping and bootstrap remain later or parallel | `REPLAN`; design only `WP-6-7A-host-selected-extension-evaluation-input-requests-observation-carrier-promotion-design`, then return to prepared-input ownership after one implementation |
| 2026-08-20 | M7A evaluation-input carrier-promotion design | Root live API/Key-value/prior-promotion audit and bounded docs design | not exposed | Exactly the existing observation key/carrier plus one opaque public error wrapper close loading visibility; no adapter, semantic caller or private error inspector is required | Wrapping only at the public Key projection preserves the private driver and lets the later prepared owner carry the child outer opaquely | `ACCEPT` design; implement only `WP-6-7A-host-selected-extension-evaluation-input-requests-observation-carrier-promotion-implementation`, then return to prepared-input ownership |
| 2026-08-20 | M7A evaluation-input carrier promotion acceptance | One Terra-medium three-file writer, root API/cap audit and independent Sol terminal review | not exposed | `50881fc0` exposes exactly the accepted evaluation-input observation key/carrier/opaque outer with three hidden reexports and no semantic caller at 59 lines | Both immediate observed children are now reachable from loading; prepared inputs is the first complete join and pure invocation remains later | ACCEPT visibility; design only `WP-6-7A-host-prepared-module-extension-inputs-observation-design` |
| 2026-08-20 | M7A prepared-input observation design | Root live prepared-driver/child-API/test audit and bounded one-file docs design | not exposed | One private shared Legacy/Observed Result-Arc+epoch sibling preserves raw-first evaluation inputs, loaded definitions and unchanged join/schema/coercion semantics with child-only events | Raw, Definitions and Merge typed outers separate carrierless child/frontier failures from prefixed local semantic terminals; pure invocation remains a later sole consumer | `ACCEPT` design; implement only `WP-6-7A-host-prepared-module-extension-inputs-observation-implementation`, then audit the pure-invocation frontier |
| 2026-08-20 | M7A pure-invocation frontier visibility stop | Root accepted prepared carrier and live pure/instantiated/validated/publication/root-mapping ownership trace | not exposed | Pure is the next semantic owner and sole prepared consumer, but its sibling module cannot name the private prepared observation key/carrier/outer; its Host-Bzl observed child is already crate-visible | A bounded crate-internal carrier surface is uniquely smaller; instantiated, validated and generated are serially later, while root mapping and canonical definition are parallel | `REPLAN`; design only `WP-6-7A-host-prepared-module-extension-inputs-observation-carrier-visibility-design`, then return to pure ownership after one implementation |
| 2026-08-20 | M7A prepared-input carrier visibility design | Root effective-visibility/live API/prior opaque-wrapper audit and bounded two-file docs design | not exposed | Promote only the existing key/constructor and carrier/concrete borrowed accessors to crate visibility; a field-private nominal wrapper at Key projection is required to keep Raw/Definitions/Merge private | One sibling-module function-pointer smoke proves the exact associated Value and accessors without construction, compute or pure activation | `ACCEPT` design; implement only `WP-6-7A-host-prepared-module-extension-inputs-observation-carrier-visibility-implementation`, then design the pure owner |
| 2026-08-20 | M7A pure-invocation observation design | Root live pure driver/children/events/retention/consumer audit and bounded one-file docs design | not exposed | One private Legacy/Observed Result-Arc+epoch owner preserves prepared-first, ordered Host-Bzl preflight, all-preflight-before-invocation execution, repository-call prefixes and pure-owned print batches | Prepared/HostBzl/Merge outers and a post-prepared Complete event predicate preserve exact carrierless terminals, compact prefixes and no replay without retaining Frozen modules/heaps | `ACCEPT` design; implement only `WP-6-7A-host-pure-module-extension-invocations-observation-implementation`, then audit instantiation frontier |
| 2026-08-21 | M7A pure-invocation proof-contract stop | Root green candidate audit, exact source/dependency trace and missing event/lifecycle proof inventory | not exposed | The one-file production owner is sound at +301/-171 production and must freeze; valid prepared inputs already passed the identical loaded-definition target parse, so the planned runtime Label injection is false evidence, while later-preflight legacy prints, prepared parity, all-row warm silence and child-carrier lifecycle subsets need explicit proof | Retain the candidate and correct only test/source-scan evidence within +650/-150 proof and 2,285 physical; production-prefix hash and accounting make any semantic change a REPLAN | `REPLAN`; implement only `WP-6-7A-host-pure-module-extension-invocations-observation-proof-correction-implementation`, then audit instantiation after acceptance |
| 2026-08-21 | M7A pure-invocation second proof-contract stop | Root retained-candidate review, committed prepared-proof trace and pure tracker-edge audit | not exposed | The prepared owner's committed same-module proof already owns its exact raw/loaded dependency vector and private loaded-request join; pure must not type-access or export that field | Preserve the static chain and lawful prepared/raw parse proof, then add only the real observed-pure dependency row to observed prepared plus ordered Host-Bzl children within existing caps and frozen production hash | `REPLAN`; one final retry only as `WP-6-7A-host-pure-module-extension-invocations-observation-proof-correction-2-implementation`, then instantiation audit on acceptance |
| 2026-08-21 | M7A instantiation-frontier pure visibility stop | Root accepted-pure consumer/visibility trace through instantiation, validation, generated publication and parallel mappings | not exposed | Instantiation is the sole pure consumer and next semantic owner, but its sibling module cannot name the private observed pure key/carrier/outer or private-alias Result accessor | A crate-internal opaque carrier surface is uniquely smaller; validation/generated are later and root mapping/canonical selection are parallel | `REPLAN`; design only `WP-6-7A-host-pure-module-extension-invocations-observation-carrier-visibility-design`, then return to instantiation ownership after one implementation |
| 2026-08-21 | M7A pure-invocation carrier visibility design | Root live effective-visibility, prepared-wrapper precedent and sibling-proof audit | not exposed | Rename the private terminal enum and wrap it only at Key projection; promote key/new plus carrier/concrete borrowed accessors crate-wide without exposing alias or terminal variants | One instantiation-sibling constructor/Display/type smoke proves the associated Value and opaque wrapper without compute or activation | `ACCEPT` design; implement only `WP-6-7A-host-pure-module-extension-invocations-observation-carrier-visibility-implementation`, then design instantiation ownership |
| 2026-08-21 | M7A instantiation observation-owner design | Root accepted pure-visibility commit, live one-child driver/consumer/event/retention audit and DICE lifecycle review | not exposed | Instantiation is a bounded private owner over exactly one observed pure child; its existing request/call/schema/namespace/attribute projection needs no epoch merge or upper prerequisite | Share Legacy/Observed execution, carry the pure epoch unchanged, keep pure batches child-owned and prove validation nonactivation in the owning file | `ACCEPT` design; implement only `WP-6-7A-host-instantiated-module-extension-repositories-observation-implementation`, then audit validation frontier |
| 2026-08-21 | M7A validation-frontier instantiation visibility stop | Root accepted instantiation owner plus live validation/generated/public/parallel consumer and effective-visibility trace | not exposed | Validation is the next semantic owner, but its sibling cannot name the private observed instantiation key/carrier/private-alias accessor/outer; generated publication is later and selected/root mapping are parallel | A two-file crate-internal opaque carrier surface and validation-sibling type smoke are uniquely smaller than validation ownership | `REPLAN`; design only `WP-6-7A-host-instantiated-module-extension-repositories-observation-carrier-visibility-design`, then return to validation ownership after one implementation |
| 2026-08-21 | M7A instantiation-carrier visibility design | Root live effective-visibility, prepared/pure wrapper precedents and validation-sibling proof audit | not exposed | Rename the private Pure terminal enum and wrap only at observed Key projection; promote key/new plus carrier/concrete borrowed accessors crate-wide without exposing alias or variant | One validation-sibling constructor/Display/function-pointer smoke proves the associated Value and opaque wrapper without carrier construction, compute or activation | `ACCEPT` design; implement only `WP-6-7A-host-instantiated-module-extension-repositories-observation-carrier-visibility-implementation`, then design validation ownership |
| 2026-08-21 | M7A validation observation-owner design | Root accepted instantiation visibility plus live validation order/error/event/retention/lifecycle and publication-boundary audit | not exposed | Validation is a bounded private owner over exactly one observed instantiation child; existing joins/import-before-override polarity and public certificate need no epoch merge or upper activation | Share Legacy/Observed execution, carry the child epoch unchanged, keep lower print batches child-owned and prove generated/canonical/public nonactivation | `ACCEPT` design; implement only `WP-6-7A-host-validated-module-extension-repositories-observation-implementation`, then audit generated publication frontier |
| 2026-08-21 | M7A generated-publication validation visibility stop | Root accepted validation owner plus live cross-crate visibility, generated/canonical/root-mapping and publication-chain trace | not exposed | Generated definition is the sole validation consumer and next semantic owner, but core cannot name loading's private observed validation key/carrier/private-alias accessor/outer | A doc-hidden three-type carrier promotion and external API smoke are uniquely smaller; canonical selection/root mapping are parallel or later and public/bootstrap stays inactive | `REPLAN`; design only `WP-6-7A-host-validated-module-extension-repositories-observation-carrier-promotion-design`, then return to generated-definition ownership after one implementation |
| 2026-08-21 | M7A validation-carrier promotion design | Root live effective-visibility, prior cross-crate wrapper and external-smoke audit | not exposed | Rename the private Instantiation terminal enum and wrap only at observed Key projection; expose exactly key/new, carrier/concrete borrowed accessors and one field-private opaque outer through three hidden reexports | One external key Display/associated-Value/accessor function-pointer smoke proves the loading API without construction, inspection, compute or core activation | `ACCEPT` design; implement only `WP-6-7A-host-validated-module-extension-repositories-observation-carrier-promotion-implementation`, then design generated-definition ownership |
| 2026-08-21 | M7A generated-definition observation-owner design | Root accepted validation promotion plus live scan/error/event/retention/canonical-branch audit | not exposed | Generated definition is bounded over exactly one observed validation child; canonical selected is an upper missing-only composition and must remain inactive | Share the Legacy/Observed full certificate scan, carry the child epoch unchanged, preserve lower event ownership and prove every upper publication branch inactive | `ACCEPT` design; implement only `WP-6-7A-host-generated-repository-definition-observation-implementation`, then audit the canonical selected/generated frontier |
| 2026-08-21 | M7A canonical selected/generated frontier stop | Root accepted-generated source/visibility, selected legacy/observation, exact composition and upper-consumer audit | not exposed | Generated observation is same-module ready, but selected definition has no observation sibling; its observed routes child is already private in the same bzlmod file | A private one-child selected-definition observation owner is uniquely smaller than cross-crate promotion or canonical composition | `REPLAN`; design only `WP-6-7A-host-canonical-selected-module-definition-observation-owner-design`, then implement privately before a visibility audit |
| 2026-08-21 | M7A canonical selected-definition observation-owner design | Root live selected key/routes-observation/terminal/event/retention/lifecycle audit | not exposed | One private Legacy/Observed owner preserves the exhaustive canonical-route scan and typed Routes outer while carrying the sole child epoch unchanged | The batchless owner retains only selected Result Arc+epoch; three tests prove exact families/events and lifecycle without exposing bzlmod or activating core | `ACCEPT` design; implement only `WP-6-7A-host-canonical-selected-module-definition-observation-implementation`, then audit selected-carrier visibility |
| 2026-08-21 | M7A selected-carrier visibility stop | Root accepted selected owner plus live effective-visibility/core-consumer/generated-sibling/upper-branch audit | not exposed | Future canonical core can use its same-module generated carrier but cannot name Bzlmod's private selected observed key/carrier/private-alias accessor/Routes outer | A three-type doc-hidden promotion with field-private outer and external smoke is uniquely smaller than canonical composition | `REPLAN`; design only `WP-6-7A-host-canonical-selected-module-definition-observation-carrier-promotion-design`, then return to canonical ownership after one implementation |
| 2026-08-21 | M7A selected-carrier promotion design | Root live effective-visibility, prior wrapper projection/external-smoke precedent and existing identity-test size audit | not exposed | Rename the private Routes terminal enum and wrap only at observed Key projection; expose exactly key/new, carrier/concrete borrowed accessors and one field-private opaque outer through three hidden reexports | One external key Display/associated-Value/accessor function-pointer smoke plus a wrapper-only existing identity-test adjustment proves the handoff without construction, inspection, compute or core activation | `ACCEPT` design; implement only `WP-6-7A-host-canonical-selected-module-definition-observation-carrier-promotion-implementation`, then design canonical selected/generated ownership |
| 2026-08-21 | M7A selected-carrier promotion proof stop | Root retained-draft/API-hash and lifecycle nonactivation source-scan review | not exposed | The narrow three-file carrier/API/smoke draft is sound, but its lifecycle denylist still requires the intentionally exporting Bzlmod crate root to contain no observed key | Freeze production, lib, smoke and identity proof; correct only the final source scan to require exactly three adjacent hidden lib reexports and absence from loading/core | `REPLAN`; implement only `WP-6-7A-host-canonical-selected-module-definition-observation-carrier-promotion-proof-correction-implementation`, then return to canonical selected/generated owner design |
| 2026-08-21 | M7A selected-carrier formatter stop | Root retained semantic/proof draft plus temp-copy rustfmt/hash/accounting preflight | not exposed | The carrier and corrected lifecycle proof are sound; only lib lexical reexport placement, projection indentation and smoke layout fail the mandatory formatter gate | Freeze all semantics/assertions and authorize only the preflighted three-file rustfmt bytes with exact post hashes and unchanged caps | `REPLAN`; implement only `WP-6-7A-host-canonical-selected-module-definition-observation-carrier-promotion-formatting-correction-implementation`, then return to canonical selected/generated owner design |
| 2026-08-21 | M7A selected-carrier final identity-layout stop | Root green formatted-draft size audit plus temp-copy one-blank rustfmt/hash preflight | not exposed | All behavior/API/proof is green, but the identity function is exactly 200 physical lines against the frozen below-200 cap | Freeze every token/assertion and delete only the rustfmt-stable blank separator between Missing and Duplicate cases, yielding 199 lines and an exact target hash | `REPLAN`; implement only `WP-6-7A-host-canonical-selected-module-definition-observation-carrier-promotion-identity-test-layout-correction-implementation`, then return to canonical selected/generated owner design |
| 2026-08-21 | M7A canonical selected/generated observation-owner design | Root accepted selected handoff plus live canonical composition/consumer/event/retention audit | not exposed | Canonical is the first common owner: selected observed always runs first and only its semantic Missing reaches the same-module generated observed child | One private shared Legacy/Observed driver preserves exact terminals and lower events, merging selected-prefix then generated epochs before generated semantic projection; upper consumers remain inactive | `ACCEPT` design; implement only `WP-6-7A-host-canonical-repository-definition-observation-implementation`, then audit its consumer frontier |
| 2026-08-21 | M7A canonical-consumer frontier root-mapping stop | Root accepted canonical carrier plus live apparent-mapping/root-definition/downstream trace | not exposed | Canonical apparent mapping can consume canonical observation in-file for nonroot context, but the same key's root context still crosses carrierless Bzlmod root mapping; early canonical visibility does not close either upper owner | Root mapping is a one-child owner over the already observed same-file extension mappings and has only apparent mapping as production consumer | `REPLAN`; design only `WP-6-7A-host-root-repository-mapping-observation-design`, then promote that carrier before canonical apparent-mapping work |
| 2026-08-21 | M7A root repository-mapping observation-owner design | Root live reducer/observed-child/event/retention/lifecycle and existing proof audit | not exposed | The public legacy root mapping is a bounded one-child reducer over private observed selected mappings; no merge or visibility change is needed for a private sibling | Reuse RoutesMode/RepoSpecChild, carry the child epoch unchanged and prove exhaustive first/conflict scan plus lower-owned events in exactly three tests | `ACCEPT` design; implement only `WP-6-7A-host-root-repository-mapping-observation-implementation`, then audit carrier visibility |
| 2026-08-21 | M7A root-mapping carrier visibility stop | Root accepted-owner effective-visibility and exact core consumer/upper-branch audit | not exposed | The observed root-mapping key/carrier/private-alias accessor/typed outer have zero callers and cannot be named by core; the legacy key's sole consumer is apparent mapping's root branch | A three-type doc-hidden Bzlmod promotion with opaque outer and external smoke is uniquely smaller than apparent-mapping ownership; canonical/root definition and route/source/public branches stay inactive | `REPLAN`; design only `WP-6-7A-host-root-repository-mapping-observation-carrier-promotion-design`, then return to apparent-mapping ownership after one implementation |
| 2026-08-21 | M7A root-mapping carrier-promotion design | Root live effective-visibility, accepted opaque-wrapper/external-smoke precedents and lifecycle source-scan audit | not exposed | Promote exactly key/new, carrier/concrete borrowed accessors and a field-private opaque outer; private Mappings inner stays driver-owned and only Key projection wraps it | Three adjacent hidden Error/Key/Observed reexports plus one external type smoke and corrected lifecycle reexport scan prove visibility without caller activation | `ACCEPT` design; implement only `WP-6-7A-host-root-repository-mapping-observation-carrier-promotion-implementation`, then design canonical apparent-mapping observation ownership |
| 2026-08-21 | M7A canonical apparent-mapping observation-owner design | Root accepted root-carrier plus live root/nonroot branch, sole-consumer, event/retention/lifecycle and upper-frontier audit | not exposed | One private owner now has both observed children: root context selects promoted root mapping, nonroot selects same-module canonical definition, and RootApparent preflight selects none | Share Legacy/Observed branch reduction, forward exactly one child epoch without merge, keep lower events child-owned and prove root-definition/route/source/public nonactivation | `ACCEPT` design; implement only `WP-6-7A-host-canonical-repository-apparent-mapping-observation-implementation`, then audit its sibling-module carrier visibility |
| 2026-08-21 | M7A apparent-mapping sibling-visibility stop | Root accepted-owner effective-visibility, sole sibling consumer and root-definition/route/source/public frontier audit | not exposed | Root definition is the sole mapping consumer but cannot name its sibling module's private observed key/carrier/private-alias accessor/child-typed outer; canonical-definition visibility is separate later work | A two-file `pub(super)` key/carrier/opaque-wrapper surface and test-only sibling type smoke are uniquely smaller than root-definition ownership | `REPLAN`; design only `WP-6-7A-host-canonical-repository-apparent-mapping-observation-carrier-visibility-design`, then return to root-definition prerequisites after one implementation |
| 2026-08-21 | M7A apparent-mapping carrier-visibility design | Root effective-visibility, private-inner projection, accepted opaque-wrapper and sibling compile-proof audit | not exposed | Promote exactly the key/new, carrier/concrete borrowed accessors and one field-private opaque outer to `pub(super)`; keep the child-typed inner, alias, fields and variants private | One root-definition sibling key/Display/associated-Value/accessor function-pointer smoke plus wrapper-only identity-proof spelling proves the handoff without compute or activation | `ACCEPT` design; implement only `WP-6-7A-host-canonical-repository-apparent-mapping-observation-carrier-visibility-implementation`, then audit root-definition prerequisites |
| 2026-08-21 | M7A root-definition prerequisite canonical-visibility stop | Root accepted apparent handoff plus live two-child order, canonical effective-visibility and sole route-consumer audit | not exposed | Mapping is sibling-nameable, but conditional canonical definition remains private; using its legacy child would discard the required epoch, while route/source/public are upper | A two-file `pub(super)` key/carrier/opaque-wrapper surface and root-definition sibling type smoke are uniquely smaller than the two-child owner | `REPLAN`; design only `WP-6-7A-host-canonical-repository-definition-observation-carrier-visibility-design`, then return to root-definition ownership after one implementation |
| 2026-08-21 | M7A canonical-definition carrier-visibility design | Root live effective-visibility, existing apparent consumer and prior opaque-wrapper/sibling-smoke audit | not exposed | Rename Selected/Generated/Merge to a private inner, wrap only at canonical Key projection and unwrap only at the same-file apparent child; promote key/new plus carrier/concrete accessors and opaque outer to `pub(super)` | Two wrapper-only identity-proof adjustments plus one root-definition sibling key/Display/associated-Value/accessor smoke prove the handoff without semantic or caller activation | `ACCEPT` design; implement only `WP-6-7A-host-canonical-repository-definition-observation-carrier-visibility-implementation`, then design root apparent-definition ownership |
| 2026-08-21 | M7A root apparent-definition observation-owner design | Root accepted two-child visibility plus live order/terminal/epoch/event/retention/lifecycle and sole route-consumer audit | not exposed | One private shared Legacy/Observed owner runs apparent mapping first, short-circuits mapping failure/Main/Builtin, then conditionally runs canonical definition and merges mapping-left observations before definition semantics | Three tests prove typed outer/prefix algebra, exact real legacy parity/lower events and held A-B-A/cancellation/upper nonactivation without exposing or activating route | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-definition-observation-implementation`, then audit its carrier visibility and sole route consumer |
| 2026-08-21 | M7A root-definition proof-contract stop | Root retained one-file candidate, production-prefix hash and proof-family/lifecycle review | not exposed | Production owner is sound and frozen; a real selected-registry parent requires inaccessible private mirror input, while lawful-family parity/events/warm and held child transaction-local lifecycle evidence are incomplete | Reuse accepted lower selected-registry proof plus static source/policy/forwarding evidence; correct only proof after line 528 within tightened headroom and forbid `HostModuleMirrorsInputKey`/second file | `REPLAN`; implement only `WP-6-7A-host-root-apparent-repository-definition-observation-proof-correction-implementation`, then audit carrier visibility |
| 2026-08-21 | M7A root-definition Missing/lifecycle proof-contract stop | Root retry candidate, frozen production hash, real-Missing feasibility and lifecycle audit | not exposed | Selected-registry and lawful real-family proofs are retained, but a lawful keyed parent cannot produce Missing and lifecycle remains only initial child subsets, parent A-B-A and neutral-change scaffold | Reuse lower canonical generated-missing evidence plus local parent algebra, and complete held mapping/definition A-B-A semantics with same-transaction child/parent/global recovery associations | `REPLAN`; retry only `WP-6-7A-host-root-apparent-repository-definition-observation-proof-correction-implementation-retry-2`, then audit carrier visibility |
| 2026-08-21 | M7A root-definition carrier visibility stop | Root accepted-owner effective-visibility, sole route consumer and later source/public frontier audit | not exposed | The observed root-definition key/carrier/private-alias accessor/typed outer have zero callers and cannot be named by the sibling route; legacy definition has only that route consumer | A two-file `pub(super)` key/carrier/opaque-wrapper surface plus route test-only sibling smoke is uniquely smaller than route ownership; source/public/command/bootstrap remain inactive | `REPLAN`; design only `WP-6-7A-host-root-apparent-repository-definition-observation-carrier-visibility-design`, then one implementation before route ownership |
| 2026-08-21 | M7A root-definition carrier-visibility design | Root live effective-visibility, private-inner projection and prior same-crate opaque-wrapper/sibling-smoke audit | not exposed | Promote exactly key/new, carrier/concrete borrowed accessors and a field-private opaque outer; keep Mapping/Definition/Merge inner, alias, fields and variants private | Wrap only the observed Key error projection and prove the exact surface with one route sibling type smoke; no caller or upper activation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-definition-observation-carrier-visibility-implementation`, then design root route observation ownership |
| 2026-08-21 | M7A root apparent-route observation-owner design | Root accepted definition handoff plus live sole-child/consumer, route algebra, event/retention/lifecycle and upper-split audit | not exposed | Route is a bounded private one-child owner over observed root definition; source input is its sole consumer and public command uses a parallel Bzlmod route | Share Legacy/Observed route algebra, forward the child epoch unchanged, keep lower events child-owned and prove source/public/bootstrap nonactivation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-route-observation-implementation`, then audit route-carrier visibility/source-input handoff |
| 2026-08-21 | M7A root-route sibling-visibility stop | Root accepted route owner effective-visibility, sole source-input consumer and source/public split audit | not exposed | The observed route key/carrier/typed Definition outer have zero callers and cannot be named by sibling source input; public command uses the parallel Bzlmod route | A two-file `pub(super)` key/carrier/opaque-wrapper surface plus source-input test-only sibling smoke is uniquely smaller than source-input ownership | `REPLAN`; design only `WP-6-7A-host-root-apparent-repository-route-observation-carrier-visibility-design`, then one implementation before source-input ownership |
| 2026-08-21 | M7A root-route carrier-visibility design | Root live effective-visibility, private-inner projection and accepted same-crate opaque-wrapper/sibling-smoke precedent | not exposed | Promote exactly route key/new, carrier/concrete borrowed accessors and a field-private opaque outer; keep Definition inner, fields and variant private | Wrap only the observed Key error projection and prove the exact surface with one source-input sibling type smoke; no caller or upper activation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-route-observation-carrier-visibility-implementation`, then design source-input observation ownership |
| 2026-08-21 | M7A root source-input observation-owner design | Root accepted route handoff plus live sole-child/consumer, projection/terminal/event/retention/lifecycle and source/public split audit | not exposed | Source input is a bounded private one-child owner over observed root route; source path is its sole consumer and public command uses a parallel Bzlmod route | Share Legacy/Observed projection, forward the child epoch unchanged, keep lower events route-owned and prove source-path/public/bootstrap nonactivation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-source-input-observation-implementation`, then audit carrier visibility/source-path handoff |
| 2026-08-21 | M7A source-input owner scheduling correction | Committed design `400b819e`, canonical/current terminal audit and unchanged one-file Rust baseline | not exposed | The complete implementation contract was accepted but the design packet remained active and Rust read-only | Preserve the frozen one-file authority/caps/proof/stops and change only scheduling authority to the exact implementation packet | `ACCEPT`; activate only `WP-6-7A-host-root-apparent-repository-source-input-observation-implementation`, then audit carrier visibility/source-path handoff |
| 2026-08-21 | M7A source-input proof-cap/authority stop | Retained 1,711-line candidate, frozen production prefix and proof partition/latest-validation audit | not exposed | Production is within cap and frozen, but proof +697 exceeds <=620, aggregate +936 exceeds <=860, and the latest refactor has no fresh serial validation | Raise only proof/aggregate caps to <=720/<=960, constrain authority to lines 440+ with 23/24/39 headroom and preserve all eight helpers/three tests/semantics | `REPLAN`; correct/validate only `WP-6-7A-host-root-apparent-repository-source-input-observation-proof-correction-implementation`, then audit carrier visibility/source-path handoff |
| 2026-08-21 | M7A source-input sibling-visibility stop | Root accepted source-input owner effective-visibility, sole source-path consumer and later source/public split audit | not exposed | The observed source-input key/carrier/typed Route outer have zero callers and cannot be named by sibling source path; public command uses the parallel Bzlmod route | A two-file `pub(super)` key/carrier/opaque-wrapper surface plus source-path test-only sibling smoke is uniquely smaller than source-path ownership | `REPLAN`; design only `WP-6-7A-host-root-apparent-repository-source-input-observation-carrier-visibility-design`, then one implementation before source-path ownership |
| 2026-08-21 | M7A source-input carrier-visibility design | Root live effective-visibility, private-inner projection and accepted same-crate opaque-wrapper/sibling-smoke precedent | not exposed | Promote exactly source-input key/new, carrier/concrete borrowed accessors and a field-private opaque Route outer; add no observation alias and preserve legacy aliases/visibility | Wrap only the observed Key error projection, adjust only owner source shape and prove the exact surface with one source-path sibling type smoke; no caller or upper activation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-source-input-observation-carrier-visibility-implementation`, then design source-path observation ownership |
| 2026-08-21 | M7A root source-path observation-owner design | Root accepted source-input handoff plus live path-first sole-child/consumer, event/retention/lifecycle and public-split audit | not exposed | Source path is a bounded private one-child owner: normalize first, then consume observed source input and forward its epoch unchanged; source observation is its sole callerless consumer | Share Legacy/Observed path algebra, preserve invalid-path no-child precedence and lower event ownership, and prove lifecycle/cancellation/upper nonactivation in one file | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-source-path-input-observation-implementation`, then audit carrier visibility/source-observation handoff |
| 2026-08-21 | M7A source-path proof-cap stop | Retained 1,687-line one-file candidate, frozen 480-line production prefix and partitioned accounting | not exposed | Production +238 is within <=240, but proof +623 exceeds <=620 and aggregate +861 exceeds <=860; the retained candidate also requires fresh complete validation | Freeze production/API/semantics/assertions/tests/helpers/spans and raise only proof/aggregate ceilings to <=640/<=880 with 17/19/63 headroom | `REPLAN`; correct/validate only `WP-6-7A-host-root-apparent-repository-source-path-input-observation-proof-correction-implementation`, then audit carrier visibility/source-observation handoff |
| 2026-08-21 | M7A source-path sibling-visibility stop | Root accepted source-path owner effective-visibility, sole source-observation consumer and public-parallel frontier audit | not exposed | The observed source-path key/carrier/typed Source outer have zero callers and cannot be named by sibling source observation; that upper key is callerless and public commands use parallel Bzlmod route keys | A two-file `pub(super)` key/carrier/opaque-wrapper surface plus source-observation test-only sibling smoke is uniquely smaller than source-observation ownership | `REPLAN`; design only `WP-6-7A-host-root-apparent-repository-source-path-input-observation-carrier-visibility-design`, then one implementation before callerless source-observation ownership |
| 2026-08-21 | M7A source-path carrier-visibility design | Root live effective-visibility, private-inner projection and accepted same-crate opaque-wrapper/sibling-smoke precedent | not exposed | Promote exactly source-path key/new, carrier/concrete borrowed accessors and a field-private opaque Source outer; add no observation alias and preserve legacy aliases/visibility | Wrap only the observed Key error projection, adjust only owner source shape and prove the exact surface with one source-observation sibling type smoke; no caller or upper activation | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-source-path-input-observation-carrier-visibility-implementation`, then design callerless source-observation ownership |
| 2026-08-21 | M7A callerless terminal source-observation owner frontier | Root accepted source-path handoff plus live conditional two-child, consumer, public-parallel and epoch-shape audit | not exposed | Source observation has zero callers but is the terminal private owner: observed path is now nameable, while the conditional Host source observation is already semantic and has no separate epoch carrier | Share Legacy/Observed staging, forward the path prefix unchanged through the conditional second child, retain exact Arcs/lower events and keep every public path inactive | `ACCEPT` audit; design only `WP-6-7A-host-root-apparent-repository-source-observation-observation-owner-design`, then one implementation before terminal carrier/publication audit |
| 2026-08-21 | M7A callerless terminal source-observation owner design | Root live staged reducer, promoted-path API, Host-observation Value, proof/lifecycle and executable-baseline audit | not exposed | One private Legacy/Observed owner uses observed path first and the same conditional semantic Host observation second; only the path child supplies an epoch | Retain exact child Arcs and path prefix with no second epoch/merge, prove three owner families plus accepted baseline diagnostics, and keep all callers/public paths inactive | `ACCEPT` design; implement only `WP-6-7A-host-root-apparent-repository-source-observation-observation-implementation`, then audit terminal carrier/publication consumers |
| 2026-08-22 | M7A terminal source-observation consumer frontier | Root accepted terminal owner, zero-caller/effective-visibility, public-route/package-policy and earlier rules_rust package-epoch REPLAN audit | not exposed | Raw-source publication is incomplete; exact generated package loading still owns deleted-package, REPO/.bazelignore and BUILD marker order, while current public route/load is builtin/direct-local only | Promote only the legacy semantic/view/result/key and observed key/carrier/opaque outer to `pub(super)` with one neutral sibling smoke before the separate package frontier | `ACCEPT` audit; design only `WP-6-7A-host-root-apparent-repository-source-observation-observation-carrier-visibility-design`, then one implementation before generated package publication audit |
| 2026-08-23 | M7A terminal source-observation carrier-visibility implementation | Root contract audit of accepted `846ef196` against design `f859ab3b`, baselines, caps, and Ubuntu 24.04 WSL validation incl. byte-identical core/runtime baselines and formatting/diff gates | exposed `pub(super)` only | Minimum same-crate handoff: certificate/view/result/key, observed key/carrier, opaque outer, private inner rename, one sibling smoke; no caller or activation | `ACCEPT`; run only `WP-6-7A-generated-repository-package-publication-frontier-audit` |
| 2026-08-24 | M7A generated package publication frontier audit | Root read-only trace of public route/policy/lookup, core Generated-route chain and terminal carrier over Rust `846ef196` | not exposed | Public route admits only DirectLocal/Builtin; root deleted-package policy and marker/BUILD-order lookup are root-repo-only; raw-source publication bypasses all three | One same-crate core generated-repository package lookup owner reusing root evidence; no bzlmod inversion or publication | `ACCEPT` audit; design only `WP-6-7A-generated-repository-package-policy-lookup-design` |
| 2026-08-24 | M7A generated package policy/lookup design | Root routed-owner trace: all package keys ride public `RootRepositoryRoute`; `source_capability()` is variant-generic; core Generated view already carries validated RepoSpec+LocalUnsupported | doc-hidden Generated variant | One constructor + capability arm on `RootRepositoryRoute` reuses REPO-file/ignore/deleted/BUILD-order drivers verbatim; no new key family or policy duplication | `ACCEPT` design; implement only `WP-6-7A-generated-repository-route-capability-promotion-implementation` |
| 2026-08-24 | M7A generated route-capability promotion implementation | Root implemented doc-hidden Generated variant + constructor + capability/hash arms + one core sibling proof; validated vs `846ef196` baselines | exposed doc-hidden only | Full Bzlmod has one pre-existing failure proven byte-identical at base `846ef196`: commit `2022a7a2` added root-mapping-observation names to core generated_repository_definition.rs while leaving the stale absence scan in selected_repo_spec.rs | `ACCEPT`; successor schedules docs-only stale absence-scan correction with next audit |
| 2026-08-24 | M7A publication frontier audit-2 | Root traced build branch vs query external graph route acquisition, crate dependency directions and downstream key polarity over `b42b004c` | not exposed | Core is the only crate that can see the apparent-mapping chain and bzlmod's doc-hidden constructor; query placement inverts dependencies; downstream load keys accept any nonroot route | First activated consumer: core external exported-source build branch with one bridge child; query deferred to bootstrap closure | `ACCEPT` audit; design only `WP-6-7A-generated-package-load-bridge-design` |
| 2026-08-24 | M7A generated package load bridge design | Root designed one private core key pair resolving apparent -> canonical-apparent-mapping -> root-apparent-definition view -> for_generated_repo_spec, with fallback polarity preserving exact direct-local diagnostics | pub(super)/private only | Bridge child runs only on Unknown/Unsupported route kinds; bridge failure adds new typed GeneratedRoute diagnostics; query deferred to bootstrap | `ACCEPT` design; implement only `WP-6-7A-generated-package-load-bridge-implementation` |
| 2026-08-25 | M7A generated package load bridge implementation scope stop | Root compile/audit plus independent Sol-high Rust privacy and DICE ownership review over retained candidate | not exposed | The accepted design assumed two classifiers were visible, but route-error kind is cross-crate private and mapping-error kind is same-module private; the candidate also lacks its observed Key and touches an unrelated route enum | Preserve exact fallback bytes with one doc-hidden Unknown/Unsupported predicate, one opaque Missing/ContextMismatch/Other disposition, and split the bridge from oversized dice.rs | `REPLAN`; design only `WP-6-7A-generated-package-load-bridge-visibility-and-split-correction-design`, retaining Rust non-writable |
| 2026-08-25 | M7A generated package bridge proof/evidence stop | One Terra-high retained implementation worker, root compile/focused-test/cap audit and independent Sol-high next-packet review | not exposed | The corrected candidate passes core check and three focused bridge tests but is +527 production against +480; generated-success evidence is the exact existing module-extension-use-repo fixture, whose checked record is stale Bazel 9.1.1 | Refresh only that hermetic fixture manifest/generated JSON to canonical Bazel 9.2 while freezing its workspace and retained Rust candidate | `REPLAN`; run only `WP-6-7A-generated-external-build-bazel-9-2-evidence-refresh`, then docs-only `WP-6-7A-generated-package-load-bridge-proof-cap-correction-design` |
| 2026-08-25 | M7A generated external-build Bazel 9.2 evidence refresh | Root pinned-version generation/replay, exact anchor/hash/cap/scope scans, focused harness checks and independent Sol-high evidence review | exact Bazel command exit, canonical generated target/source classification and success; harness normalization Slug-native | Existing hermetic fixture refreshed from Bazel 9.1.1 to canonical 9.2.0 with workspace frozen; 29/30 manifest, 38/45 JSON, 67/75 aggregate and seven anchors resolved | Evidence closes generated-success oracle gap but does not accept the retained bridge or its missing non-Generated/opaque-outer proof | `ACCEPT`; design only `WP-6-7A-generated-package-load-bridge-proof-cap-correction-design`, retaining Rust and fixture non-writable |
| 2026-08-25 | M7A generated package bridge proof/cap correction design | Root exact retained-diff/helper/command/fixture/utility audit, independent Sol-high architecture review and success-kind correction rereview | test-only runtime visibility and success-kind parameter plus private production finisher | Exact retained accounting is +531 production/+194 proof/+725 aggregate; lawful archive materialization requires the existing cfg(test) helper to accept matching Immutable success while existing callers retain Local; opaque outer is provable through one production-used pure finisher; generated success uses accepted fixture unchanged | Preserve five-file semantics and compact retained representation; add two command proofs under 540/370/900 caps and seven-file authority | `ACCEPT`; implement only `WP-6-7A-generated-package-load-bridge-proof-cap-correction-implementation` |
| 2026-08-25 | M7A generated package bridge proof/cap implementation command-input stop | Terra-high implementation, root focused/full/rebuilt-fixture validation and independent Sol-high terminal review, with `../zabel` producer/request input architecture audit | not exposed | +539/+332/+871 candidate passes focused proofs and full Bzlmod 532; full core adds forbidden protected external-build failure because `HostModuleMirrorsInputKey` is uninjected, and rebuilt accepted generated fixture aborts on the same key; runtime retains only its accepted baseline | Retain candidate; design one producer-owned extension of existing Bzlmod registry injection facade for private normalized URL/empty-mirror/refresh facts, guided by Zabel's typed request projection boundary | `REPLAN`; design only `WP-6-7A-generated-package-host-registry-input-ownership-design` |
| 2026-08-25 | M7A generated package Host registry input ownership design | Root Slug request/key/consumer audit, explicit `../zabel` typed request-projection guidance and independent Sol-high architecture review | private facade only; no Host key/value export | Empty registries must canonicalize to the default before both Root and Host facts; existing registry generation supplies a distinct typed Host refresh token; vendor stays independent; absent mirror occurrences exactly match the admitted CLI | Extend only `registry_dice.rs` plus core's exact normalized-initial line; one facade A/B/A proof under 45/180/225 successor caps | `ACCEPT`; implement only `WP-6-7A-generated-package-host-registry-input-ownership-implementation` |
| 2026-08-25 | M7A generated package selected-extension demand stop | Root serial validation of retained bridge/input candidate, rebuilt Bazel 9.2 fixture, pinned Bazel per-extension source trace, explicit `../zabel` demand-owner guidance and independent Sol-high terminal review | not exposed | Registry input gap is closed, but the workspace-wide extension certificate lets unrelated unsupported `@bazel_tools` xcode usage poison the requested root extension before load/evaluation | Select a producer-authenticated extension owner from typed graph facts before a per-owner load/evaluate/instantiate/validate chain; preserve complete aggregate carriers and typed direct-unsupported terminals | `REPLAN`; design only `WP-6-7A-generated-package-selected-extension-demand-owner-design`, retaining Rust and fixture non-writable |
| 2026-08-25 | M7A generated package selected-extension demand-owner design | Root typed-graph, complete-usage/mapping, two-key DICE/event/epoch/cap audit; Bazel 9.2 per-ID ordering; explicit `../zabel` demand-seed/owner execution guidance; independent correction rereview | doc-hidden Bzlmod owner/demand/input and final loading certificate only | Authenticate canonical imports before execution; preserve same-owner multi-module uses and final/base mapping polarity; one private pure key plus one eventless validation key; complete aggregates unchanged | `ACCEPT`; implement only `WP-6-7A-generated-package-selected-extension-demand-owner-implementation` under 620/700/1,320 caps |
| 2026-08-25 | M7A selected-extension demand-owner implementation cap stop | Worker frozen-hash/code-shape estimate and independent large-file/privacy/cap audit; no Rust edit | not exposed | Mandatory Bzlmod owner/two-key/projection production is >520 lines before proof, exceeding the 423-line selected-file headroom; cap-only growth worsens a 13,397-line file | Use a nested `selected_repo_spec/selected_extension_demand.rs` child to retain parent privacy and exact key count; correct caps only | `REPLAN`; design only `WP-6-7A-generated-package-selected-extension-demand-owner-scope-cap-correction-design` |
| 2026-08-25 | M7A selected-extension demand-owner scope/cap correction | Root nested-child/privacy/key-count/cap integration and independent rereview | nested child plus existing doc-hidden cross-crate surfaces only | Child module accesses parent-private mappings without visibility widening or adapter key; preserve accepted semantics/proof and exact two Bzlmod/two loading keys under 950/700/1,650 | `ACCEPT`; implement only `WP-6-7A-generated-package-selected-extension-demand-owner-scope-cap-correction-implementation` |
| 2026-08-25 | M7A selected-extension demand-owner Phase-2 cap stop | Passing Bzlmod Phase 1, worker loading/core reuse estimate and independent semantic/proof/cap review | retained nested child exports only | Phase 1 uses +722 production, leaving 228 versus ~430-520 needed; core also needs typed disposition and owner rows need normalized-version/real-projection proof | `REPLAN`; design only `WP-6-7A-generated-package-selected-extension-demand-owner-phase-2-cap-correction-design`, freeze Rust |
| 2026-08-25 | M7A selected-extension demand-owner Phase-2 cap correction | Root live/original baseline, three-gap, remaining-owner/cap integration and independent correction rereview | unchanged nine-file surfaces | Preserve Phase 1; add typed disposition, normalized versions and real owner-input proof; complete two loading keys/core under unchanged physical and 1,200/700/1,900 semantic caps | `ACCEPT`; implement only `WP-6-7A-generated-package-selected-extension-demand-owner-phase-2-cap-correction-implementation` |
| 2026-08-25 | M7A selected-extension demand-owner final Phase-2 cap stop | Corrected passing Bzlmod Phase 1 plus second worker reuse/cap audit | retained nested Bzlmod surfaces only | Phase 1 uses +779 production, leaving 421; lawful two loading keys/core need ~430 and global keys violate isolation | `REPLAN`; design only `WP-6-7A-generated-package-selected-extension-demand-owner-phase-2-final-cap-correction-design`, freeze Rust |
| 2026-08-25 | M7A selected-extension demand-owner final Phase-2 cap correction | Root live-freeze/remaining-owner/cap integration and independent correction review | unchanged nine-file surfaces | Preserve corrected Bzlmod Phase 1; complete exact two loading keys/core under unchanged physical and 1,300/700/2,000 semantic caps | `ACCEPT`; implement only `WP-6-7A-generated-package-selected-extension-demand-owner-phase-2-final-cap-correction-implementation` |
| 2026-08-25 | M7A selected-extension owner-pure representation stop | Two no-edit Phase-2 attempts, root loading-container audit, worker scratch feasibility and independent retained-representation review | not exposed | Global stage values retain global predecessors; use compute-local load/prepared scratch, minimal owner-pure receipt and one final per-request certificate, leaving globals unchanged | `REPLAN`; design only `WP-6-7A-generated-package-selected-extension-owner-pure-representation-correction-design` under unchanged physical and 1,500/800/2,300 semantic caps |
| 2026-08-25 | M7A selected-extension owner-pure representation correction | Root retained-state/cap integration and independent exact-lifetime correction rereview, with explicit Zabel architectural guidance | unchanged nine-file surfaces | Semantic errors retain authenticated inputs plus diagnostic prefix; observed outers retain only prior completed semantic state; globals remain unchanged | `ACCEPT`; implement only `WP-6-7A-generated-package-selected-extension-owner-pure-representation-correction-implementation` under unchanged physical and 1,500/800/2,300 semantic caps |
| 2026-08-25 | M7A generated package source-preflight polarity stop and correction design | Retained candidate validation, rebuilt Bazel 9.2 fixture, root/worker key trace, pinned `../zabel` natural-producer guidance and independent architecture review | private `RepositoryPackageSourceKey` branch only | Generated route construction succeeds, but the pre-existing source owner reconstructs it through direct-local MODULE support and fails the absent root route; Generated must enter existing lookup/materialization with an empty prefix while DirectLocal/Builtin stay unchanged | `REPLAN` then design `ACCEPT`; implement only `WP-6-7A-generated-package-source-preflight-polarity-correction-implementation` in `host_package.rs` under 20/120/140 caps |
| 2026-08-25 | M7A generated repository file-effect owner stop | Retained source-preflight proof, rebuilt Bazel 9.2 fixture, dual fallback/source-owner audit and pinned `../zabel` selected-call producer guidance | not exposed | Route polarity is repaired and the fixture now reaches materialization; `RepoSpec` erases the callable, native request/core dispatch admits only local/http/git, and no `repository_ctx.file` effect owner exists | `REPLAN`; design only `WP-4-5-6-generated-repository-file-effect-owner-design`, retaining all Rust/fixture bytes |
| 2026-08-25 | M7A selected repository file-effect producer design | Root/worker exact crate, certificate/ordinal, Host-Bzl reload, Starlark ABI, event/epoch, representation and cap audit; pinned Bazel 9.2 plus `../zabel` c729 guidance | doc-hidden keyless Bzlmod plan and one loading key pair only | Full producer-to-root vertical is oversized; first close the heap-free selected-call string-only `ctx.file` producer, then separately design route/request/root application | `ACCEPT` design; implement only `WP-4-5-selected-repository-file-effect-producer-implementation` under 560/650/1,210 caps |
| 2026-08-25 | M7A selected repository file-effect producer cap stop | Compiled formatted seven-file candidate, exact partition/hash audit, four focused passes and pinned `../zabel` natural-owner guidance | unchanged doc-hidden Bzlmod plan and loading key pair | Required owner/ABI structure is +900 production versus +560; only +168 proof exists and the mandatory key/lifecycle matrix needs 500–650 more | `REPLAN`; design only `WP-4-5-selected-repository-file-effect-producer-cap-correction-design` at 930/850/1,780, freezing Rust |
| 2026-08-25 | M7A selected repository file-effect producer cap correction | Root exact retained accounting/freeze audit and independent architecture review, with pinned `../zabel` concept-only producer guidance | unchanged doc-hidden Bzlmod plan and loading key pair | 930/850/1,780 leaves 30 production and 682 proof lines for the original ABI/DICE/lifecycle matrix without semantic or caller growth | `ACCEPT`; retry only `WP-4-5-selected-repository-file-effect-producer-implementation-retry` in the same seven paths |
| 2026-08-25 | M7A selected repository file-effect producer proof-contract stop | Retained +901/+562/+1,463 candidate, seven loading/two Bzlmod focused passes and independent lawful-constructor review | unchanged doc-hidden Bzlmod plan and loading key pair | Stale projection and contradictory epoch/outer states cannot reach this key; lawful upper Need/sibling/event/identity/shape proof remains | `REPLAN`; design only `WP-4-5-selected-repository-file-effect-producer-proof-contract-correction-design`, freezing Rust and caps |
| 2026-08-25 | M7A selected repository file-effect producer proof-contract correction | Root lawful-constructor/source-shape audit, exact headroom correction and independent rereview | unchanged doc-hidden Bzlmod plan and loading key pair | Prove five lawful upper rows directly; compose unreachable projection/outer/merge states from named accepted lower proof plus exact source shape; no injection | `ACCEPT`; implement only `WP-4-5-selected-repository-file-effect-producer-proof-contract-correction-implementation` under unchanged caps |
| 2026-08-25 | M7A selected repository file-effect producer implementation | Root full Bzlmod/loading baselines, dependent core check, exact accounting/hashes and two-round independent terminal proof review | doc-hidden keyless Bzlmod plan and callerless loading selected-owner/ordinal key pair | Exact string-path `ctx.file` producer is complete and heap-free; route/request/immutable-root handoff remains absent | `ACCEPT`; design only `WP-4-5-6-generated-repository-file-effect-handoff-application-design`, with pinned `../zabel` concept-only ownership guidance |
| 2026-08-25 | M7A generated repository file-effect handoff/application design | Root route/request/materializer and constructor-ripple audit, pinned `../zabel` natural-producer guidance, full-hash/identity/mode correction and independent rereview | private plan-bearing Generated route/capability; existing public request with new plan kind | Core route consumes retained owner+ordinal; full request Eq carries plan while result-key hash stays ID-only; existing session atomically publishes a framed generated root | `ACCEPT`; implement only `WP-4-5-6-generated-repository-file-effect-handoff-application-implementation` in seven files under 500/850/1,350 |
| 2026-08-25 | M7A generated file-effect handoff proof/accounting stop and correction | Retained seven-file hashes, focused passes, mechanical `b360be14` diff, worker proof audit and independent terminal architecture review with pinned Zabel ownership guidance | proof-only Bzlmod source projection and core route; two core materializer fixes plus proof | Non-Unix mode must fail typed; generated kind must reject recognized local/http/git specs; conservatively charge +1,451 retained production and complete lawful direct/composed route/session proof without injection | `REPLAN` then correction `ACCEPT`; implement only `WP-4-5-6-generated-repository-file-effect-handoff-application-proof-accounting-correction-implementation` in three files under retry 30/1,250/1,280 and cumulative 1,500/1,250/2,750 caps |
| 2026-08-25 | M7A generated file-effect handoff terminal harness stop and tool-shape correction | Root full serial validation, rebuilt generated-source fixture, exact comparator trace and independent terminal review; pinned Zabel role audited as guidance-only | fixture-command Bazel/Slug-specific message-shape fields only | Rust candidate is structurally sound and Slug exits zero at `dice_exported_source_file`, but Bazel-only common stderr assertions make the mandatory unchanged harness exit nonzero; manual acceptance would be a waiver | `REPLAN` then design `ACCEPT`; freeze Rust and implement only `WP-4-5-6-generated-source-oracle-tool-specific-message-shape-correction-implementation` in four harness paths under 70/120/190 caps |
| 2026-08-25 | M7A generated repository file-effect vertical acceptance | Root focused/full-baseline validation, dual Bazel/Slug fixture replay, exact scope/cap/hash audit and independent terminal review; pinned Zabel role guidance-only | fail-closed Bazel/Slug fixture message shapes | `3ac0a85b` preserves common exit and exact Bazel assertions while accepting Slug's exact exported-source JSON; +59/+98/+157, both fixtures `status: ok`, Rust/oracle/workspace frozen | `ACCEPT`; audit only `WP-6-7A-bootstrap-critical-repository-ruleset-frontier-audit` before any further implementation or M7A closure claim |
| 2026-08-25 | M7A selected-registry extension source implementation observable stop | Root frontier/design, independent architecture review, exact dirty-entry freeze and no-edit implementation preflight; pinned Zabel producer/source guidance only | six-file design remains unimplemented | Ownership and self/mapped-load feasibility are sound, but actual rules_rust evaluates rejected top-level `repository_rule(doc=...)` and collection schemas before export, contradicting the packet's success proof and explicit deferrals | `REPLAN`; design only `WP-4-5-6-7A-selected-registry-extension-bzl-source-observable-scope-correction-design`, preserving the source owner and exact rules_rust declaration terminal |
| 2026-08-25 | M7A selected-registry source observable correction | Root pinned Bazel/source/fixture audit, two independent read-only owner/evidence audits and disposable local-only Bazel 9.2 probe; pinned Zabel guidance only | one Bazel-only oracle; Rust remains frozen | Existing evidence covers pieces but not root association + self cross-package + mapped selected child + clean export; one local-registry `mod show_extension` row discriminates all four without repository-rule semantics | `ACCEPT` evidence selection; first reset overdue fixture hygiene from `51540963` through `3ac0a85b`, then run only `WP-1-4-5-6-7A-selected-registry-extension-source-observable-oracle` |
| 2026-08-25 | M7A selected-registry source oracle and corrected owner design | Exact 46-file local-only fixture, pinned Bazel 9.2 generation/two fresh-root replays, corrected hygiene reset and independent evidence/design review; pinned Zabel guidance only | private six-file selected source owner | Producer-view composite is exact without repository rules; retain selected definition/RepoSpec/mapping in Bzlmod and consume typed source facts in loading while real rules_rust stays at its declaration stop | Oracle/design `ACCEPT`; implement only `WP-4-5-6-7A-selected-registry-extension-bzl-source-owner-implementation-r2` under unchanged authority/caps |
| 2026-08-25 | M7A selected-registry source-owner r2 authority stop/correction | Restored implementation preflight, exact request-constructor/source-owner trace and frozen-entry hash verification; pinned Zabel guidance only | request-owned seven-file selected source owner | `HostSelectedExtensionOwnerInputs` directly retains a request constructed in `selected_extension_demand.rs`; association reconstruction through a container/global lookup would violate request identity and producer-view lifetime | `REPLAN`; review then implement only `WP-4-5-6-7A-selected-registry-extension-bzl-source-owner-implementation-r3`, adding exactly that seventh file under unchanged aggregate caps |
| 2026-08-25 | M7A selected-registry source-owner implementation | Root focused/full Bzlmod-loading validation, exact ceilings/conservative additions and independent terminal architecture review; pinned Zabel guidance only | private request-owned selected source route and existing loading consumers | Both request constructors retain the producer descriptor; structural equality covers it; self/mapped loads preserve or switch producer view without path inference, cache repair or root-carrier drift | `ACCEPT`; return only to docs packet `WP-6-7A-bootstrap-critical-repository-ruleset-frontier-audit-r2` at the unchanged rules_rust `repository_rule(doc=...)` terminal |
| 2026-08-25 | M7A bootstrap frontier live-terminal correction | Root rebuilt-CLI/direct rules_rust replay, disposable wildcard-only bypass, route/consumer audit and independent architecture review; pinned Zabel guidance only | existing root route must gain selected-registry projection before root package loading can consume it | Unchanged command first stops at parked M8 wildcard registration; next M7A stop is root BUILD external Bzl loading, because loading is root-only and the route owner rejects registry-selected dependencies | `REPLAN`; design only `WP-4-5-7A-root-package-external-bzl-load-owner-design`; Stage 5 produces the structural route and Stage 4 consumes it; repository-rule doc remains downstream |
| 2026-08-25 | M7A root package external-Bzl owner design | Root Bazel 9.2 package/load/mapping trace, live route/loading/DICE audit, pinned Zabel package-source/resolved-view guidance and independent lifecycle rereview | admission mode on existing root route; existing external-Bzl child; ordinary consumers path-only | Only original Unsupported enters root-BUILD selected admission; selected observation errors exhaustively project to path retry or typed infrastructure terminal without activating ordinary callers | `ACCEPT`; implement only `WP-4-5-6-7A-root-package-external-bzl-load-owner-implementation` in eight files under 900/1,050/1,950 caps; no new fixture prerequisite |
| 2026-08-25 | M7A root external-load/native-archive frontier correction | Retained candidate focused/full validation, rebuilt CLI, wildcard-only rules_rust replay, exact materialization-result probe, Bazel 9.2 archive source/artifact trace, pinned Zabel semantic/physical guidance and independent correction review | accepted route/load vertical; sole native materializer; bounded archive owner unselected | Route admission advances to `rules_rust+`; corrected proof accepts the already-materialized source slice, while the older materializer rejects the complete BCR archive and local-path disguise is inequivalent | `REPLAN` resolved to design only `WP-5-6-7A-selected-registry-bcr-archive-materialization-design`; split one private archive owner and lawful async/session boundary before Rust |
| 2026-08-25 | M7A selected-registry BCR archive lifecycle correction | Root Hyper/Hyper-util connector, executor, pool and DNS source trace; existing command/runtime/materializer audit; pinned Zabel semantic/physical guidance; independent implementation review | rejected shared legacy client; archive-only direct HTTP/1 connector; synchronous command-owned resolver; existing publication seam | Legacy Hyper necessarily spawns connection drivers and default DNS may spawn blocking work; instead resolve before Tokio and drive/join each direct connection/body future in-tree without changing registry | `REPLAN`; design only `WP-5-6-7A-selected-registry-bcr-http-lifecycle-design`; no Rust until the full DNS/runtime/connection shutdown trace is independently accepted |
| 2026-08-25 | M7A BCR archive corrected implementation contract | Root direct Hyper HTTP/1/drop source trace, synchronous progress/runtime/materializer trace, Rustls feature audit, pinned Bazel archive shape, pinned Zabel ownership guidance and independent lifecycle review | archive-only explicit Ring TLS config; immutable per-attempt resolver; pinned cross-entry connection/sender/body; existing publication seam | Resolve outside Tokio; yield one frame per bounded runtime entry; write/hash between entries; drive shutdown; direct Ring-only dependency avoids workspace AWS-LC/registry provider ambiguity | `ACCEPT`; implement only `WP-5-6-7A-selected-registry-bcr-archive-materialization-implementation` in seven files, with registry/global provider read-only and two fresh real-command replays |
| 2026-08-25 | M7A BCR archive dependency-closure correction | Clean worker stop, isolated Cargo resolution, unrelated-`wasip2` restoration, exact lock diff and Ring-only feature audit; pinned Zabel ownership guidance only | five core dependency edges; eight new flate2/tar closure packages; omitted lock authority; no Rust candidate | Exact accepted lock delta is 77 added lines, five direct names and eight new packages with no existing version drift; workspace Rustls/AWS-LC remains excluded | `REPLAN` resolved to docs-only dependency correction; freeze `Cargo.lock` at 4,952 lines/`ecedcf98…`, then select the corrected implementation packet |
| 2026-08-25 | M7A BCR archive implementation contract with lock authority | Accepted lifecycle/archive contract plus isolated exact Cargo closure and independent dependency review; pinned Bazel behavior and Zabel ownership guidance | eight authorized files including exact Cargo.lock delta; direct Ring transport; existing materializer seam | Preserve all existing lock entries and add only 77 lines/five names/eight packages; then implement frame-yielding direct HTTP and bounded extraction | `ACCEPT`; implement `WP-5-6-7A-selected-registry-bcr-archive-materialization-implementation`; registry/global provider read-only; two fresh real-command replays |
| 2026-08-25 | M7A BCR producer-shape/runtime candidate correction | Selected RepoSpec producer/test audit, compiled candidate diff/lifetime/proof audit, restored-hash check, pinned Bazel authority and Zabel ownership guidance | required `type = "tar.gz"`; separate local/BCR plans; accepted existing-runtime direct Hyper owner; exact lock closure retained | Candidate exposed wrong absent-type packet assumption but broke local dispatch, runtime/HTTP lifecycle, SRI/root/fallback order, split ceiling and proof; rejected and removed | `REPLAN`; design only `WP-5-6-7A-selected-registry-bcr-producer-shape-runtime-correction`; no Rust until corrected packet is reviewed |
| 2026-08-25 | M7A exact BCR plan/local archive split | Corrected producer-shape audit, mixed archive block/proof boundary, result-generation audit, one-line base64 lock proof, pinned Bazel field authority and Zabel ownership guidance | mutually exclusive LocalTar/SelectedBcrTarGz private plans; unchanged local materializer; inactive BCR physical branch | Split admission from realization: exact BCR reaches generation-scoped deferred `TransportError`, malformed shapes stable `SpecError`, no I/O/root; local bytes/diagnostics remain exact | `ACCEPT`; implement `WP-5-7A-selected-registry-bcr-plan-local-archive-split-implementation` in six files under 300/400/1,900/2,600 caps |
| 2026-08-26 | M7A lint-test label-default canonical-target stop | Root stopped the second unretained implementation attempt, authenticated pinned Bazel no-colon package/implicit-target parsing, and consulted pinned Zabel package/target ownership guidance | unchanged loading attribute-default owner and two-file proof surface | `//rust/private/lint_test_runner` means package `rust/private/lint_test_runner`, implicit target `lint_test_runner`; the prior expected `//rust/private:lint_test_runner` was a material contract error | `REPLAN`; review then implement only `WP-4-7A-lint-test-label-default-loading-r2` under unchanged 40/100/140 caps |
| 2026-08-26 | M7A lint-test bool-documentation live-source stop | Root ran the exact fixed-dictionary proof, restored both code files byte-for-byte after failure, and authenticated pinned Bazel bool-doc ABI; pinned Zabel remains label-ownership guidance only | existing bool descriptor validation plus unchanged label-default owner and proof surface | Actual rules_rust stops first at `attr.bool(doc=...)`; Slug lacks only the shared validation-only named parameter, before either selected label default | `REPLAN`; review then implement only `WP-4-7A-lint-test-label-default-loading-r3` under unchanged 40/100/140 caps |
| 2026-08-26 | M7A clippy aspect source-order correction | Root ran the selected packet's source-shaped proof, restored both Rust files byte-for-byte after failure, and rechecked the live aspect constructor; pinned Zabel remains ownership guidance only | unchanged fixed aspect-attribute owner; toolchain parser remains later | Keyword values evaluate, but `aspect_attributes` runs before toolchain parsing and rejects clippy's 11 private labels because only the rustfmt pair is admitted | `REPLAN`; run only docs audit `WP-4-7A-clippy-aspect-attribute-audit` before another implementation |
| 2026-08-26 | M7A clippy rule proof-only source-closure stop | Root extended the exact helper/rule source, ran focused compilation, restored the sole test file byte-for-byte after failure, and audited pinned Bazel plus Zabel guidance | fixed `.bzl` native-provider declaration global; no configured output-group construction or consumer | Function bodies stay lazy for invocation, but Starlark compilation resolves `OutputGroupInfo`; Slug lacks Bazel's fixed `.bzl` provider global before the already-admitted rule declaration can freeze | `REPLAN`; run only docs audit `WP-4-7A-output-group-info-global-audit` before another implementation |
| 2026-08-26 | M7A OutputGroupInfo declaration and rust_clippy source closure | Root audit/design, bounded three-file implementation, serial validation, exact attached-producer correction and independent review; Zabel guidance only | one `.bzl`-only zero-state native token plus exact lazy helper and rule schema | `993ba5e4` keeps constructor/equality/configured semantics deferred, exact repr and BUILD absence, and proves the rule's attached aspect is the identical frozen export; 221/24/31 suites pass within 28/124/152 additions | `ACCEPT`; run only docs audit `WP-4-7A-post-rust-clippy-source-audit` before another implementation |
| 2026-08-26 | M7A post-rust_clippy recursive source correction, RunEnvironmentInfo acceptance and tail audit | Root exact child/import replay, pinned Bazel audit, bounded implementation/validation, then exact clippy-tail/provider/common audit; Buck2 review and independent review; clean Zabel guidance only | distinct `.bzl`-only `RunEnvironmentInfo`; exact lint child; existing provider/aspect/rule/build-setting owners; no configured values | `45b479e56` proves the exact child and four imports at 222/24/31 and 28/217/245 additions; lines 463-596 then contain only accepted loading shapes, with provider construction confined to lazy helpers | `ACCEPT`; implement only proof packet `WP-4-7A-clippy-test-tail-loading-proof` in one test file under 0/260/260 caps |
| 2026-08-26 | M7A clippy-tail imported frozen-transition stop | Root exact recursive proof attempt, byte-for-byte candidate restoration, transient/frozen converter trace, pinned Bazel transition ownership and clean Zabel producer-guidance audit | existing attribute converter gains frozen-to-transient projection; exact tail proof; no new retained fields or configured semantics | Exact `platform_transition` is valid but imported frozen; Slug discarded the frozen union half and failed at `clippy.bzl:502`; rejected 246-line proof left no code changes | `REPLAN`; implement only `WP-4-7A-imported-frozen-transition-clippy-tail-loading` in two files under 20/260/280 caps |
| 2026-08-26 | M7A clippy-tail imported frozen-attribute stop | Root corrected transition attempt, exact second failure, full candidate restoration, frozen attribute/schema trace and pinned Zabel producer-guidance audit | existing rule adapter gains plain frozen-attribute plus frozen-transition projection; rich frozen attrs stay rejected | Transition projection reaches the merged attrs; first child-owned `platform` descriptor is frozen and the rule adapter discards that union half; rejected 9/248/257 candidate left no code changes | `REPLAN`; implement only `WP-4-7A-imported-frozen-attribute-transition-clippy-tail-loading` in two files under 55/260/315 caps |
| 2026-08-26 | M7A imported frozen lint descriptor and clippy-tail acceptance | Root bounded loading projection, exact recursive rules_rust tail proof, serial validation, cap/hash audit and two-round independent terminal review; clean Zabel guidance only | existing transient rule schema projected from plain frozen attrs and frozen transition; no new retained owner or configured semantics | `db51996b9` freezes exact `clippy.bzl:463-596`, retaining producer identities, canonical common defaults and transition implementation/output; rich imports reject; 223/24/31 pass at 39/259/298 additions | `ACCEPT`; run only docs audit `WP-4-7A-post-clippy-parent-frontier-audit` before further implementation |
| 2026-08-26 | M7A post-clippy parent frontier audit | Root exact defs/common/providers/lints hash and source-order trace, live Slug/pinned Bazel constructor audit, clean Zabel concept-only declaration-order review and independent audit | exact lints child proof over existing provider/rule/dictionary owners; no production change | cached common/providers leave `lints.bzl` as first new child; its helper/provider call is lazy and its sole rule uses admitted ordered dictionary descriptors | `ACCEPT`; implement only `WP-4-7A-lints-child-loading-proof` in one test file under 0/220/220 caps |
| 2026-08-26 | M7A exact lints child acceptance | Root exact source fixture, recursive provider-owner proof, serial validation, hash/cap audit and independent terminal review; clean Zabel guidance only | proof-only exact lints module over existing owners | `227257a90` retains pointer-identical LintsInfo and ordered four-kind None-default schema without invocation; 224/24/31 pass at 0/180/180 additions | `ACCEPT`; run only docs audit `WP-4-7A-post-lints-parent-frontier-audit` before further implementation |
| 2026-08-26 | M7A post-lints bazel_skylib paths audit | Root exact defs/rust/paths source-order and hash audit, live Slug/pinned Bazel struct review, clean Zabel closure-reachability guidance and independent correction review | proof-only exact paths child; no production owner | `paths.bzl` is declaration-only, but Bazel sorts schemaless struct keys while Slug preserves constructor order, so only the member/function binding is exact | `ACCEPT`; implement `WP-4-7A-bazel-skylib-paths-loading-proof` in one test file under 0/420/420 and keep order Slug-native |
| 2026-08-26 | M7A configuration-field binding ABI correction | Root bounded two-file candidate, full validation, pinned Bazel `StarlarkBuildApiGlobals` plus `Param` default trace, complete candidate removal and independent implementation review; clean Zabel guidance only | same `.bzl` binding and fail-closed boundary; corrected dual positional/named ABI | `named = true` does not disable default `positional = true`; the rejected Rust named-only signature silently narrowed valid two-positional and positional-then-named calls | `REPLAN`; implement only `WP-4-7A-bazel-configuration-field-loading-binding-r2` under unchanged 20/80/100 caps, routing every lawful positional/named form to one fail-closed diagnostic |
| 2026-08-26 | M7A configuration-field loading binding acceptance | Root corrected dual-ABI implementation, full serial validation, cap/hash audit and independent terminal review; pinned Bazel ABI authority and clean Zabel binding/descriptor guidance only | `.bzl`-only zero-result binding; BUILD absent; no late-bound value | `fc131d7aa` accepts all lawful positional/named forms and rejects invalid ABI shapes; valid calls fail before a result with one stable Slug-native diagnostic; 249/24/31 pass at 9/59/68 additions | `ACCEPT`; retry only `WP-4-7A-rules-cc-semantics-complete-loading-proof-r2` under 0/550/550 caps and invoke no function |
| 2026-08-26 | M7A complete compilation-helper name-resolution and universal-environment replan | Root exact 666-line helper attempt/candidate removal, complete active-context audit, pinned Bazel universe/flag/constructor trace, vendored starlark-rust extension/static-owner audit and clean Zabel peer-architecture review | new low-level process-stable exact 30-name universe composed with existing BUILD/Bzl/MODULE/REPO/core overlays | Lazy `_module_map_struct_to_module_map_content` resolves absent `set` at line 251; per-context drift also leaks non-Bazel `chr`/`ord` and leaves REPO on an always-disabled shim despite Bazel 9.2 default semantics | `REPLAN`; implement only `WP-4-5-7A-bazel-universal-builtins-environment` under 220/300/520 caps, migrate every active route, then retry the helper from a clean base |
| 2026-08-26 | M7A exact universal environment acceptance | Root centralized Rust implementation, complete cross-context set/overlay proof, full serial regressions, cap/lifetime audit and two-round independent terminal review; clean Zabel peer guidance only | process-stable exact 30-name universe plus distinct BUILD/Bzl/MODULE/REPO/core overlays | `cb71a302d` removes universe drift, activates the real default set, removes REPO's stale shim, excludes `chr`/`ord`, proves non-alias copy and frozen mutation rejection; 252/547/24/31 pass at 122/179/301 additions | `ACCEPT`; retry only `WP-4-7A-rules-cc-compilation-helper-complete-loading-proof-r2` under 0/1050/1050 caps and invoke nothing |
| 2026-08-26 | M0/M7A Starlark archive whitelist correction | Root exact one-pathspec implementation, shell/scope/archive assertions and independent terminal review | checker-only admission of accepted `app/slug_starlark_v2/**` | `5c3b4492f` restores the V2 app gate without weakening other paths; only the three longstanding thoughts baseline rows remain | `ACCEPT`; retry only `WP-4-7A-rules-cc-compilation-helper-complete-loading-proof-r2` under 0/1050/1050 and invoke nothing |
| 2026-08-26 | M7A complete compilation-helper acceptance and compile frontier audit | Root exact source/identity/inventory proof, full serial validation, mapping correction and independent rereview; recursive compile/action-template/private-cc-common source trace; clean Zabel peer guidance only | proof-only complete helper; next proof-only native wrapper plus complete compile-variable producer | `3060e4d4d` accepts all helper bytes and eager bindings; `compile_build_variables.bzl` is the first shared incomplete child for compile, action-template and proxy/toolchain paths and uses only accepted eager shapes including `set` | `ACCEPT`; implement only `WP-4-7A-rules-cc-compile-build-variables-complete-loading-proof` under 0/1050/1050 and invoke nothing |
| 2026-08-26 | M7A complete action-template acceptance and compile-parent audit | Root exact proof/full serial validation/independent review plus complete compile child/eager-shape trace; clean Zabel peer guidance only | proof-only complete action templates; next proof-only complete compile producer | `bb11a1f73` accepts all action-template bytes and identities; all eleven compile children are accepted and its eager sets/provider/functions use admitted shapes | `ACCEPT`; implement only `WP-4-7A-rules-cc-compile-complete-loading-proof` under 0/3000/3000 and invoke nothing |
| 2026-08-26 | M7A complete compile-parent acceptance and private-cc-common frontier audit | Root exact source/recursive identity/inventory proof, full serial validation and independent terminal review; private-cc-common source-order trace; clean Zabel peer guidance only | proof-only complete compile parent; next proof-only complete linkstamp compile | `d32e2602d` accepts all compile bytes, 25 imports, four sets, initialized provider/raw constructor and 28 functions; linkstamp compile is the first incomplete cc_common child and all six children are accepted | `ACCEPT`; implement only `WP-4-7A-rules-cc-linkstamp-compile-complete-loading-proof` under 0/300/300 and invoke nothing |
| 2026-08-27 | M7A root subtree loading-owner extraction cap correction | Root exact query-owner move and addition/deletion accounting before semantic validation; pinned Bazel authority and Zabel peer guidance only | unchanged four-file owner extraction; no external traversal, mapping or expansion | The existing 583-line query owner needs 688 production and 19 proof additions once the loading terminal, exports and consumer boundary are honestly counted; the 620 production cap was too small although the repository shrinks | `REPLAN` only the caps to 740/100/840; preserve scope, moved behavior and stop conditions |
| 2026-08-27 | M7A selected-external subtree package-owner design | Root live route/materialization/package/observation trace, pinned Bazel 9.2 recursive traversal audit, Zabel peer-architecture audit and independent correction review | policy-free routed direct-directory listing over the complete repository route and root-capable package path; no traversal or activation | Materialized roots and namespaces are private to bzlmod source preparation, built-in bazel_tools is catalog-backed rather than materialized, and the point external lookup conflates ignored-subtree with deleted-current-package | `REPLAN` to `WP-4-5-7A-routed-repository-directory-listing-owner-implementation`; later add one typed bzlmod package-boundary projection before the loading-owned external subtree producer |
| 2026-08-27 | M7A external package-boundary built-in disposition stop | Root clean implementation attempt/removal, live optional-input/source-owner trace, pinned Bazel 9.2 and verbatim catalog audit, Zabel peer-architecture guidance and independent REPLAN review | accepted routed directory-listing owner; unchanged materialized source/path owners | Catalog-backed routes turn normal absent REPO/ignore inputs into materialization errors and cannot probe BUILD markers, while globally mapping exact-source `UnsupportedCatalog` to absence would weaken integrity | `REPLAN`; design only `WP-4-5-7A-builtin-optional-package-input-projection-design`, consume listings only for the built-in disposition, then resume the unchanged external boundary projection |
| 2026-08-27 | M7A built-in optional package-input implementation ordering/cap stop | Root exact compiled candidate, focused behavior proof, formatted accounting and independent cap/ordering review | same three semantic owners and four-file allowlist; corrected built-in REPO dependency order | Candidate measures 252 production/148 proof and passes only because its test injects policy; it requests root Starlark semantics before proving built-in REPO absence, contrary to Bazel's file-before-semantics order | `REPLAN`; design moving built-in absence before semantics plus a no-policy regression and 280 production cap, then retry only `WP-4-5-7A-builtin-optional-package-input-projection-implementation-r2` |
| 2026-08-27 | M7A canonical repository load-route implementation proof/format stop | One bounded writer, root test-only correction, full serial validation and independent latest-diff terminal review; Bazel 9.2 authority and Zabel peer guidance only | unchanged accepted apparent-free source/load route over existing materialization/path owners; no package or registration activation | The 897/623 candidate passed all gates but proved exact deepest ownership only for built-ins, omitted route/effect failure-prefix rows and lacked full source/mapping/plan hash plus retained-size accounting; normal formatting measures 1,118 production | `REPLAN`; retain no Rust and retry only `WP-4-5-7A-canonical-repository-load-route-implementation-r2` under 1,200/1,100 caps with complete four-disposition/failure/hash/size matrices and no new-module `rustfmt::skip` |
| 2026-08-30 | M7A complete repository-rule attribute-family architecture R1/R2 | Root audited all thirteen pinned Bazel descriptors, ordinary/innate live bridges, retained RepoSpec equality/hash consumers, Buck2 compact-map guidance and clean Zabel peer design | accepted complete `tools/build_defs/repo` package; scalar-only call bridges plus ruleset-specific ordered publication identity | Bazel accepts all thirteen kinds; Slug's recursive carrier already stores them, but ordinary calls reject collections and nested map order is absent from generic cutoff/hash identity | Independent R1 review returned `REPLAN` on the false complete-identity premise; R2 keeps `SmallMap` storage and freezes one generic recursively ordered nested-map publication projection across all three hash owners | Review corrected design only; implement the complete family after independent R2 `ACCEPT`, never a `string_list_dict`, parser, `set`, `cc_common`, or ruleset special case |
| 2026-08-27 | M7A repository source-observation convergence | Root exact producer/consumer audit and six-file implementation, one architecture review, full serial validation, one legacy-accessor correction and independent terminal rereview; Bazel 9.2 authority and Zabel peer guidance only | existing zero-copy `HostRepositorySourceObservation`; shared Root/Canonical owner plus observed sibling; old root keys/consumers unchanged | `9764f8a4f` retains exact payload pointers and dependency/epoch order, preserves all root/loading/core boundaries and delegates all four temporary wrappers; focused/full/dependent/CLI/structural gates pass | `ACCEPT`; implement corrected `WP-4-5-7A-canonical-source-policy-convergence-implementation-r2`, including the built-in Arc accessor and load-route proof, then delete wrappers |
| 2026-08-30 | M7A module-loaded native context implementation acceptance | Root implemented accepted BUILD/Bzlmod namespace and recursive key separation, full serial validation, rebuilt CLI, two fresh rules_rust replays and corrected external root/canonical terminal proof; Bazel 9.2 authority, Buck2 utility review and clean Zabel peer guidance only | loading-private compact context enum, shared native method table, exact Bzlmod-only version field; no parser or C++ semantics | `1f9433600` passes 593 Bzlmod and 440 loading units plus integrations at 212/300/512 net; both replays clear `native.bazel_version` and next fail at authenticated `repository_ctx.attr` while resolving the generated compatibility route | Initial terminal review requested external legacy/observed root/mapped-canonical A/B/A proof; the added regression passes and reviewer returns `ACCEPT` | Implementation `ACCEPT`; design the complete `repository_ctx` value-bearing field category (`name`, `original_name`, all thirteen-kind `attr`) before Rust; `cc_common` stays only a downstream discriminator |
| 2026-08-30 | M7A rule-side subrule architecture R1 | Root pinned-source/rules_cc/Zabel audit plus independent architecture review | declaration, hidden dependencies, configured context and later action integration; no Rust | R1 incorrectly included defining-module identity in `configuration_field`, conflated ordered descriptor lifting with set-semantic authorization, left the first successor unbounded, and implied complete FDO invocation despite missing `args`/`run`/`symlink` and `cc_common.absolute_symlink` | `REPLAN`; R2 must use typed fragment-class/field/tools-repository identity, separate ordered rows from semantic sets with reorder/duplicate/diamond proofs, freeze paths/blobs/caps and the first replay stop, and publish an exact context/action member ledger |
| 2026-08-28 | M7A provider-independent configured selection acceptance | Root full owner/downstream validation, bounded fixture experiments, baseline/cap audit, independent Sol R2/R3/R4/R5 design and terminal reviews, and formatted full integration; Bazel 9.2 authority and Zabel peer guidance only | generic provider-free selector, exact zero-type platform selection, target/selected-exec configuration identity, core terminal association and hermetic configured server proof | R4 closes all 14 missing-platform failures; R5 proves four target/exec C0/C1 projections; full server is 50/51 with only the unchanged inherited event-replay assertion, which passes in isolation | Implementation `ACCEPT`; commit category 4, then continue only with categories 5-6 before M8 |
| 2026-08-28 | M7A recursive analysis evaluator-adapter implementation R1 | Root retained-graph implementation, serial focused/cross-crate validation and independent terminal architecture review; pinned Bazel 9.2 authority, Buck2 utilities and clean Zabel peer guidance only | accepted Arc value/publication graph; rejected cross-class provider/depset/struct adapter, eager dependency-depset flattening and user-only target lookup | Build-api/loading/analysis pass; core shows only two independently reproduced inherited failures; review finds fresh/rematerialized equality/hash asymmetry, deferred depset validation/recomposition loss and hidden typed providers | `REPLAN`; commit no Rust, review then implement only `WP-4-5-7A-recursive-analysis-evaluator-adapter-implementation-r2` with shared loading classes, native structs and complete provider lookup |
| 2026-08-28 | M7A recursive analysis evaluator-adapter R2 design acceptance | Root pinned Bazel `StructImpl`/`Depset`/`NestedSet`/configured-target source audit, Buck2 utility review, clean Zabel peer guidance and two-round independent architecture review | accepted retained Arc graph plus one loading-owned evaluator class per family and one shared exact build-api depset DAG owner | First review exposed non-exact generic topological/depth behavior plus missing ToolchainInfo barrier/marker proofs; R2 now admits the shared core, pinned `[3, 5, 6, 4, 2]` and depth matrix, frozen list/dict barriers, and marker-class deletion | Design `ACCEPT`; commit docs only, then implement the corrected packet without retaining the rejected R1 wrappers or eager flattening |
