# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect a route not decided by
the skill.

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-08-30 | M7A repository-context attribute namespace architecture R1-R3 and implementation R1 proof stop | Root pinned Bazel/Slug owner audit, Buck2 utility review, clean Zabel concept guidance, independent architecture/correction/replan review, one two-file writer and root proof audit | active goal continuation | Architecture R2 wrongly published retained `None`; accepted R3 corrects it. Implementation R1 is 290 production/197 proof/487 total and passes focused context/selected-effect tests, but omits ordinary/innate key-level A/B/A and no-effect rows | Architecture R2 `REPLAN` then R3 `ACCEPT`; implementation root review rejects allocator-only evidence. Existing test-only transactions already accept changed MODULE/extension source on one Dice and avoid an allowlist expansion | Implementation `REPLAN`; independent review accepts R2 changing only proof/aggregate caps from 650/950 to 1,100/1,400; complete both owner matrices with no semantic/production/file widening |
| 2026-08-30 | M7A repository-qualified external `.bzl` route R1/R2/R3 | Root implemented the independently reviewed Root/Canonical handoff, audited Bazel 9.2 and clean Zabel owner models, corrected R2's pre-override mapping as R3, then ran full serial validation and two cold rules_rust replays | active goal continuation | `f747507f6` resolves the exact selected-parent generated canonical target, selects the canonical definition host and fully substituted mapping, admits nonroot-only ordinary execution, and preserves each usage-owner mapping; both cold replays continue through the real rules_cc owner and stop at missing `tools/build_defs/repo/utils.bzl` | R1 exposed the owner gap; independent R2 review rejected its pre-override namespace row; independent R3 design and terminal implementation reviews return `ACCEPT` | Implementation `ACCEPT`; import Bazel 9.2's complete direct `tools/build_defs/repo` embedded package next, not only `utils.bzl`, with Zabel as peer guidance only |
| 2026-08-28 | M7A target-platform and builtin-mapping ownership stops | Root retained the platform candidate through three correction reviews, terminal implementation review and mapping-design pre-review; Bazel 9.2 authority and Zabel peer guidance only | platform/alias work remains coherent; exact builtin source lacks mapping; first mapping design selected the overbroad route owner | R2-R4 corrected cycle/proofs, terminal review found the missing mapping, then design review showed `HostSelectedModuleRoutesKey` also awaits unrelated registry RepoSpecs/source metadata unlike Bazel's graph-only mapping owner | `REPLAN`; commit no candidate Rust; design only `WP-4-5-7A-builtin-bazel-tools-selected-mapping-design-r2`, extracting one graph-only full-mapping owner before corrected R5 |
| 2026-08-28 | M7A provider-independent configured selection acceptance | Root full owner/downstream validation, bounded fixture experiments, baseline/cap audit, independent Sol R2/R3/R4/R5 design and terminal reviews, and formatted full integration; Bazel 9.2 authority and Zabel peer guidance only | generic provider-free selector, exact zero-type platform selection, target/selected-exec configuration identity, core terminal association and hermetic configured server proof | R4 closes all 14 missing-platform failures; R5 proves four target/exec C0/C1 projections; full server is 50/51 with only the unchanged inherited event-replay assertion, which passes in isolation | Implementation `ACCEPT`; commit category 4, then continue only with categories 5-6 before M8 |
| 2026-08-28 | M7A recursive analysis evaluator-adapter implementation R1 | Root retained-graph implementation, serial focused/cross-crate validation and independent terminal architecture review; pinned Bazel 9.2 authority, Buck2 utilities and clean Zabel peer guidance only | accepted Arc value/publication graph; rejected cross-class provider/depset/struct adapter, eager dependency-depset flattening and user-only target lookup | Build-api/loading/analysis pass; core shows only two independently reproduced inherited failures; review finds fresh/rematerialized equality/hash asymmetry, deferred depset validation/recomposition loss and hidden typed providers | `REPLAN`; commit no Rust, review then implement only `WP-4-5-7A-recursive-analysis-evaluator-adapter-implementation-r2` with shared loading classes, native structs and complete provider lookup |
| 2026-08-28 | M7A recursive analysis evaluator-adapter R2 design acceptance | Root pinned Bazel `StructImpl`/`Depset`/`NestedSet`/configured-target source audit, Buck2 utility review, clean Zabel peer guidance and two-round independent architecture review | accepted retained Arc graph plus one loading-owned evaluator class per family and one shared exact build-api depset DAG owner | First review exposed non-exact generic topological/depth behavior plus missing ToolchainInfo barrier/marker proofs; R2 now admits the shared core, pinned `[3, 5, 6, 4, 2]` and depth matrix, frozen list/dict barriers, and marker-class deletion | Design `ACCEPT`; commit docs only, then implement the corrected packet without retaining the rejected R1 wrappers or eager flattening |
| 2026-08-28 | M7A recursive analysis evaluator-adapter R2 canonical-successor stop | Root full focused/loading/analysis validation, independent terminal correction rereview and pinned Bazel 9.2 `NestedSet` source confirmation; Buck2 utilities and clean Zabel peer guidance only | unchanged retained graph and R2 evaluator adapter; exact sole-successor physical topology plus vendored scope correction | R2 passes focused/build-api/loading/analysis gates, but keeps an extra transitive node for a sole compatible different-order child where Bazel dereferences and shares the child's successors; it also changes an unrelated JSON-order assertion | `REPLAN`; commit no Rust, review then run only R3 with sole-successor topology/publication proof and byte-for-byte JSON assertion restoration |
| 2026-08-28 | M7A selected-toolchain R1/R2 stop and registered-closure design | Root retained the candidate through focused/full validation, corrected pointer/identity proofs, pinned embedded-tools/registration/generated-repository audit, and two-round independent closure review; Bazel 9.2 authority, Buck2 utilities and clean Zabel peer guidance only | generic selected context retained; exact four-row closure split into catalog, declaration metadata and Host-input design | R2 proofs pass, then REAPI reaches absent `tools/launcher`; trace also requires source-launcher/res bytes, ordinary rules_cc/rules_shell and canonical `local_config_winsdk`; review rejects premature post-Host manifests, then accepts exact three-packet scheduling | Design `ACCEPT`; implement only `WP-4-5-7A-builtin-bazel-tools-registered-toolchain-catalog-implementation`, with later effects still unauthorized |
| 2026-08-28 | M7A repository-rule declaration metadata R1 downstream-catalog proof correction | Root four-file compact metadata implementation, focused/full serial validation and exact failure isolation; Bazel 9.2 authority, Buck2 `SmallSet` reuse and clean Zabel peer guidance only | unchanged producer-owned local/configure/environ metadata; one stale built-in subtree expected vector at the accepted catalog boundary | Focused metadata tests pass; full loading is 411/412 because its base expectation predates the exact launcher/res/source-launcher packages imported by `87d332cf6` | `REPLAN` proof only to R2; admit one existing test blob and only its two expected package vectors, then rerun full loading and terminal review |
| 2026-08-28 | M7A effective repository Host-input architecture R1-R5 | Root pinned Bazel/DICE/live-owner audit, Buck2 retained-utility review, clean Zabel concept-only guidance and independent Sol architecture/correction reviews | command snapshot plus per-name injected environment/platform ABI, typed Need/frontier retry, rollback authorization, canonical effect successor and proof-only registration successor | R1-R4 exposed missing effect identity, overlapping successor blobs, Host/rc overclaims, cold-absent injection, rejected-cache authorization and core/loading dependency direction; no Rust was authorized | R5 design `ACCEPT`; implement only `WP-4-5-7A-effective-repository-host-input-implementation` under its exact blobs and 1750/1900/3650 caps |
| 2026-08-29 | M7A canonical repository Host-capability innate-owner stop | Root generic context/effect draft and exact authenticated BCR winsdk proof, then independent Sol architecture review; Bazel 9.2 authority and clean Zabel concept/optimization guidance only | retained Host-capability draft; missing generic innate `use_repo_rule` selected-owner projection and certificate | The first whitespace guard conceals a larger mismatch: Slug also requires a root owner, loads synthetic `//:MODULE.bazel`, and authenticates `module_extension`, while Bazel uses one possibly nonroot owner mapping to resolve and authenticate the actual repository rule | `REPLAN`; park the Host draft recoverably, freeze then implement only the generic innate owner certificate, and resume Host capabilities afterward without a winsdk special case |
| 2026-08-29 | M7A innate repository-rule owner certificate R2 | Root implementation, exact winsdk/direct-rules_cc proof, full serial validation and independent terminal review; Bazel 9.2 authority and clean Zabel concept/optimization guidance only | generic explicit innate owner/certificate plus one isolated external `repository_rule` label-normalization hunk; all selected-context overlay hunks excluded | `7bcac3da3` authenticates root/nonroot actual rules, preserves synthetic label conversion, ordered calls/mappings/validation and exact `@@rules_cc+//...`; 589 Bzlmod and 420 loading tests pass at 1,227/891/2,118 additions | Implementation `ACCEPT`; restore and reissue only `WP-4-5-7A-canonical-repository-rule-host-capability-implementation`, then prove the registered four-row closure |
| 2026-08-29 | M7A registered-toolchain generated-repository proof stop | Root exact selected-row and generated-route/effect/inventory proof, focused command/REAPI dependents, pinned Bazel 9.2 and verbatim catalog trace; clean Zabel guidance only | passing generic winsdk row-3 draft; zero-production packet cannot alter loading/analysis | Four source rows and empty row 3 pass, then real REAPI reaches launcher row 1 and fails because immutable loading schema drops parsed exec/executable flags and package invocation rejects before inventory | `REPLAN`; preserve the draft and design only generic exec-configured label-attribute loading retention with analysis-time fail-closed behavior before resuming registration proof |
| 2026-08-29 | M7A registered closure row-2 selected-BCR transform stop | Root accepted exec-attribute implementation/full validation, real registration/REAPI replay, pinned Bazel 9.2/BCR archive/patch/order/mode audit, clean Zabel owner-guidance review and two-round independent architecture review | passing four-row inventory and launcher row 1; selected BCR producer sorts patch order and core admits only empty explicit-type transforms | Authentic rules_shell omits type, strips a prefix, applies an ordered verified patch at strip 1 and has 0664/0775 tar modes; R1 incorrectly treated insertion-ordered `SmallMap` as order-sensitive equality, R2 freezes narrow collision-safe RepoSpec patch-order identity through both route/request lanes; activation preflight exposes one required local serde dependency-list lock row | Design `ACCEPT`; corrected implementation admits only that mechanical lock edge, then ordered producer/publication identity before private archive realization and unchanged proof closure |
| 2026-08-30 | M7A selected-BCR archive realization R1 global-PAX stop | Root complete nine-file candidate, hermetic/full serial validation, authentic fresh-root REAPI replay, physical tar-header inspection and independent terminal review; Bazel 9.2/BCR authority and clean Zabel owner guidance only | retained archive transform candidate; exact global PAX metadata category missing | Authentic rules_shell starts with one well-formed global PAX `comment` record before all logical entries; Bazel consumes it generically, while the frozen contract defers every PAX shape and the implementation rejects before publication | `REPLAN`; preserve R1 recoverably and run only `WP-5-7A-selected-bcr-archive-transform-implementation-r2`, admitting bounded global `comment` records while local/malformed/semantic/unknown PAX remains fail-closed |
| 2026-08-30 | M7A selected-BCR archive realization R2 acceptance and TestingBootstrap frontier | Root bounded generic PAX correction, focused/full serial validation, two authentic fresh-root REAPI replays and independent terminal review; Bazel 9.2/BCR authority and clean Zabel peer guidance only | complete nine-file private archive transform owner; next fixed testing-support predeclared Host-ABI category | `1599d730c` passes 32 focused rows and authentic PAX/prefix/patch/MODULE loading twice; only three unchanged dirty core failures remain, and both real replays next stop at absent `coverage_common` from Bazel's five-binding `TestingBootstrap` | Implementation `ACCEPT`; design only `WP-6-7A-testing-bootstrap-host-abi-architecture` for the complete category before resuming the proof-only registration closure |
| 2026-08-30 | M7A TestingBootstrap Host-ABI architecture R1-R4 acceptance | Root complete Bazel bootstrap/member/call audit, live loading/provider trace, clean Zabel peer guidance, independent architecture/correction review and first compile | complete five-binding category and builtin-provider declaration/rematerialization identity; bounded clean-test carrier fallout | R1 omitted identity/callability/context distinctions; R2-R3 corrected three scalar assertions; first compile exposed one fourth mapped assertion; R4 admits exactly four explicit `.user_id()` projections and no generic `Display` | Design `ACCEPT`; implement only `WP-6-7A-testing-bootstrap-loading-implementation-r4` under unchanged 500/700/1,200 caps |
| 2026-08-30 | M7A external `.bzl` immutable-source projection and acceptance | Root live source-key/consumer and pinned embedded-tools audit, clean Zabel content/optimization guidance, two-round design review, four-file implementation, full serial validation and corrected terminal rereview | existing Root/Canonical route and shared observation owner; complete direct `tools/build_defs/cc` category ordered separately | `1b997c5ef` preserves exact Root-request source-file dependency identity/order; Root built-ins and Canonical reads use typed observation projections; 591 bzlmod and 428 loading unit suites plus integrations pass; two cold rules_rust replays stop identically at exact `action_names.bzl` `UnsupportedCatalog` | R1 full convergence was `REPLAN`; terminal proof correction changed the discriminator to exact `tools:build_defs.bzl` and retained the expected downstream `@platforms` error | Implementation `ACCEPT`; implement the complete three-file pinned `tools/build_defs/cc` direct-package catalog, never only the demanded file and never parser/`cc_common` semantics |
| 2026-08-30 | M7A complete direct cc built-in package catalog | Root pinned embedded-tools/direct-package audit, exact five-file implementation plus two proof-only allowlist corrections, full serial validation, two cold real replays and independent terminal review; Bazel authority and clean Zabel guidance only | existing versioned catalog/manifest/directory/source owners; exact `tools/build_defs/cc` direct package only | `84190d95c` imports verbatim 0644 `BUILD`, `action_names.bzl`, and `cc_import.bzl`; exact hashes/sizes/listing, derived manifest `6001b11d…`, 591 Bzlmod and 428 loading unit suites plus integrations pass | Initial allowlist missed one separately pinned manifest row and one package-set vector; both were explicitly admitted before edits and no production owner widened | Implementation `ACCEPT`; design generic repository-qualified external-`.bzl` resolution for extension-generated mapped children; `cc_common` remains only the discriminator |
| 2026-08-30 | M7A module-loaded native context implementation acceptance | Root implemented accepted BUILD/Bzlmod namespace and recursive key separation, full serial validation, rebuilt CLI, two fresh rules_rust replays and corrected external root/canonical terminal proof; Bazel 9.2 authority, Buck2 utility review and clean Zabel peer guidance only | loading-private compact context enum, shared native method table, exact Bzlmod-only version field; no parser or C++ semantics | `1f9433600` passes 593 Bzlmod and 440 loading units plus integrations at 212/300/512 net; both replays clear `native.bazel_version` and next fail at authenticated `repository_ctx.attr` while resolving the generated compatibility route | Initial terminal review requested external legacy/observed root/mapped-canonical A/B/A proof; the added regression passes and reviewer returns `ACCEPT` | Implementation `ACCEPT`; design the complete `repository_ctx` value-bearing field category (`name`, `original_name`, all thirteen-kind `attr`) before Rust; `cc_common` stays only a downstream discriminator |
