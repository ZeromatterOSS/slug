# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect a route not decided by
the skill.

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-09-02 | M7A repository-source glob routing R1/R2 | Root generic DICE/loading implementation, existing catalog/listing/boundary owners, accepted Bazel 9.2 glob evidence, clean Zabel peer guidance and independent architecture/correction review | active goal continuation | One Host/catalog traversal and request adapter; exact catalog match slices, package-boundary pruning, empty Host observations, fail-closed component/kind projection, cross-target catalog route; 228/302/530 gross production/proof/total | R1 production clears `GlobUnsupported`, but its proof overclaimed successful `tools/res` package publication; R2 narrows proof, passes complete serial gates and authentic replay, and stops at the independent existing `toolchain_type` Label-versus-string schema error | Repository-source glob routing implemented; audit the complete generic label-like builtin declaration/schema category next, with no ruleset/toolchain/C++ consumer special case |
| 2026-09-01 | M7A package-context label-string category R1-R4 | Root shared-parser implementation, serial owner/direct-dependent validation, authentic rules_rust replay, clean Zabel peer guidance and independent architecture/terminal reviews | active goal continuation | One borrowed syntax owner projects to existing `CanonicalLabel`; distinct defining-Bzl, BUILD, tag, ordinary/innate repository contexts; typed passthrough and collision rejection; 312/692/1,004 gross production/proof/total | R1/R2 corrected inventory, innate mapping, `@//`/`@@//` and collisions; terminal R2 `REPLAN` corrected ordinary evaluation base and output non-widening; terminal R3 `REPLAN` restored definition-owned repository-output base and pre-packet special-package parsing; focused R4 rereviews `ACCEPT` | Implementation terminally `ACCEPTED`; authentic replay clears `@rust_host_tools` and selects the generic built-in-catalog `glob()` boundary in `@@bazel_tools//tools/res`, with no ruleset/C++ consumer branch |
| 2026-09-01 | M7A shared root repository load route R1/R2/replan R1 | Root cross-crate DICE/publication implementation, pinned Bazel 9.2 sibling-generated-repository oracle, rebuilt Slug replay, Buck2 retained-utility check, clean Zabel peer guidance and independent architecture/terminal reviews | active goal continuation | One admission-aware loading route serves root BUILD, exported-source build and external query; exact owner namespace plus certificate-owned existence; private 997-line core bridge deletion; 596 Bzlmod, 503 loading, 55 query and 6 adapter tests | Initial candidate needed a read-only source-error projection; recursive sibling proof then `REPLAN`ed import-only demand authentication to the producer-owned `unique_name + "+"` namespace; focused correction and terminal rereviews `ACCEPT` | Implementation terminally `ACCEPTED`; authentic replay clears generated-root publication and selects a docs-first complete `repository_rule` declaration/schema category audit, with no ruleset/C++/parser special case |
| 2026-09-01 | M7A dependency-attribute property flags R1-R4 | Root complete Bazel 9.2 25-property audit and implementation, live Slug owner/effect trace, Buck2 compact-bit owner and clean Zabel peer guidance | active goal continuation | Five constructors plus sibling-keyword surface; one ordered final set through rules/aspects/macros/subrules; exact existing-field reconciliation and generated-only file-check bypass; 537/371/908 gross production/proof/total | Architecture R1/R2 corrections fixed source/generated skip, ordering, subrules, caps and tag/repository classification; terminal R3 `REPLAN` added generic unowned-predicate publication failure, default/flags/later-property order and final-property boundary proofs; focused R4 reviews `ACCEPT` | Implementation terminally `ACCEPTED`; authentic replay clears flags and selects generic `attr.label_list(allow_rules = ["sh_library"])` for a complete rule-class restriction audit, with no ruleset/C++ consumer branch |
| 2026-09-01 | M7A module-extension metadata architecture/implementation R1/R2 | Root complete-category design and implementation over shared Bzlmod metadata/lockfile-facts owner, selected and legacy invocation routes, Bazel 9.2 authority, Buck2 compact utilities and clean Zabel peer guidance | active goal continuation | Four-keyword constructor, opaque detached/default-normalized return, root proxy retention, root-conditioned generated-repository validation and authentic rules_rust discriminator; 584/356/940 gross production/proof/total | Architecture R1 `REPLAN` corrected empty-list `"all"`, default equality and root presence; terminal R1 `REPLAN` corrected metadata-before-imports and dev-name-before-regular failure order; both focused rereviews `ACCEPT` | Implementation terminally `ACCEPTED`; authentic replay clears metadata and selects the generic `SKIP_CONSTRAINTS_OVERRIDE` attribute-flag category for a docs-first audit, with no consumer special case |
| 2026-09-01 | M7A rule predeclared outputs R2 implementation stop / R3 correction | Root generic cross-crate implementation, complete serial owner/downstream gates, authentic rules_rust replay and independent terminal architecture review; Bazel 9.2 authority, Buck2 compact utilities and clean Zabel peer guidance only | active goal continuation | R2 reaches the later generic rules_rust `aspect(...)` frontier at exact 360/417 net production/proof, but its focused matrix misses residual callback names, absent-provider default files and reserved executable output key; nonmerged proof has no lawful command producer | Terminal review returns `REPLAN`; R3 removes unreachable split-genfiles plumbing/claim, admits only default merged mode, and adds the three exact semantics under unchanged owners/caps | Obtain focused R3 architecture rereview before Rust; keep split-root command/configuration plus bytes deferred, with no rules_rust, `cc_common`, `cc_internal`, parser or C++ special case |
| 2026-08-31 | M7A coverage configuration-field architecture R1/R2 | Root pinned the one-field Bazel 9.2 coverage fragment, traced live structural option/late-bound/fragment owners, reviewed starlark-rust/Buck2 compact utility reuse and clean Zabel's typed declaration split, then obtained independent architecture review/rereview | closed typed multi-fragment field enum; existing coverage Boolean/label option vector and ordinary/lifted resolver; exact command override; root/subrule public fragment facade; aspects/instrumentation/actions deferred | R1 failed to distinguish public `coverage.output_generator` from the private allowlisted C++ facade; R2 forbids caller manifests and BuiltinRestriction, requiring non-allowlisted ordinary/subrule true-label and false-None proofs | Focused R2 architecture rereview `ACCEPT`; commit design, then implement only the frozen eight-file production/proof boundary and 520/650 net, 850/900 gross, 1,750 total caps |
| 2026-08-31 | M7A label file-admissibility cap, Host, complexity and proof corrections | Root measured the atomic loading/analysis carrier replacement, reran complete analysis after lazy structural-Host correction, and obtained two focused design reviews plus terminal implementation review/rereview | unchanged six production/five proof file contract; structural Host only for suffix predicates; existing generated binder and configured-analysis orchestrator retained as cohesive owners; no parser, ruleset, C++, `cc_common`, or `cc_internal` branch | Initial candidate exceeded the old production estimate; unconditional Host lookup failed Boolean policies; terminal review found production sound but declaration-local A/B/A and Boolean control proofs under-discriminated. The correction adds same-DICE configured result/error restoration, scalar invalids and suffix-specific macro/aspect/repository rows only | Terminal rereview `ACCEPT`; corrected 214/650 production, 448/606 proof and 1,256 total gross pass 552 loading plus 117 analysis tests, rebuilt replay and all gates. Commit and advance to the complete coverage configuration-field audit |
| 2026-08-31 | M7A FilesToRun Spawn expansion design R1 evidence correction | Root pinned Bazel/live-owner/Zabel audit, compact packet rollover, independent retained-representation review and focused correction rereview | active goal continuation | Complete direct/associated executable/tool category, stable provider topology, root/subrule and top-level/nested-depset split, one alias-aware equality state, migration-bit deletion proof and bounded module split | Initial review accepted the architecture but returned `REPLAN` because the recorded `SpawnAction.java` SHA-256 omitted two hex characters; correction restores the locally recomputed pinned Bazel 9.2 digest only | Focused rereview `ACCEPT`; implement with every retained and scope boundary unchanged |
| 2026-08-31 | M7A four runfiles-support actions R3/R4 Host-flavor correction | Root implementation over accepted typed-provider/action design, full serial validation, terminal architecture review, docs-only R4 replan and focused correction/terminal rereviews | active goal continuation | R3 candidate atomically registers the exact four-action non-Windows suffix, shares one typed support carrier, preserves graph-aware equality, and passes owner/downstream/A-B-A/cap/hygiene gates | First terminal review returned `REPLAN` because analysis discarded `HostPathFlavor`; focused R4 review and terminal rereview `ACCEPT` the preserved typed pair, pre-publication Windows rejection and lazy nonexec proof while retaining all prior ownership/equality/topology/atomicity/isolation boundaries | R4 passes full serial validation at 526/591/1,117 net and 620/615/1,235 gross production/proof/total; commit the implementation, then activate Spawn expansion |
| 2026-08-31 | M7A transitive runfiles package-mapping R3/R4 loading owner | Root implementation over accepted R3 retained/DICE design, terminal architecture review, then bounded R4 type-isolation correction and independent design rereview | active goal continuation | R3 complete Host Legacy/Observed root mapping and retained metadata pass owner/loading/analysis gates, but active pre-Host `PackageLoadKey` still returned an empty-mapping `LoadedPackage` to legacy query/runtime adapters | Terminal R3 review returned `REPLAN`; R4 separates shared evaluation fields from noninterconvertible complete/legacy wrappers, records the Host-cutover fallback ledger, synchronizes direct query fixtures and adds the missing innate-owner cohort discriminator; full owner/direct-dependent gates and terminal correction rereview `ACCEPT` | Loading owner accepted within 986 gross Rust lines; implement the configured collector next, then the four actions and Spawn expansion |
| 2026-08-31 | M7A generic Args/run/symlink and vector/param-file successors | Root pinned Bazel 9.2 Args/path/private-API and rules_cc 0.2.17 audit, Buck2 retained-utility guidance, independent reserved-architecture and terminal reviews | active goal continuation | Scalar vertical accepted one evaluator-owned Args and generic typed actions; vector R1 adds retained sequence/depset recipes, param policy/write and passes all focused/full crate gates | Scalar design correction and implementation `ACCEPT`; vector R1 terminal review `REPLAN`s Bazel binder order for invalid supplied sources before callbacks and one unallowlisted no-op sink signature adapter; focused R2 and terminal rereview `ACCEPT` | Vector R2 validates sources before callback rejection, formally admits the frozen adapter, and passes all owner/direct-dependent gates within 891/747/1,638; complete the remaining generic non-callback Spawn envelope next, with no parser, C++/FDO branch, callback or duplicate retained owner |
| 2026-08-30 | M7A configured-hidden R4 index stop and selected-context prerequisite R2/R3 | Root exact staged/index isolation, cold serial analysis regression, bounded DICE trace, retained-utility review and independent reserved-architecture/terminal review | active goal continuation | Combined dirty tests concealed two base failures: broad Exec resolution follows an injected mismatched platform terminal into recursion, while zero-toolchain Exec children reach the Target-only action-context rejection; R2's one-shot REAPI proof reaches unrelated rules_shell attribute and recursive-glob gaps before assertions | R3 replaces only the proof route with a genuine validated configured-action REAPI consumer; clean-index owner/direct dependents pass, core retains five base failures, both reviews `ACCEPT`, and `ebd19e3b1` commits the prerequisite; configured-hidden R4 terminal review then `REPLAN`s its Target-before-Exec partition and selected-toolchain-only guard consumption | R5 preserves one descriptor-ordinal stream and one parent guard across selected-toolchain plus declared-child analysis; retain the pre-call stop without a narrower Target-only slice or C++ special case |
| 2026-08-30 | M7A effective native-command FDO prerequisite and configured-hidden R4 activation | Root pinned Bazel 9.2 option-parser/CppOptions audit, live command/DICE/configuration owner trace, Buck2 retained-utility guidance and clean Zabel peer review | active goal continuation | Configured-subrule R2 lacked an authorized native producer; R3 then overclaimed configured edges from a mandatory error, rejected executable files, widened `fdo_optimize`, and used an inexact source/blob ledger | `4425d3bfb` accepts the lawful producer; independent R3 plan review `REPLAN`; R4 defers configured edges to direct-call success, admits inherent file executables, restricts raw `//`, and records exact sources/blobs | Implement R4 through one shared typed projection and existing target/selected-Exec child pipeline; validate before the pre-call stop and defer evaluator values/configured edges/XML/aspects |
| 2026-08-30 | M7A subrule loading authenticated-source and terminal correction | Root exact rules_cc/Bazel 9.2 replay, retained-utility review, bounded owner correction, full gates and independent terminal review/rereview | active goal continuation | Exact toolchain adds `cpp.libc_top`, `cpp.zipper` and `TemplateVariableInfo`; first terminal review finds lost direct roots/callable routes, order-sensitive provider predicates, repository/tag deferred-default leaks, non-reflexive transient equality and missing persistent token proof | First implementation review `REPLAN`; correction retains distinct direct/transitive sets plus frozen callable routes, canonical shared provider predicates, fail-closed consumers and missing equality/token regressions; focused rereview `ACCEPT` | Implementation `ACCEPT`; full live/index-only gates and rebuilt CLI replay pass to the later positional-`module_extension` frontier; commit, then activate configured hidden-dependency resolution |
| 2026-08-30 | M7A Bazel `.bzl` global capability architecture R1-R3 and macro/provider implementation R1/R2 preflight | Root pinned Bazel 9.2 lifecycle audit, live Slug ownership/dirty-overlay trace, Buck2 utility guidance, clean Zabel concept guidance, independent architecture/activation reviews, then fresh-evaluator carrier preflight | active goal continuation | R3 freezes fresh-evaluator package macros, nonconstructible provider identity, dependency-only subrules and bounded late enforcement; activation fixes the ledger to five present plus absent subrule. Implementation R1 then finds starlark-rust has no print-handler getter and its five-file envelope cannot share the BUILD event sink | Architecture R1/R2 `REPLAN`, R3 `ACCEPT`; activation correction `ACCEPT`; implementation R1 `REPLAN` for clean `bzl_module.rs`; first R2 review adds direct/nested/error/disabled event proof and focused rereview returns `ACCEPT` | Implement R2 under 2,250/2,200/4,450 with one shared capture/two BUILD handoffs; late enforcement remains next |
| 2026-08-30 | M7A repository-context attribute namespace architecture R1-R3 and implementation R1/R2 stops | Root pinned Bazel/Slug owner audit, Buck2 utility review, clean Zabel concept guidance, independent architecture/correction/replan review, one two-file writer and root production/proof audits | active goal continuation | Architecture R2 wrongly published retained `None`; accepted R3 corrects it. Implementation R1 is 290 production/197 proof/487 total and passes focused tests, but omits owner A/B/A rows and uses tagged `Override`/`Coerced` views with parallel allocator branches | Architecture R2 `REPLAN` then R3 `ACCEPT`; implementation R2 proof-cap replan `ACCEPT`; pre-proof production audit then exposes the normalized-view contract miss | Implementation `REPLAN`; independent review accepts R3 changing only production/aggregate caps to 500/1,600; replace rather than layer source branches, then complete unchanged 1,100-proof/two-file contract |
| 2026-08-30 | M7A repository-qualified external `.bzl` route R1/R2/R3 | Root implemented the independently reviewed Root/Canonical handoff, audited Bazel 9.2 and clean Zabel owner models, corrected R2's pre-override mapping as R3, then ran full serial validation and two cold rules_rust replays | active goal continuation | `f747507f6` resolves the exact selected-parent generated canonical target, selects the canonical definition host and fully substituted mapping, admits nonroot-only ordinary execution, and preserves each usage-owner mapping; both cold replays continue through the real rules_cc owner and stop at missing `tools/build_defs/repo/utils.bzl` | R1 exposed the owner gap; independent R2 review rejected its pre-override namespace row; independent R3 design and terminal implementation reviews return `ACCEPT` | Implementation `ACCEPT`; import Bazel 9.2's complete direct `tools/build_defs/repo` embedded package next, not only `utils.bzl`, with Zabel as peer guidance only |
| 2026-08-28 | M7A selected-toolchain R1/R2 stop and registered-closure design | Root retained the candidate through focused/full validation, corrected pointer/identity proofs, pinned embedded-tools/registration/generated-repository audit, and two-round independent closure review; Bazel 9.2 authority, Buck2 utilities and clean Zabel peer guidance only | generic selected context retained; exact four-row closure split into catalog, declaration metadata and Host-input design | R2 proofs pass, then REAPI reaches absent `tools/launcher`; trace also requires source-launcher/res bytes, ordinary rules_cc/rules_shell and canonical `local_config_winsdk`; review rejects premature post-Host manifests, then accepts exact three-packet scheduling | Design `ACCEPT`; implement only `WP-4-5-7A-builtin-bazel-tools-registered-toolchain-catalog-implementation`, with later effects still unauthorized |
| 2026-08-28 | M7A repository-rule declaration metadata R1 downstream-catalog proof correction | Root four-file compact metadata implementation, focused/full serial validation and exact failure isolation; Bazel 9.2 authority, Buck2 `SmallSet` reuse and clean Zabel peer guidance only | unchanged producer-owned local/configure/environ metadata; one stale built-in subtree expected vector at the accepted catalog boundary | Focused metadata tests pass; full loading is 411/412 because its base expectation predates the exact launcher/res/source-launcher packages imported by `87d332cf6` | `REPLAN` proof only to R2; admit one existing test blob and only its two expected package vectors, then rerun full loading and terminal review |
| 2026-08-28 | M7A effective repository Host-input architecture R1-R5 | Root pinned Bazel/DICE/live-owner audit, Buck2 retained-utility review, clean Zabel concept-only guidance and independent Sol architecture/correction reviews | command snapshot plus per-name injected environment/platform ABI, typed Need/frontier retry, rollback authorization, canonical effect successor and proof-only registration successor | R1-R4 exposed missing effect identity, overlapping successor blobs, Host/rc overclaims, cold-absent injection, rejected-cache authorization and core/loading dependency direction; no Rust was authorized | R5 design `ACCEPT`; implement only `WP-4-5-7A-effective-repository-host-input-implementation` under its exact blobs and 1750/1900/3650 caps |
