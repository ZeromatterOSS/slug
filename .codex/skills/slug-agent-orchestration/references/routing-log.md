# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-26 | M1 Stage 5 local registry-directory transport oracle design | Root pinned-source/live-fixture synthesis, two distinct-root lockfile probes, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-text reviews | not exposed | Exact six-path/four-entry reuse freezes one `devonly` URL through regular, directory, mutable-metadata fail-open, absent, and exact restoration states; 80/44-byte SRIs, two relative symlinks, five semantic manifests, and packet-five growth accounting are bounded without fixture or code edits | Reviews replaced the larger discovery fixture, corrected metadata fail-open and visible local-hash filtering, disproved a provisional normalizer prerequisite with two byte-identical path-free lockfiles, removed a nondiscriminating subject repair, and all three final reviews returned `ACCEPT` | `ACCEPT`; implement only the exact oracle, then run the required five-packet fixture-growth review before a sixth oracle |
| 2026-07-26 | M1 Stage 5 private Host registry-file owner redesign | Parallel pinned-source, live-DICE, and architecture audits; root synthesis; one focused text correction; terminal latest-text rereview | not exposed | The stopped two-file draft corrected checksum-mode identity, local recordability/JDK path conversion, and exact vendor selection/fallback without Rust or fixture edits | Native feasibility and architecture accepted, but source rereview found Bazel directory-listing bytes contradict the accepted runtime bridge's directory read error; this second material prerequisite miss ends the packet | `REPLAN`; design only a focused pinned local-directory Registry IO oracle |
| 2026-07-26 | M1 Stage 5 local native-path Registry IO bridge correction | Exact two-file writer, serial focused/full/downstream/cross-target validation, and three terminal latest-diff reviews | not exposed | Defaulted native-path capability and production direct-Path override passed focused 1+2, bzlmod 201+184, loading 54, core 104+13, all doctests, all 20 GNU-Windows executables, and auxiliary gates at +113/-2 | No correction; legacy remote/local behavior and all external implementations remain compatible, with no Host owner or activation, and all reviews returned `ACCEPT` | `ACCEPT`; redesign only the private Host registry-file owner |
| 2026-07-26 | M1 Stage 5 local native-path Registry IO bridge correction design | Root live-trait/implementation synthesis plus source/contract, implementation-feasibility, and architecture/orchestration terminal latest-text reviews | not exposed | Exact two-file/220-addition design adds one defaulted native-path capability method, preserves all existing implementations and remote/legacy semantics, and freezes direct production Path use plus local IO/generation-order evidence | All three reviewers accepted the defaulted compatibility boundary, focused path-versus-decoy/non-UTF-8 tests, no-DICE-IO rule, and exclusion of the blocked Host owner without correction | `ACCEPT`; implement only the exact two-file local native-path bridge correction |
| 2026-07-26 | M1 Stage 5 Host registry-file owner pre-implementation audit | Parallel pinned-source, live-seam implementation, and architecture/DICE audits with root adjudication before Rust | not exposed | Exact owner order, typed Need/error boundaries, DICE-owned vendor observation, local replay edge, and five-file eventual closure are feasible, but the accepted local bridge ignores its native path argument while production URL-only IO re-derives paths without exact decoding | Root rejected a non-DICE vendor capability because it hides path lifecycle and Need evidence; no Rust, fixture, Cargo, dependency, API, DICE, consumer, or activation changed | `REPLAN`; design only the focused local native-path Registry IO bridge correction |
| 2026-07-26 | M1 Stage 5 Host registry-file vendor oracle | Exact six-path fixture writer, pinned generation, two absolute distinct-root replays, root validation, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Four assets and five inserted Off rows prove vendored hit/fatal/fallback/restoration plus checksum-empty Refresh; exact request transitions and lockfile preservation passed with fixture 22/0/1,340 and full tree 1,301/14/36,603 | One generation-time assertion correction matched the existing normalizer's collapsed fatal file-URL/workspace seam; parser, 10 validator tests, hashes, scope, anchors, schema, archive, credential, host-path, and diff gates passed, and all three final reviews returned `ACCEPT` | `ACCEPT`; design only the separate private Host registry-file owner |
| 2026-07-26 | M1 Stage 5 corrected Host registry-file vendor oracle design | Exact four-hunk text correction plus source/parity, implementation/evidence, and architecture/orchestration terminal latest-text reviews | not exposed | Misleading aaa and Refresh now share `vendor-hit`, retaining checksum-present yyy vendor use and checksum-empty aaa bypass with exact 4→5/5/5/6 yyy MODULE counts; stop gates distinguish MODULE bytes from permitted RepoSpec metadata | The distinct correction packet changed only the two recorded semantic issues and preserved the six-path/four-asset/fourteen-command contract, caps, anchors, metadata requirements, and all exclusions; all three reviewers returned `ACCEPT` | `ACCEPT`; implement only the exact Host registry-file vendor oracle |
| 2026-07-26 | M1 Stage 5 Host registry-file vendor oracle design | Root existing-fixture/pinned-source synthesis plus source/parity, native-evidence, and architecture/orchestration terminal latest-text reviews | not exposed | Stopped six-path/four-asset/fourteen-command contract preserved exact vendor hit/fatal/fallback/Refresh boundaries without fixture, Rust, Cargo, dependency, API, DICE, consumer, or activation changes | One correction added cache application, RepoSpec deltas, asset hashes, shifted counts, and metadata truth; final reviews found Refresh with `vendor-missing` forces an extra yyy MODULE request and the stop gate contradicts permitted yyy source metadata, so the second material correction ends the packet | `REPLAN`; correct only aaa asset/Refresh vendor placement and the yyy MODULE stop gate |
| 2026-07-26 | M1 Stage 5 Host Registry IO bridge | Exact one-file writer, serial focused/full/downstream/cross-target validation, and source/parity plus architecture/orchestration terminal latest-diff reviews | not exposed | Closed Host remote plan and typed local executor preserve legacy wrappers and exact generation ordering; focused 4+5, bzlmod 201 unit + 184 integration, loading 54, core 115, zero doctests, all 20 GNU-Windows builds, and exact 833/96 scope plus auxiliary gates passed | First review accepted production semantics but required discriminating complete-value, error-equality, successful-unverified-refetch, and checksum-stickiness evidence; one test-only correction closed all gaps and both final rereviews returned `ACCEPT` | `ACCEPT`; design only the focused Host registry-file vendor oracle before the separate file owner |
| 2026-07-26 | M1 Stage 5 Host Registry IO bridge design | Root pinned-source/live-seam synthesis plus source/parity, implementation-feasibility, and architecture/orchestration terminal latest-text reviews | not exposed | Exact one-file/850-addition bridge contract freezes a closed remote plan, typed Host remote/local entrypoints, the 4×3 hash-mode/expectation matrix, and generation-before/after-IO evidence while preserving every legacy wrapper | Reviews corrected a provisional Host remote Ignore fetch: factory-reachable Ignore is file-only, so Host remote rejects it and legacy Off selects internal unverified fetch directly; all three returned `ACCEPT` | `ACCEPT`; implement only the exact one-file private Host Registry IO bridge |
| 2026-07-26 | M1 Stage 5 pure Host RegistryFunction owner | Exact three-file writer, serial focused/full/cross-target validation, and source/parity plus architecture/orchestration terminal latest-diff reviews | not exposed | Root-free descriptor construction, Java-URI-compatible parsing, exact mode/vendor/Refresh/visible/mirror order, narrow hash/yanked projections, and retained equality/pruning passed focused 11, full 197 unit + 184 integration, zero doctests, and all 12 GNU-Windows test builds; exact 1,543-addition scope and auxiliary gates passed | Two terminal replans corrected URI authority/IPv6/fragment semantics, exact factory order, nonallocating yanked lookup, dependency-prefix and recomputation evidence, and empty-mirror equality; both final reviews returned `ACCEPT` | `ACCEPT`; design only the one-file private Host Registry IO bridge |
| 2026-07-26 | M1 Stage 5 dormant Host registry-input owners | Exact three-file writer, serial focused/full/cross-target validation, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Separate compact registry/mirror/token inputs and vendor projection passed focused 4, full 190 unit + 184 integration, zero doctests, and all 12 GNU-Windows test builds; exact 899-addition scope and auxiliary gates passed | Writer trimmed an over-cap intermediate, then a removed audit proved absent InjectedKeys have zero dependencies and are non-replayable setup invariants; all three restored-diff reviews returned `ACCEPT` | `ACCEPT`; implement only the pure root-free Host RegistryFunction owner |
| 2026-07-26 | M1 Stage 5 Host registry-input owners design | Root pinned-source/live-seam synthesis plus source/parity, implementation-feasibility, and architecture/orchestration terminal latest-text reviews | not exposed | Exact three-file/900-line contract freezes separate normalized registry set, complete mirror map, vendor projection, and opaque Refresh token with test-only injection and focused retained-DICE evidence | Reviews corrected implicit-default slash identity, structured converter boundary, order-insensitive equality plus old-value retention on equal reinjection, and the deferred fatal vendor-file read boundary; all three returned `ACCEPT` | `ACCEPT`; implement only the exact dormant three-file prerequisite |
| 2026-07-26 | M1 Stage 5 corrected Host RegistryFunction oracle | Exact four-path writer, pinned generations, two distinct fresh-root replays per fixture, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Nine yanked and twelve transport rows passed exact cold-cache counts, digest restoration, mirror URL order/comma, exit-2 unknown registry, 29-file/zero-link inventory, and 1,659-line cap; validators, anchors, archive, diff, and credential gates passed | Two stopped generations drove the accepted clean-cache and embedded-BCR empty-override corrections; the complete corrected diff then received three terminal `ACCEPT` verdicts | `ACCEPT`; design only the private Host registry input/projection owners |
| 2026-07-26 | M1 Stage 5 Host RegistryFunction oracle BCR-mirror correction | Stopped two-fixture generation, isolated pinned transport probe, and pinned-source/parity, native-observation, plus architecture/orchestration terminal latest-text reviews | not exposed | Yanked clean-cache sequence passed; transport URL lists were correct but default mirrors leaked to embedded BCR archives. Explicit empty BCR mirrors then preserved exact default/specific/empty lists with no observed subject failed-download diagnostics | Reviews corrected the formatter comma, filtered-log claim, tracked/ignored state, and subject-only archive boundary; all three returned `ACCEPT` | `ACCEPT`; rerun only the complete corrected four-path oracle |
| 2026-07-26 | M1 Stage 5 Host RegistryFunction oracle clean-cache correction | Stopped first generation, two isolated native invalidation probes, and pinned-source/parity, native-implementability, plus architecture/orchestration terminal latest-text reviews | not exposed | Root and trailing-registry changes left counts warm; expunge plus a fresh run-local repository cache and disabled contents cache produced absence `1,1,1,1`→`1,2,2,1`, later reason-a zero requests, and exact SHA yyy +1/metadata +0 | Reviews rejected unproved cache-child claims, distinguished surviving shared-cache isolation from deletion, replaced a redundant Update row to preserve +7 net commands, and corrected pinned source ranges; all three returned `ACCEPT` | `ACCEPT`; rerun only the corrected four-path oracle implementation |
| 2026-07-26 | M1 Stage 5 Host RegistryFunction oracle design | Root existing-fixture/source synthesis plus pinned-source/parity, native-implementability, and architecture/orchestration terminal latest-text reviews | not exposed | Exact four-path/seven-row reuse freezes remote Off absence/yanked/SHA after root recomputation and `show_repo` default/specific/later-wins/empty/unknown mirror projections, with zero new files/links and a final 1,800-line cap | Reviews added the root cache discriminator, exact digest restoration/request deltas, exit-2 proto/formatter anchors, truthful metadata, and archive-only claim boundary; all three returned `ACCEPT` | `ACCEPT`; implement only the exact two-fixture/four-path oracle |
| 2026-07-26 | M1 Stage 5 Host RegistryFunction boundary redesign | Root pinned Bazel/live-seam synthesis plus source/parity, native-implementability, and architecture/orchestration terminal latest-text reviews | not exposed | Root-free construction, exact mode/vendor/Refresh/visible/mirror/validation order, raw identity, hash/yanked/vendor rules, compact selective equality, sticky RegistryIo reuse, and an oracle-first serial owner sequence were frozen without Rust or fixtures | Reviews removed the historical root edge, separated hourly Refresh invalidation from request generation, corrected `%workspace%`/mirror/primary precedence, recorded-absence yanked reuse, fatal vendor reads, and redundant root-Need propagation; all three returned `ACCEPT` | `ACCEPT`; design only the focused Host RegistryFunction oracle |
| 2026-07-26 | M1 Stage 5 corrected Host visible-lockfile owner | Exact four-file implementation, serial focused/full/cross-target validation, and source/parity, implementation/evidence, plus architecture/orchestration terminal reviews | not exposed | Private file-before-mode Host owner, sole-v28 typed parser, exact caught/direct diagnostics, complete semantic equality, retained lifecycle, and opaque downstream pruning passed focused 9, complete bzlmod 370, zero doctests, and all 12 GNU-Windows executables | First exact merge row forced a four-file design correction; final reviews added Version-only class-prefixed message retention and a non-Version safety regression, then all three latest-diff verdicts returned `ACCEPT` | `ACCEPT`; design only the Host registry function boundary |
| 2026-07-26 | M1 Stage 5 Host visible-lockfile owner merge-message correction design | Root pinned Gson/Bazel and sole-reader synthesis plus source/parity, implementation-feasibility, and architecture/orchestration terminal reviews | not exposed | Exact four-file retry permits only a Version-domain failed-conversion message correction, inline full-reader regression, exact Host merge/delete diagnostics, and ignored-marker false-positive discriminator while preserving typed metadata and non-Version behavior | Reviews froze the class-prefixed `NumberFormatException` text, rejected whole-input scanning, closed the shared Facts-domain spillover, retained separately allocated equality evidence, and all three returned `ACCEPT` | `ACCEPT`; implement only the exact four-file corrected Host visible-lockfile owner |
