# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-25 | M1 Stage 5 loading globals owner correction design | Root one-heap owner synthesis plus independent feasibility and architecture reviews | not exposed | The corrected five-file retry builds standard, Print, and loading-specific globals in one package-owned Globals heap and removes every copied/process-static overlay while preserving the green producer | Architecture returned `ACCEPT`; feasibility agreed technically and its sole scope revision—explicitly add `package.rs`—was already incorporated, with exact loading/core/runtime/Windows and static stop gates | `ACCEPT`; implement only the corrected five-file loading event producer |
| 2026-07-25 | M1 Stage 5 loading event producer first implementation | Bounded four-file writer, root focused/downstream validation, and independent API/evidence plus architecture terminal reviews | not exposed | Local `.bzl`/BUILD batches, explicit empty replacement, pre-eval empty, runtime prefixes, and marker-off behavior reached 54 loading and 98 core passing tests after a lifetime correction | The first run exposed missing Print globals; its copied-FrozenValue overlay then caused a downstream runtime SIGSEGV. A process-static owner passed, but this was the second material correction and terminal review required `REPLAN` | `REPLAN`; design only a one-heap loading-globals owner correction before retrying the producer |
| 2026-07-25 | M1 Stage 5 terminal event/execution oracle corrected retry | Corrected seven-file writer, pinned generation, root two fresh replays/full harness tests, and two terminal reviews | not exposed | Nine exact retained records, runtime-split failure sentinels, V1/V2 manifests, parser names/mutations, zero new entries, and +321 fixture lines passed with 52 harness tests | The sole prior stale parser consumer is now exact; both latest reviews returned `ACCEPT` with no semantic oracle change | `ACCEPT`; implement only the loading event producer |
| 2026-07-25 | M1 Stage 5 terminal oracle allowlist correction | Root direct-consumer diagnosis plus two independent exact correction reviews | not exposed | The sole missing consumer is the fixture-specific parser regression; the corrected seven-file contract freezes all nine command names and the two ordered row-4 mutations while preserving the accepted oracle unchanged | Both reviews agreed the six fixture files fully conform and the parser test is the only correction; zero fixture entries and +321 fixture lines remain | `ACCEPT`; retry only the corrected seven-file terminal oracle |
| 2026-07-25 | M1 Stage 5 terminal event/execution oracle first implementation | Bounded six-file writer, pinned generation, two fresh-root replays, root downstream harness validation, and two terminal reviews | not exposed | All nine Bazel rows, channels, sentinels, manifests, provenance, no-entry scope, and +321-line growth passed; runtime-assembled failure tokens prevented echoed-source false positives | The top-level harness exposed a direct parser test hard-coded to two commands/index one outside the exact allowlist; both reviews returned `REPLAN` and the full draft was removed to clean baseline | `REPLAN`; correct only the exact allowlist/test contract before retry |
| 2026-07-25 | M1 Stage 5 terminal-oracle REAPI applicability | Bounded three-file harness implementation, root full top-level oracle tests, and independent contract plus architecture corrected-diff reviews | not exposed | Remote flags now apply only to build commands; positive completed evidence applies exactly to expected-success builds while actual exits remain independently checked; 52 top-level oracle-harness tests passed | Review added the bidirectional expected/actual mismatch discriminator; an actual-exit suggestion was withdrawn after the accepted contract was reread, and both latest reviews returned `ACCEPT` | `ACCEPT`; implement only the six-file nine-row terminal oracle |
| 2026-07-25 | M1 Stage 5 terminal event/execution oracle design | Confined two-run Bazel probe, live harness/fixture/source audits, root nine-row synthesis, and three corrected terminal rereviews | not exposed | Nine retained rows freeze cold producer order, warm build/query nonreplay, empty-query dependency edit, deferred analysis, eligible failure prefix, action diagnostic ordering, and post-failure reuse in six existing fixture files | Reviews proved the three-file REAPI applicability prerequisite, retained the required query edit/warm split, fixed contract-based evidence gating, all stdout/exits/manifests, and one complete sentinel namespace; all three latest rereviews returned `ACCEPT` | `ACCEPT`; implement only generic terminal-oracle REAPI applicability |
| 2026-07-25 | M1 Stage 5 shared retry/publication driver checkpoint | Live typed-boundary, discovery/source, lifecycle/architecture, and implementation-feasibility audits with root serial synthesis and corrected terminal rereviews | not exposed | One abort-owned typed retry loop, fixed full request inputs, exact closure selection, logical output buffering before lease close, post-close external effects, Host migrations, typed command roots, and vertically atomic activation are split behind explicit gates | Reviews added exact fixture/files, independently reached Need union, registry-policy propagation, build/query roots, preactivation Host migrations and forbidden scans, mandatory output consumption, phased REAPI/legacy work, and retained discovery package-policy prerequisites; all three latest rereviews returned `ACCEPT` | `ACCEPT`; design only the retained terminal event/execution oracle |
| 2026-07-25 | M1 Stage 5 runtime native demand session | Bounded three-file implementation, root serial focused/full/cross-target validation, and independent lifecycle plus architecture terminal rereviews | not exposed | Dormant whole-transition lease, fixed generations, cumulative repo/path epochs, owner-branded exact-closure sidecars, strict restore-on-error, and real-DICE combined Need handshake passed focused 6, repository 23, core 85+13+0, and GNU-Windows compilation | Reviews made progress errors consume and restore the command, added combined repository/path and exact generation/epoch evidence, moved the effect owner into the command, made terminal transitions non-cancellable, and branded selections against foreign replay; both latest-diff rereviews returned `ACCEPT` | `ACCEPT`; design only the bounded shared retry/publication driver checkpoint |
| 2026-07-25 | M1 Stage 5 retained native materialization bridge | Bounded one-file implementation, root serial focused/full/cross-target validation, and independent source/contract plus architecture/hot-path terminal reviews | not exposed | One-pass deduplicated preflight, exact reusable/full-request ownership, rootless Local, retained root/token checks, and staged HTTP/Git native materialization passed focused 23, core 79+13+0, and GNU-Windows compilation | Reviews restored accepted malformed-checksum capture precedence, distinguished inherited A-to-B replacement from current-command conflicts, and added deterministic Git Materialization-stage evidence; both corrected latest-diff rereviews returned `ACCEPT` | `ACCEPT`; implement only the dormant three-file runtime native demand session |
| 2026-07-25 | M1 Stage 5 workspace demand provenance bootstrap | Bounded four-file implementation, root retained-DICE/full/cross-target validation, and independent DICE/source plus architecture/hot-path terminal reviews | not exposed | Weak-engine sparse catalogue, sole passive/eventful tracker, five-site factory, exact installed-owner terminal binding, one-closure semantic selection, and retained warm/late/unreachable evidence passed demand 7, events 4, core 75+13+0, and GNU-Windows compilation | Reviews corrected partial marker installation, dense sparse-copy reservation, missing warm/late/foreign evidence, and caller-substitutable terminal owner selection; both latest-diff rereviews returned `ACCEPT` | `ACCEPT`; implement only the one-file retained native materialization bridge |
| 2026-07-25 | M1 Stage 5 demand-key metadata | Bounded two-file implementation, root focused/full/cross-target validation, and independent DICE/source plus architecture/hot-path terminal reviews | not exposed | Exact lazy `Key::provide` path demand, shared full request, and normalized workspace/module source scope passed workspace 30, bzlmod 233, GNU-Windows compilation, and scope/format/archive gates | Direct erased-key tests discriminate source path exclusion and workspace/module identity; architecture review replaced eager payload construction with lazy `provide_value_with`; both latest rereviews returned `ACCEPT` | `ACCEPT`; implement only the four-file workspace demand provenance bootstrap |
| 2026-07-25 | M1 Stage 5 corrected runtime-native demand-producer design | Independent DICE/source, architecture/lifecycle, and live native-feasibility audits with root contract synthesis and corrected terminal rereviews | not exposed | Key-static `Key::provide` producers, one passive/eventful tracker, workspace-lifetime node provenance, one exact closure, semantic scope traversal, fixed generations, strict repo/path progression, rootless Local behavior, whole-transition command lease, retained pins, and four serial packets are frozen | Reviews replaced evaluation-data demand capture, made passive tracking universal, isolated native failure staging, added the workspace lease, and removed eager Local containment that contradicted accepted boundaryless/symlink behavior; all three latest rereviews returned `ACCEPT` | `ACCEPT`; implement only the two-file demand-key metadata packet |
| 2026-07-25 | M1 Stage 5 root-MODULE composite event producer | Bounded three-file implementation, root retained-DICE and sidecar validation, and independent Bazel/source and DICE/architecture terminal reviews | not exposed | Sole private marker-conditional producer, one final batch store, default direct printing, exact inline/repeated stream, explicit empty failures, runtime prefix, nonreplay, policy/lockfile ownership, and semantic recovery passed bzlmod 231 plus core sidecar and GNU-Windows gates | Live review strengthened direct-include/nested preflight suppression, real visible-lockfile reuse, semantic and ignore recovery, and duplicate-producer discrimination; both final reviews returned `ACCEPT`; ignored untracked Cargo.lock required no delta | `ACCEPT`; design only the corrected workspace-lifetime runtime-native demand producer |
| 2026-07-25 | M1 Stage 5 root-MODULE composed evaluator implementation | Bounded four-file implementation, serial host/cross-target validation, root semantic inspection, and two independent corrected-diff terminal rereviews | not exposed | Private evaluator below the lockfile join, opaque ignore-dev projection, complete preflight/preparation, inline per-file bindings and repeated execution, source-free aggregate schema, and retained-DICE transitions passed bzlmod 228, core 67+13, and GNU-Windows check | Reviews strengthened post-nested binding isolation, full source A-B-A equality, and ordered root/child/nested traceback evidence; both latest-diff reviews returned `ACCEPT` | `ACCEPT`; implement only the marker-conditional root-MODULE composite event producer |
| 2026-07-25 | M1 Stage 5 root-MODULE composed evaluator/event design | Independent pinned-source/consumer, Rust/DICE, and architecture audits with root lockfile/policy/schema adjudication and terminal review | not exposed | One private evaluator below the lockfile join, opaque ignore-dev projection, full prepared closure, aggregate Bazel-shaped module schema, and one marker-conditional batch split into two serial four-file packets | Reviews corrected the initial eventful `RootModuleFilesKey` proposal, rejected the per-file compatibility surface and unnecessary preparation/GC owners, and all three terminal reviews returned `ACCEPT` | `ACCEPT`; implement only the four-file composed evaluator/schema packet before event activation |
| 2026-07-25 | M1 Stage 5 root-MODULE include-composition event oracle | Bounded five-file fixture writer, root exact-record adjudication and repeated Bazel replay, independent maintainability and harness terminal reviews | not exposed | Eight retained-daemon rows prove exact root/nested/repeated inline order, semantic and print-only whole-stream invalidation, warm nonreplay, missing/parse preflight suppression, runtime prefix, and recovery; generation plus three replays passed | One assertion-only correction made parse/runtime/recovery reject prior diagnostics; latest replay passed and both corrected terminal reviews returned `ACCEPT` | `ACCEPT`; design only the bzlmod-local composed evaluator/event owner |
| 2026-07-25 | M1 Stage 5 root-MODULE include-composition event oracle design | Independent pinned-source, fixture-maintainability, and harness audits with root compact-row synthesis and corrected terminal rereviews | not exposed | Eight cumulative retained-daemon rows in the existing fixture prove exact root/nested/repeated inline order, semantic and print-only edits, warm nonreplay, missing/parse preflight suppression, runtime prefix, and recovery in five files with one addition | Competing nine-row/seven-file and eight-row/five-file designs were reconciled; the final correction made root-before/root-after mandatory so separate root ownership cannot pass. All three terminal reviews returned `ACCEPT` | `ACCEPT`; implement only the five-file root include-composition event oracle |
| 2026-07-25 | M1 Stage 5 root-MODULE event-producer design correction | Independent pinned-Bazel, live include-layout, and Rust/DICE audits with root source-contract adjudication and corrected terminal reviews | not exposed | Bazel precompiles the include horizon and executes root plus repeated/nested includes inline on one thread/handler; Slug executes root then deduplicated BFS include keys, so per-file batches cannot preserve one local event stream or preflight suppression | Initial per-file feasibility reviews were withdrawn after the accepted one-batch contract and discriminating before/include/after, repeat, and missing-include cases were applied; all three corrected reviews returned `REPLAN` | `REPLAN`; design only a focused root include-composition event oracle before Rust |
| 2026-07-25 | M1 Stage 5 runtime attempt/effect sidecar implementation | Root four-file implementation, lifecycle correction, serial host/cross-target validation, and three independent corrected-diff terminal rereviews | not exposed | Neutral request marker, private sparse command lineage, serial attempt-local roots/seals, exact closure selection, and real in-flight quarantine passed event 4, core 67+13+0, and both GNU-Windows test executables | Reviews required owner-locked install-once state, consuming selection, explicit checked-ID exhaustion, and a genuinely pending callback at seal; all three corrected-diff rereviews returned `ACCEPT` | `ACCEPT`; design only the root-MODULE event-producer correction |
