# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-27 | M1 pure Host glob traversal oracle | One Terra-medium five-path writer, root serial clean-root/harness validation, and one Terra-medium independent implementation reviewer with correction rereview | not exposed | Pinned generation plus three fresh-root exact replays and 107 oracle tests prove nine indexed traversal labels while preserving the complete six-state record; exact scope is +55/-3, fixture 18/0/140, whole tree 1,325/24/39,419 | A same-output-base replay stopped on Bazel's known workspace-switch warning; clean roots passed without expectation broadening. Review found one regenerated raw Invocation ID in the protected first record; restoring it made that record structurally exact and rereview accepted | `ACCEPT`; implement only `WP-5-m1-loading-pure-host-glob-traversal-owner` |
| 2026-07-27 | M1 pure Host glob traversal design | One Terra-medium bounded design writer, root pinned-source/fixture synthesis, and one Sol-low independent reviewer with correction rereview | not exposed | Exact one-pattern private FIFO traversal contract composes the accepted segment and boundary keys, freezes Latin-1 package identity, multi-segment/standalone-`**` state, operation filtering, raw order/dedup, complete-error-before-Need, and a five-path/+130 oracle gate | Review required replacing redundant package-directory identity, removing an impossible compute error, defining total reached-error rank, splitting future Rust into three files, and making the oracle implementation-ready; root corrected all five and rereview accepted | `ACCEPT`; implement only `WP-5-m1-loading-pure-host-glob-traversal-oracle`, then the frozen private traversal owner |
| 2026-07-27 | M1 public Host root-package boundary projection | One Terra-high three-file writer, root serial focused/downstream/cross-target validation, and one Sol-low independent implementation reviewer with correction rereview | not exposed | Ignore-first four-kind opaque projection and selected-root equality passed focused 7, bzlmod 210+integrations, loading 73, workspace 36, all doctests, and 20 GNU-Windows executables at exact +1,133/-0 | Review accepted production semantics but found nondiscriminating zero-lookup and deleted-policy lifecycle tests; one tests-only correction added exact activation/dependency, policy restoration, Need, and both error-branch evidence, then rereview accepted | `ACCEPT` in `ad6751ef`; design only `WP-5-m1-loading-pure-host-glob-traversal-design` |
| 2026-07-27 | M1 Host package-boundary oracle | One Terra-medium fixture writer, root serial fresh-root/protected validation, and one Terra-medium independent implementation reviewer | not exposed | One pinned generation plus three exact subject replays and the protected callable replay prove the six-state three-label boundary projection; 107 harness tests, schema/compile/asset/process guards pass at 15/0/88, +5/+0/-15 | The worker's first warm replay exposed only an output-base workspace-switch warning; scoped shutdown produced a clean exact replay, both root fresh-root replays passed, and the complete diff review accepted without correction | `ACCEPT`; implement only `WP-5-m1-loading-host-package-boundary-projection` |
| 2026-07-27 | M1 Host package-boundary oracle design | One Terra-medium design writer, one Terra-medium independent reviewer, and root source/contract synthesis | not exposed | Exact one-row Bazel 9.2 replacement freezes six directory states, three visible `label_kind` lines, a 15-regular-file/zero-link allowlist, exact generation plus two fresh-root replays, and packet-two growth accounting without fixture or code edits | Root corrected raw-versus-normalized stdout and exact asset contents before review; review found only inventory arithmetic, corrected from 14/+4 to 15/+5, and accepted the reread | `ACCEPT`; implement only `WP-5-m1-loading-host-package-boundary-oracle` |
| 2026-07-27 | M3 `query --output=label_kind` | One Sol-low formatter pre-review, one Terra-high writer, one Terra-medium implementation reviewer, root correction and serial focused validation | not exposed | Bazel 9.2 formatter sources plus accepted 8 rule-class/2 generated-file rows now pass one-shot and retained-daemon paths; output-specific package-kind completion preserves Standard text/graph dependencies and ordering | A cross-package depth boundary rejected source-file fallback and added a narrow completion mode; review then required exact 8+2 daemon replay and broken-producer failure framing, after which correction rereview accepted | `ACCEPT`; resume `WP-5-m1-loading-host-package-boundary-oracle-design` |
| 2026-07-27 | M1 Stage 5 public Host root-package boundary projection design | Parallel pinned-source/behavior, live API/implementation, and architecture/orchestration audits; root contract synthesis; corrected terminal latest-text rereviews | not exposed | Root-only `{workspace, PackagePath}` key, ignore-first four-kind opaque projection, selected-root accessor/equality, transient Need, typed error, exact three-file/+1,450 implementation gate, and six-state oracle-first matrix were frozen without Rust or fixture edits | Reviews removed unreachable nested-repository behavior, reconciled raw-byte ingress and semantic key identity, corrected exact source provenance and Stage 9 residual, required full stale-fixture pruning, and all three accepted | `ACCEPT`; design only `WP-5-m1-loading-host-package-boundary-oracle-design` |
| 2026-07-27 | M1 Stage 4/5 private Host glob segment-candidate owner | Exact three-file writer, serial focused/full/downstream/native-cross validation, root correction, and source/parity, implementation/evidence, plus architecture/orchestration terminal rereviews | not exposed | Private Unix-dormant raw-byte literal/simple-star key passed focused 19, loading 73, workspace 36, bzlmod 387, all doctests, and 20 GNU-Windows no-run executables at exact +1,605/-0 | Reviews corrected physical matched-child identity beneath a symlinked base, terminal-symlink invariants, downstream equal-Complete pruning, and zero evaluation data; scope/caps/archive/credential/process/forbidden guards passed and all three rereviews accepted | `ACCEPT` in `bd12c015`; design only `WP-5-m1-loading-host-package-boundary-projection-design` |
| 2026-07-27 | M1 Stage 5 simple Host glob segment matcher oracle | Exact five-path writer, pinned Bazel generation, two distinct-root callable replays, protected-fixture replays, root harness/evidence validation, and three terminal latest-diff reviews | not exposed | Six exact labels prove bare-star hidden and BUILD membership, suffix hidden exclusion, explicit-dot inclusion, and two nonempty non-adjacent spans at +86/-8; callable is 20/0/306 with five rows and whole fixtures are 1,317/24/39,382 | The generated schema refresh added empty startup fields and removed one stale startup-warning record without changing the first four accepted semantic projections; 97 harness tests and every scope/cap/cleanup guard passed, packet-one growth is +3/+0/+78, and all reviewers accepted | `ACCEPT`; implement only `WP-5-m1-loading-pure-host-glob-segment-candidates-owner` |
| 2026-07-27 | M1 Stage 5 simple Host glob segment matcher oracle design | Parallel pinned Bazel source/behavior, fixture/implementation, and architecture/orchestration audits; root minimal-fixture synthesis; terminal latest-text reviews | not exposed | Exact five-path/+100/-25 `glob-callable-contract` extension adds one isolated package, two one-line assets, four direct matcher comprehensions, and one exact query row proving bare-star hidden membership, suffix hidden exclusion, explicit-dot inclusion, and two nonempty non-adjacent star spans | Reviews compared a larger zero/nonnull matrix and conditional labels, then selected the source-audited six-label direct-membership projection; the design protects all existing glob fixtures and first four callable rows, records packet-one growth, and all three accepted | `ACCEPT`; implement only `WP-5-m1-loading-host-glob-segment-matcher-oracle`, then the private segment-candidate owner |
| 2026-07-27 | M1 Stage 5 corrected pure Host glob owner design | Parallel pinned Bazel source/API, live implementation/DICE feasibility, and architecture/orchestration audits; root blocker split and terminal corrected latest-text rereviews | not exposed | Exact private three-file/+1,750 segment-candidate contract owns one raw-byte literal/simple-`*` frontier, matched-only symlink resolution, error-before-Need precedence, and Unix-only byte preservation without package lookup, caller, parser, or event activation | Reviews found the existing package key collapses ignored/deleted states and lacks nested-repository boundaries, so full traversal was split; corrections froze syntax taxonomy, wildcard-only ancestor expansion, missing-directory error, semantic error fields, matcher oracle-first sequencing, and Stage 9 disposition; all three accepted | `ACCEPT`; design only `WP-5-m1-loading-host-glob-segment-matcher-oracle-design`, then implement the private candidate owner |
| 2026-07-27 | M1 Stage 5 Linux raw-name pattern-lazy glob oracle and packet-five growth checkpoint | Exact ten-path writer, pinned Bazel generation, distinct absolute-root exact replays, four-fixture archive/hygiene audit, bounded 11-path prune, and source/parity, implementation/evidence, plus architecture/orchestration terminal rereviews | not exposed | Four exact rows freeze Linux `c3 a9` versus `e9` order/matching, BUILD `?` rejection, warm delete/recreate, epochs `1/1/1/1`, empty manifests, and ASCII evidence at +660/-7; accepted checkpoint `22de3631`→`e2cc891d` is +11 regular/+8 links/+2,319 lines across 47 rows | Reviews required exact wildcard/helper calls, exact comparison, the 30-line cap, unchanged FIFO diagnostic, two no-follow symlink tests, `UnixGlob` provenance, and then pruned nine unused registry markers, one unaddressed root BUILD, and one redundant dangling link; both affected fixtures replayed | `ACCEPT`; design only `WP-5-m1-loading-pure-host-glob-owner-design`, keeping transactional evaluator retry separate |
| 2026-07-27 | M1 Stage 5 Linux raw-name pattern-lazy glob oracle design | Parallel pinned Bazel source/native behavior, harness/fixture feasibility, and architecture/orchestration audits; root narrow-boundary synthesis; terminal corrected latest-text rereviews | not exposed | New four-row Linux fixture freezes raw `c3 a9` versus `e9` identity/order, loaded-helper wildcard results, successful warm delete/recreate, and exact BUILD `?` rejection while retaining the accepted 13-row POSIX symlink/special oracle unchanged | Reviews rejected positive `?`, generic POSIX framing, narrowing the existing fixture, and unrelated `**`/dot/parenthesis breadth; they selected final-component raw create/delete only, exact wildcard calls, a ten-path/+1,200 contract, and the mandatory packet-five growth review | `ACCEPT`; implement only `WP-5-m1-loading-raw-name-pattern-lazy-glob-oracle`, then design the pure Host glob owner |
| 2026-07-27 | M1 Stage 5 dormant Bazel-internal string parser seam | Exact eight-file writer, serial focused/broad/downstream/cross-target validation, clean baseline worktree comparison, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Token-local byte carrier, original source/spans, separate byte-column reporter, exact oracle diagnostics, runtime equality, equal-key lookup, and frozen/unfrozen function pass-through passed focused 2+5, broad 822 excluding 11 exact clean-baseline failures, downstream suites, doctests, and five-crate GNU-Windows linkage | Reviews found missing cross-parse `Value::equals` and actual frozen-function invocation; the bounded test correction closed both at exact eight-file +696/-2 scope, and all terminal rereviews accepted | `ACCEPT`; design only `WP-5-m1-loading-raw-name-pattern-lazy-glob-oracle-design`; the eventual oracle is post-checkpoint packet five and must run the fixture-growth review |
| 2026-07-27 | M1 Stage 5 Bazel-internal string oracle | Exact six-file oracle writer, pinned Bazel 9.2 generation, two distinct absolute-root replays, root structural/projection validation, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Eight rows prove ASCII static/dynamic byte-string projections, exact `\400`/x/u/U/q failures, and the discriminating `3:24` Bazel byte column on epoch 1 with empty manifests; final scope is six regular files/zero links and +519/-0 | The first generation exposed only normalized backslash spellings; five exact needles were corrected to the harness's observed slash-normalized forms. All three runs then matched, caps/scope/cleanup gates passed, and all terminal reviewers accepted | `ACCEPT`; implement only `WP-5-m1-starlark-bazel-internal-string-seam`; the next accepted oracle is packet five and must run the fixture-growth review |
| 2026-07-27 | M1 Stage 5 Bazel-internal string oracle/feasibility design | Parallel pinned Bazel source, live starlark-rust feasibility, and architecture/orchestration audits; root serial-boundary synthesis; corrected terminal latest-text rereviews | not exposed | Bazel Latin-1 parser input makes source UTF-8 bytes differ from one-octal bytes, rejects x/u/U/unknown escapes, and reports byte columns; an eight-row Bazel oracle precedes an eight-file dormant token-local carrier with original source/spans and a separate opt-in byte-column reporter | Reviews rejected whole-source projection, separated Unicode caret rendering, added epoch-1 evidence, exact packet-five growth timing, mixed-ingress adapter gates, per-file caps, and the six-file oracle→seam serial stop; all three final reviewers accepted | `ACCEPT`; implement only `WP-5-m1-loading-bazel-internal-string-oracle` |
| 2026-07-27 | M1 Stage 5 Host typed no-follow dirent observation | Tests-first exact five-file writer, serial owner/downstream/cross-target validation, root focused evidence rerun, and source/API, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Dedicated kind/entry plus one shared Arc slice, stable duplicate-retaining name sort, complete kind equality, one DirectoryEntries DICE demand, Unix d_type/after-close unknown refinement, and reparse-aware Windows no-follow classification passed workspace 36, core 109+13, downstream bzlmod/loading, and four-crate GNU-Windows no-run | Pre-audits caught reparse-point classification, Unix unknown-status evidence, and one per-file cap; all were corrected. Exact five-file +698/-139 scope, per-file caps, fmt/diff/archive/API/dependency/process/forbidden-owner gates, root focused reruns, and all terminal reviews passed | `ACCEPT`; design only `WP-5-m1-loading-byte-string-oracle-feasibility-design` |
| 2026-07-27 | M1 Stage 5 focused Host-dirent glob oracle | Exact sixteen-path writer, one pinned Bazel generation, two distinct fresh-root replays, focused direct validators, and source/parity, native implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Thirteen one-epoch rows prove exact `5/3/4/3/5/3/5` classification through two absent observations, matched ELOOP failure, recovery, empty manifests, and byte-preserved first-four projection; inventory is 10 regular/6 links/667 lines at +669/-10 | Two stopped attempts drove the accepted ELOOP and allow-empty/absent-observation redesigns; the final exact 16-path implementation, 20 pinned anchors, replay/cleanup/scope/cap guards, py-compile, direct assertions, and diff check passed; pytest itself was unavailable, and all three final reviewers accepted | `ACCEPT`; implement only `WP-5-m1-loading-host-dirent-observation` |
| 2026-07-27 | M1 Stage 5 Host-dirent glob oracle semantic redesign | Pinned-source audit, isolated cold controls, same-daemon direct/absent/sibling experiments, final full-core probe, root synthesis, and three terminal latest-text reviews | not exposed | Explicit allow-empty cold matrix is regular 2 state targets, directory 1, FIFO 0, symlink-to-FIFO 2; atomic regular→directory and directory→symlink retain prior membership, sibling churn is ineffective, while exactly two absent observations yield the source-correct `5/3/4/3/5/3/5` matrix on PID/starttime `3982243/110904713` | The accepted redesign expands only 11→13 rows and fixture line cap 550→750, retains topology/assets/allowlist/+900 cap, keeps atomic symlink→FIFO/FIFO→regular, and narrows typed-observation invalidation claims; all probe servers and temp state were removed | `ACCEPT`; implement only `WP-5-m1-loading-host-dirent-glob-oracle` |
| 2026-07-27 | M1 Stage 5 corrected Host-dirent glob oracle retry stop | Tests-first retry, one pinned Bazel 9.2 generation, root stop-gate adjudication, full draft removal, and source/orchestration audits | not exposed | All eleven rows stayed on epoch 1; empty `specials/*` failed rows 1–4, empty files-only `state*` failed the direct-FIFO row under default `allow_empty = false`, and the successful warm directory row unexpectedly retained both files-only and files-and-directories targets | The cycle diagnostic and BUILD comprehension corrections worked, but two independent remaining semantics invalidate a narrow patch. Every harness, fixture, generated, BUILD, regular-asset, and symlink draft was removed; no implementation or post-checkpoint growth was accepted | `REPLAN`; design only `WP-5-m1-loading-host-dirent-glob-oracle-semantics-redesign` |
