# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-23 | M3 visibility representation and oracle design | Terra high pinned-source/V2/V1 audit, root source adjudication, Sol low review/re-review | not exposed | `visible` requires Stage 4 typed effective/raw visibility, package groups, source/generated/fake behavior, nodep/include edges, then Stage 8 universal filtering; V1 registry rejected | Root corrected inherited `labels(visibility)` and explicit `--noimplicit_deps`; Sol required cycle and direct-spec projection rows, then accepted the corrected one-fixture packet | `ACCEPT`; add only `query-visible-visibility`, then design Stage 4 representation |
| 2026-07-23 | M3 request-local strict-suite and `tests()` activation | Terra high test-first implementation, root independent full validation, Sol low final review | not exposed | `3a8ae78a` activates the twelfth default function; exact 27 one-shot and 21 daemon rows, zero-invalidations strict toggles, DICE graph reuse, 118 affected tests, CLI build, fmt/archive/diff passed | One pre-edit allowlist correction added the registry owner test in `0e571f45`; implementation review returned `ACCEPT` without correction | `ACCEPT`; audit Bazel visibility representation and design the smallest truthful `visible()` oracle |
| 2026-07-23 | M3 corrected strict-suite/`tests()` activation design | Root synthesis after accepted oracle, Sol low re-review | not exposed | Exact 27-row gate; request-local policy; generic iterative expansion; minimal accessor; private lookup causes; no DICE/formatter scope | No correction after `1edb2775`; Sol found worklist order, fake candidates, edge recording, wrappers, and allowlist unambiguous | `ACCEPT`; implement the bounded activation packet |
| 2026-07-23 | M3 source-critical `tests()` oracle extension | Terra medium fixture worker, root independent Bazel run, Sol low review | not exposed | `1edb2775` extends tests 29→32 for nested filter isolation, filter-before-uniqueness, and literal `-+tag`; worker generation/clean and root clean runs passed | No correction; original 29 definitions/records stayed unchanged and Sol accepted each discriminator | `ACCEPT`; re-review the corrected request-local strict-suite and `tests()` activation design |
| 2026-07-23 | M3 `tests()` strict-policy/activation design | Terra medium pinned-source/V2 audit, root source verification, Sol low adjudication | not exposed | Gate A supports 24 non-build rows; request-local policy, generic iterative expansion, separate uniqueness, accessor resolution, and no new DICE identity are viable | Sol narrowed unused public error-code work and found missing discriminators for nested filter isolation, filter-before-uniqueness, and literal `-+tag` | `REPLAN`; append three Bazel oracle rows before re-reviewing activation |
| 2026-07-23 | M3 `tests` loading/query metadata Gate A final retry | Terra high test-first implementation, root full owning-crate validation, Sol low review | not exposed | `7abcbdce` adds native suites, typed test metadata, exact capability, total query explicitness, per-suite filtering, and same-DICE lifecycle; focused 68 tests plus full loading/query suites, CLI build, fmt/archive/diff passed | No correction; Sol accepted semantics, invalidation, scope, and Buck compact-storage reuse | `ACCEPT`; review request-local strict-suite policy and the bounded `tests()` activation seam |
| 2026-07-23 | M3 structural label comparison and direct duplicate rejection | Terra high test-first implementation, root full focused validation, Sol low review | not exposed | `5bbc4604` adds borrowed Bazel tuple comparison and canonical duplicate errors for native/Starlark direct lists; 70 focused tests, fmt/archive/diff passed | No correction; Sol accepted semantics and Buck2 utility reuse | `ACCEPT`; retry tests loading-metadata Gate A with suite ordering/duplicates on this foundation |
| 2026-07-23 | M3 ordering and three-surface duplicate-label oracle | Terra fixture design attempt, root implementation/generation, Sol low review | not exposed | `57192df9` extends labels 37→39 and tests 25→29; both generated and independently verified cleanly against Bazel 9.2 | Worker stalled and was interrupted; root corrected illegal Starlark `configurable=False`, removed generated UUID/path/graph churn, and Sol accepted the append-only evidence | `ACCEPT`; implement structural label comparison and direct-list duplicate rejection before Gate A |
| 2026-07-23 | M3 order-independent value audit/design | Terra high pinned-source and Bazel executable audit, root synthesis, Sol low review | not exposed | Bazel BUILD strings use internal UTF-8 bytes and match Rust string order; labels sort structurally; duplicate strings remain but every direct label-list duplicate errors after canonicalization | Audit disproved the preceding UTF-16 premise and exposed accepted filegroup/Starlark duplicate behavior; Sol accepted the comparator boundary but rejected a suite-only oracle | `REPLAN`; oracle native filegroup, direct Starlark label-list, and native-suite duplicate errors plus string/label ordering before any fix |
| 2026-07-23 | M3 `tests` loading-metadata Gate A third attempt | Terra high test-first implementation, root source checks/full validation, Sol low review | not exposed | Attempt passed loading 47 and query 50 with metadata, explicitness, suite membership, lifecycle, and no activation; all nine files reverted | Root corrected pre-review numeric-sort misread; Sol's one permitted correction fixed `-+tag`; final review found Rust `Ord` differs from Java UTF-16 order for supplementary Unicode | `REPLAN`; design and oracle the exact Bazel order-independent string/label comparator before Gate A retries |
| 2026-07-23 | M3 package-context loading label foundation retry | Terra high test-first implementation, root full loading/query validation, Sol low review | not exposed | `40ac1cd2` centralizes BUILD/defining-`.bzl` conversion, canonical native storage, output ownership, duplicate/edge separation, and same-DICE lifecycle; loading 43 and query 48 passed; CLI rebuilt | Sol accepted; root used the one correction to add pinned `LabelParser` triple-dot package rejection, then Sol accepted again | `ACCEPT`; retry `tests` loading-metadata Gate A |
| 2026-07-23 | M3/Stage 3 target-name validation implementation | Terra high test-first implementation, root identity/downstream validation, Sol low review | not exposed | `22313daa` matches pinned target-name validation and trailing `/.` normalization; identity 10 passed; loading/query test targets compiled | Sol requested one focused correction to pin exact diagnostic precedence; corrected tests accepted | `ACCEPT`; resume the package-context loading foundation |
| 2026-07-23 | M3/Stage 3 target-name validation design | Terra medium source/V2 boundary audit, root source confirmation, Sol low adjudication | not exposed | Pinned `LabelValidator.validateTargetName` and `LabelParser.validateAndProcessTargetName` require exact ASCII/path validation plus trailing `/.` normalization in the central value constructor | Sol accepted `TargetName::parse` alone; `PackagePath`, label splitting, and raw package-context classification stay separate | `ACCEPT`; implement identity code/tests, then resume the loading foundation |
| 2026-07-23 | M3 package-context loading label foundation | Terra high implementation probe with root source confirmation | not exposed | Clean stop proved `CanonicalLabel` plus permissive `TargetName` accepts the extra colon in invalid relative `pkg:target`; no files changed | Explicit identity-parser stop triggered before implementation; no correction attempted | `REPLAN`; Stage 3 reviews/fixes central target-name validation before loading resumes |
| 2026-07-23 | M3 package-context loading label oracles | Terra medium two-fixture worker, root independent Bazel runs, Sol low evidence review | not exposed | `3621b3e7` extends labels 33→37 and tests 23→25 for bare/slash sources, `.bzl` default ownership, invalid relative syntax, and native suite source edges; both worker/root runs passed | Root removed explicit exports so source-node evidence was genuinely implicit; corrected final diff accepted | `ACCEPT`; implement shared loading label normalization only, then resume Gate A |
| 2026-07-23 | M3 package-context loading label design | Terra medium Bazel/V2 boundary audit, root source verification, Sol low adjudication | not exposed | One converter covers native/Starlark labels; explicit values use target package, defaults use defining `.bzl`; native storage canonicalizes; outputs retain ownership; existing DICE keys suffice | No correction after the second Gate A replan; Sol accepted the separate foundation and identity-parser stop | `ACCEPT`; add labels/tests package-context oracle rows, then implement the foundation before Gate A |
| 2026-07-23 | M3 `tests` loading-metadata Gate A retry | Terra high bounded implementation, root source/downstream validation, Sol low full-diff review | not exposed | Total explicitness and suite/test metadata passed loading 13, invalidation 22, query 27; full loading/query targets compiled; no code retained | Root fixed one downstream exhaustive filegroup pattern; Sol then found native suite bare source labels incorrectly rejected by the reused dependency-label helper, exhausting the correction budget | `REPLAN`; review shared native/Starlark bare-label coercion and oracle scope before another Gate A retry |
| 2026-07-23 | M3 native filegroup provenance oracle | Terra medium fixture worker, root independent Bazel run, Sol low evidence review | not exposed | `e1d3f910` extends query-labels metadata to 33 commands; exact build rows distinguish omitted `srcs` from explicit `srcs=[]`; worker/root clean runs passed | Worker refined one initial anchor; final three-file diff was accepted with representation-only scope | `ACCEPT`; retry tests loading/query Gate A with total exact explicitness |
| 2026-07-23 | M3 total query-attribute explicitness design | Terra medium producer/source audit, root synthesis, Sol low adjudication | not exposed | Total Bazel explicitness bit covers retained native filegroup input provenance, mandatory alias, Starlark provenance, suite tests, and generated implicit tests; existing package/graph DICE equality owns it | No correction after the Gate A replan; Sol accepted the exact total meaning and formatter boundary | `ACCEPT`; add the missing native filegroup omitted/explicit-empty oracle, then retry Gate A |
| 2026-07-23 | M3 `tests` loading-metadata Gate A implementation | Terra high bounded implementation, root diff/source validation, Sol low full-diff review | not exposed | Seven allowed files implemented suite/test loading and graph projection; focused loading 13, invalidation 21, query 25, fmt/diff/archive checks passed; no code was retained | Root corrected generated `$implicit_tests` explicitness; Sol then found the general query-attribute boolean falsely hard-coded omitted `filegroup.srcs` explicit, exhausting the one-correction budget | `REPLAN`; audit total native/Starlark query-attribute provenance before retrying Gate A; strict plumbing and activation remain deferred |
