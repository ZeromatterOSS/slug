# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-25 | M1 Stage 5 retained materializer implementation checkpoint split | Independent pinned-source, implementation-feasibility, and architecture reviews with one focused Git/PAX correction | not exposed | Three serial checkpoints isolate HTTP captured USTAR parsing, callerless retained-session state, and the native observer/runtime-owner bridge while preserving the accepted three-file union | Pinned review found real git archives emit PAX; the correction leaves Git byte-identical on its private external-tar path and scopes A to HTTP. All three terminal rereviews returned `ACCEPT` | `ACCEPT`; implement only the one-file HTTP captured-USTAR regular/directory subset |
| 2026-07-25 | M1 Stage 5 retained materializer/session/archive implementation | Bounded three-file implementation route with concurrent pinned tar and architecture state-machine acceptance audits | not exposed | Pre-edit audits froze raw USTAR name/prefix/padding/type/EOF behavior, exact normalized streamed collisions, PAX/GNU/link rejection, complete dirtiness rows, and owner-held provisional-root/token transitions | The exact parser and retained-session matrices are independently large and no source edit was retained; combining them would not produce a reviewable correction surface | `REPLAN`; design serial one-file archive, one-file pure session, and native-owner bridge checkpoints |
| 2026-07-25 | M1 Stage 5 retained materializer session/archive-order design correction | Focused root synthesis with independent pinned-Bazel and architecture/DICE terminal reviews | not exposed | One active token lease, no-mutation Busy/stale operations, owner-quarantined provisional roots, explicit terminal release, exact Bazel capture/checksum stage order, and normalized/relativized prefix semantics fit the retained three-file boundary | Terminal reviews both returned `ACCEPT`; implicit session Drop must not release provisional roots, and regular/directory-only tar support leaves symlink/hardlink breadth explicit | `ACCEPT`; implement only the private three-file retained materializer/session and corrected archive subset |
| 2026-07-25 | M1 Stage 5 retained runtime materializer and captured-archive design | Independent pinned Bazel, live-tree feasibility, and architecture/DICE audits with root provisional-session and archive synthesis plus terminal rereviews | not exposed | A bounded three-file private owner can retain exact accepted successes/validation, keep roots provisional across retries, burn exposed instance IDs, reuse clean repositories offline, and use one captured archive artifact without IO or locks in DICE | The focused correction fixed terminal promotion, full-request dirtiness, capture, and prefix selection; terminal source rereview found a second material gap in overlapping-session semantics and Bazel archive stage/normalization order, so no Rust started | `REPLAN`; correct only one-session lease/stale tokens and exact archive order/normalize-before-prefix semantics |
| 2026-07-25 | M1 Stage 5 Windows native path-observation adapter | Bounded Terra-high one-file writer, root tests-only semantic correction and serial host/cross-target validation, independent pinned-Bazel/windows-sys and architecture/unsafe-owner terminal reviews | not exposed | Safe scripted Windows layer plus copied kernel32 ABI/native owners passed 10 focused, 49 unit, 13 integration, and zero doctests; real GNU-Windows unit/integration executables linked and imported kernel32 | Root corrected error refinement for raw 267/32/5 and added exhaustive attributes, FILETIME, long-path, parser-capacity, and Find failure rows; production then remained unchanged and both terminal reviewers returned `ACCEPT` | `ACCEPT`; design only the corrected retained outside-DICE materializer including single-capture archives |
| 2026-07-25 | M1 Stage 5 Unix native path-observation adapter | Bounded Terra-high two-file writer, root tests-only lifecycle/iterator/retry tightening and serial validation, independent pinned-Bazel and architecture/unsafe-owner terminal reviews | not exposed | Exact cfg-Unix operations, raw same-handle Option owner, four contained libc calls, 10 scripted/real tests, and observed file/directory/symlink/escaped-path lifecycles passed with core 39+13+0 and Windows neutral compilation | Root added direct Interrupted, iterator-candidate, directory mutation/delete, and symlink-delete assertions; production stayed unchanged and both terminal reviewers returned `ACCEPT` | `ACCEPT`; implement only the one-file Windows native adapter and exhaustive host-pure/link evidence |
| 2026-07-25 | M1 Stage 5 platform-neutral path-observation kernel | Bounded Terra-high two-file writer, root tests-only assertion tightening and serial validation, independent pinned-contract and architecture/performance terminal reviews | not exposed | Private dormant compact authority, sorted zero-op preflight/execution, adapter primary/final/refine state, exact auxiliary tables, and 15 scripted tests passed with core 29+13+0 and host/Windows all-target checks | Root strengthened exact structural error, direct Lstat Missing/I/O, and generic ReadLink I/O evidence; production was unchanged and both terminal reviews returned `ACCEPT` | `ACCEPT`; implement only the cfg-Unix native adapter, raw directory owner, and complete real lifecycle |
| 2026-07-25 | M1 Stage 5 native path-observation implementation checkpoint split | Independent neutral-kernel, Unix-owner, and Windows-source audits with root serial synthesis and two terminal rereviews | not exposed | Three independently landable private checkpoints preserve the final four-file union: dormant neutral scripted kernel, complete Unix adapter plus real lifecycle, then complete Windows adapter plus host-pure matrices and real kernel32 linkage | Root corrected support preflight to Lstat only and removed direct primary Missing so adapters cannot bypass auxiliary races; both terminal reviewers returned `ACCEPT`, and no fourth lifecycle packet is needed | `ACCEPT`; implement only the two-file dependency-free neutral kernel |
| 2026-07-25 | M1 Stage 5 native path-observation producer final implementation | Bounded Terra-high four-file writer, one focused correction, root Linux/source inspection and provisioned GNU-Windows kernel32 link validation | not exposed | Draft reached 6 focused, 20 unit, 13 integration, and zero doctests; formatting/diff/allowlist and real Windows test-executable linkage passed | The correction fixed concrete Windows-name, shared-refinement, Unix errno/NUL, and pure Windows-helper defects but still omitted both platform owner matrices, sorted zero-op preflight, complete operation races, reparse/ABI coverage, and full real lifecycle evidence; all draft edits were removed | `REPLAN`; design serial platform-neutral, Unix, Windows, and lifecycle checkpoints before more Rust |
| 2026-07-25 | M1 Stage 5 native path-observation producer final design correction | Independent pinned-source, operational/ABI, and architecture terminal rereviews with root operation/authority/platform synthesis | not exposed | A lifetime-bound normalized compact authority, sorted complete preflight/execution, primary-operation-first race tables, raw Unix and Windows directory owners, exact Windows formulas/path/parser safety, and exhaustive portable/platform evidence fit the frozen four files | All three rereviews returned `ACCEPT`; operational review made typed NotALink auxiliary lstat and a real kernel32-linked Windows test build explicit terminal gates | `ACCEPT`; implement only the private four-file native producer, and replan if a real Windows link gate cannot be provisioned |
| 2026-07-25 | M1 Stage 5 native path-observation producer implementation retry | Bounded Terra-high four-file writer, root live cross-platform inspection, independent pinned-Bazel and Unix/architecture terminal reviews | not exposed | Draft compiled with 10 focused and 36 full core tests, raw same-handle Unix ownership, and a host-pure WTF-16 Windows parser, without runtime/DICE/public wiring | Both reviews found operation-first race/error precedence, deterministic order, normalized retained authority, Windows permission/time/path/slice safety, portable tests, and Unix lifecycle evidence incomplete; all draft edits were removed | `REPLAN`; design only the final operation/order/platform correction before another Rust attempt |
| 2026-07-25 | M1 Stage 5 NotALink observation schema prerequisite | Bounded Terra-high three-file worker, root serial full/downstream validation, independent pinned-Bazel/schema and architecture/DICE corrected-diff reviews | not exposed | Fieldless NotALink is exact through resolver, semantic byte, and repository-source projection; retained equal-pruning and both Io transition directions passed with workspace 29, bzlmod 226, and core 27 | Implementation needed no correction; exact allowlist, formatting, diff, diagnostics, complete validity/equality, and no exhaustive consumer were independently accepted | `ACCEPT`; implement only the private four-file native path-observation producer |
| 2026-07-25 | M1 Stage 5 native observation schema and Unix directory design | Independent pinned-source, Rust/ABI feasibility, and architecture/consumer audits with root contract and serial-packet synthesis | not exposed | A fieldless NotALink is exact across operational and semantic consumers; direct nix libc plus errno supports a private same-handle Unix directory owner with exact open/read/close precedence | Reviews accepted the three-file schema prerequisite followed by a four-file producer, including target-Unix nix, contained unsafe, raw-name copying, scripted handle/error evidence, and unchanged native Windows contracts | `ACCEPT`; implement only the NotALink schema prerequisite before the native producer |
| 2026-07-25 | M1 Stage 5 native Windows ReadLink design correction | Independent pinned-source, Rust/Win32 feasibility, and architecture/schema audits with root parser/output synthesis | not exposed | A checked std-only 16 KiB reparse parser can cover NTFS/LX/mount-point tags, exact target normalization, ProjFS/unknown tags, and native errors in the private producer file | Feasibility found the native seam bounded; architecture proposed existing-schema projections. Pinned source superseded acceptance: ProjFS needs a distinct NotALink discriminator, and exact readdir EIO retains the same handle/partial names beyond std ReadDir's capabilities. | `REPLAN`; design only the public NotALink schema correction and raw Unix directory owner |
| 2026-07-25 | M1 Stage 5 runtime path-observation producer implementation | Bounded Terra-high two-file writer, root correction and exact-scope/archive audit, independent pinned-source and architecture final reviews | not exposed | Private producer reached 15 focused passes for compact authority, complete batch, exact Unix operations/metadata, lifecycle freshness, and pure Windows lstat helpers without runtime/DICE/public wiring | Root added portable operation/lifecycle/retry evidence. Final pinned-source review found std Windows ReadLink omits Bazel's LX_SYMLINK/native error behavior and Unix EIO retry was too broad; the unreviewed native parser boundary required removing the complete draft. | `REPLAN`; design only native Windows ReadLink and iterator-only Unix EIO before retrying Rust |
| 2026-07-25 | M1 Stage 5 runtime path-observation Windows-lstat design correction | Root exact pinned-source synthesis with independent Bazel 9.2 and architecture terminal reviews | not exposed | Windows lstat now stages initial size/mtime/attributes/kind separately from native ctime and uses Bazel's exact ReparsePoint→Symlink, Device→SpecialFile, Directory→Directory, otherwise RegularFile order | Both reviews returned ACCEPT; overlapping reparse+device and device+directory tests are mandatory, and no dependency, unsafe, ownership, API, or file expansion is needed. | `ACCEPT`; implement only the private two-file outside-DICE producer |
| 2026-07-25 | M1 Stage 5 runtime path-observation producer design correction | Independent pinned-source, Rust/Windows feasibility, and architecture rereviews of the exact private Win32 seam | not exposed | Raw UTF-16 long-path conversion, std-only no-follow ctime ABI, operation-level native errors, initial-metadata versus later-ctime staging, and Bazel's Windows missing policy fit the same private two-file packet | Feasibility and architecture returned ACCEPT. Pinned-source correction moved mtime/attributes back to the first metadata read; terminal rereview then found the second material miss that Bazel treats every reparse point, not only name-surrogate tags, as a link. | `REPLAN`; correct only Windows lstat kind order before implementation |
| 2026-07-25 | M1 Stage 5 runtime path-observation producer design | Independent live Rust/Win32 feasibility, pinned Bazel 9.2, and architecture audits with root compact-owner and operation synthesis | not exposed | A private synchronous two-file kernel can prevalidate compact instance authority and produce deterministic exact Host/Materialization Lstat/ReadLink/FileBytes/DirectoryEntries epochs outside DICE without containment or locks | Reviews corrected operation-specific wrong-kind/missing races, Unix EIO retry, exact metadata/dirtiness boundaries, and desktop Windows ctime through raw no-follow FFI. Terminal source/feasibility review then found the correction still omitted Bazel's UTF-16 long-path transform and treated query failures as outer errors. | `REPLAN`; correct only exact Windows path/query and operation-result semantics before Rust |
| 2026-07-25 | M1 Stage 5 Local repository lifecycle oracle | Bounded existing-fixture worker, root pinned generation/replay and exact-growth validation, independent pinned-source and maintainability reviews | not exposed | `dcc19327` adds five retained-daemon rows for same-request Local A→B retarget, missing, regular-file wrong kind, boundaryless directory, and exact A recovery; all 11 prior records stayed exact and two fresh replays passed | Review added the distinct boundaryless preflight before implementation. Final growth is two files, three relative symlinks, five entries, and 282 lines; unavailable pytest is nonblocking because no harness code changed and direct harness assertions passed. | `ACCEPT`; design only the additive outside-DICE path-observation producer |
| 2026-07-25 | M1 Stage 5 runtime materialization preflight/retry design | Independent pinned Bazel, live runtime/harness, and terminal architecture audits with root state-machine and oracle synthesis | not exposed | Fixed command generations, complete replacement epochs, materialization-first typed progress, exact retained successes/final demands, per-repository observed-path dirtiness, ctime-only cleanliness, offline reuse, final-closure selection, and terminal semantic publication are frozen | Architecture review showed the live tree lacks the path producer, retained materializer, effect sidecar/producers, and production preparation caller. Oracle review added the distinct boundaryless-directory preflight and rejected a nondiscriminating unused override. | `REPLAN`; add only the six-state Local lifecycle suffix to the existing source-preparation oracle before prerequisite Rust |
