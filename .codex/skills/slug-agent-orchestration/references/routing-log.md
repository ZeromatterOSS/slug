# Recent Slug Packet Rollups

Read this only when a recent analogous packet may affect routing. The normal
summary is [routing-guide.md](./routing-guide.md).

Keep at most 20 terminal packet rows or 250 lines, whichever comes first.
Archive older rows by month as `routing-history-YYYY-MM.md`. Record one row
only when a packet reaches `ACCEPT`, `REPLAN`, or a genuine stop; fold audit,
implementation, review rounds, and corrections into that row.

| Date | Packet | Route | Wall time | Evidence | Review/rework | Result and next-use note |
|------|--------|-------|-----------|----------|---------------|--------------------------|
| 2026-07-27 | M1 Stage 5 dormant Bazel-internal string parser seam | Exact eight-file writer, serial focused/broad/downstream/cross-target validation, clean baseline worktree comparison, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Token-local byte carrier, original source/spans, separate byte-column reporter, exact oracle diagnostics, runtime equality, equal-key lookup, and frozen/unfrozen function pass-through passed focused 2+5, broad 822 excluding 11 exact clean-baseline failures, downstream suites, doctests, and five-crate GNU-Windows linkage | Reviews found missing cross-parse `Value::equals` and actual frozen-function invocation; the bounded test correction closed both at exact eight-file +696/-2 scope, and all terminal rereviews accepted | `ACCEPT`; design only `WP-5-m1-loading-raw-name-pattern-lazy-glob-oracle-design`; the eventual oracle is post-checkpoint packet five and must run the fixture-growth review |
| 2026-07-27 | M1 Stage 5 Bazel-internal string oracle | Exact six-file oracle writer, pinned Bazel 9.2 generation, two distinct absolute-root replays, root structural/projection validation, and source/parity, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Eight rows prove ASCII static/dynamic byte-string projections, exact `\400`/x/u/U/q failures, and the discriminating `3:24` Bazel byte column on epoch 1 with empty manifests; final scope is six regular files/zero links and +519/-0 | The first generation exposed only normalized backslash spellings; five exact needles were corrected to the harness's observed slash-normalized forms. All three runs then matched, caps/scope/cleanup gates passed, and all terminal reviewers accepted | `ACCEPT`; implement only `WP-5-m1-starlark-bazel-internal-string-seam`; the next accepted oracle is packet five and must run the fixture-growth review |
| 2026-07-27 | M1 Stage 5 Bazel-internal string oracle/feasibility design | Parallel pinned Bazel source, live starlark-rust feasibility, and architecture/orchestration audits; root serial-boundary synthesis; corrected terminal latest-text rereviews | not exposed | Bazel Latin-1 parser input makes source UTF-8 bytes differ from one-octal bytes, rejects x/u/U/unknown escapes, and reports byte columns; an eight-row Bazel oracle precedes an eight-file dormant token-local carrier with original source/spans and a separate opt-in byte-column reporter | Reviews rejected whole-source projection, separated Unicode caret rendering, added epoch-1 evidence, exact packet-five growth timing, mixed-ingress adapter gates, per-file caps, and the six-file oracle→seam serial stop; all three final reviewers accepted | `ACCEPT`; implement only `WP-5-m1-loading-bazel-internal-string-oracle` |
| 2026-07-27 | M1 Stage 5 Host typed no-follow dirent observation | Tests-first exact five-file writer, serial owner/downstream/cross-target validation, root focused evidence rerun, and source/API, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Dedicated kind/entry plus one shared Arc slice, stable duplicate-retaining name sort, complete kind equality, one DirectoryEntries DICE demand, Unix d_type/after-close unknown refinement, and reparse-aware Windows no-follow classification passed workspace 36, core 109+13, downstream bzlmod/loading, and four-crate GNU-Windows no-run | Pre-audits caught reparse-point classification, Unix unknown-status evidence, and one per-file cap; all were corrected. Exact five-file +698/-139 scope, per-file caps, fmt/diff/archive/API/dependency/process/forbidden-owner gates, root focused reruns, and all terminal reviews passed | `ACCEPT`; design only `WP-5-m1-loading-byte-string-oracle-feasibility-design` |
| 2026-07-27 | M1 Stage 5 focused Host-dirent glob oracle | Exact sixteen-path writer, one pinned Bazel generation, two distinct fresh-root replays, focused direct validators, and source/parity, native implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Thirteen one-epoch rows prove exact `5/3/4/3/5/3/5` classification through two absent observations, matched ELOOP failure, recovery, empty manifests, and byte-preserved first-four projection; inventory is 10 regular/6 links/667 lines at +669/-10 | Two stopped attempts drove the accepted ELOOP and allow-empty/absent-observation redesigns; the final exact 16-path implementation, 20 pinned anchors, replay/cleanup/scope/cap guards, py-compile, direct assertions, and diff check passed; pytest itself was unavailable, and all three final reviewers accepted | `ACCEPT`; implement only `WP-5-m1-loading-host-dirent-observation` |
| 2026-07-27 | M1 Stage 5 Host-dirent glob oracle semantic redesign | Pinned-source audit, isolated cold controls, same-daemon direct/absent/sibling experiments, final full-core probe, root synthesis, and three terminal latest-text reviews | not exposed | Explicit allow-empty cold matrix is regular 2 state targets, directory 1, FIFO 0, symlink-to-FIFO 2; atomic regular→directory and directory→symlink retain prior membership, sibling churn is ineffective, while exactly two absent observations yield the source-correct `5/3/4/3/5/3/5` matrix on PID/starttime `3982243/110904713` | The accepted redesign expands only 11→13 rows and fixture line cap 550→750, retains topology/assets/allowlist/+900 cap, keeps atomic symlink→FIFO/FIFO→regular, and narrows typed-observation invalidation claims; all probe servers and temp state were removed | `ACCEPT`; implement only `WP-5-m1-loading-host-dirent-glob-oracle` |
| 2026-07-27 | M1 Stage 5 corrected Host-dirent glob oracle retry stop | Tests-first retry, one pinned Bazel 9.2 generation, root stop-gate adjudication, full draft removal, and source/orchestration audits | not exposed | All eleven rows stayed on epoch 1; empty `specials/*` failed rows 1–4, empty files-only `state*` failed the direct-FIFO row under default `allow_empty = false`, and the successful warm directory row unexpectedly retained both files-only and files-and-directories targets | The cycle diagnostic and BUILD comprehension corrections worked, but two independent remaining semantics invalidate a narrow patch. Every harness, fixture, generated, BUILD, regular-asset, and symlink draft was removed; no implementation or post-checkpoint growth was accepted | `REPLAN`; design only `WP-5-m1-loading-host-dirent-glob-oracle-semantics-redesign` |
| 2026-07-27 | M1 Stage 5 Host-dirent glob cycle-diagnostic correction | Pinned-source control-flow audit plus stopped-artifact inspection; root correction synthesis; source/behavior, fixture/implementation, and architecture/orchestration terminal latest-text reviews | not exposed | Hybrid missing-glob delegation and `UnixGlob` following stat prove the matched self-link reaches native `FileSymlinkLoopException` before Skyframe logical-chain restart; stable exact fragments are package-level `error globbing`, the matched path, and `Too many levels of symbolic links`, while same-epoch rename recovery remains exact | The correction retains all eleven rows and topology, rejects a topology/keep-going expansion, replaces the invalid BUILD `def` with five top-level list comprehensions, and preserves every allowlist/cap/growth gate without repository changes | `ACCEPT`; retry only `WP-5-m1-loading-host-dirent-glob-oracle` |
| 2026-07-27 | M1 Stage 5 Host-dirent glob oracle implementation stop | Tests-first harness/fixture writer, one pinned Bazel 9.2 generation, root stop-gate adjudication, full draft removal, and source/orchestration audit | not exposed | The isolated matched self-cycle exited 7 as package-level `error globbing [matched-*] op=FILES` from `unix_jni.cc:382` with `Too many levels of symbolic links`, then recovered to exact `//cycle_error:probe` after rename on the same nonzero epoch | The observed diagnostic materially contradicted the frozen Skyframe `Symlink cycle` shape; a separate BUILD helper `def` was also invalid. Every harness, fixture, generated, regular-asset, and symlink draft was removed, so no implementation or post-checkpoint growth was accepted | `REPLAN`; design only `WP-5-m1-loading-host-dirent-glob-oracle-cycle-diagnostic-correction` |
| 2026-07-27 | M1 Stage 5 focused Host-dirent glob oracle design | Parallel pinned-source/behavior, fixture/harness feasibility, and architecture/orchestration audits; root synthesis; corrected terminal latest-text rereviews | not exposed | Exact existing-fixture extension freezes eleven one-epoch Bazel 9.2 rows over regular, directory, symlink, FIFO, matched-cycle failure, and recovery, with one POSIX/FIFO harness extension, four regular assets, six exact relative symlinks, empty manifests, and +900/-100 cap | Reviews required explicit epoch observation/capture while preserving the first-four record projection, deterministic FIFO mode, exact symlink targets, corrected Bazel source paths, run-scoped cleanup wording, and bounded routing rotation | `ACCEPT`; implement only `WP-5-m1-loading-host-dirent-glob-oracle` |
| 2026-07-27 | M1 Stage 5 Host typed no-follow dirent observation design | Parallel pinned-source/API, live implementation-feasibility, and architecture/orchestration audits; root caller/platform synthesis; corrected terminal latest-text rereviews | not exposed | Exact five-file/+750 contract replaces names-only observations with stable name-sorted duplicate-retaining typed entries; Unix direct kinds and post-close unknown refinement differ intentionally from Windows enumerate-then-swallowing no-follow statuses | Reviews rejected legacy kind reuse and compatibility shims, corrected Bazel's no-dedup behavior, platform-specific tolerated errors, full public/test migration scope, native source anchors, and froze a focused ASCII oracle before Rust; all three accepted | `ACCEPT`; design only `WP-5-m1-loading-host-dirent-glob-oracle-design` |
| 2026-07-27 | M1 Stage 5 byte-capable pattern-lazy loading/glob design | Parallel pinned-source/API, live implementation-feasibility, and architecture/orchestration audits; root live-seam synthesis; corrected terminal latest-text rereviews | not exposed | Pinned source and live code prove current strings collapse literal UTF-8 and octal bytes, directory observations omit Bazel's cached direct kind, and synchronous BUILD evaluation cannot await matched-path DICE dependencies | Reviews split typed dirent observation, byte-string/oracle feasibility, pure Host glob ownership, and transactional evaluator retry; corrections selected source-required typed dirents, token-local codemap-safe lexing, platform/error ownership, a truthful public-export allowlist, and oracle-first serial implementations | `REPLAN`; design only `WP-5-m1-loading-host-dirent-observation-design`; no Rust or fixture changed |
| 2026-07-27 | M1 Stage 5 public bzlmod root-loading anchor projection | Tests-first two-file writer, root serial owner/downstream/cross-target validation, and source/API, implementation/evidence, plus architecture/orchestration terminal latest-diff reviews | not exposed | Opaque retained carriers, verbatim Need, complete-only equality, sole private dependency, and private-only event ownership passed focused 2, bzlmod 203+184, loading 54, zero doctests, all 18 GNU-Windows executables, and exact +314/−4 scope | The first test draft was compacted under the frozen cap before validation; root added Complete success/error validity assertions, lifecycle/diagnostic/privacy/forbidden/archive gates passed, and all three final reviews accepted | `ACCEPT`; design only `WP-5-m1-loading-byte-capable-pattern-lazy-glob-design` |
| 2026-07-27 | M1 Stage 5 bzlmod root-loading anchor projection design | Parallel source/API, implementation/evidence, and architecture/orchestration audits; root contract synthesis; corrected terminal latest-text rereviews | not exposed | Exact two-file projection freezes one normalized-workspace public key and two opaque wrappers retaining the private root-module carrier, with verbatim Need, complete-only equality, no wrapper event batch, and the private producer as sole direct dependency | Reviews removed an unnecessary workspace accessor, made Debug opaque while preserving the existing user-facing error diagnostic/source behavior, bounded dependency/event-closure evidence, and all three accepted the final no-code record | `ACCEPT`; implement only `WP-5-m1-bzlmod-root-loading-anchor-projection` |
| 2026-07-27 | M1 Stage 5 loading typed propagation design | Parallel live-call-graph, Host-path/evidence, and architecture/orchestration audits; pinned Bazel raw-name/symlink correction; terminal latest-text rereviews | not exposed | A typed package root cannot keep or drop the eager `RootModuleGraphKey` anchor and cannot access the private Host replacement; pinned source also requires raw internal-byte names, pattern-lazy symlink/special classification, and regular-or-special BUILD/.bzl reads that current loading/workspace values cannot express | Reviews rejected in-place downstream key changes and eager resolve-all listing, froze the independent two-file opaque root-anchor prerequisite, retained separate byte-capable glob and Host byte-projection gates, and all three accepted the corrected no-code record | `REPLAN`; design only `WP-5-m1-bzlmod-root-loading-anchor-projection-design`; no loading implementation is authorized |
| 2026-07-27 | M1 Stage 5 Host JVM registry-byte execution feasibility | Parallel pinned-source, native-observation/live-seam, and architecture/orchestration audits; root synthesis; terminal corrected latest-text rereviews | not exposed | Explicit `--server_javabase` shell launchers and arbitrary Java/native agents can distinguish the original Bazel server command from any bounded Slug-owned helper; exact original-command execution is delegation, while current Slug lacks authenticated output-base lifecycle, executor identity, external-directory DICE ownership, and native-Windows evidence | Reviews qualified the impossibility boundary, corrected same-path reuse semantics and source paths, recorded retained `/tmp` probes and zero live processes, froze trusted-local-code transport constraints and the exact conditional activation sequence, and all three accepted the final text | `REPLAN`; design only independent `WP-5-m1-loading-typed-propagation-design`; no JVM executor implementation is authorized |
| 2026-07-27 | M1 Stage 5 corrected host-JVM startup/reuse oracle | One seven-path writer, parallel pinned-source and architecture reviews, root exactness corrections, pinned generation, and distinct-root replay/probe validation | not exposed | Twenty-two rows prove epochs `2/2/2/2/2/2/3/4`, ordered CLI/RC startup diagnostics, exact raw query stdout, 94/91-byte registry bytes, stable lock manifests, verified endpoint cleanup, and checked raw-BEP purge; 82 tests passed at exact +1,800 and 58/5/1,845 | Reviews tightened missing capture fields, exact query output, empty-only synthetic BEP records, payload labels, prelaunch capture validation, source provenance, raw-BEP cleanup, and raw-LF comparison; generation, six full replays, three workspace-digest probes, archive/diff/credential/process gates, and all terminal rereviews passed | `ACCEPT`; design only `WP-5-m1-host-jvm-registry-byte-execution-feasibility` |
| 2026-07-26 | M1 Stage 5 corrected host-JVM startup/reuse oracle command shape | Parallel pinned-source live query/BEP probes, native sequence/cleanup audit, and architecture/harness review; root synthesis; terminal latest-text rereviews | not exposed | Seven-path contract freezes 22 commands: five semantic mod rows plus three visible successful query companions, exact CLI/RC startup records, epochs 1→2/2/2→3→4, 91/94-byte SRIs, unchanged lockfile manifests, and fixture-opt-in Bazel endpoint cleanup | Reviews replaced unrelated failing builds with `query //:BUILD.bazel`, corrected relative RC BEP versus absolute announcement source, defined absent-field parsing and command-port lifecycle, added exact mutation/manifests/cwd/error chaining, and all three accepted | `ACCEPT`; implement only corrected `WP-5-m1-host-jvm-registry-startup-reuse-oracle`, then run JVM executor feasibility |
| 2026-07-26 | M1 Stage 5 host-JVM startup/reuse oracle pre-implementation stop | Parallel writer preflight plus pinned-source and architecture/harness audits; writer draft removal; terminal corrected latest-text reviews | not exposed | Bazel 9.2 `mod` accepts the JSON BEP flag but is excluded from the BEP service allowed commands, so transport/file creation never runs; a build probe emitted one original startup section with synthetic empties encoded as absent `combinedForm` | A temporary 395-line failing-test draft was fully removed. Reviews also confined epoch observation to the opted-in fixture and exact cleanup to Bazel because authenticated Slug Status/shutdown is a later packet; all probes died and all three accepted the corrected clean-tree stop text | `REPLAN`; design only explicit build diagnostic companions and the Bazel-only/no-Slug command-shape correction |
| 2026-07-26 | M1 Stage 5 host-JVM registry byte oracle-boundary correction | Parallel pinned Bazel/OpenJDK source, live harness/native, and architecture/lifecycle/DICE audits; root synthesis; terminal latest-text rereviews | not exposed | Exact seven-path startup/reuse sub-oracle reuses the existing seven-name fixture with one hermetic RC and five rows proving 91/94-byte last-wins, equal-multiset order/source reuse, occurrence restart, default restoration, exact request diagnostics, and verified server epochs/cleanup | Reviews made top-level RC dedup and JVM prefix/suffix wording exact, applied one fixture-wide RC/environment baseline to all 19 commands and cleanup, and rejected a custom JVM main/JAR because Bazel-valid agents can distinguish it; all three accepted the corrected text | `REPLAN` for execution; implement only `WP-5-m1-host-jvm-registry-startup-reuse-oracle`, then run exact-executor feasibility |
