/target/
target/
/.codex-cargo-target/
# Bazel/Slug generated workspace output links and trees.
buck-out
bazel-external
bazel-bin
bazel-out
bazel-testlogs
bazel-*
execroot
external/
/.direnv
.envrc.local

# Python bytecode and test caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
/slug
/slug.exe
tests/manual_test/**

# Hermes DICE benchmark traces (raw CSV output; see thoughts/shared/research/ for summaries)
.hermes/

# Accidental npm metadata (this is a Cargo workspace, not an npm project)
package-lock.json

# Historical benchmark artifacts can remain in local checkouts, but they are not
# part of the active V2 root.
benchmarks/**/*.pb.zst
benchmarks/**/build.log
benchmarks/**/summary.err
