[package]
build = "build.rs"
description = "Kuro external cells bundled data"
edition = "2024"
license = { workspace = true }
name = "kuro_external_cells_bundled"
repository = { workspace = true }
version = "0.1.0"

[dependencies]
async-trait = { workspace = true }

kuro_common = { workspace = true }

[build-dependencies]
walkdir = { workspace = true }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(buck_build)"] }
