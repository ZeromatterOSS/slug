[package]
authors = ["Meta"]
description = "Eden integration for Slug"
edition = "2024"
license = { workspace = true }
name = "slug_eden"
repository = { workspace = true }
version = "0.1.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(fbcode_build)"] }
