[package]
description = "utilities to configure RE in Kuro"
edition = "2024"
license = { workspace = true }
name = "kuro_re_configuration"
repository = { workspace = true }
version = "0.1.0"

[dependencies]
allocative = { workspace = true }
kuro_common = { workspace = true }
kuro_core = { workspace = true }
kuro_error = { workspace = true }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(fbcode_build)"] }
