[package]
description = "Slug V2 Bazel Build Event Protocol subset"
edition = "2024"
license = { workspace = true }
name = "slug_bep_v2"
repository = { workspace = true }
version = "0.1.0"

[dependencies]