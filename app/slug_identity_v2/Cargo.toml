[package]
description = "Slug V2 Bazel identity and layout types"
edition = "2024"
license = { workspace = true }
name = "slug_identity_v2"
repository = { workspace = true }
version = "0.1.0"

[dependencies]